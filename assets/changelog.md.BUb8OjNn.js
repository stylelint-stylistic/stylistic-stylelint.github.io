import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.B-Bf4DF5.js";var i=JSON.parse(`{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"changelog.md","filePath":"CHANGELOG.md","lastUpdated":1788339418000}`),a={name:`changelog.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to “Changelog”">​</a></h1><p>All notable changes to\xA0this project will be documented in\xA0this file.</p><p>The\xA0format is based on\xA0<a href="https://keepachangelog.com" target="_blank" rel="noreferrer">Keep\xA0a\xA0Changelog</a>, and\xA0this project adheres to\xA0<a href="https://semver.org" target="_blank" rel="noreferrer">Semantic\xA0Versioning</a>.</p><h2 id="unreleased" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v5.3.0...HEAD" target="_blank" rel="noreferrer">Unreleased</a> <a class="header-anchor" href="#unreleased" aria-label="Permalink to “Unreleased”">​</a></h2><h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The\xA0rules no\xA0longer read an\xA0SCSS stylesheet (<code>postcss-scss</code>) under\xA0their core names: such a\xA0file now gets one warning naming the\xA0<code>@stylistic/scss/</code> namespace, which\xA0holds the\xA0same rules with\xA0the\xA0same options and\xA0documentation, and\xA0nothing else of\xA0it is checked. A\xA0project linting SCSS renames the\xA0rules in\xA0the\xA0<code>overrides</code> block that\xA0sets <code>customSyntax</code>\xA0— <code>@stylistic/scss/color-hex-case</code> in\xA0place of\xA0<code>@stylistic/color-hex-case</code>\xA0— and\xA0everything else stays as\xA0it was: plain CSS, and\xA0a\xA0plain block of\xA0an\xA0HTML page beside a\xA0<code>lang=&quot;scss&quot;</code> one, the\xA0namespace reads exactly as\xA0the\xA0core does. With\xA0this the\xA0core is written for\xA0plain CSS alone, and\xA0its reading of\xA0a\xA0plain CSS file narrows where it used to\xA0pass a\xA0construct only Sass spells over\xA0as\xA0one: a\xA0reading through\xA0a\xA0Sass module (<code>ns.$a</code>, <code>ns.f()</code>), a\xA0placeholder selector (<code>%a</code>), a\xA0nested property (<code>font: { … }</code>), an\xA0at-rule carrying neither block nor\xA0parameters, and\xA0a\xA0comment the\xA0parser itself marked as\xA0opened by\xA0a\xA0double slash are read as\xA0the\xA0CSS they are. What a\xA0plugin spells over\xA0plain CSS the\xA0core still passes over\xA0as\xA0it did\xA0— a\xA0<code>$</code> variable, its map or\xA0list, and\xA0the\xA0<code>$(…)</code> of\xA0postcss-simple-vars, an\xA0interpolation (<code>#{…}</code>, <code>@{…}</code>) and\xA0the\xA0text inside\xA0it\xA0— since\xA0a\xA0file linted with\xA0such a\xA0plugin is a\xA0plain CSS file to\xA0Stylelint, and\xA0a\xA0fix that\xA0read those as\xA0code would rewrite them.</li><li>The\xA0rules no\xA0longer read a\xA0Less stylesheet (<code>postcss-less</code>) under\xA0their core names: such a\xA0file now gets one warning naming the\xA0<code>@stylistic/less/</code> namespace, which\xA0holds the\xA0same rules with\xA0the\xA0same options and\xA0documentation, and\xA0nothing else of\xA0it is checked. A\xA0project linting Less renames the\xA0rules in\xA0the\xA0<code>overrides</code> block that\xA0sets <code>customSyntax</code>\xA0— <code>@stylistic/less/color-hex-case</code> in\xA0place of\xA0<code>@stylistic/color-hex-case</code>\xA0— and\xA0everything else stays as\xA0it was: plain CSS, and\xA0a\xA0plain block of\xA0an\xA0HTML page beside a\xA0Less one, the\xA0namespace reads exactly as\xA0the\xA0core does. One reading of\xA0plain CSS narrows with\xA0the\xA0turning: a\xA0value opening with\xA0<code>@</code>, which\xA0the\xA0core used to\xA0pass over\xA0as\xA0a\xA0Less variable, is read as\xA0the\xA0CSS it is, so\xA0<code>unit-case</code> now reports a\xA0miscased unit in\xA0a\xA0value such as\xA0<code>@{}10PX</code>.</li><li>The\xA0rules no\xA0longer read a\xA0styled template (<code>postcss-styled-syntax</code>) under\xA0their core names: such a\xA0file now gets one warning naming the\xA0<code>@stylistic/styled/</code> namespace, which\xA0holds the\xA0same rules with\xA0the\xA0same options and\xA0documentation, and\xA0nothing else of\xA0it is checked. A\xA0project linting styled templates renames the\xA0rules in\xA0the\xA0<code>overrides</code> block that\xA0sets <code>customSyntax</code>\xA0— <code>@stylistic/styled/indentation</code> in\xA0place of\xA0<code>@stylistic/indentation</code>\xA0— and\xA0everything else stays as\xA0it was, plain CSS included, which\xA0the\xA0namespace reads exactly as\xA0the\xA0core does.</li><li>The\xA0documentation now lives at\xA0<a href="https://stylelint-stylistic.github.io/stylelint-stylistic/" target="_blank" rel="noreferrer">stylelint-stylistic.github.io/stylelint-stylistic</a>, and\xA0the\xA0URL a\xA0rule reports beside its problems (<code>meta.url</code>) points at\xA0that\xA0rule&#39;s page there instead of\xA0its <code>README.md</code> on\xA0GitHub. A\xA0rule carries the\xA0same URL under\xA0every namespace, since\xA0the\xA0rule and\xA0its documentation are the\xA0same; what a\xA0namespace reads differently is written on\xA0a\xA0page of\xA0its own, linked from\xA0the\xA0<a href="https://stylelint-stylistic.github.io/stylelint-stylistic/user-guide/custom-syntaxes" target="_blank" rel="noreferrer">Custom syntaxes</a> guide.</li><li>The\xA0<code>named-grid-areas-alignment</code> rule now reads the\xA0rows of\xA0the\xA0<code>grid-template</code> and\xA0<code>grid</code> shorthands as\xA0it reads the\xA0value of\xA0<code>grid-template-areas</code> (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/45" target="_blank" rel="noreferrer">#45</a>): every string at\xA0the\xA0top level of\xA0the\xA0value is a\xA0row, its cells are aligned with\xA0the\xA0cells of\xA0the\xA0other rows, and\xA0a\xA0row&#39;s size, its line names, the\xA0solidus and\xA0the\xA0columns behind it go back as\xA0the\xA0file spells them. A\xA0stylesheet spelling its grids with\xA0a\xA0shorthand meets the\xA0rule&#39;s warnings on\xA0them now, where it used to\xA0pass them in\xA0silence, and\xA0<code>--fix</code> aligns their cells; under\xA0<code>alignQuotes</code> the\xA0sizes behind the\xA0rows come to\xA0stand in\xA0a\xA0column of\xA0their own. The\xA0message names the\xA0property the\xA0value belongs to.</li><li>The\xA0plugin now reads a\xA0line break the\xA0way PostCSS does: a\xA0line feed, with\xA0or\xA0without\xA0the\xA0carriage return of\xA0a\xA0Windows pair in\xA0front of\xA0it, and\xA0nothing else. A\xA0bare carriage return and\xA0a\xA0form feed are whitespace to\xA0every rule, as\xA0they are to\xA0PostCSS&#39;s line counter\xA0— a\xA0rule reading a\xA0line in\xA0either reported a\xA0position the\xA0file does not\xA0have, since\xA0Stylelint counts lines by\xA0PostCSS. Every rule about\xA0a\xA0break, an\xA0empty line, an\xA0inline comment&#39;s end or\xA0a\xA0multi-line list is affected: a\xA0file broken with\xA0bare carriage returns or\xA0form feeds is one line to\xA0all of\xA0them now. The\xA0<code>linebreaks</code> rule tells <code>\\n</code> from\xA0<code>\\r\\n</code> and\xA0leaves a\xA0bare carriage return where it stands.</li><li>Every rule that\xA0writes a\xA0line break now writes the\xA0one the\xA0<code>linebreaks</code> rule asks for, wherever the\xA0configuration lists that\xA0rule, and\xA0otherwise the\xA0one the\xA0file spells its lines with\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/352" target="_blank" rel="noreferrer">#352</a>). Stylelint runs each rule once and\xA0in\xA0the\xA0order the\xA0configuration lists them, so\xA0a\xA0break another rule wrote behind <code>linebreaks</code> used to\xA0stay a\xA0line feed in\xA0a\xA0file of\xA0Windows pairs until\xA0the\xA0next run of\xA0<code>--fix</code>; there is no order the\xA0two\xA0disagree in\xA0any more. A\xA0file that\xA0ends no line at\xA0all is given a\xA0line feed, never the\xA0line ending of\xA0the\xA0machine.</li><li>The\xA0<code>declaration-block-trailing-semicolon</code> rule now writes the\xA0semicolon its <code>always</code> option adds behind a\xA0declaration with\xA0whatever the\xA0<code>declaration-block-semicolon-newline-before</code> and\xA0<code>declaration-block-semicolon-space-before</code> rules ask to\xA0stand in\xA0front of\xA0one, wherever the\xA0configuration lists either of\xA0them (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/354" target="_blank" rel="noreferrer">#354</a>), and\xA0the\xA0one it adds behind an\xA0at-rule with\xA0whatever the\xA0<code>at-rule-semicolon-space-before</code> rule asks for, which\xA0has no fixer to\xA0put it there itself (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/477" target="_blank" rel="noreferrer">#477</a>). Stylelint runs each rule once and\xA0in\xA0the\xA0order the\xA0configuration lists them, so\xA0a\xA0bare semicolon written behind one of\xA0those rules used to\xA0stay bare until\xA0the\xA0next run of\xA0<code>--fix</code>\xA0— and\xA0behind an\xA0at-rule for\xA0good, since\xA0that\xA0rule has no fixer\xA0— and\xA0the\xA0block spelled its last semicolon unlike\xA0the\xA0others; there is no order the\xA0rules disagree in\xA0any more.</li><li>A\xA0rule configured with\xA0a\xA0<code>-single-line</code> or\xA0<code>-multi-line</code> option now takes its turn in\xA0the\xA0plugin&#39;s own order rather than\xA0in\xA0the\xA0one the\xA0configuration happens to\xA0list it in\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/502" target="_blank" rel="noreferrer">#502</a>). The\xA0check of\xA0such a\xA0rule waits for\xA0the\xA0run&#39;s writers already (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/355" target="_blank" rel="noreferrer">#355</a>), and\xA0behind that\xA0line two\xA0of\xA0them used to\xA0answer between\xA0themselves in\xA0the\xA0configuration&#39;s order: <code>--fix</code> left one file where a\xA0rule stood in\xA0front of\xA0its neighbour and\xA0another where it stood behind, and\xA0often one of\xA0the\xA0two\xA0files still violated a\xA0rule the\xA0user had turned on. Reordering two\xA0such entries changes nothing now\xA0— the\xA0order among them is the\xA0text whose lineness a\xA0rule reads, innermost first, a\xA0call&#39;s arguments before\xA0the\xA0declaration around them and\xA0the\xA0declaration before\xA0the\xA0statement&#39;s own prelude and\xA0block, with\xA0the\xA0rules whose subject is a\xA0line break in\xA0front of\xA0the\xA0rules whose subject is a\xA0space, and\xA0the\xA0options about\xA0a\xA0single-line text in\xA0front of\xA0the\xA0options about\xA0a\xA0multi-line one. Of\xA0the\xA0105\xA0and\xA095\xA0rows measured over\xA0two\xA0corpora where the\xA0two\xA0orders left different files, 61\xA0and\xA053\xA0had one order leaving a\xA0rule unsatisfied; none does now. The\xA0same holds for\xA0one rule listed under\xA0two\xA0namespaces, <code>@stylistic/indentation</code> beside <code>@stylistic/scss/indentation</code> for\xA0one, which\xA0a\xA0plain CSS file is read by\xA0both of: those are ordered by\xA0the\xA0namespace, the\xA0core&#39;s first. A\xA0pair of\xA0which\xA0at\xA0least one rule carries no such option is untouched by\xA0all this, and\xA0can still answer differently under\xA0the\xA0two\xA0orders.</li><li>False negatives are fixed\xA0— a\xA0violation the\xA0plugin used to\xA0pass over\xA0in\xA0silence, whether <code>--fix</code> was asked for\xA0or\xA0not, is now reported: <ul><li>The\xA0<code>named-grid-areas-alignment</code> rule now reads its properties in\xA0whatever case they are written (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/614" target="_blank" rel="noreferrer">#614</a>). It matched <code>grid-template-areas</code> in\xA0lower case alone, so\xA0<code>GRID-TEMPLATE-AREAS: &quot;a a&quot;</code> was passed over\xA0— and\xA0with\xA0<code>property-case: &quot;upper&quot;</code> listed ahead of\xA0it, <code>--fix</code> uppercased the\xA0property first and\xA0the\xA0rule read nothing behind it, so\xA0the\xA0two\xA0orders of\xA0the\xA0configuration left two\xA0files and\xA0neither carried a\xA0warning.</li><li>The\xA0<code>declaration-colon-space-after</code> and\xA0<code>declaration-colon-space-before</code> rules now find the\xA0declaration&#39;s colon the\xA0way its parser found it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/92" target="_blank" rel="noreferrer">#92</a>). What the\xA0file spells between\xA0the\xA0property and\xA0the\xA0value is handed to\xA0the\xA0tokenizer that\xA0parser reads by, so\xA0a\xA0colon standing inside\xA0a\xA0comment, a\xA0string, a\xA0parenthesised group, an\xA0at-word or\xA0an\xA0escape is text there and\xA0opens no declaration. The\xA0search these rules used read a\xA0comment by\xA0rules of\xA0its own\xA0— it never closed one whose text ends in\xA0a\xA0backslash, <code>/*x\\*/</code>, and\xA0it read the\xA0<code>*/</code> and\xA0<code>/*</code> of\xA0two\xA0comments abutting as\xA0opening a\xA0third\xA0— and\xA0it took a\xA0double slash for\xA0a\xA0comment in\xA0every file and\xA0wherever it stood, so\xA0<code>a { b/*x\\*/:pink; }</code>, <code>a { b/*x*//*y*/:pink; }</code>, <code>a { b //: red; }</code>, <code>a { b //&quot;x:&quot;: red; }</code> and\xA0<code>a { b $//: red; }</code> were passed over\xA0by\xA0every option of\xA0both rules; and\xA0it read into\xA0a\xA0parenthesised group rather than\xA0over\xA0it, so\xA0the\xA0space asked for\xA0in\xA0<code>a { b (x:y): red; }</code> was written inside\xA0the\xA0group. Every one of\xA0them is answered like\xA0any other declaration now, which\xA0cuts both ways: a\xA0declaration whose real colon already has the\xA0space these rules were writing elsewhere is no\xA0longer reported at\xA0all. Where the\xA0text in\xA0front of\xA0the\xA0colon ends in\xA0an\xA0inline comment the\xA0fix still declines to\xA0write, a\xA0space put there being commented out (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/88" target="_blank" rel="noreferrer">#88</a>).</li><li><code>function-parentheses-newline-inside</code> reads the\xA0runs beside a\xA0function&#39;s parentheses the\xA0way PostCSS&#39;s tokenizer reads whitespace now (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/496" target="_blank" rel="noreferrer">#496</a>), so\xA0a\xA0line break standing behind a\xA0vertical tab no\xA0longer satisfies an\xA0<code>always</code> option\xA0— such a\xA0character is a\xA0word, the\xA0run beside the\xA0parenthesis holds no break, and\xA0the\xA0break the\xA0option asks for\xA0is written and\xA0reported.</li><li>The\xA0<code>color-hex-case</code>, <code>function-comma-*</code>, <code>number-*</code> and\xA0<code>unit-case</code> rules now read a\xA0call whose name a\xA0hexadecimal escape welds to\xA0the\xA0word in\xA0front of\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/344" target="_blank" rel="noreferrer">#344</a>). CSS closes such an\xA0escape with\xA0one whitespace character belonging to\xA0the\xA0escape rather than\xA0to\xA0the\xA0text, so\xA0<code>a { b: \\61 url(13PX); }</code> names one call, <code>aurl</code>, which\xA0<code>lightningcss</code> compiles to\xA0<code>a { b: aurl(13px); }</code>, lowercasing that\xA0unit itself, and\xA0Sass to\xA0<code>a { b: aurl(13PX); }</code>\xA0— an\xA0ordinary call to\xA0both, whose arguments are the\xA0author&#39;s. <code>postcss-value-parser</code> reads no escape and\xA0hands that\xA0call back named <code>url</code> alone, which\xA0is what these rules matched against, so\xA0everything such a\xA0call held was passed over\xA0as\xA0an\xA0address&#39;s. The\xA0name is gathered back together now, and\xA0what stands inside\xA0is read like\xA0any other value. The\xA0other half of\xA0this reading, where an\xA0address written with\xA0an\xA0escape stops being rewritten, is under\xA0<code>Fixed</code> below.</li><li>The\xA0<code>selector-descendant-combinator-no-non-space</code> rule now reads a\xA0selector whose comment the\xA0parser carries across\xA0a\xA0parenthesised group (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/159" target="_blank" rel="noreferrer">#159</a>). A\xA0group standing where a\xA0combinator belongs is nothing <code>postcss-selector-parser</code> has a\xA0place for: it reads the\xA0group as\xA0a\xA0combinator whose value is the\xA0whitespace in\xA0front of\xA0it and\xA0the\xA0group itself, and\xA0files a\xA0comment standing between\xA0the\xA0two\xA0behind that\xA0node rather than\xA0inside\xA0it, so\xA0<code>.foo /*c*/\\t( )\\t.bar</code> came back out of\xA0the\xA0parser as\xA0<code>.foo ( )/*c*/\\t\\t.bar</code>\xA0— the\xA0comment across\xA0the\xA0group, and\xA0a\xA0tab where none was written. The\xA0rule passed such a\xA0selector over, and\xA0a\xA0list at\xA0a\xA0time, so\xA0a\xA0run standing in\xA0a\xA0neighbouring selector went unreported with\xA0it. The\xA0node is given its own text back now, so\xA0the\xA0group is reported where it stands and\xA0the\xA0whitespace behind it is collapsed like\xA0any other descendant combinator&#39;s, the\xA0comment keeping the\xA0place the\xA0file gives it. Only a\xA0comment with\xA0a\xA0space in\xA0front of\xA0it was ever read that\xA0way, however wide the\xA0whitespace is: that\xA0space is what the\xA0value of\xA0such a\xA0combinator opens with, while\xA0a\xA0comment standing behind a\xA0tab or\xA0a\xA0line break the\xA0parser already prints as\xA0the\xA0file spells it.</li><li>The\xA0<code>selector-descendant-combinator-no-non-space</code> rule now reads a\xA0selector carrying an\xA0inline comment under\xA0<code>postcss-scss</code> (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/158" target="_blank" rel="noreferrer">#158</a>). That\xA0syntax rewrites such a\xA0comment into\xA0a\xA0block comment in\xA0the\xA0copy the\xA0rule reads, keeps the\xA0source spelling in\xA0the\xA0copy it prints, and\xA0the\xA0two\xA0drift apart by\xA0two\xA0characters per\xA0comment, so\xA0the\xA0rule used to\xA0pass the\xA0whole selector list over. Every position is now translated back into\xA0the\xA0file&#39;s own coordinates, and\xA0a\xA0fix is written to\xA0both copies with\xA0the\xA0comments spelled the\xA0way the\xA0file spells them. The\xA0whitespace standing behind an\xA0inline comment is passed over\xA0on\xA0its own: the\xA0line break in\xA0it is what closes the\xA0comment, so\xA0a\xA0single space could not\xA0be written there, and\xA0asking for\xA0one would leave a\xA0warning no run of\xA0<code>--fix</code> could clear.</li><li>The\xA0<code>selector-pseudo-class-parentheses-space-inside</code> rule now reads a\xA0selector carrying an\xA0inline comment under\xA0<code>postcss-scss</code> (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/163" target="_blank" rel="noreferrer">#163</a>). It used to\xA0pass such a\xA0selector over, as\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/161" target="_blank" rel="noreferrer">#161</a> left it: the\xA0two\xA0copies that\xA0syntax keeps of\xA0a\xA0selector drift apart by\xA0two\xA0characters per\xA0comment, so\xA0a\xA0position counted in\xA0the\xA0copy the\xA0rule reads points past the\xA0character it is about, and\xA0a\xA0fix written there never reaches the\xA0file. Both are answered now, so\xA0the\xA0pseudo-classes of\xA0such a\xA0selector are spaced out like\xA0any other. Where the\xA0whitespace in\xA0front of\xA0the\xA0closing parenthesis is the\xA0line break that\xA0closes an\xA0inline comment, that\xA0end is passed over\xA0instead: a\xA0space written there would take the\xA0parenthesis into\xA0the\xA0comment, and\xA0taking the\xA0break away would do the\xA0same.</li><li>The\xA0<code>selector-pseudo-class-parentheses-space-inside</code> rule now reports the\xA0whitespace standing in\xA0front of\xA0a\xA0closing parenthesis where a\xA0comment closes the\xA0arguments (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/128" target="_blank" rel="noreferrer">#128</a>). The\xA0parser kept none of\xA0it, so\xA0the\xA0<code>never</code> options passed <code>a:not( /**/ )</code> over\xA0while\xA0reporting the\xA0same space in\xA0<code>a:not( b /**/ )</code>; both are now read alike.</li><li>The\xA0<code>declaration-colon-space-after</code> rule now looks for\xA0the\xA0whitespace after\xA0the\xA0colon where a\xA0custom property whose value holds a\xA0comment actually keeps it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/109" target="_blank" rel="noreferrer">#109</a>) (<a href="https://github.com/VChet" target="_blank" rel="noreferrer">@VChet</a>): <ul><li>the\xA0<code>always</code> options no\xA0longer report <code>--a: /*comment*/ !important;</code>, whose single space is already in\xA0place, and\xA0no\xA0longer pass over\xA0<code>--a:/*comment*/ !important;</code>, which\xA0has none at\xA0all;</li><li>their fix no\xA0longer adds a\xA0space of\xA0its own on\xA0every run, so\xA0two\xA0spaces or\xA0a\xA0tab after\xA0the\xA0colon now become the\xA0single space asked for;</li><li>the\xA0<code>never</code> option now takes the\xA0whitespace away instead of\xA0reporting a\xA0fix and\xA0leaving the\xA0declaration as\xA0it was, and\xA0no\xA0longer reports one that\xA0has no whitespace to\xA0begin with;</li><li>a\xA0space or\xA0a\xA0tab standing in\xA0front of\xA0the\xA0colon is no\xA0longer counted as\xA0one standing after\xA0it.</li></ul></li><li>The\xA0<code>declaration-colon-*-after</code> rules now look for\xA0the\xA0whitespace behind the\xA0colon where a\xA0declaration whose value holds no word of\xA0its own actually keeps it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/371" target="_blank" rel="noreferrer">#371</a>). PostCSS hands that\xA0whitespace to\xA0<code>raws.between</code> only where the\xA0value has a\xA0word to\xA0leave behind; where all that\xA0stands behind the\xA0colon is whitespace, comments and\xA0a\xA0flag, <code>raws.between</code> ends at\xA0the\xA0colon and\xA0the\xA0run stays at\xA0the\xA0head of\xA0the\xA0value instead\xA0— in\xA0the\xA0raw of\xA0it where PostCSS keeps one, and\xA0in\xA0the\xA0value itself where it does not. Only the\xA0guard <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/109" target="_blank" rel="noreferrer">#109</a> above left behind ever looked there, and\xA0only for\xA0a\xA0custom property. The\xA0declaration is read now down to\xA0the\xA0end of\xA0its value, as\xA0the\xA0file prints it, and\xA0the\xA0run is moved to\xA0where PostCSS itself keeps it whenever a\xA0value has a\xA0word before\xA0anything is written over\xA0it, so\xA0<code>a { color: !important; }</code> and\xA0<code>a { color: /*comment*/\\t!important; }</code> are answered like\xA0any other declaration: <ul><li>the\xA0<code>always</code> and\xA0<code>always-single-line</code> options of\xA0<code>declaration-colon-space-after</code> no\xA0longer report a\xA0declaration whose single space is already in\xA0place, and\xA0their fix no\xA0longer writes a\xA0space of\xA0its own beside the\xA0run rather than\xA0over\xA0it, which\xA0used to\xA0grow the\xA0declaration by\xA0a\xA0space on\xA0every run;</li><li>the\xA0text those options count the\xA0lines of\xA0is that\xA0declaration&#39;s own value now, where it used to\xA0be the\xA0text around it: the\xA0value of\xA0<code>a { color\\n: !important; }</code> is empty, and\xA0an\xA0empty text stands on\xA0one line, so\xA0<code>always-single-line</code> speaks about\xA0that\xA0declaration as\xA0it always has about\xA0<code>a { color\\n: pink; }</code>, and\xA0<code>always-multi-line</code> of\xA0<code>declaration-colon-newline-after</code> is silent about\xA0it as\xA0it always has been about\xA0the\xA0twin;</li><li>its <code>never</code> option now reports such a\xA0run and\xA0takes it away, instead of\xA0passing the\xA0declaration over\xA0in\xA0silence;</li><li><code>declaration-colon-newline-after</code> no\xA0longer reports a\xA0declaration whose break already stands where the\xA0option asks for\xA0it, and\xA0no\xA0longer writes a\xA0line break of\xA0its own on\xA0every run under\xA0either of\xA0its options;</li><li>the\xA0break that\xA0rule writes now goes behind a\xA0comment standing on\xA0the\xA0colon&#39;s own line rather than\xA0in\xA0front of\xA0it, as\xA0it always has where the\xA0value has a\xA0word, so\xA0the\xA0position such a\xA0problem is reported at\xA0moves to\xA0the\xA0end of\xA0that\xA0comment.</li></ul></li><li>The\xA0<code>declaration-colon-space-before</code> and\xA0<code>declaration-colon-space-after</code> rules now work on\xA0the\xA0declaration&#39;s own colon instead of\xA0the\xA0first colon they come across\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/92" target="_blank" rel="noreferrer">#92</a>). A\xA0comment standing in\xA0front of\xA0the\xA0colon may hold a\xA0colon of\xA0its own, an\xA0URL for\xA0one, and\xA0used to\xA0be taken for\xA0the\xA0declaration&#39;s: <ul><li>the\xA0fix no\xA0longer edits the\xA0text of\xA0such a\xA0comment;</li><li>the\xA0<code>never</code> options no\xA0longer miss the\xA0real violation hiding behind it;</li><li>the\xA0<code>always</code> options no\xA0longer report a\xA0declaration that is already correct.</li></ul></li><li>The\xA0<code>declaration-block-semicolon-newline-after</code>, <code>declaration-block-semicolon-newline-before</code>, <code>declaration-block-semicolon-space-after</code> and\xA0<code>declaration-block-semicolon-space-before</code> rules now check the\xA0declarations of\xA0an\xA0inline <code>style</code> attribute (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/49" target="_blank" rel="noreferrer">#49</a>). A\xA0page gives such an\xA0attribute a\xA0root of\xA0its own with\xA0no rule inside\xA0it, and\xA0the\xA0four\xA0rules used to\xA0pass over\xA0every declaration whose parent is not\xA0a\xA0rule, so\xA0nothing was reported for\xA0<code>style=&quot;color: pink;top: 0;&quot;</code> in\xA0an\xA0HTML, Vue, Svelte or\xA0Astro file, while\xA0the\xA0<code>&lt;style&gt;</code> block of\xA0the\xA0same page was checked as\xA0usual. The\xA0top level of\xA0a\xA0Sass file is left alone as\xA0before, since\xA0a\xA0variable written there is not\xA0a\xA0declaration block.</li><li>The\xA0<code>selector-descendant-combinator-no-non-space</code> rule now reports a\xA0parenthesised group standing after\xA0a\xA0space where a\xA0combinator belongs, <code>.foo ( ) .bar</code> for\xA0one (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/125" target="_blank" rel="noreferrer">#125</a>). The\xA0rule used to\xA0read a\xA0combinator only where its value was a\xA0single space, so\xA0every such selector went past unseen; a\xA0group holding one space or\xA0none, <code>.foo ( ) .bar</code> and\xA0<code>.foo () .bar</code>, is reported alongside the\xA0rest. Nothing can be written to\xA0repair such a\xA0selector, so\xA0the\xA0problem is reported and\xA0the\xA0code left exactly as\xA0it was.</li><li>The\xA0<code>selector-descendant-combinator-no-non-space</code> rule no\xA0longer turns itself off for\xA0a\xA0selector carrying a\xA0comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/124" target="_blank" rel="noreferrer">#124</a>). One comment anywhere in\xA0a\xA0selector list used to\xA0silence the\xA0rule for\xA0the\xA0whole list, so\xA0a\xA0violation standing in\xA0a\xA0neighbouring selector went unreported along with\xA0it: <ul><li>a\xA0descendant combinator holding comments is now measured run by\xA0run, each stretch of\xA0whitespace reported at\xA0its own position and\xA0collapsed on\xA0its own, so\xA0that\xA0every comment stays where the\xA0author put it;</li><li>the\xA0whitespace after\xA0a\xA0comment that\xA0follows a\xA0combinator of\xA0another kind, <code>.foo &gt; /*comment*/ .bar</code> for\xA0one, is passed over, since\xA0there is no descendant combinator there to\xA0measure;</li><li>an\xA0illegal combinator carrying a\xA0comment, <code>.foo /*comment*/ ( ) .bar</code> for\xA0one, is reported as\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/125" target="_blank" rel="noreferrer">#125</a> has it, and\xA0left exactly as\xA0it was.</li><li>a\xA0selector carrying an\xA0inline comment is read as\xA0well, which\xA0was <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/158" target="_blank" rel="noreferrer">#158</a> and\xA0is answered below.</li><li>a\xA0selector the\xA0parser does not\xA0give back as\xA0it took it is passed over\xA0too, which\xA0is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/159" target="_blank" rel="noreferrer">#159</a>: a\xA0comment standing in\xA0front of\xA0a\xA0parenthesised group is filed behind it, and\xA0nothing the\xA0rule could say about\xA0such a\xA0selector would match the\xA0file.</li></ul></li><li>The\xA0<code>indentation</code> rule now measures the\xA0lines a\xA0comment used to\xA0hide from\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/194" target="_blank" rel="noreferrer">#194</a>). A\xA0selector line holding nothing but\xA0a\xA0block comment was blank in\xA0the\xA0copy the\xA0rule read and\xA0passed over\xA0in\xA0silence, and\xA0is brought to\xA0its level now, in\xA0a\xA0plain CSS file as\xA0under\xA0<code>postcss-scss</code>. A\xA0pair of\xA0literal slashes in\xA0a\xA0plain CSS selector\xA0— text to\xA0CSS, which\xA0has no comment of\xA0that\xA0kind\xA0— used to\xA0swallow the\xA0line break behind it whole, so\xA0the\xA0lines after\xA0it went unmeasured too, and\xA0are measured now. And\xA0a\xA0line that\xA0was already at\xA0its level behind a\xA0comment is no\xA0longer reported at\xA0a\xA0drifted position, so\xA0a\xA0false warning goes with\xA0the\xA0false silences.</li><li>The\xA0<code>indentation</code> rule now measures the\xA0lines standing behind an\xA0inline comment in\xA0a\xA0declaration&#39;s value or\xA0in\xA0an\xA0at-rule&#39;s parameters (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/236" target="_blank" rel="noreferrer">#236</a>). The\xA0search that\xA0finds the\xA0line breaks of\xA0such a\xA0text read the\xA0break closing an\xA0inline comment as\xA0part of\xA0that\xA0comment and\xA0never handed its position over, so\xA0every line after\xA0it went unmeasured: the\xA0second line of\xA0<code>a { b: 1px // c</code> drew nothing at\xA0all, where the\xA0same file spelled with\xA0a\xA0block comment of\xA0exactly that\xA0width was reported and\xA0brought to\xA0its level. That\xA0search is handed a\xA0copy with\xA0the\xA0comments blanked out of\xA0it now, the\xA0one the\xA0rules reading a\xA0value for\xA0a\xA0comma or\xA0a\xA0bang are already handed, so\xA0a\xA0value, the\xA0arguments of\xA0a\xA0function inside\xA0it and\xA0an\xA0at-rule&#39;s parameters are all measured whichever way their comments are spelled, under\xA0<code>postcss-scss</code> and\xA0<code>postcss-less</code> alike. A\xA0line of\xA0a\xA0selector holding nothing but\xA0an\xA0inline comment is measured too, which\xA0it had not\xA0been since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/194" target="_blank" rel="noreferrer">#194</a> left such a\xA0line blank in\xA0the\xA0copy the\xA0scan reads: every line is measured in\xA0the\xA0text the\xA0file spells instead, so\xA0<code>a,</code> followed by\xA0a\xA0line carrying only <code>// c</code> is brought to\xA0the\xA0level of\xA0the\xA0list like\xA0any other. In\xA0a\xA0value and\xA0in\xA0a\xA0set of\xA0parameters such a\xA0line was measured all along, and\xA0still is. And\xA0a\xA0double slash the\xA0file spells no comment with\xA0no\xA0longer hides the\xA0lines behind it, in\xA0a\xA0selector and\xA0in\xA0a\xA0set of\xA0parameters as\xA0in\xA0a\xA0value: <code>a { b: url(http://x/y.png)</code> followed by\xA0a\xA0mis-indented line is reported like\xA0any other, <code>@import url(http://x/y.css)</code> followed by\xA0one is too, and\xA0a\xA0plain CSS selector carrying such a\xA0pair no\xA0longer has the\xA0line break behind it swallowed along with\xA0the\xA0pair. The\xA0scan is told which\xA0syntax spelled the\xA0text now, so\xA0a\xA0double slash is read as\xA0a\xA0comment only where one is spelled that\xA0way\xA0— and\xA0the\xA0<code>*/</code> closing one block comment beside the\xA0<code>/*</code> opening the\xA0next is no\xA0longer read as\xA0a\xA0pair at\xA0all, so\xA0<code>a { b: 1px/*x*//*y*/</code> followed by\xA0a\xA0mis-indented line is reported as\xA0well.</li><li>The\xA0<code>indentation</code> rule now reports every mis-indented line of\xA0an\xA0at-rule&#39;s parameters holding comments, at\xA0the\xA0line and\xA0column the\xA0file spells (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/65" target="_blank" rel="noreferrer">#65</a>). The\xA0lines were sought in\xA0a\xA0copy of\xA0the\xA0parameters with\xA0the\xA0comments taken out: a\xA0line holding nothing but\xA0a\xA0comment was blank there and\xA0passed over\xA0in\xA0silence, and\xA0the\xA0warning that\xA0did appear was placed by\xA0an\xA0offset into\xA0that\xA0shorter text, at\xA0<code>1:30</code> where the\xA0file has <code>2:3</code>. The\xA0scan reads the\xA0copy the\xA0file spells now and\xA0the\xA0fix writes to\xA0it, so\xA0both mis-indented lines of\xA0the\xA0issue&#39;s <code>@use &#39;./button&#39; with ( … )</code> are reported, each asking for\xA0the\xA0<code>1 tab</code> it needs, and\xA0every comment survives the\xA0fix that\xA0used to\xA0strip all three.</li><li>The\xA0<code>value-list-comma-*</code>, <code>media-query-list-comma-*</code> and\xA0<code>function-comma-*</code> rules now report a\xA0comma standing behind an\xA0address (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/153" target="_blank" rel="noreferrer">#153</a>). The\xA0search that\xA0finds the\xA0commas of\xA0a\xA0value and\xA0of\xA0a\xA0media query list read the\xA0double slash of\xA0<code>url(http://x/y.png)</code> as\xA0the\xA0opening of\xA0an\xA0inline comment, so\xA0everything from\xA0there to\xA0the\xA0end of\xA0the\xA0line was comment text to\xA0it. In\xA0a\xA0value that\xA0took the\xA0closing parenthesis of\xA0the\xA0address with\xA0it, and\xA0the\xA0count of\xA0open parentheses never came back down, so\xA0every comma that\xA0followed\xA0— on\xA0the\xA0address&#39;s line and\xA0on\xA0every line after\xA0it\xA0— stood inside\xA0a\xA0function&#39;s arguments as\xA0far as\xA0the\xA0search was concerned, where it passes them over; a\xA0media query list is read without\xA0skipping a\xA0function&#39;s arguments, so\xA0there it was the\xA0commas on\xA0the\xA0address&#39;s own line that\xA0were lost. The\xA0four\xA0<code>function-comma-*</code> rules read the\xA0same double slash the\xA0same way, through\xA0a\xA0pattern of\xA0their own, and\xA0said nothing about\xA0the\xA0comma of\xA0<code>image-set(url(//cdn/a.png) 1x ,url(//cdn/b.png) 2x)</code>. All twelve\xA0are told which\xA0double slash opens a\xA0comment and\xA0which\xA0only looks like\xA0one now, so\xA0<code>a { b: url(http://x/y.png),&#39;z&#39;; }</code> is reported like\xA0any other value list, and\xA0fixed by\xA0the\xA0ten\xA0of\xA0them that\xA0fix anything. Nothing about\xA0this needs a\xA0comment anywhere, or\xA0any syntax but\xA0plain CSS: an\xA0address on\xA0its own was enough.</li><li>The\xA0<code>declaration-bang-space-*</code>, <code>media-feature-colon-space-*</code> and\xA0<code>media-feature-range-operator-space-*</code> rules now read what stands behind an\xA0address (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/215" target="_blank" rel="noreferrer">#215</a>). The\xA0search these rules place their matches with\xA0reads a\xA0double slash as\xA0the\xA0opening of\xA0an\xA0inline comment wherever it stands, the\xA0one in\xA0<code>url(http://x)</code> among them, and\xA0everything from\xA0there to\xA0the\xA0end of\xA0the\xA0line was comment text to\xA0it, so\xA0<code>a { b: url(http://x)!important; }</code> drew nothing from\xA0either bang rule and\xA0the\xA0second feature of\xA0<code>@media (min-width: url(http://x)) and (max-width:1px)</code> drew nothing from\xA0any of\xA0the\xA0other four. They are told which\xA0double slash opens a\xA0comment and\xA0which\xA0only looks like\xA0one now, as\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/153" target="_blank" rel="noreferrer">#153</a> told the\xA0comma rules; nothing about\xA0this needs a\xA0comment anywhere, or\xA0any syntax but\xA0plain CSS.</li><li>The\xA0<code>value-list-comma-*</code>, <code>media-query-list-comma-*</code>, <code>function-comma-*</code>, <code>declaration-bang-space-*</code>, <code>media-feature-colon-space-*</code> and\xA0<code>media-feature-range-operator-space-*</code> rules now read three\xA0double slashes the\xA0file does not\xA0spell a\xA0comment with\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/216" target="_blank" rel="noreferrer">#216</a>). The\xA0search these rules place their matches with\xA0was handed a\xA0copy in\xA0which\xA0every double slash that\xA0opens no comment is written over, and\xA0three\xA0of\xA0them could not\xA0be: the\xA0<code>*/</code> closing one block comment and\xA0the\xA0<code>/*</code> opening the\xA0next spell a\xA0pair between\xA0them, and\xA0neither slash can be written over\xA0without\xA0taking a\xA0comment away, so\xA0<code>1px/*x*//*y*/,2px</code> drew nothing; a\xA0pair whose second slash opens a\xA0block comment was held back for\xA0the\xA0same reason, though\xA0the\xA0one in\xA0<code>url(a//*b*/)</code> opens nothing, so\xA0<code>url(a//*b*/),2px</code> drew nothing either; and\xA0a\xA0pair standing in\xA0plain CSS, which\xA0spells no comment that\xA0way at\xA0all, was read as\xA0one, so\xA0<code>myurl(//a),2px</code>, <code>image-url(//a),2px</code> and\xA0<code>a//b,2px</code> drew nothing either. The\xA0comments are blanked out of\xA0the\xA0copy now, delimiters and\xA0all, so\xA0no reader is left to\xA0find them for\xA0itself, and\xA0a\xA0double slash is taken for\xA0a\xA0comment only where the\xA0syntax spells one\xA0— which\xA0plain CSS does not, and\xA0<code>postcss-scss</code> and\xA0<code>postcss-less</code> do.</li><li>The\xA0<code>function-whitespace-after</code> rule now reads what stands behind an\xA0address (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/225" target="_blank" rel="noreferrer">#225</a>). It places its matches with\xA0the\xA0same search the\xA0rules above do, and\xA0was left on\xA0the\xA0old reading of\xA0it, so\xA0the\xA0closing parenthesis of\xA0<code>a { b: myurl(//a)red; }</code> drew nothing. It is handed the\xA0copy the\xA0others are handed now. The\xA0blindness was never plain CSS&#39;s alone: the\xA0search reads the\xA0double slash of\xA0<code>url(http://x)</code> as\xA0opening a\xA0comment whatever the\xA0file is written in, so\xA0<code>@import url(http://x)screen;</code> drew nothing under\xA0<code>postcss-scss</code> and\xA0<code>postcss-less</code> either, and\xA0the\xA0parenthesis in\xA0front of\xA0<code>screen</code> is reported now whichever of\xA0the\xA0three\xA0the\xA0file is.</li><li>The\xA0<code>media-feature-parentheses-space-inside</code> rule now reads a\xA0media feature standing behind a\xA0double slash the\xA0file spells no comment with\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/225" target="_blank" rel="noreferrer">#225</a>). It finds the\xA0comments of\xA0the\xA0parameters for\xA0itself, to\xA0keep <code>postcss-value-parser</code> out of\xA0their text, and\xA0read a\xA0double slash as\xA0opening one whatever syntax had spelled them, so\xA0everything behind <code>myurl(//a)</code> was comment text to\xA0it and\xA0neither space of\xA0the\xA0second feature of\xA0<code>@media (myurl(//a)) and ( min-width:1px )</code> was reported. That\xA0scan is told which\xA0syntax spelled the\xA0parameters now, as\xA0the\xA0searches above are. An\xA0address was never the\xA0trouble here: the\xA0scan told the\xA0double slash of\xA0<code>url(http://x)</code> from\xA0a\xA0comment all along.</li><li>Every <code>*-multi-line</code> option now counts a\xA0form feed as\xA0the\xA0line break it is (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/244" target="_blank" rel="noreferrer">#244</a>). <code>isSingleLineString</code>\xA0— which\xA0<code>whitespaceChecker</code> asks for\xA0every one of\xA0those options, and\xA0which\xA0<code>block-closing-brace-empty-line-before</code>, <code>block-closing-brace-newline-before</code>, <code>function-parentheses-newline-inside</code> and\xA0<code>function-parentheses-space-inside</code> ask directly\xA0— read a\xA0line feed and\xA0a\xA0carriage return and\xA0nothing else, so\xA0a\xA0construct broken with\xA0a\xA0form feed alone passed for\xA0a\xA0single line and\xA0every multi-line option passed it over\xA0in\xA0silence: <code>a {color: pink;\\ftop: 0;}</code> drew nothing from\xA0<code>block-opening-brace-newline-after</code>, <code>a { b: fn(1,\\f2); }</code> nothing from\xA0<code>function-parentheses-newline-inside</code>. Thirty-five\xA0rules ask that\xA0question, and\xA0all of\xA0them read the\xA0three\xA0characters every syntax this plugin reads through\xA0ends a\xA0line on\xA0now, as\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/173" target="_blank" rel="noreferrer">#173</a>, <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/196" target="_blank" rel="noreferrer">#196</a>, <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/204" target="_blank" rel="noreferrer">#204</a> and\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/209" target="_blank" rel="noreferrer">#209</a> each taught one reading before\xA0them. Two\xA0rules were left half repaired by\xA0that\xA0change, each because\xA0a\xA0second reading of\xA0its own counted line feeds alone, and\xA0each already wrong that\xA0way for\xA0a\xA0bare carriage return: <code>block-closing-brace-empty-line-before</code> read an\xA0empty line as\xA0two\xA0line feeds, which\xA0is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/245" target="_blank" rel="noreferrer">#245</a>, and\xA0<code>function-parentheses-newline-inside</code> asked whether the\xA0break it wants is already standing by\xA0looking for\xA0a\xA0line feed, which\xA0is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/246" target="_blank" rel="noreferrer">#246</a>. Both are answered below.</li><li>The\xA0<code>no-eol-whitespace</code> rule now sees the\xA0whitespace at\xA0the\xA0end of\xA0a\xA0line a\xA0form feed ends (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/247" target="_blank" rel="noreferrer">#247</a>). The\xA0scan that\xA0finds where the\xA0lines end read a\xA0line feed and\xA0a\xA0carriage return and\xA0nothing else, so\xA0the\xA0space in\xA0<code>a { color: pink; \\f}</code> was reported by\xA0none of\xA0the\xA0rule&#39;s options and\xA0left standing by\xA0<code>--fix</code>, while\xA0the\xA0same stylesheet spelled with\xA0any of\xA0the\xA0other three\xA0breaks was reported at\xA0<code>1:17</code> and\xA0came back trimmed. The\xA0three\xA0characters every syntax this plugin reads through\xA0ends a\xA0line on\xA0are read now, in\xA0the\xA0text of\xA0a\xA0comment as\xA0in\xA0the\xA0CSS around it, at\xA0the\xA0end of\xA0the\xA0file, and\xA0where <code>ignore: [&quot;empty-lines&quot;]</code> measures a\xA0line for\xA0emptiness. The\xA0warning keeps the\xA0position postcss gives it, which\xA0counts a\xA0form feed as\xA0no line at\xA0all, so\xA0a\xA0file broken with\xA0one draws every one of\xA0its warnings on\xA0line\xA01\xA0— a\xA0reading of\xA0the\xA0file that\xA0happens before\xA0any rule of\xA0this plugin and\xA0is not\xA0one it can put right.</li><li>The\xA0<code>function-whitespace-after</code> rule now reads a\xA0call whose name closes on\xA0a\xA0digit or\xA0an\xA0underscore where no other call is open around it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/230" target="_blank" rel="noreferrer">#230</a>). The\xA0search the\xA0rule used to\xA0place its matches with\xA0read a\xA0parenthesis as\xA0a\xA0call&#39;s only where an\xA0ASCII letter stood in\xA0front of\xA0it, and\xA0let every parenthesis standing inside\xA0a\xA0call&#39;s arguments past whatever named it, so\xA0<code>a { b: atan2(1,2)red; }</code>, <code>a { b: a_(1)red; }</code> and\xA0<code>@import col-6(a)screen;</code> drew nothing while\xA0the\xA0same three\xA0calls nested inside\xA0another were reported all along. The\xA0rule looks for\xA0the\xA0calls of\xA0the\xA0text now, rather than\xA0coming across\xA0the\xA0parentheses of\xA0it. The\xA0<code>never</code> half of\xA0an\xA0escaped parenthesis is read now too: <code>a { b: url(a\\)b) red; }</code> drew nothing, the\xA0search having placed the\xA0address&#39;s closing parenthesis two\xA0characters early, and\xA0the\xA0space behind the\xA0address is closed up like\xA0any other.</li><li>The\xA0<code>function-whitespace-after</code> rule now reads a\xA0call whose name CSS spells in\xA0a\xA0way a\xA0run of\xA0ASCII characters cannot (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/252" target="_blank" rel="noreferrer">#252</a>). The\xA0name was read back from\xA0the\xA0parenthesis as\xA0a\xA0run of\xA0letters, digits, underscores and\xA0hyphens, and\xA0an\xA0identifier is wider than\xA0that\xA0in\xA0three\xA0directions and\xA0is not\xA0a\xA0run of\xA0characters at\xA0all: it may hold code points outside ASCII, it may close on\xA0a\xA0hyphen, it may open on\xA0anything behind a\xA0leading <code>--</code>, and\xA0an\xA0escape spells one of\xA0its characters with\xA0several. So\xA0<code>a { b: 日本(1)red; }</code>, <code>a { b: foo-(1)red; }</code>, <code>a { b: --1(2)red; }</code> and\xA0<code>a { b: fo\\6f(1)red; }</code>\xA0— the\xA0last of\xA0them <code>foo</code>, spelled with\xA0an\xA0escape\xA0— drew nothing under\xA0either option. Less names one call by\xA0an\xA0operator rather than\xA0by\xA0an\xA0identifier, and\xA0<code>a { b: %(&quot;%dpx&quot;, @a)red; }</code> drew nothing either. The\xA0name is read forwards now, the\xA0way the\xA0scan already steps over\xA0a\xA0backslash, so\xA0all five\xA0are read wherever they stand. The\xA0refusal that\xA0keeps a\xA0dimension from\xA0naming a\xA0call reaches across\xA0such a\xA0character now as\xA0well, so\xA0<code>a { b: 2\\65 f(1)red; }</code> and\xA0<code>a { b: 2日e(1)red; }</code> stop being read: an\xA0escape and\xA0a\xA0code point outside ASCII are characters of\xA0a\xA0name, and\xA0a\xA0name opening on\xA0a\xA0digit is none.</li><li>The\xA0<code>media-feature-colon-space-*</code>, <code>media-feature-range-operator-space-*</code> and\xA0<code>media-query-list-comma-*</code> rules now leave the\xA0arguments of\xA0such a\xA0call alone (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/252" target="_blank" rel="noreferrer">#252</a>). These eight\xA0read a\xA0media query out of\xA0every parenthesis no call opened, so\xA0a\xA0call whose name they could not\xA0read handed them its arguments: <code>@media a-(max-width:600px)</code> and\xA0<code>@media fo\\6f(max-width:600px)</code> were reported as\xA0a\xA0feature, and\xA0a\xA0fix written there landed inside\xA0the\xA0call. It runs the\xA0other way too, where the\xA0wider reading refuses a\xA0run the\xA0narrow one accepted: <code>@media 2日e(max-width:600px)</code> and\xA0<code>@media 2\\65 f(max-width:600px)</code> hold a\xA0feature rather than\xA0a\xA0call&#39;s arguments now, an\xA0escape and\xA0a\xA0code point outside ASCII being characters of\xA0a\xA0name and\xA0a\xA0name opening on\xA0a\xA0digit being none. The\xA0parentheses of\xA0a\xA0call are passed over, whatever the\xA0file names it\xA0— save for\xA0<code>and</code>, <code>or</code>, <code>not</code> and\xA0<code>only</code>, which\xA0join two\xA0media features rather than\xA0open a\xA0call whatever the\xA0case they are written in, and\xA0which\xA0these rules go on\xA0reading as\xA0they always have.</li><li>The\xA0<code>media-feature-colon-space-*</code>, <code>media-feature-range-operator-space-*</code> and\xA0<code>media-query-list-comma-*</code> rules now read a\xA0media query whose parenthesis stands behind something that\xA0names no call (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/230" target="_blank" rel="noreferrer">#230</a>). Nothing is named in\xA0front of\xA0the\xA0parentheses of\xA0<code>@media -(min-width:1px)</code>, <code>@media 2-(width&gt;=1px)</code>, <code>@media 2(a,b)</code> or\xA0<code>@media 2and(a,b)</code>, a\xA0number and\xA0an\xA0operator being all that\xA0stands there; all four\xA0used to\xA0be read as\xA0a\xA0call&#39;s arguments, which\xA0these eight\xA0rules pass over, and\xA0every colon, range operator and\xA0comma inside\xA0them went unreported.</li><li>The\xA0<code>block-closing-brace-empty-line-before</code> rule now counts an\xA0empty line however the\xA0file spells its breaks (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/245" target="_blank" rel="noreferrer">#245</a>). An\xA0empty line was read as\xA0two\xA0line feeds with\xA0nothing but\xA0horizontal whitespace between\xA0them, so\xA0a\xA0block broken with\xA0bare carriage returns or\xA0with\xA0form feeds had none to\xA0the\xA0rule whatever it held: under\xA0<code>never</code> <code>a {\\rcolor: pink;\\r\\r}</code> and\xA0<code>a {\\fcolor: pink;\\f\\f}</code> drew nothing where their line-feed twin was reported and\xA0trimmed. Each of\xA0the\xA0four\xA0spellings is read as\xA0the\xA0break it is now, a\xA0Windows pair counting as\xA0one, so\xA0a\xA0lone <code>\\r\\n</code> still leaves no empty line behind it.</li><li>The\xA0<code>media-feature-range-operator-space-*</code> rules now read a\xA0range operator standing right behind another one (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/240" target="_blank" rel="noreferrer">#240</a>). The\xA0search that\xA0places their matches reports every two-character operator twice\xA0— once whole, and\xA0once again by\xA0the\xA0<code>=</code> its second character matches on\xA0the\xA0next index\xA0— and\xA0the\xA0guard that\xA0dropped the\xA0second of\xA0the\xA0two\xA0asked whether a\xA0<code>&lt;</code> or\xA0a\xA0<code>&gt;</code> stood in\xA0front of\xA0the\xA0match rather than\xA0where the\xA0operator before\xA0it ended. So\xA0every match standing behind one of\xA0those two\xA0characters was passed over, whatever it was: the\xA0whole of\xA0the\xA0<code>&gt;=</code> in\xA0<code>@media (a&lt;&gt;=b)</code> and\xA0the\xA0second <code>&lt;</code> of\xA0<code>@media (a&lt;&lt;b)</code> alike, each read as\xA0the\xA0leftovers of\xA0its neighbour rather than\xA0as\xA0an\xA0operator of\xA0its own. In\xA0<code>media-feature-range-operator-space-after</code> that\xA0silence lasted until\xA0a\xA0run of\xA0<code>--fix</code> had put a\xA0space between\xA0the\xA0two, so\xA0a\xA0row of\xA0such operators took as\xA0many runs as\xA0the\xA0row is long, each run reporting a\xA0problem the\xA0one before\xA0it had left; in\xA0<code>media-feature-range-operator-space-before</code> it never ended, since\xA0the\xA0space that\xA0rule writes goes in\xA0front of\xA0the\xA0row rather than\xA0between\xA0its operators. A\xA0row of\xA0<code>&gt;=</code> was read in\xA0full all along, every member of\xA0it ending in\xA0<code>=</code>; one run of\xA0either rule now reports every operator of\xA0any row and\xA0writes every space its option asks for.</li><li>The\xA0<code>declaration-block-trailing-semicolon</code> rule now reads the\xA0declaration a\xA0comment closes the\xA0block behind (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/217" target="_blank" rel="noreferrer">#217</a>). Such a\xA0comment is a\xA0node of\xA0its own, and\xA0the\xA0last one of\xA0the\xA0block, so\xA0the\xA0declaration in\xA0front of\xA0it was never looked at: <code>a { color: pink /* c */ }</code> drew nothing under\xA0<code>always</code> where <code>a { color: pink }</code> drew <code>Expected a trailing semicolon</code>, <code>never</code> passed <code>a { color: pink; /* c */ }</code> over\xA0the\xA0same way, and\xA0<code>--fix</code> left both files as\xA0they were. The\xA0block&#39;s <code>raws.semicolon</code>, which\xA0the\xA0rule reads to\xA0tell whether the\xA0semicolon is there, speaks of\xA0the\xA0last node that is not\xA0a\xA0comment, and\xA0the\xA0declaration is now sought the\xA0same way, so\xA0the\xA0two\xA0agree by\xA0construction. A\xA0nested rule or\xA0at-rule closing the\xA0block is not\xA0passed over: the\xA0flag speaks of\xA0it then, and\xA0the\xA0declaration in\xA0front of\xA0it keeps its own semicolon. Under\xA0<code>postcss-scss</code> this reaches a\xA0declaration an\xA0inline comment closes the\xA0block behind as\xA0well, and\xA0the\xA0semicolon is written in\xA0front of\xA0that\xA0comment rather than\xA0into\xA0it. Where <code>never</code> finds the\xA0semicolon behind a\xA0bodiless at-rule or\xA0behind a\xA0custom property with\xA0a\xA0comment closing the\xA0block, the\xA0problem is reported rather than\xA0fixed: PostCSS writes that\xA0semicolon back whatever the\xA0flag is set to, since\xA0without\xA0it the\xA0comment would be read as\xA0more of\xA0the\xA0at-rule&#39;s parameters or\xA0of\xA0the\xA0custom property&#39;s value.</li><li>The\xA0<code>declaration-block-trailing-semicolon</code> rule under\xA0<code>never</code> now reads the\xA0semicolon the\xA0block actually ends on, wherever in\xA0the\xA0block that\xA0semicolon stands (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/325" target="_blank" rel="noreferrer">#325</a>). The\xA0block&#39;s <code>raws.semicolon</code>, which\xA0the\xA0rule read, speaks of\xA0one semicolon alone\xA0— the\xA0one written straight behind the\xA0last node\xA0— and\xA0any other is parsed into\xA0another raw entirely: the\xA0block&#39;s own trailing raw, or\xA0the\xA0leading raw of\xA0a\xA0comment closing the\xA0block. So\xA0under\xA0<code>postcss-scss</code> a\xA0Sass nested property carrying a\xA0semicolon behind its closing brace was passed over\xA0in\xA0silence, that\xA0character living in\xA0the\xA0block&#39;s trailing raw while\xA0the\xA0flag stayed unset; it is reported now, and\xA0dart-sass compiles such a\xA0file with\xA0it and\xA0without\xA0it, so\xA0it is taken away like\xA0any other. Where the\xA0rule did report, the\xA0fix reached the\xA0flag&#39;s semicolon and\xA0no further: <code>a { color: pink;; }</code> came back out of\xA0<code>--fix</code> as\xA0<code>a { color: pink; }</code>, the\xA0run ended clean over\xA0a\xA0file the\xA0rule still had something to\xA0say about, and\xA0the\xA0next parse read what was left as\xA0the\xA0flag&#39;s in\xA0its turn, so\xA0a\xA0second run was needed to\xA0finish the\xA0work. Every semicolon standing behind the\xA0node that\xA0closes the\xA0block is taken away now, in\xA0the\xA0one run, and\xA0the\xA0whitespace around each of\xA0them is left where the\xA0file puts it\xA0— wherever the\xA0fix runs at\xA0all, that is: behind a\xA0custom property or\xA0a\xA0bodiless at-rule that\xA0a\xA0comment closes the\xA0block behind, it is still declined whole, since\xA0PostCSS writes one of\xA0those semicolons back whatever the\xA0flag says. The\xA0warning stands on\xA0the\xA0semicolon it is named for\xA0as\xA0well, rather than\xA0at\xA0the\xA0end of\xA0the\xA0declaration in\xA0front of\xA0it\xA0— <code>1:17</code> in\xA0that\xA0block, where it used to\xA0be <code>1:15</code>\xA0— and\xA0it is counted in\xA0the\xA0file rather than\xA0in\xA0the\xA0printed copy of\xA0the\xA0node, so\xA0an\xA0inline comment inside\xA0a\xA0declaration no\xA0longer moves it. The\xA0<code>always</code> option is untouched: what it asks for\xA0is a\xA0semicolon closing the\xA0last node, and\xA0one standing further behind answers nothing about\xA0that, which\xA0is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/336" target="_blank" rel="noreferrer">#336</a>.</li><li>The\xA0<code>indentation</code> rule now measures the\xA0lines of\xA0a\xA0file broken with\xA0a\xA0bare carriage return or\xA0a\xA0form feed (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/294" target="_blank" rel="noreferrer">#294</a>). A\xA0carriage return ends a\xA0line to\xA0every syntax the\xA0plugin reads through, and\xA0a\xA0form feed ends one to\xA0CSS and\xA0to\xA0Sass; the\xA0rule ended a\xA0line on\xA0a\xA0line feed alone, so\xA0a\xA0file broken with\xA0either was a\xA0single line to\xA0it and\xA0nothing in\xA0it was measured. A\xA0declaration standing at\xA0no indentation drew nothing, nor\xA0did a\xA0closing brace, a\xA0selector list, a\xA0value or\xA0a\xA0set of\xA0parameters broken that\xA0way, and\xA0<code>--fix</code> left the\xA0file as\xA0it was. All four\xA0spellings are read now, in\xA0the\xA0copy the\xA0search for\xA0the\xA0lines is handed and\xA0in\xA0the\xA0whitespace in\xA0front of\xA0a\xA0node and\xA0of\xA0a\xA0closing brace alike, and\xA0the\xA0fix writes back the\xA0break the\xA0file is spelled with\xA0instead of\xA0a\xA0line feed of\xA0its own. The\xA0contents of\xA0a\xA0styled template are read the\xA0same way, being a\xA0stylesheet; the\xA0JavaScript around it is not, since\xA0JavaScript ends no line on\xA0a\xA0form feed. Less ends no line on\xA0a\xA0form feed at\xA0all, and\xA0keeps one written inside\xA0a\xA0value there; the\xA0rule reads all three\xA0characters under\xA0every syntax alike, so\xA0such a\xA0value is measured as\xA0two\xA0lines and\xA0the\xA0fix writes the\xA0indentation into\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/373" target="_blank" rel="noreferrer">#373</a>). A\xA0stylesheet embedded in\xA0a\xA0page is measured too, the\xA0level it is indented to\xA0being read off the\xA0markup around it: a\xA0line separator of\xA0Unicode standing in\xA0a\xA0comment or\xA0a\xA0string of\xA0that\xA0stylesheet used to\xA0set that\xA0level wrong and\xA0have <code>--fix</code> outdent the\xA0whole block, since\xA0the\xA0reading was left to\xA0a\xA0flag whose idea of\xA0a\xA0line is JavaScript&#39;s rather than\xA0CSS&#39;s.</li><li>The\xA0<code>block-closing-brace-newline-before</code> and\xA0<code>block-closing-brace-empty-line-before</code> rules now report the\xA0whitespace in\xA0front of\xA0a\xA0closing brace an\xA0at-rule with\xA0neither a\xA0block nor\xA0a\xA0semicolon of\xA0its own closes (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/292" target="_blank" rel="noreferrer">#292</a>). The\xA0whitespace there is filed into\xA0the\xA0at-rule rather than\xA0into\xA0the\xA0block, and\xA0both rules read the\xA0block&#39;s own raw, which\xA0is empty, so\xA0<code>never-multi-line</code> passed over\xA0the\xA0run it is named for\xA0and\xA0<code>never</code> passed over\xA0an\xA0empty line standing in\xA0plain sight: <code>a {</code> followed by\xA0a\xA0break, <code>@extend .b</code> and\xA0two\xA0breaks drew nothing at\xA0all from\xA0either. Both report it now.</li><li>The\xA0<code>named-grid-areas-alignment</code> rule now reports rows aligned to\xA0the\xA0width of\xA0a\xA0row standing in\xA0an\xA0end-of-line comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/322" target="_blank" rel="noreferrer">#322</a>). <code>a { grid-template-areas: &quot;a a&quot;</code> with\xA0<code>// &quot;bbb bbb&quot;</code> and\xA0<code>&quot;c c&quot;</code> on\xA0the\xA0lines below satisfied the\xA0rule before, the\xA0widest cell of\xA0the\xA0three\xA0rows being the\xA0one written in\xA0the\xA0comment, which\xA0is no row of\xA0the\xA0grid. It is reported now and\xA0written back as\xA0<code>&quot;a a&quot;</code> and\xA0<code>&quot;c c&quot;</code>, and\xA0the\xA0comment&#39;s text is left exactly as\xA0it was.</li><li>The\xA0five\xA0<code>at-rule-*</code> rules and\xA0<code>no-extra-semicolons</code> now read an\xA0at-rule spelled without\xA0a\xA0space in\xA0front of\xA0its options (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/357" target="_blank" rel="noreferrer">#357</a>). Every parser this plugin reads gives <code>@import(reference) &quot;x&quot;</code>, <code>@supports(a: b)</code>, <code>@layer(l)</code>, <code>@plugin(args) &quot;p&quot;</code> and\xA0<code>@whatever(x)</code> the\xA0shape it gives a\xA0call to\xA0a\xA0Less detached ruleset, and\xA0the\xA0plugin told a\xA0call from\xA0an\xA0at-rule by\xA0the\xA0parenthesis the\xA0parameters open with, so\xA0all five\xA0were walked past\xA0— in\xA0plain CSS and\xA0under\xA0Sass as\xA0much as\xA0under\xA0Less. <code>@LAYER (l);</code> was recased under\xA0<code>lower</code> and\xA0<code>@LAYER(l);</code> was not. A\xA0detached ruleset takes no arguments, so\xA0a\xA0call to\xA0one is told now by\xA0the\xA0<code>()</code> its parameters open on\xA0— and\xA0<code>@dr ()</code>, which\xA0Less reads as\xA0an\xA0at-rule like\xA0any other, is read as\xA0one here too.</li><li>The\xA0<code>declaration-block-trailing-semicolon</code> rule under\xA0<code>never</code> now takes away the\xA0semicolon behind a\xA0call to\xA0a\xA0Less detached ruleset that\xA0carries a\xA0lookup (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/357" target="_blank" rel="noreferrer">#357</a>). Such a\xA0call was read as\xA0an\xA0at-rule and\xA0its semicolon left standing, on\xA0the\xA0ground that\xA0anything behind the\xA0parentheses ends the\xA0call; <code>less@4.9.0</code> inlines <code>@dr()[key]</code> exactly as\xA0it inlines <code>@dr()</code>, so\xA0it is a\xA0call and\xA0parts with\xA0that\xA0semicolon. A\xA0space in\xA0front of\xA0the\xA0parentheses does end the\xA0call, and\xA0<code>@dr ()</code> keeps its semicolon with\xA0the\xA0at-rules.</li><li>The\xA0<code>max-empty-lines</code> rule now counts the\xA0empty line a\xA0file ends on\xA0whether or\xA0not\xA0a\xA0run of\xA0spaces or\xA0tabs is written behind the\xA0last line break (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/390" target="_blank" rel="noreferrer">#390</a>). Where the\xA0file ends was read as\xA0the\xA0last character of\xA0the\xA0text, so\xA0such a\xA0run stood between\xA0that\xA0and\xA0the\xA0file&#39;s last break, and\xA0the\xA0line this rule adds to\xA0the\xA0count for\xA0a\xA0file that\xA0ends on\xA0a\xA0break went uncounted: <code>a {}</code> closed by\xA0two\xA0line feeds was reported under\xA0a\xA0maximum of\xA0one, and\xA0the\xA0same file with\xA0three\xA0spaces written behind them was not. That\xA0run is a\xA0line of\xA0its own and\xA0emptying it is what <code>no-eol-whitespace</code> is for, so\xA0the\xA0count no\xA0longer turns on\xA0it\xA0— the\xA0empty line is reported and\xA0taken away, and\xA0the\xA0spaces are left exactly where they stood. Under\xA0a\xA0maximum of\xA0zero nothing changes, since\xA0that\xA0option adds no line at\xA0the\xA0end of\xA0the\xA0file to\xA0begin with. What the\xA0old reading was costing is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/390" target="_blank" rel="noreferrer">#390</a>: <code>no-missing-end-of-source-newline</code> takes such a\xA0run off, so\xA0whether the\xA0empty line behind it was counted at\xA0all depended on\xA0which\xA0of\xA0the\xA0two\xA0rules the\xA0configuration listed first.</li><li>The\xA0<code>unit-case</code> rule now names every unit of\xA0a\xA0word holding a\xA0multiplication, one warning apiece, and\xA0no\xA0longer names the\xA0word itself as\xA0one (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/297" target="_blank" rel="noreferrer">#297</a>). Such a\xA0word is more than\xA0one dimension, and\xA0<code>postcss-value-parser</code> has no reading of\xA0it: <code>valueParser.unit</code> answers any word opening with\xA0a\xA0number and\xA0calls everything standing behind that\xA0number a\xA0unit, so\xA0<code>10PX*2REM*3EM</code> came back as\xA0the\xA0unit <code>PX*2REM*3EM</code>\xA0— a\xA0name no language writes and\xA0no user can act on. The\xA0rule read the\xA0word both ways, part by\xA0part and\xA0whole, so\xA0under\xA0<code>lower</code> the\xA0same <code>PX</code> drew two\xA0warnings, one from\xA0each reading, while\xA0<code>REM</code> and\xA0<code>EM</code> drew none at\xA0all: the\xA0walk over\xA0the\xA0parts stopped at\xA0the\xA0first part it found something wrong with. Every part is walked now, and\xA0the\xA0whole-word reading is out of\xA0the\xA0report, so\xA0such a\xA0word draws one warning per\xA0unit, each underlining the\xA0unit it names. The\xA0fix reaches every unit of\xA0the\xA0word as\xA0it did before, since\xA0one write recases the\xA0whole of\xA0it. What it no\xA0longer recases is a\xA0word not\xA0one part of\xA0which\xA0holds a\xA0miscased unit: <code>a { b: 1px*A; }</code> came out of\xA0<code>--fix</code> under\xA0<code>lower</code> as\xA0<code>a { b: 1px*a; }</code>, and\xA0<code>a { b: 10px*2E5; }</code> as\xA0<code>a { b: 10px*2e5; }</code>, the\xA0whole-word reading having called <code>px*A</code> and\xA0<code>px*2E5</code> units. Neither is written now, and\xA0neither is written where some other word of\xA0the\xA0same value is fixed either. Inside\xA0<code>calc()</code> the\xA0value parser splits such a\xA0word into\xA0nodes of\xA0its own, and\xA0what is reported and\xA0written there does not\xA0change.</li><li>The\xA0<code>function-max-empty-lines</code> rule now pairs a\xA0call&#39;s parentheses the\xA0way the\xA0file writes them, a\xA0comment&#39;s aside (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/503" target="_blank" rel="noreferrer">#503</a>). <code>postcss-value-parser</code> closes a\xA0comment opening <code>/*/</code> on\xA0the\xA0star it opened with, and\xA0knows no comment opened by\xA0a\xA0double slash at\xA0all, so\xA0a\xA0closing parenthesis written in\xA0the\xA0text of\xA0either used to\xA0close the\xA0call for\xA0the\xA0rule, and\xA0the\xA0empty lines standing behind such a\xA0comment belonged to\xA0no call and\xA0were passed over\xA0in\xA0silence\xA0— under\xA0<code>postcss-scss</code> and\xA0<code>postcss-less</code> an\xA0ordinary end-of-line comment did it. The\xA0call is read in\xA0a\xA0copy of\xA0the\xA0value with\xA0every comment blanked now, so\xA0it runs to\xA0the\xA0parenthesis the\xA0file spells as\xA0code and\xA0the\xA0empty lines it holds are reported and\xA0collapsed. A\xA0quotation mark written in\xA0the\xA0text of\xA0such a\xA0comment opens no string either: it used to\xA0pair with\xA0the\xA0next quotation mark of\xA0the\xA0value, and\xA0a\xA0call standing behind that\xA0pair fell inside\xA0a\xA0string that\xA0ran to\xA0the\xA0end of\xA0the\xA0value, so\xA0the\xA0empty lines it held were passed over\xA0as\xA0well.</li><li>The\xA0<code>function-parentheses-newline-inside</code> and\xA0<code>function-parentheses-space-inside</code> rules now pair a\xA0call&#39;s parentheses across\xA0a\xA0quotation mark a\xA0comment leaves open (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/508" target="_blank" rel="noreferrer">#508</a>). <code>postcss-value-parser</code> opens a\xA0string on\xA0a\xA0mark written in\xA0the\xA0text of\xA0a\xA0comment it reads as\xA0code\xA0— one opened by\xA0a\xA0double slash, or\xA0one opening <code>/*/</code>, which\xA0it closes on\xA0the\xA0star it opened with\xA0— and\xA0pairs that\xA0mark with\xA0the\xA0next one of\xA0the\xA0value, so\xA0the\xA0string that\xA0pairing invents took the\xA0closing parenthesis the\xA0file writes: <code>a { b: g(1 /*/ &quot; */ &quot;1&quot;); }</code> reached both rules as\xA0a\xA0call the\xA0parser never closed, and\xA0a\xA0call marked unclosed is one neither of\xA0them reads a\xA0parenthesis of. The\xA0mark a\xA0comment leaves open is masked out of\xA0the\xA0copy the\xA0value is parsed in\xA0now, so\xA0such a\xA0call is closed where the\xA0file closes it and\xA0read like\xA0any other. A\xA0pair the\xA0comment closes around itself is left exactly as\xA0it stands: the\xA0string it opens reaches past nothing, and\xA0it hides from\xA0the\xA0parser whatever stands between\xA0the\xA0two\xA0marks\xA0— a\xA0parenthesis among it, which\xA0the\xA0parser would otherwise close a\xA0call on.</li><li>The\xA0<code>named-grid-areas-alignment</code> rule now measures a\xA0cell in\xA0the\xA0characters it is written with\xA0rather than\xA0in\xA0the\xA0code units it is stored in\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/520" target="_blank" rel="noreferrer">#520</a>). Every code point from\xA0U+10000\xA0up is an\xA0ident code point to\xA0the\xA0grammar, so\xA0a\xA0cell may be named with\xA0one, and\xA0<code>lightningcss</code> lays the\xA0grid out below and\xA0keeps the\xA0name\xA0— while\xA0JavaScript stores such a\xA0character as\xA0a\xA0pair of\xA0UTF-16 code units, and\xA0the\xA0width of\xA0a\xA0column, the\xA0padding written out to\xA0it and\xA0the\xA0width of\xA0a\xA0whole row under\xA0<code>alignQuotes</code> were all counted in\xA0those units. So\xA0a\xA0grid the\xA0file spells out of\xA0line was passed over\xA0in\xA0silence wherever the\xA0miscount happened to\xA0make the\xA0padding come out right, and\xA0<code>&quot;a𝐚b c&quot;</code> over\xA0<code>&quot;dd d&quot;</code>, written across\xA0two\xA0lines under\xA0the\xA0default <code>gap</code>, drew no warning at\xA0all; a\xA0grid the\xA0file already spells lined up was reported and\xA0then rewritten, <code>&quot;𝐚𝐚 ccc&quot;</code> over\xA0<code>&quot;b d&quot;</code> coming back with\xA0its second row as\xA0<code>&quot;b d&quot;</code>; and\xA0everywhere else the\xA0column came out as\xA0wide as\xA0the\xA0code units of\xA0its widest cell rather than\xA0as\xA0the\xA0characters of\xA0it, the\xA0padding falling on\xA0every cell of\xA0that\xA0column but\xA0the\xA0widest\xA0— as\xA0readily a\xA0row holding no such character as\xA0the\xA0row holding it\xA0— and\xA0<code>--fix</code> left the\xA0value out of\xA0line with\xA0no warning behind to\xA0say so, which\xA0is the\xA0one thing this rule is about. Plain CSS reaches all of\xA0it, with\xA0no comment and\xA0no custom syntax needed. A\xA0grapheme cluster spelled with\xA0several code points, and\xA0a\xA0character an\xA0editor draws two\xA0columns wide, are measured by\xA0neither reading and\xA0are questions of\xA0their own.</li><li>The\xA0<code>max-line-length</code> rule now finds the\xA0address of\xA0a\xA0<code>url()</code> the\xA0way the\xA0rest of\xA0the\xA0plugin does (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/427" target="_blank" rel="noreferrer">#427</a>). It looked for\xA0one with\xA0a\xA0pattern that\xA0asked nothing about\xA0the\xA0name in\xA0front of\xA0the\xA0parenthesis and\xA0ran to\xA0the\xA0last parenthesis of\xA0the\xA0line, so\xA0a\xA0line carrying an\xA0address and\xA0anything parenthesised behind it had that\xA0taken off its count as\xA0well: <code>a { background: url(a.png) no-repeat, linear-gradient(red, blue); }</code> was measured at\xA024\xA0characters where the\xA0address alone comes off at\xA062, and\xA0<code>@import url(a.css) screen and (orientation:landscape);</code> at\xA014\xA0rather than\xA049. A\xA0<code>url(</code> written inside\xA0a\xA0comment or\xA0a\xA0quoted string opened a\xA0call to\xA0that\xA0pattern too, and\xA0so\xA0did a\xA0call whose name merely ends in\xA0those three\xA0letters, <code>image-url(…)</code>, whose arguments were taken off the\xA0count as\xA0though\xA0they were an\xA0address. Only what a\xA0<code>url()</code> really holds comes off a\xA0line now, so\xA0a\xA0line that\xA0used to\xA0pass the\xA0limit in\xA0silence is reported. It cuts the\xA0other way as\xA0well: an\xA0address of\xA0a\xA0single character comes off the\xA0line like\xA0any other, where the\xA0pattern needed two, and\xA0an\xA0address whose three\xA0letters are spelled with\xA0an\xA0escape\xA0— <code>u\\rl(…)</code>, which\xA0CSS names <code>url</code>\xA0— comes off at\xA0all. What the\xA0<code>url()</code> function holds is read apart from\xA0what the\xA0<code>url()</code> token holds, so\xA0only the\xA0string comes off <code>url(&quot;a.woff2&quot;, format(&quot;woff2&quot;))</code> and\xA0the\xA0arguments behind it are counted, while\xA0the\xA0whole of\xA0what a\xA0token&#39;s parentheses hold still comes off, comment delimiters and\xA0all.</li><li>The\xA0<code>max-line-length</code> rule now finds the\xA0address of\xA0an\xA0<code>@import</code> with\xA0the\xA0same walk (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/552" target="_blank" rel="noreferrer">#552</a>). It looked for\xA0one with\xA0a\xA0pattern that\xA0ran to\xA0the\xA0last quotation mark of\xA0the\xA0line and\xA0asked nothing about\xA0what stood around the\xA0word it matched, so\xA0<code>@import &quot;a.css&quot;; a { b: &quot;x&quot;; }</code> was measured at\xA011\xA0characters where the\xA0address alone comes off at\xA023, and\xA0an\xA0<code>@import</code> written inside\xA0a\xA0comment or\xA0inside\xA0a\xA0string opened an\xA0at-rule of\xA0the\xA0stylesheet: <code>/* @import &quot;a.css&quot;; */ a { b: 1px; }</code> was measured at\xA029\xA0rather than\xA036, and\xA0<code>a { b: &quot;@import &#39;x.css&#39;; c&quot; }</code> at\xA018\xA0rather than\xA029. The\xA0line keeps everything the\xA0capture used to\xA0take past the\xA0address, and\xA0an\xA0<code>@import</code> standing in\xA0a\xA0comment or\xA0in\xA0a\xA0string takes nothing off it at\xA0all, so\xA0a\xA0line that\xA0used to\xA0pass the\xA0limit in\xA0silence is reported. It cuts the\xA0other way as\xA0well: the\xA0pattern asked for\xA0a\xA0whitespace run between\xA0the\xA0name and\xA0the\xA0address, which\xA0the\xA0grammar asks for\xA0none of, so\xA0the\xA0address of\xA0<code>@import&quot;a.css&quot;</code> and\xA0of\xA0<code>@import /* c */ &quot;a.css&quot;</code> comes off the\xA0line now, where neither used to\xA0come off at\xA0all. A\xA0name spelled with\xA0an\xA0escape names the\xA0same at-rule, <code>@i\\6dport</code> for\xA0one, which\xA0<code>lightningcss</code> and\xA0dart-sass both read as\xA0<code>@import</code>.</li><li>The\xA0<code>indentation</code> rule now measures the\xA0line a\xA0block&#39;s closing brace stands on\xA0wherever the\xA0parser filed the\xA0run in\xA0front of\xA0that\xA0brace (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/509" target="_blank" rel="noreferrer">#509</a>). An\xA0at-rule carrying neither a\xA0block nor\xA0a\xA0semicolon of\xA0its own runs to\xA0that\xA0brace, so\xA0PostCSS files everything standing between\xA0its parameters and\xA0the\xA0brace into\xA0the\xA0at-rule and\xA0leaves the\xA0block&#39;s own whitespace empty; the\xA0rule read the\xA0brace&#39;s line out of\xA0that\xA0empty raw alone, and\xA0the\xA0text it measures such an\xA0at-rule&#39;s parameters in\xA0is trimmed of\xA0exactly the\xA0run the\xA0brace stands behind, so\xA0the\xA0line was measured by\xA0nobody. <code>a {</code> with\xA0<code>@extend .b</code> indented once below it and\xA0the\xA0closing brace indented twice drew nothing at\xA0all under\xA0<code>&quot;tab&quot;</code>, where the\xA0same block written with\xA0a\xA0declaration in\xA0place of\xA0the\xA0at-rule, or\xA0with\xA0a\xA0semicolon behind it, drew <code>Expected indentation of 0 tabs</code> and\xA0was settled in\xA0one run; and\xA0under\xA0<code>indentClosingBrace</code> such a\xA0brace was never asked for\xA0the\xA0level the\xA0option adds. The\xA0run is read and\xA0written where the\xA0parser put it now\xA0— the\xA0raw the\xA0three\xA0<code>block-closing-brace-*-before</code> rules have read it in\xA0since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/292" target="_blank" rel="noreferrer">#292</a>\xA0— so\xA0such a\xA0brace is reported and\xA0brought to\xA0its level in\xA0one run of\xA0<code>--fix</code>, in\xA0a\xA0plain CSS file as\xA0under\xA0every syntax registered beside it. A\xA0comment the\xA0at-rule swallowed along with\xA0that\xA0run keeps the\xA0line it stands on, which\xA0is measured as\xA0a\xA0line of\xA0the\xA0block (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/510" target="_blank" rel="noreferrer">#510</a> below). A\xA0Less mixin call carrying a\xA0bang flag is left alone: <code>postcss-less</code> collects that\xA0call&#39;s whitespace from\xA0both sides of\xA0the\xA0flag and\xA0prints all of\xA0it on\xA0one, so\xA0the\xA0run in\xA0front of\xA0the\xA0brace belongs to\xA0neither raw (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/374" target="_blank" rel="noreferrer">#374</a>).</li><li>The\xA0<code>indentation</code> rule now reports a\xA0line an\xA0at-rule swallowed that\xA0stands a\xA0level deeper than\xA0the\xA0block it is a\xA0line of\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/510" target="_blank" rel="noreferrer">#510</a>). An\xA0at-rule carrying neither a\xA0block nor\xA0a\xA0semicolon of\xA0its own runs to\xA0the\xA0brace closing its block, and\xA0a\xA0comment written on\xA0a\xA0line of\xA0its own between\xA0the\xA0two\xA0is filed into\xA0the\xA0at-rule rather than\xA0into\xA0a\xA0node of\xA0the\xA0block; the\xA0rule measured it with\xA0the\xA0parameters, a\xA0level deeper than\xA0the\xA0block, so\xA0<code>a {</code> with\xA0<code>@extend .b</code> indented once and\xA0<code>/* c */</code> indented twice below it drew nothing under\xA0<code>&quot;tab&quot;</code>, where the\xA0same comment behind a\xA0declaration drew <code>Expected indentation of 1 tab</code>. It is reported and\xA0brought back now, and\xA0neither <code>except: [&quot;param&quot;]</code> nor\xA0<code>ignore: [&quot;param&quot;]</code> has a\xA0say over\xA0it, since\xA0the\xA0line is no parameter\xA0— under\xA0<code>ignore: [&quot;param&quot;]</code> such a\xA0line used to\xA0go unmeasured altogether. The\xA0comment at\xA0the\xA0block&#39;s level, which\xA0the\xA0rule used to\xA0report and\xA0<code>--fix</code> used to\xA0carry a\xA0level in, is under\xA0<code>Fixed</code> below.</li><li>The\xA0plugin now reads a\xA0backslash in\xA0front of\xA0a\xA0form feed or\xA0a\xA0bare carriage return as\xA0CSS does\xA0— a\xA0delimiter spelling nothing, as\xA0it already read one in\xA0front of\xA0a\xA0line feed or\xA0a\xA0Windows pair (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/566" target="_blank" rel="noreferrer">#566</a>). A\xA0form feed and\xA0a\xA0bare carriage return used to\xA0be read as\xA0characters an\xA0escape spells, so\xA0the\xA0name behind such a\xA0backslash was a\xA0name of\xA0its own, and\xA0every reading built on\xA0a\xA0name inherited it. A\xA0<code>url(</code> written behind one is the\xA0address it is to\xA0CSS now, and\xA0every rule reading such a\xA0text answers as\xA0it answers a\xA0plain <code>url(</code>: under\xA0<code>postcss-scss</code> and\xA0<code>postcss-less</code>, where a\xA0double slash inside\xA0the\xA0parentheses used to\xA0open a\xA0comment running to\xA0the\xA0end of\xA0the\xA0line, the\xA0<code>function-parentheses-newline-inside</code>, <code>function-parentheses-space-inside</code> and\xA0<code>value-list-comma-space-before</code> rules now report what stands beside such an\xA0address; and\xA0the\xA0<code>and(</code> of\xA0an\xA0<code>@media</code> written behind one, which\xA0Less compiles, is the\xA0feature <code>and(min-width:1px)</code> is to\xA0the\xA0media rules, so\xA0<code>media-feature-colon-space-after</code> now spaces its colon. Sass refuses a\xA0backslash in\xA0front of\xA0any of\xA0the\xA0four\xA0newlines, and\xA0Less refuses one in\xA0front of\xA0a\xA0<code>url(</code>, so\xA0much of\xA0what changes is what the\xA0plugin says about\xA0a\xA0file its compiler already rejects.</li><li>The\xA0<code>unit-case</code> rule now reads every dimension of\xA0a\xA0word <code>postcss-value-parser</code> hands over\xA0as\xA0one (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/526" target="_blank" rel="noreferrer">#526</a>). A\xA0unit ends at\xA0the\xA0first character that is no code point of\xA0an\xA0identifier, and\xA0not\xA0every such character parts the\xA0word that\xA0parser gives back: <code>10PX%2REM</code> is the\xA0dimension <code>10PX</code>, a\xA0delimiter and\xA0the\xA0dimension <code>2REM</code> to\xA0<code>@csstools/css-tokenizer</code>, <code>10PX.2REM</code> and\xA0<code>10PX+2REM</code> are two\xA0dimensions standing next to\xA0each other with\xA0nothing between, and\xA0a\xA0backslash in\xA0front of\xA0a\xA0line break is a\xA0delimiter of\xA0its own with\xA0the\xA0dimension <code>2REM</code> behind it\xA0— and\xA0<code>lightningcss</code> recases the\xA0units of\xA0both in\xA0every one of\xA0them. The\xA0rule cut such a\xA0word at\xA0the\xA0stars it spells and\xA0read each part as\xA0one dimension, so\xA0the\xA0second of\xA0any other pair was reached by\xA0nothing: <code>10PX%2REM</code> under\xA0<code>lower</code> came back <code>10px%2REM</code>. The\xA0word is read into\xA0its tokens now, by\xA0the\xA0tokenizer the\xA0plugin already depends on, and\xA0every dimension among them is reported and\xA0written on\xA0its own: <code>10px%2rem</code>, <code>10px.2rem</code>, <code>10px+2rem</code>, and\xA0the\xA0<code>2REM</code> standing on\xA0the\xA0line behind a\xA0backslash and\xA0a\xA0break. A\xA0star is one of\xA0those characters and\xA0reads as\xA0it did\xA0— <code>10PX*2REM</code> is two\xA0dimensions, <code>10PX\\*2REM</code> one (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/414" target="_blank" rel="noreferrer">#414</a>)\xA0— and\xA0under\xA0the\xA0<code>@stylistic/scss/</code> and\xA0<code>@stylistic/less/</code> namespaces the\xA0first dimension of\xA0a\xA0word an\xA0escaped star welds to\xA0a\xA0reading through\xA0a\xA0Sass module, <code>10PX\\*ns.$V</code>, is named again, <code>PX\\*ns</code>, the\xA0guard that\xA0turns such a\xA0value away being asked about\xA0the\xA0dimension rather than\xA0about\xA0the\xA0word.</li><li>The\xA0<code>indentation</code> rule now reports the\xA0continuation line of\xA0a\xA0statement opening on\xA0the\xA0line of\xA0its tag that\xA0used to\xA0answer for\xA0its own level (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/594" target="_blank" rel="noreferrer">#594</a>). <code>&lt;style&gt;a,</code> with\xA0<code>b {}</code> on\xA0the\xA0next line passed under\xA0<code>&quot;tab&quot;</code> at\xA0whatever depth the\xA0second selector stood, since\xA0the\xA0base level was read off that\xA0very line; it is asked for\xA0the\xA0tag line&#39;s level now, exactly as\xA0it is where the\xA0rule opens on\xA0a\xA0line of\xA0its own. The\xA0same holds for\xA0every line a\xA0statement on\xA0the\xA0tag&#39;s line is asked to\xA0keep at\xA0that\xA0level\xA0— a\xA0closing parenthesis of\xA0a\xA0value on\xA0a\xA0line of\xA0its own, and\xA0a\xA0value&#39;s continuation under\xA0<code>except: [&quot;value&quot;]</code>.</li><li>The\xA0<code>@stylistic/less/unit-case</code> rule now names the\xA0unit Less reads in\xA0front of\xA0an\xA0exponent (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/646" target="_blank" rel="noreferrer">#646</a>). To\xA0CSS an\xA0exponent belongs to\xA0the\xA0number, so\xA0<code>1E5PX</code> is one dimension whose number is <code>1E5</code>\xA0— <code>@csstools/css-tokenizer</code> reads it so, <code>lightningcss</code> compiles it to\xA0<code>100000px</code> and\xA0dart-sass to\xA0<code>100000PX</code>\xA0— and\xA0that is the\xA0reading the\xA0core keeps, with\xA0the\xA0<code>@stylistic/scss/</code> and\xA0<code>@stylistic/styled/</code> namespaces. Less reads a\xA0number as\xA0digits and\xA0at\xA0most one period, so\xA0it compiles <code>@v: 1E5PX;</code> to\xA0the\xA0dimension <code>1E</code>, whose unit is <code>E</code>, beside the\xA0dimension <code>5PX</code>, and\xA0<code>1E+5PX</code> to\xA0<code>6E</code>. The\xA0namespace inherited the\xA0core&#39;s reading and\xA0named <code>PX</code> alone, leaving the\xA0<code>E</code> in\xA0whatever case the\xA0file spelled it. Both units are named now, wherever Less reads a\xA0value\xA0— a\xA0variable&#39;s value, a\xA0set of\xA0media parameters, a\xA0calculation, the\xA0arguments of\xA0a\xA0call and\xA0a\xA0custom property\xA0— and\xA0in\xA0a\xA0declaration closing on\xA0a\xA0semicolon too, whose value Less stores unread: the\xA0case of\xA0an\xA0exponent is nothing to\xA0CSS either, <code>lightningcss</code> compiling <code>1E5px</code> and\xA0<code>1e5px</code> alike. A\xA0word the\xA0tokenizer reads as\xA0a\xA0number or\xA0a\xA0percentage carries a\xA0unit under\xA0this namespace as\xA0well, <code>1E5</code> being <code>1E</code> and\xA0<code>5</code>. With\xA0the\xA0same reading a\xA0digit ends a\xA0unit as\xA0a\xA0hyphen does: <code>10PX9</code> is the\xA0dimension <code>10PX</code> and\xA0the\xA0number <code>9</code> to\xA0Less, so\xA0the\xA0warning names <code>PX</code> where it used to\xA0name <code>PX9</code>\xA0— a\xA0digit has no case, and\xA0<code>--fix</code> writes what it wrote before.</li><li>The\xA0<code>@stylistic/less/unit-case</code> rule now reads the\xA0value of\xA0a\xA0Less variable under\xA0every spelling of\xA0the\xA0whitespace around its colon (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/577" target="_blank" rel="noreferrer">#577</a>). <code>postcss-less</code> marks such a\xA0declaration <code>variable</code> only where the\xA0colon closed the\xA0name, and\xA0the\xA0rule went by\xA0that\xA0mark, so\xA0<code>@v: 10PX</code> was reported while\xA0<code>@v : 10PX</code>, <code>@v :10PX</code>, <code>@v : 10PX</code> and\xA0the\xA0same declaration written with\xA0a\xA0tab or\xA0a\xA0line break in\xA0front of\xA0the\xA0colon were passed over\xA0— Less declares <code>@v</code> on\xA0every one of\xA0them and\xA0gives a\xA0use of\xA0it <code>10PX</code> in\xA0each. The\xA0shape of\xA0the\xA0node answers now\xA0— the\xA0parser&#39;s mark, or\xA0a\xA0node carrying no block whose params open on\xA0the\xA0colon\xA0— so\xA0the\xA0value is walked wherever it stands, in\xA0a\xA0block as\xA0at\xA0the\xA0root, with\xA0a\xA0comment or\xA0a\xA0bang flag in\xA0it, and\xA0<code>--fix</code> writes the\xA0params the\xA0stringifier prints for\xA0such a\xA0node. A\xA0directive Less falls back to\xA0where the\xA0text behind the\xA0colon parses as\xA0no expression, <code>@custom-media :x (min-width: 10PX)</code>, has its parameters read as\xA0a\xA0value with\xA0it, where a\xA0unit is a\xA0unit of\xA0CSS either way. Where neither whitespace nor\xA0a\xA0comment stands in\xA0front of\xA0the\xA0colon nothing changes: the\xA0parser welds the\xA0first word behind it into\xA0the\xA0at-rule&#39;s name and\xA0what is left in\xA0the\xA0params is no value of\xA0its own, so\xA0such a\xA0declaration is passed over\xA0whole as\xA0it was.</li><li>The\xA0<code>@stylistic/less/unit-case</code> rule now reads the\xA0part of\xA0a\xA0Less variable&#39;s value that\xA0the\xA0parser welds into\xA0the\xA0at-rule&#39;s name (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/649" target="_blank" rel="noreferrer">#649</a>). <code>postcss-less</code> marks such a\xA0declaration and\xA0closes its name at\xA0the\xA0colon only where the\xA0at-word ends there; where the\xA0at-word runs on, the\xA0word behind the\xA0colon goes into\xA0the\xA0name with\xA0it, so\xA0<code>@v:10PX 1px</code> comes back named <code>v:10PX</code> with\xA0<code>1px</code> for\xA0params and\xA0<code>@v:10PX</code> keeps its whole value in\xA0the\xA0name. Every rule of\xA0this plugin reads an\xA0at-rule&#39;s text out of\xA0its params, so\xA0that\xA0word was read by\xA0nobody and\xA0<code>@v:10PX 1px</code> drew nothing under\xA0<code>lower</code>. The\xA0value is gathered from\xA0the\xA0name, the\xA0raw behind it and\xA0the\xA0params now, and\xA0a\xA0fix is written back to\xA0the\xA0two\xA0of\xA0them that\xA0hold it, the\xA0raw between\xA0them left as\xA0the\xA0file spells it. Reading it whole is what makes the\xA0rest right: <code>@v:url(10PX)</code> is one address again rather than\xA0the\xA0nameless group <code>(10PX)</code> the\xA0params alone would have shown, and\xA0it is passed over\xA0as\xA0every address is. A\xA0bodiless at-rule whose name spells a\xA0colon is a\xA0variable to\xA0Less whatever it is named\xA0— <code>@page:10PX;</code> declares <code>@page</code>, and\xA0so\xA0do <code>@media:10PX;</code> and\xA0<code>@keyframes:10PX;</code>\xA0— and\xA0is read as\xA0one; the\xA0at-rule of\xA0CSS by\xA0each of\xA0those names carries a\xA0block, and\xA0a\xA0block shuts this reading. Since\xA0the\xA0namespace reads a\xA0plain CSS file as\xA0the\xA0core does, a\xA0stylesheet with\xA0no Less in\xA0it meets the\xA0same reading of\xA0such an\xA0at-rule.</li></ul></li><li>A\xA0warning now asks for\xA0what its option asks for: <ul><li><p>The\xA0<code>value-list-comma-space-before</code> rule no\xA0longer says <code>Unexpected whitespace</code> where <code>always-single-line</code> wants a\xA0space put in\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/175" target="_blank" rel="noreferrer">#175</a>). The\xA0message of\xA0that\xA0option was spelled with\xA0the\xA0text of\xA0the\xA0<code>never</code> ones, telling the\xA0reader to\xA0take away the\xA0very space the\xA0fix then writes, and\xA0every sibling rule already words it the\xA0other way around. It reads <code>Expected single space before &quot;,&quot; in a single-line list</code> now.</p></li><li><p>The\xA0<code>indentation</code> rule now asks for\xA0an\xA0indentation a\xA0file can be written with\xA0where a\xA0closing parenthesis opens a\xA0line (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/237" target="_blank" rel="noreferrer">#237</a>). Two\xA0subtractions of\xA0one reading took the\xA0count a\xA0step past the\xA0outermost level of\xA0the\xA0text being measured, and\xA0at\xA0the\xA0root that\xA0step fell out of\xA0what a\xA0file can hold: <code>@media (min-width: 100px</code> with\xA0a\xA0tab and\xA0a\xA0closing parenthesis on\xA0the\xA0line below drew <code>Expected indentation of -1 tabs</code>, which\xA0is no level at\xA0all. Both are answered:</p><ul><li>a\xA0closing bracket opening a\xA0line unwinds only a\xA0bracket that\xA0was opened at\xA0the\xA0end of\xA0a\xA0line, one opened in\xA0the\xA0middle of\xA0a\xA0line having raised nothing. Such a\xA0closing bracket therefore asks for\xA0the\xA0outermost level of\xA0the\xA0selector, the\xA0value or\xA0the\xA0set of\xA0parameters it belongs to\xA0— the\xA0first column for\xA0an\xA0at-rule at\xA0the\xA0root, the\xA0level of\xA0the\xA0at-rule itself for\xA0one nested in\xA0a\xA0rule, the\xA0level of\xA0the\xA0declaration for\xA0a\xA0value. A\xA0selector reads the\xA0same way, <code>a:not(b</code> with\xA0a\xA0closing parenthesis alone below it among them, and\xA0so\xA0does the\xA0brace of\xA0a\xA0Sass interpolation, <code>@media (min-width: #{$a</code> with\xA0a\xA0closing brace below it;</li><li>the\xA0level a\xA0text is measured at\xA0is read off the\xA0caller rather than\xA0guessed from\xA0the\xA0node. A\xA0value and\xA0a\xA0set of\xA0parameters stand one level above their node, so\xA0the\xA0parentheses their first line opens are already counted and\xA0one level comes off again; a\xA0text standing at\xA0the\xA0node&#39;s own level counts nothing and\xA0owes nothing, and\xA0four\xA0do\xA0— <code>@nest</code> and\xA0Sass&#39;s <code>@at-root</code>, whose parameters are a\xA0selector, and\xA0whatever <code>except: [&quot;param&quot;]</code> or\xA0<code>except: [&quot;value&quot;]</code> takes a\xA0level away from. The\xA0discount fired for\xA0those four\xA0all the\xA0same, so\xA0every line inside\xA0their brackets was measured one level below the\xA0node and\xA0a\xA0closing parenthesis one level below that: <code>@at-root (without: media</code> with\xA0a\xA0closing parenthesis alone below it drew <code>-1 tabs</code> under\xA0the\xA0plain <code>&quot;tab&quot;</code> option and\xA0no secondary options at\xA0all.</li></ul><p>What <code>--fix</code> writes moves with\xA0the\xA0count. It never wrote a\xA0negative indentation\xA0— the\xA0count was held at\xA0nothing where the\xA0indentation is written and\xA0not\xA0where it was reported\xA0— so\xA0under\xA0a\xA0primary option alone a\xA0text measured at\xA0the\xA0root of\xA0a\xA0stylesheet comes out of\xA0the\xA0fixer exactly as\xA0it did, the\xA0message having named a\xA0level the\xA0fixer never wrote. Everywhere else the\xA0fixer used to\xA0write the\xA0first column where the\xA0outermost level stands above it, and\xA0writes the\xA0level now: the\xA0closing parenthesis of\xA0a\xA0declaration&#39;s value moves under\xA0the\xA0declaration, the\xA0one closing a\xA0nested at-rule&#39;s parameters under\xA0the\xA0at-rule, and\xA0under\xA0either <code>except</code> option the\xA0lines inside\xA0a\xA0bracket are indented by\xA0that\xA0bracket, the\xA0option taking a\xA0level from\xA0the\xA0text and\xA0not\xA0from\xA0what stands inside\xA0it.</p></li><li><p>The\xA0<code>declaration-colon-space-after</code> and\xA0<code>declaration-colon-newline-after</code> rules now count the\xA0lines of\xA0a\xA0declaration&#39;s value as\xA0the\xA0file spells it, comments and\xA0all, where their <code>always-single-line</code> and\xA0<code>always-multi-line</code> options ask whether the\xA0declaration stands on\xA0one line (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/389" target="_blank" rel="noreferrer">#389</a>). They used to\xA0count the\xA0copy PostCSS builds with\xA0every comment taken out, so\xA0a\xA0declaration whose only line break stands inside\xA0a\xA0comment, or\xA0between\xA0a\xA0comment and\xA0the\xA0code beside it\xA0— <code>a { color: /*c*/\\nx; }</code> for\xA0one\xA0— was single-line to\xA0both options at\xA0once: <code>always-single-line</code> reported it and\xA0collapsed the\xA0run behind the\xA0colon into\xA0one space, while\xA0<code>always-multi-line</code> passed it over. Such a\xA0declaration is multi-line to\xA0both now, as\xA0it is to\xA0a\xA0reader:</p><ul><li><code>always-multi-line</code> of\xA0<code>declaration-colon-newline-after</code> reports it where no break stands behind the\xA0colon, or\xA0behind the\xA0comment standing on\xA0the\xA0colon&#39;s own line, and\xA0writes one there;</li><li><code>always-single-line</code> of\xA0<code>declaration-colon-space-after</code> no\xA0longer reports it, and\xA0no\xA0longer rewrites the\xA0run behind its colon;</li><li>where the\xA0rules about\xA0the\xA0run behind the\xA0colon and\xA0the\xA0run in\xA0front of\xA0the\xA0semicolon settle between\xA0them which\xA0of\xA0them writes a\xA0run the\xA0two\xA0are, whether such an\xA0option speaks of\xA0the\xA0declaration is read the\xA0same way, so\xA0under\xA0<code>declaration-colon-newline-after: always</code> listed with\xA0<code>declaration-colon-space-after: always-single-line</code> the\xA0break is now written into\xA0<code>a { b: x /*c\\n*/; }</code>, where it used to\xA0be reported on\xA0every run and\xA0written on\xA0none. A\xA0custom property is read there over\xA0what its value holds, as\xA0an\xA0ordinary property is, where it used to\xA0count as\xA0single-line whatever it held: the\xA0same break is now written into\xA0<code>a { --b: x\\n b; }</code> too.</li></ul></li></ul></li><li>False negatives under\xA0<code>--fix</code> are fixed\xA0— a\xA0warning that\xA0used to\xA0vanish along with\xA0a\xA0fix that\xA0never reached the\xA0file now survives the\xA0run: <ul><li>A\xA0fix written to\xA0a\xA0declaration&#39;s value or\xA0to\xA0an\xA0at-rule&#39;s parameters now reaches the\xA0file under\xA0<code>postcss-scss</code>, even where that\xA0value or\xA0those parameters hold an\xA0inline comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/115" target="_blank" rel="noreferrer">#115</a>). That\xA0syntax keeps two\xA0copies of\xA0such a\xA0text and\xA0prints the\xA0one no rule was writing to, so\xA0<code>--fix</code> used to\xA0report a\xA0clean pass on\xA0a\xA0file it had not\xA0touched, and\xA0the\xA0warning went with\xA0it. Every rule that\xA0fixes a\xA0value or\xA0a\xA0set of\xA0parameters is affected: <ul><li>the\xA0fix now arrives, so\xA0a\xA0stylesheet carrying a\xA0<code>//</code> comment converges instead of\xA0drawing the\xA0same warning on\xA0every run;</li><li>a\xA0position reported inside\xA0a\xA0value or\xA0a\xA0set of\xA0parameters no\xA0longer drifts two\xA0characters per\xA0comment, so\xA0it points at\xA0the\xA0character the\xA0warning is about;</li><li>the\xA0<code>media-feature-parentheses-space-inside</code> rule no\xA0longer rewrites every <code>//</code> comment of\xA0a\xA0media query into\xA0a\xA0block comment as\xA0a\xA0side effect of\xA0its own fix;</li><li>an\xA0inline comment closed by\xA0a\xA0bare carriage return, or\xA0by\xA0a\xA0form feed, no\xA0longer hides the\xA0rest of\xA0the\xA0declaration or\xA0of\xA0the\xA0media query from\xA0the\xA0rules that\xA0look through\xA0it for\xA0a\xA0bang, a\xA0comma, a\xA0colon or\xA0a\xA0range operator.</li></ul></li><li>The\xA0<code>selector-attribute-brackets-space-inside</code> rule now writes its fix to\xA0both spellings of\xA0a\xA0selector carrying an\xA0inline comment under\xA0<code>postcss-scss</code> (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/190" target="_blank" rel="noreferrer">#190</a>). The\xA0syntax keeps two\xA0— the\xA0raw the\xA0rule reads, with\xA0every <code>//</code> comment spelled as\xA0a\xA0block one, and\xA0the\xA0copy the\xA0file spells, which\xA0is the\xA0one that is printed\xA0— and\xA0the\xA0rule wrote only the\xA0first, so\xA0on\xA0such a\xA0selector <code>--fix</code> reported a\xA0clean pass on\xA0a\xA0file it never changed. The\xA0copies it left out of\xA0step also grew into\xA0corruption beside a\xA0rule that\xA0reads the\xA0pair: <code>.a // c</code> came back doubled, <code>.a // ca // c</code>, wider with\xA0every further run. The\xA0positions of\xA0its warnings drifted two\xA0characters per\xA0comment for\xA0the\xA0same reading, and\xA0stand in\xA0the\xA0file&#39;s own coordinates now.</li><li>Nine\xA0more rules now write their fix to\xA0both spellings of\xA0a\xA0selector carrying an\xA0inline comment under\xA0<code>postcss-scss</code> (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/193" target="_blank" rel="noreferrer">#193</a>): <code>selector-pseudo-class-case</code>, <code>selector-list-comma-space-before</code> and\xA0<code>-after</code>, <code>selector-list-comma-newline-before</code> and\xA0<code>-after</code>, <code>selector-max-empty-lines</code>, <code>selector-attribute-operator-space-before</code> and\xA0<code>-after</code>, and\xA0<code>no-eol-whitespace</code>. Each wrote only the\xA0copy it reads, never the\xA0one the\xA0file spells and\xA0the\xA0stringifier prints, so\xA0on\xA0such a\xA0selector <code>--fix</code> reported a\xA0clean pass on\xA0a\xA0file it never changed\xA0— and\xA0left the\xA0two\xA0copies out of\xA0step for\xA0whatever read the\xA0pair next, the\xA0corruption of\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/190" target="_blank" rel="noreferrer">#190</a>. The\xA0positions of\xA0their warnings drifted two\xA0characters per\xA0comment for\xA0the\xA0same reading, and\xA0stand in\xA0the\xA0file&#39;s own coordinates now. In\xA0<code>no-eol-whitespace</code> the\xA0whitespace at\xA0stake may lie in\xA0the\xA0text of\xA0the\xA0comment itself, so\xA0that\xA0rule fixes the\xA0copy the\xA0file spells and\xA0refills the\xA0raw beside it, the\xA0way a\xA0declaration&#39;s value is written. And\xA0a\xA0comma whose whitespace holds the\xA0break that\xA0closes an\xA0inline comment is reported and\xA0left standing by\xA0the\xA0four\xA0<code>selector-list-comma-*</code> rules, since\xA0taking that\xA0break away, or\xA0writing a\xA0space over\xA0it, would carry the\xA0comma\xA0— or\xA0the\xA0rest of\xA0the\xA0list\xA0— into\xA0the\xA0comment&#39;s text.</li><li>A\xA0Less at-variable now keeps the\xA0fix written to\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/99" target="_blank" rel="noreferrer">#99</a>). <code>postcss-less</code> gives such a\xA0variable two\xA0copies of\xA0its text and\xA0prints the\xA0one no rule writes, so\xA0<code>--fix</code> used to\xA0leave the\xA0file exactly as\xA0it was and\xA0drop the\xA0warning along with\xA0it, reporting a\xA0clean pass on\xA0a\xA0file it never touched. This held for\xA0every rule that\xA0fixes an\xA0at-rule&#39;s parameters: <code>indentation</code>, <code>no-eol-whitespace</code>, <code>number-leading-zero</code>, <code>number-no-trailing-zeros</code> and\xA0<code>string-quotes</code>.</li><li>The\xA0<code>indentation</code> rule now corrects every mis-indented line of\xA0an\xA0at-rule, and\xA0not\xA0only one of\xA0them (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/103" target="_blank" rel="noreferrer">#103</a> and\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/64" target="_blank" rel="noreferrer">#64</a>). This holds for\xA0the\xA0parameters and\xA0for\xA0anything standing between\xA0the\xA0name and\xA0them, a\xA0multi-line comment for\xA0one. The\xA0remaining lines used to\xA0keep their indentation while\xA0all of\xA0the\xA0warnings disappeared, so\xA0a\xA0second run reported nothing left to\xA0fix.</li><li>The\xA0<code>unit-case</code> rule now fixes an\xA0at-rule whose name is not\xA0spelled in\xA0lower case, <code>@MEDIA</code> for\xA0one (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/104" target="_blank" rel="noreferrer">#104</a>). Such a\xA0query used to\xA0be reported and\xA0then left exactly as\xA0it was, the\xA0warning disappearing with\xA0the\xA0fix, so\xA0the\xA0problem survived any number of\xA0runs.</li><li>The\xA0<code>selector-pseudo-class-parentheses-space-inside</code> rule now fixes the\xA0whitespace next to\xA0a\xA0closing parenthesis that\xA0a\xA0comment stands in\xA0front of\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/123" target="_blank" rel="noreferrer">#123</a>). The\xA0parser keeps such a\xA0comment, and\xA0the\xA0space on\xA0either side of\xA0it, in\xA0the\xA0raws of\xA0the\xA0node before\xA0it, and\xA0prints the\xA0raw one; the\xA0rule wrote to\xA0the\xA0clean value instead, so\xA0the\xA0problem was reported, <code>--fix</code> claimed a\xA0clean pass and\xA0the\xA0parenthesis stayed exactly where it was. A\xA0nested pseudo-class was left half fixed at\xA0every level. Under\xA0<code>always</code> a\xA0line break in\xA0front of\xA0the\xA0closing parenthesis now collapses to\xA0a\xA0single space where a\xA0comment stands before\xA0it, as\xA0it already did where none does.</li><li>The\xA0<code>value-list-comma-space-after</code>, <code>value-list-comma-space-before</code> and\xA0<code>value-list-comma-newline-after</code> rules no\xA0longer drop the\xA0warning about\xA0a\xA0comma they cannot fix (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/126" target="_blank" rel="noreferrer">#126</a>). A\xA0comma the\xA0value does not\xA0hold is out of\xA0their reach, and\xA0they said so\xA0by\xA0returning <code>false</code> from\xA0the\xA0fixer\xA0— a\xA0refusal Stylelint has never read, since\xA0it counts a\xA0fixer as\xA0applied whatever it does. So\xA0<code>--fix</code> ended with\xA0a\xA0clean pass on\xA0a\xA0file it had not\xA0finished fixing, and\xA0only a\xA0second run over\xA0the\xA0output brought the\xA0surviving problem back. A\xA0warning of\xA0that\xA0kind now stands where it used to\xA0vanish. A\xA0comma opening the\xA0value was among them at\xA0the\xA0time, and\xA0is fixed by\xA0all three\xA0rules now, which\xA0was <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/134" target="_blank" rel="noreferrer">#134</a> for\xA0two\xA0of\xA0them and\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/166" target="_blank" rel="noreferrer">#166</a> for\xA0the\xA0third.</li><li>The\xA0<code>no-eol-whitespace</code> rule now trims the\xA0end of\xA0a\xA0Less file whose last line is an\xA0inline comment closed by\xA0a\xA0bare carriage return (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/247" target="_blank" rel="noreferrer">#247</a>). <code>postcss-less</code> ends such a\xA0comment on\xA0a\xA0line feed and\xA0on\xA0nothing else, so\xA0where the\xA0file ends on\xA0a\xA0carriage return that\xA0break stays in\xA0the\xA0raw behind the\xA0comment&#39;s text, with\xA0the\xA0whitespace in\xA0front of\xA0it; only the\xA0very end of\xA0that\xA0raw was trimmed, and\xA0the\xA0break stands there, so\xA0<code>// c \\r</code> and\xA0<code>a { b: 1; } // c \\r</code> were reported while\xA0<code>--fix</code> came back with\xA0a\xA0clean pass on\xA0a\xA0file it had not\xA0touched. The\xA0lines of\xA0that\xA0raw are walked now, and\xA0the\xA0same file ending on\xA0a\xA0form feed\xA0— which\xA0said nothing at\xA0all until\xA0the\xA0widened scan above reached it\xA0— is trimmed alongside. A\xA0Less file ending on\xA0a\xA0line feed was never affected: that\xA0break closes the\xA0comment, and\xA0the\xA0whitespace in\xA0front of\xA0it is the\xA0stylesheet&#39;s own to\xA0trim.</li><li>The\xA0<code>no-eol-whitespace</code> rule now trims an\xA0inline comment that\xA0holds nothing but\xA0whitespace (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/254" target="_blank" rel="noreferrer">#254</a>). Such a\xA0comment is filed as\xA0an\xA0empty text with\xA0every character of\xA0the\xA0whitespace in\xA0the\xA0raw in\xA0front of\xA0it and\xA0nothing in\xA0the\xA0raw behind, and\xA0only the\xA0raw behind was trimmed at\xA0its end, so\xA0a\xA0line holding <code>// </code> and\xA0nothing else was reported while\xA0<code>--fix</code> came back with\xA0a\xA0clean pass on\xA0a\xA0file it had not\xA0touched, and\xA0the\xA0warning went with\xA0it. The\xA0end of\xA0an\xA0inline comment is taken to\xA0be the\xA0end of\xA0whichever raw closes it now, so\xA0such a\xA0line comes back as\xA0<code>//</code> and\xA0a\xA0second run has nothing to\xA0say. Under\xA0<code>postcss-scss</code> that\xA0holds wherever the\xA0comment stands and\xA0whichever of\xA0the\xA0four\xA0spellings of\xA0the\xA0break closes it, the\xA0end of\xA0the\xA0file included. Under\xA0<code>postcss-less</code>, which\xA0files that\xA0whitespace elsewhere wherever a\xA0line feed closes the\xA0comment, it holds for\xA0a\xA0file that\xA0ends inside\xA0one: whatever spaces and\xA0tabs such a\xA0comment ends on\xA0are trimmed now, however many bare carriage returns or\xA0form feeds stand in\xA0front of\xA0them, while\xA0the\xA0whitespace in\xA0front of\xA0each of\xA0those breaks was trimmed already\xA0— in\xA0front of\xA0a\xA0bare carriage return by\xA0the\xA0walk over\xA0the\xA0lines of\xA0that\xA0same raw, and\xA0in\xA0front of\xA0a\xA0form feed since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/247" target="_blank" rel="noreferrer">#247</a> taught the\xA0scan to\xA0read one as\xA0the\xA0end of\xA0a\xA0line. A\xA0comment that\xA0holds text keeps the\xA0single space between\xA0the\xA0slashes and\xA0that\xA0text, which\xA0ends no line, and\xA0a\xA0block comment holding nothing but\xA0whitespace, <code>/* */</code>, is untouched as\xA0before, its whitespace standing in\xA0front of\xA0the\xA0closing delimiter rather than\xA0at\xA0the\xA0end of\xA0a\xA0line.</li><li>The\xA0<code>linebreaks</code> rule now rewrites a\xA0line break standing in\xA0a\xA0set of\xA0at-rule parameters (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/270" target="_blank" rel="noreferrer">#270</a>). The\xA0warning came from\xA0a\xA0scan of\xA0the\xA0file itself, which\xA0sees every break whichever text of\xA0whichever node it stands in, while\xA0the\xA0fix walked the\xA0tree and\xA0wrote to\xA0a\xA0selector, a\xA0value, the\xA0text of\xA0a\xA0comment and\xA0the\xA0whitespace around a\xA0node\xA0— a\xA0set of\xA0parameters being none of\xA0those, and\xA0the\xA0one at-rule the\xA0fix did reach being a\xA0Less at-variable, which\xA0<code>postcss-less</code> gives a\xA0value beside its params. So\xA0<code>@media (min-width: 1px),</code> broken with\xA0a\xA0line feed under\xA0<code>windows</code> was reported at\xA0<code>1:25</code>, <code>--fix</code> reported a\xA0clean pass, the\xA0file came back byte for\xA0byte as\xA0it went in, and\xA0a\xA0second run reported <code>1:25</code> again. The\xA0parameters of\xA0every at-rule are read and\xA0written now, in\xA0the\xA0copy the\xA0file spells, so\xA0a\xA0comment they hold survives the\xA0fix.</li><li>The\xA0<code>linebreaks</code> rule now rewrites a\xA0line break standing in\xA0four\xA0more of\xA0the\xA0texts a\xA0node holds (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/283" target="_blank" rel="noreferrer">#283</a>). The\xA0warning comes from\xA0a\xA0scan of\xA0the\xA0file itself, which\xA0sees every break whichever text of\xA0whichever node it stands in, while\xA0the\xA0fix walked the\xA0tree and\xA0wrote to\xA0a\xA0selector, a\xA0value, a\xA0set of\xA0at-rule parameters, the\xA0text of\xA0a\xA0comment and\xA0the\xA0whitespace in\xA0front of\xA0a\xA0node and\xA0at\xA0the\xA0end of\xA0its block. Four\xA0texts were none of\xA0those: what an\xA0at-rule writes between\xA0its name and\xA0its parameters, what a\xA0rule, an\xA0at-rule or\xA0a\xA0declaration writes between\xA0its two\xA0halves, the\xA0raw of\xA0a\xA0<code>!important</code>, and\xA0the\xA0whitespace inside\xA0the\xA0delimiters of\xA0a\xA0comment. So\xA0a\xA0stylesheet broken over\xA0<code>a</code> and\xA0<code>{ b: c; }</code> was reported at\xA0<code>1:2</code> under\xA0<code>windows</code>, <code>--fix</code> reported a\xA0clean pass, the\xA0file came back byte for\xA0byte as\xA0it went in, and\xA0a\xA0second run reported <code>1:2</code> again. Seven\xA0shapes went that\xA0way: a\xA0break between\xA0the\xA0name of\xA0an\xA0at-rule and\xA0its parameters, between\xA0those parameters and\xA0the\xA0block, between\xA0a\xA0selector and\xA0its block, between\xA0a\xA0property and\xA0its value, between\xA0a\xA0value and\xA0the\xA0bang behind it, behind the\xA0opening delimiter of\xA0a\xA0comment, and\xA0in\xA0front of\xA0the\xA0closing one. All seven\xA0are rewritten now, and\xA0a\xA0colon standing in\xA0one of\xA0those four\xA0texts is left exactly where it is\xA0— the\xA0colon that\xA0parts a\xA0declaration, and\xA0the\xA0one <code>postcss-less</code> files behind the\xA0name of\xA0a\xA0Less at-variable\xA0— breaks being all the\xA0fix reads.</li><li>The\xA0<code>linebreaks</code> rule now rewrites the\xA0line break standing in\xA0front of\xA0a\xA0stray semicolon behind a\xA0rule&#39;s block (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/372" target="_blank" rel="noreferrer">#372</a>). It is the\xA0last of\xA0the\xA0texts the\xA0walk of\xA0the\xA0fix did not\xA0reach, and\xA0it went the\xA0way the\xA0seven\xA0shapes of\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/283" target="_blank" rel="noreferrer">#283</a> went. PostCSS files that\xA0whitespace, together with\xA0the\xA0semicolon itself, in\xA0the\xA0<code>raws.ownSemicolon</code> of\xA0the\xA0rule the\xA0semicolon stands behind\xA0— a\xA0nested one and\xA0a\xA0rule of\xA0the\xA0top level alike\xA0— and\xA0<code>postcss-scss</code> and\xA0<code>postcss-less</code> read the\xA0shape exactly as\xA0it does. So\xA0a\xA0stylesheet whose block is followed by\xA0such a\xA0semicolon on\xA0the\xA0line under\xA0it was reported at\xA0the\xA0break under\xA0<code>windows</code>, <code>--fix</code> reported a\xA0clean pass, the\xA0file came back byte for\xA0byte as\xA0it went in, and\xA0a\xA0second run reported the\xA0same place again. The\xA0raw is written now, and\xA0the\xA0semicolon it holds is left exactly where it stands\xA0— breaks being all the\xA0fix reads.</li><li>The\xA0<code>function-parentheses-newline-inside</code> rule no\xA0longer reports a\xA0problem solved that\xA0it could only half write (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/285" target="_blank" rel="noreferrer">#285</a>). The\xA0walk that\xA0measures the\xA0whitespace behind an\xA0opening parenthesis steps over\xA0the\xA0nodes an\xA0inline comment is spelled with, and\xA0the\xA0<code>never-multi-line</code> fix stops at\xA0the\xA0first of\xA0them, so\xA0what the\xA0fix could not\xA0reach stayed and\xA0the\xA0option stayed violated: <code>a { t: translate( // c</code> followed by\xA0a\xA0line break, <code> /*x*/</code>, a\xA0line break and\xA0<code> , 1px); }</code> came out of\xA0<code>--fix</code> with\xA0the\xA0space behind the\xA0parenthesis taken and\xA0the\xA0line break left, and\xA0<code>stylelint</code> reported on\xA0the\xA0very file <code>stylelint --fix</code> had just called clean. The\xA0fix is written now only where it empties every stretch that\xA0was measured, and\xA0the\xA0problem is reported where it is not.</li><li>The\xA0<code>declaration-block-trailing-semicolon</code> rule no\xA0longer reports a\xA0Sass nested property solved and\xA0leaves it exactly as\xA0it was (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/335" target="_blank" rel="noreferrer">#335</a>). Such a\xA0property is a\xA0declaration carrying a\xA0block, which\xA0<code>postcss-scss</code> alone reads as\xA0one\xA0— every other syntax reads <code>font: 12px { … }</code> as\xA0a\xA0rule\xA0— and\xA0the\xA0fix of\xA0this rule writes no character itself: it sets the\xA0block&#39;s <code>raws.semicolon</code> and\xA0leaves the\xA0printing to\xA0the\xA0stringifier, which\xA0drops the\xA0semicolon it is handed for\xA0a\xA0declaration carrying a\xA0block exactly as\xA0PostCSS drops it for\xA0an\xA0at-rule carrying one. So\xA0<code>a { font: 12px { family: serif; } }</code> under\xA0<code>always</code> was reported, called fixed and\xA0written back byte for\xA0byte, the\xA0warning gone with\xA0the\xA0run. The\xA0at-rule walk of\xA0the\xA0rule turns such a\xA0node away already; the\xA0fix is now declined for\xA0the\xA0declaration too, so\xA0the\xA0warning survives <code>--fix</code> rather than\xA0vanishing with\xA0a\xA0write that\xA0never lands. Whether such a\xA0semicolon can be written at\xA0all is a\xA0question of\xA0its own, and\xA0where such a\xA0property already carries one behind its closing brace the\xA0warning is raised over\xA0a\xA0file that\xA0needs nothing\xA0— that\xA0semicolon is parsed into\xA0the\xA0block&#39;s own trailing raw rather than\xA0into\xA0the\xA0flag the\xA0rule reads, which\xA0is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/336" target="_blank" rel="noreferrer">#336</a> and\xA0older than\xA0either.</li><li>The\xA0three\xA0<code>block-closing-brace-*-before</code> rules now write the\xA0whitespace in\xA0front of\xA0a\xA0closing brace wherever the\xA0parser filed it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/292" target="_blank" rel="noreferrer">#292</a>). Where an\xA0at-rule carrying neither a\xA0block nor\xA0a\xA0semicolon of\xA0its own closes the\xA0block, that\xA0whitespace stands inside\xA0the\xA0at-rule, and\xA0all three\xA0rules wrote the\xA0block&#39;s own raw, which\xA0is empty there. So\xA0under\xA0<code>never</code>, <code>never-single-line</code> and\xA0<code>never-multi-line</code> nothing was written at\xA0all, and\xA0under\xA0<code>always</code>, <code>always-single-line</code> and\xA0<code>always-multi-line</code> the\xA0character went into\xA0the\xA0empty raw beside the\xA0whitespace it was about, satisfying nothing: <code>a {</code> followed by\xA0a\xA0break, <code>@extend .b</code> and\xA0a\xA0break came back from\xA0<code>block-closing-brace-space-before</code> one space longer on\xA0every run, for\xA0ever. Either way the\xA0warning vanished with\xA0the\xA0fix and\xA0the\xA0next run raised it again. One shape of\xA0that\xA0kind is left exactly as\xA0it was\xA0— a\xA0Less mixin call carrying <code>!important</code>, where <code>postcss-less</code> collects the\xA0whitespace on\xA0both sides of\xA0the\xA0flag into\xA0one raw and\xA0prints all of\xA0it on\xA0one side, so\xA0that\xA0neither raw describes the\xA0run in\xA0front of\xA0the\xA0brace (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/374" target="_blank" rel="noreferrer">#374</a>). Every other is written exactly like\xA0the\xA0same block ending in\xA0a\xA0declaration with\xA0no semicolon now, under\xA0every option of\xA0the\xA0three\xA0rules and\xA0under\xA0all three\xA0syntaxes, and\xA0a\xA0comment the\xA0at-rule swallowed along with\xA0the\xA0whitespace keeps its place. A\xA0fix that\xA0could only be written by\xA0taking the\xA0brace into\xA0an\xA0inline comment is still held back, and\xA0its warning still stands.</li><li>The\xA0<code>max-empty-lines</code> rule now takes the\xA0empty lines standing in\xA0front of\xA0a\xA0closing brace away (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/481" target="_blank" rel="noreferrer">#481</a>). Its fix rewrote the\xA0whitespace in\xA0front of\xA0every node, the\xA0runs around a\xA0comment&#39;s text and\xA0the\xA0tail of\xA0the\xA0stylesheet, and\xA0the\xA0run between\xA0a\xA0block&#39;s last node and\xA0its closing brace is none of\xA0those, so\xA0<code>a {\\n\\tb: c;\\n\\n\\n}</code> was reported without\xA0<code>--fix</code> and\xA0handed back as\xA0it was with\xA0it, the\xA0warnings gone with\xA0the\xA0run. That\xA0run is now collapsed to\xA0the\xA0option&#39;s count like\xA0any other, in\xA0the\xA0block&#39;s own raw and\xA0inside\xA0an\xA0<code>@include</code> or\xA0a\xA0mixin call written without\xA0a\xA0semicolon, where the\xA0parser leaves it; a\xA0Less mixin call carrying <code>!important</code> is left as\xA0it was (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/374" target="_blank" rel="noreferrer">#374</a>).</li></ul></li></ul><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to “Added”">​</a></h3><ul><li>The\xA0package now exports a\xA0<code>defineStylistic</code> function for\xA0a\xA0JavaScript configuration (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/624" target="_blank" rel="noreferrer">#624</a>). It takes the\xA0rules by\xA0their short names and\xA0the\xA0syntax they are read under, <code>{ syntax: &quot;scss&quot;, rules: { &quot;color-hex-case&quot;: &quot;lower&quot; } }</code>, and\xA0returns the\xA0settings under\xA0the\xA0names Stylelint reads, typed off the\xA0rules themselves: an\xA0editor completes the\xA0names and\xA0the\xA0options, and\xA0the\xA0compiler refuses a\xA0name, an\xA0option, a\xA0key or\xA0a\xA0syntax the\xA0plugin does not\xA0take. A\xA0second argument holds what several rules take alike, <code>ignoreFunctions</code>, <code>ignoreProperties</code>, <code>severity</code> and\xA0<code>disableFix</code>, written into\xA0every rule that\xA0takes the\xA0key, the\xA0rule&#39;s own option winning. <code>defineStylisticOverride</code> takes <code>files</code> besides and\xA0returns the\xA0whole <code>overrides</code> entry, with\xA0the\xA0<code>customSyntax</code> the\xA0syntax is parsed with.</li><li>The\xA0<code>named-grid-areas-alignment</code> rule now has an\xA0additional <code>alignColumns</code> option, which\xA0lays the\xA0line names and\xA0the\xA0sizes beside the\xA0rows of\xA0a\xA0<code>grid-template</code> or\xA0<code>grid</code> shorthand out as\xA0columns of\xA0a\xA0table, the\xA0padding written between\xA0the\xA0tokens of\xA0a\xA0line and\xA0never in\xA0front of\xA0its first one, so\xA0that\xA0the\xA0indentation stays what the\xA0<code>indentation</code> rule asks for; a\xA0row without\xA0a\xA0line name in\xA0front of\xA0it stands at\xA0the\xA0indentation, and\xA0an\xA0empty list of\xA0names, <code>[]</code>, puts it under\xA0the\xA0others. Where the\xA0configuration lists the\xA0option, <code>no-multiple-whitespaces</code> leaves those runs alone, whichever order the\xA0two\xA0rules are listed in\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/45" target="_blank" rel="noreferrer">#45</a>).</li><li>The\xA0<code>max-line-length</code> rule now has an\xA0additional <code>tabSize</code> option (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/10" target="_blank" rel="noreferrer">#10</a>). A\xA0tab used to\xA0count as\xA0one character, so\xA0a\xA0file indented with\xA0tabs measured shorter than\xA0the\xA0editor showed it; with\xA0the\xA0option a\xA0tab reaches the\xA0next tab stop of\xA0the\xA0given width, as\xA0the\xA0CSS <code>tab-size</code> property has it, and\xA0the\xA0limit is the\xA0one the\xA0editor&#39;s ruler shows.</li><li>The\xA0plugin now says so\xA0when\xA0it is listed in\xA0<code>extends</code> instead of\xA0<code>plugins</code> (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/14" target="_blank" rel="noreferrer">#14</a>). It is a\xA0plugin rather than\xA0a\xA0shareable config, and\xA0Stylelint could not\xA0tell the\xA0one use from\xA0the\xA0other: it loaded the\xA0package as\xA0a\xA0config, found no rules in\xA0it, and\xA0reported every <code>@stylistic/</code> rule of\xA0the\xA0config as\xA0unknown\xA0— an\xA0error about\xA0the\xA0rules, whose cause is the\xA0field they were never reached from. A\xA0run now stops with\xA0a\xA0configuration error naming the\xA0field to\xA0move the\xA0package to.</li><li>The\xA0<code>aspect-ratio-notation</code> rule, which\xA0specifies how the\xA0value of\xA0<code>aspect-ratio</code> is written (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/18" target="_blank" rel="noreferrer">#18</a>). One and\xA0the\xA0same ratio can be written in\xA0more than\xA0one way, and\xA0the\xA0rule reads a\xA0value along two\xA0axes that\xA0do not\xA0depend on\xA0each other: the\xA0primary option <code>&quot;ratio&quot;|&quot;number-where-possible&quot;|&quot;as-written&quot;</code> says how many of\xA0the\xA0two\xA0numbers are written, since\xA0the\xA0grammar reads <code>2</code> as\xA0<code>2 / 1</code>, and\xA0the\xA0optional <code>smallestIntegers</code> option says what those numbers are, since\xA0<code>16 / 8</code> spells the\xA0ratio <code>2 / 1</code> as\xA0well. Either axis can be given without\xA0the\xA0other, and\xA0both are settled before\xA0anything is written, so\xA0one run of\xA0<code>--fix</code> leaves the\xA0value as\xA0the\xA0configuration asks for\xA0it. The\xA0solidus the\xA0fix adds is spelled the\xA0way the\xA0<code>value-slash-space-before</code> and\xA0<code>value-slash-space-after</code> rules ask wherever the\xA0configuration lists them, whichever order it lists the\xA0rules in, and\xA0with\xA0a\xA0single space on\xA0either side where it lists neither (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/550" target="_blank" rel="noreferrer">#550</a>). The\xA0rule reads the\xA0<code>&lt;ratio&gt;</code> of\xA0a\xA0media feature under\xA0the\xA0same options\xA0— <code>aspect-ratio</code> and\xA0<code>device-aspect-ratio</code>, their <code>min-</code> and\xA0<code>max-</code> spellings, the\xA0plain form and\xA0the\xA0range form alike\xA0— and\xA0the\xA0solidus it adds there is spelled as\xA0the\xA0<code>media-feature-slash-space-*</code> rules ask; the\xA0secondary option <code>ignore: [&quot;at-rules&quot;]</code> leaves the\xA0media features alone (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/551" target="_blank" rel="noreferrer">#551</a>).</li><li>The\xA0<code>value-slash-space-before</code>, <code>value-slash-space-after</code>, <code>media-feature-slash-space-before</code> and\xA0<code>media-feature-slash-space-after</code> rules, which\xA0require a\xA0single space or\xA0disallow whitespace on\xA0either side of\xA0the\xA0solidus that\xA0separates the\xA0parts of\xA0a\xA0value\xA0— the\xA0numbers of\xA0a\xA0ratio, the\xA0sizes of\xA0a\xA0font shorthand, the\xA0lines of\xA0a\xA0grid area, the\xA0colour and\xA0the\xA0alpha of\xA0a\xA0colour function\xA0— and\xA0of\xA0the\xA0ratio of\xA0a\xA0media feature (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/548" target="_blank" rel="noreferrer">#548</a>). Nothing decided between\xA0<code>1/2</code> and\xA0<code>1 / 2</code> before: <code>no-multiple-whitespaces</code> collapses <code>1 / 2</code> and\xA0leaves the\xA0other spellings as\xA0they stand, and\xA0<code>aspect-ratio-notation</code> leaves a\xA0ratio already written alone. The\xA0value rules take the\xA0options of\xA0the\xA0<code>value-list-comma-space-*</code> rules, with\xA0<code>ignoreFunctions</code> and\xA0<code>ignoreProperties</code> besides, and\xA0the\xA0media rules those of\xA0the\xA0<code>media-feature-range-operator-space-*</code> rules. A\xA0solidus inside\xA0the\xA0arguments of\xA0a\xA0math function is the\xA0division operator and\xA0is passed over, as\xA0is one inside\xA0a\xA0<code>url()</code> address, a\xA0string or\xA0a\xA0comment; under\xA0the\xA0<code>@stylistic/scss/</code> namespace a\xA0solidus Sass divides at\xA0— one beside a\xA0variable or\xA0a\xA0call of\xA0Sass&#39;s own\xA0— is passed over\xA0as\xA0well, since\xA0the\xA0whitespace beside an\xA0operator is no separator&#39;s.</li><li>The\xA0<code>value-slash-newline-before</code> and\xA0<code>value-slash-newline-after</code> rules, which\xA0require a\xA0newline or\xA0disallow whitespace on\xA0either side of\xA0the\xA0same solidus (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/622" target="_blank" rel="noreferrer">#622</a>). They take the\xA0options of\xA0the\xA0<code>value-list-comma-newline-*</code> rules\xA0— <code>always</code>, <code>always-multi-line</code> and\xA0<code>never-multi-line</code>\xA0— and\xA0read the\xA0same solidi the\xA0<code>value-slash-space-*</code> rules read, so\xA0the\xA0two\xA0families divide a\xA0declaration between\xA0them as\xA0the\xA0comma families do: the\xA0<code>space</code> rules under\xA0<code>-single-line</code> speak of\xA0a\xA0declaration on\xA0one line, the\xA0<code>newline</code> rules under\xA0<code>-multi-line</code> of\xA0one over\xA0several. Both rules are autofixable, the\xA0<code>before</code> one too, where its comma twin is not: the\xA0solidus opening a\xA0line of\xA0its own, as\xA0the\xA0columns of\xA0a\xA0<code>grid-template</code> are often written, is that\xA0side&#39;s. <code>aspect-ratio-notation</code> reads the\xA0pair for\xA0the\xA0solidus it adds, as\xA0it reads the\xA0<code>space</code> rules.</li><li>The\xA0plugin now ships a\xA0type declaration for\xA0what it exports, so\xA0a\xA0TypeScript configuration or\xA0a\xA0script importing the\xA0package reads the\xA0plugin list as\xA0<code>Plugin[]</code> rather than\xA0as\xA0<code>any</code>. Nothing about\xA0configuring the\xA0plugin changes.</li><li>The\xA0<code>declaration-block-single-line-max-declarations</code> rule, which\xA0limits the\xA0number of\xA0declarations within\xA0a\xA0single-line declaration block (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/639" target="_blank" rel="noreferrer">#639</a>). It is the\xA0one stylistic rule Stylelint\xA016 kept when\xA0it removed the\xA0other\xA076, and\xA0Stylelint\xA018 removes it in\xA0its turn; under\xA0the\xA0<code>@stylistic/</code> prefix it takes the\xA0same option and\xA0reports as\xA0it did there, so\xA0a\xA0configuration moves it by\xA0renaming it to\xA0<code>@stylistic/declaration-block-single-line-max-declarations</code>; one thing it reads that\xA0Stylelint&#39;s never did is the\xA0single-line block of\xA0an\xA0at-rule, <code>@font-face { font-family: x; src: y; }</code> for\xA0one, which\xA0is counted as\xA0a\xA0rule&#39;s block is (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/640" target="_blank" rel="noreferrer">#640</a>). Under\xA0<code>--fix</code> it takes its turn after\xA0the\xA0rules that\xA0write line breaks, so\xA0it reports the\xA0blocks the\xA0fixed file holds on\xA0one line, and\xA0it is autofixable, which\xA0Stylelint&#39;s never was (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/641" target="_blank" rel="noreferrer">#641</a>): the\xA0fix breaks the\xA0block over\xA0lines, one per\xA0declaration, each run written as\xA0the\xA0<code>block-opening-brace-*-after</code>, <code>declaration-block-semicolon-*-after</code>, <code>block-closing-brace-*-after</code> and\xA0<code>block-closing-brace-*-before</code> rules ask wherever the\xA0configuration lists them, the\xA0indentation left to\xA0<code>indentation</code> in\xA0the\xA0same run; where those rules leave no run a\xA0line break, the\xA0problem is reported and\xA0the\xA0block left as\xA0it is.</li></ul><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><p>The\xA0<code>declaration-colon-space-after</code> rule no\xA0longer writes over\xA0the\xA0value a\xA0custom property standing last at\xA0the\xA0top level of\xA0a\xA0stylesheet keeps the\xA0tail of\xA0the\xA0file in\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/546" target="_blank" rel="noreferrer">#546</a>). PostCSS keeps a\xA0whitespace-only value in\xA0<code>decl.value</code> itself, and\xA0a\xA0custom property is where it keeps one that\xA0a\xA0plain property would have handed to\xA0the\xA0raw behind it, so\xA0where such a\xA0declaration closes the\xA0stylesheet with\xA0no semicolon behind it the\xA0line break the\xA0last line is closed by\xA0stands inside\xA0the\xA0declaration. The\xA0rule writes the\xA0whitespace behind the\xA0colon where the\xA0run stands, which\xA0for\xA0such a\xA0value is the\xA0value itself, so\xA0<code>--a: </code> followed by\xA0a\xA0break came back from\xA0<code>always</code> as\xA0<code>--a: </code> with\xA0the\xA0break gone and\xA0from\xA0<code>never</code> as\xA0<code>--a:</code>, and\xA0beside <code>no-missing-end-of-source-newline</code> the\xA0two\xA0rules took that\xA0break in\xA0turns for\xA0as\xA0long as\xA0<code>--fix</code> ran, in\xA0either order the\xA0configuration listed them in. Listed beside <code>declaration-colon-newline-after</code>, whose fix moves the\xA0run out of\xA0the\xA0value ahead of\xA0it in\xA0the\xA0same pass, it took the\xA0break in\xA0one run and\xA0rested there. The\xA0rule now passes such a\xA0declaration over, exactly as\xA0it does one whose run has left the\xA0declaration for\xA0the\xA0stylesheet&#39;s own trailing raw (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/537" target="_blank" rel="noreferrer">#537</a>): it has no spelling of\xA0that\xA0text answering both its option and\xA0a\xA0closed last line\xA0— <code>always</code> asks for\xA0a\xA0single space with\xA0no whitespace behind it, <code>never</code> for\xA0no whitespace at\xA0all\xA0— so\xA0any write there takes the\xA0file&#39;s last break away, whether or\xA0not\xA0the\xA0rule that\xA0puts it back is configured. Its neighbour keeps the\xA0declaration, since\xA0the\xA0break it writes is one the\xA0stylesheet may end on, and\xA0now writes it under\xA0either order. A\xA0custom property carrying a\xA0flag is read as\xA0it always was: what the\xA0file ends on\xA0is printed out of\xA0the\xA0raw the\xA0flag is written behind rather than\xA0out of\xA0the\xA0value, so\xA0<code>--a: !important</code> is settled by\xA0<code>always</code> as\xA0any other declaration is. <code>always-single-line</code> loses its say over\xA0such a\xA0declaration with\xA0it, having none over\xA0one whose value holds the\xA0break the\xA0file ends on\xA0to\xA0begin with, so\xA0a\xA0run of\xA0two\xA0spaces at\xA0the\xA0very end of\xA0a\xA0stylesheet is now left as\xA0it stands.</p></li><li><p>The\xA0<code>at-rule-semicolon-space-before</code> rule no\xA0longer reports the\xA0whitespace in\xA0front of\xA0a\xA0semicolon the\xA0file does not\xA0spell (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/395" target="_blank" rel="noreferrer">#395</a>). The\xA0rule asks about\xA0the\xA0position one character past the\xA0end of\xA0the\xA0at-rule, as\xA0though\xA0a\xA0semicolon always stood there, and\xA0an\xA0at-rule carrying no block of\xA0its own spells none where it runs to\xA0the\xA0brace closing its container or\xA0to\xA0the\xA0end of\xA0the\xA0file. Inside\xA0a\xA0block that\xA0position holds the\xA0whitespace standing in\xA0front of\xA0the\xA0closing brace, which\xA0the\xA0parser files into\xA0the\xA0at-rule, so\xA0<code>a { @import &quot;x&quot; }</code> drew <code>Unexpected whitespace before &quot;;&quot;</code> under\xA0<code>&quot;never&quot;</code> over\xA0the\xA0space in\xA0front of\xA0the\xA0brace. At\xA0the\xA0top of\xA0a\xA0file it holds nothing at\xA0all, so\xA0<code>@import &quot;x&quot;</code> drew <code>Expected single space before &quot;;&quot;</code> under\xA0<code>&quot;always&quot;</code> over\xA0a\xA0semicolon that\xA0was not\xA0there. Such an\xA0at-rule is now passed over\xA0under\xA0either option; one the\xA0file does spell a\xA0semicolon behind is read exactly as\xA0it was.</p></li><li><p>The\xA0<code>declaration-colon-space-after</code> and\xA0<code>declaration-colon-newline-after</code> rules no\xA0longer write into\xA0the\xA0tail of\xA0a\xA0file (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/537" target="_blank" rel="noreferrer">#537</a>). PostCSS asks no block of\xA0a\xA0declaration, so\xA0a\xA0stylesheet may hold one at\xA0its top level, and\xA0where such a\xA0declaration prints nothing behind its colon and\xA0carries no semicolon behind it, the\xA0whitespace behind that\xA0colon runs on\xA0to\xA0the\xA0end of\xA0the\xA0file: it stands in\xA0the\xA0stylesheet&#39;s own trailing raw, the\xA0very text <code>no-missing-end-of-source-newline</code> writes the\xA0last line break into. Both rules used to\xA0report such a\xA0declaration and\xA0write their spelling into\xA0the\xA0raw between\xA0the\xA0property and\xA0the\xA0value, from\xA0where the\xA0next parse carried it back out to\xA0the\xA0tail, so\xA0<code>color: </code> came back from\xA0<code>always</code> as\xA0<code>color: </code> and\xA0gained a\xA0character on\xA0every run of\xA0<code>--fix</code> without\xA0ever coming to\xA0rest. Neither rule speaks of\xA0such a\xA0declaration now. <code>declaration-colon-space-after</code> has no spelling of\xA0that\xA0raw answering both its option and\xA0the\xA0break a\xA0file&#39;s last line is closed by\xA0— <code>always</code> asks for\xA0a\xA0single space with\xA0no whitespace behind it, <code>never</code> for\xA0none at\xA0all\xA0— so\xA0a\xA0write there takes the\xA0file&#39;s last break away, whether or\xA0not\xA0<code>no-missing-end-of-source-newline</code> is configured to\xA0put it back. Its neighbour, whose <code>always</code> such a\xA0break already answers, passes the\xA0declaration over\xA0with\xA0it: the\xA0two\xA0rules are handed one text and\xA0read that\xA0run alike, and\xA0the\xA0break this one writes goes in\xA0front of\xA0whatever run already stands there rather than\xA0over\xA0it, which\xA0at\xA0the\xA0end of\xA0a\xA0file would leave the\xA0last line a\xA0run of\xA0whitespace. Where a\xA0comment stands behind the\xA0declaration instead, the\xA0run is bounded by\xA0that\xA0comment exactly as\xA0it is inside\xA0a\xA0block, and\xA0it is read and\xA0written there like\xA0any other, so\xA0<code>color: /*comment*/</code> is settled by\xA0one run of\xA0<code>--fix</code> and\xA0<code>never</code> reports the\xA0run it used to\xA0pass over. A\xA0declaration standing at\xA0the\xA0top of\xA0a\xA0<code>&lt;style&gt;</code> element of\xA0an\xA0HTML page, or\xA0of\xA0a\xA0styled template of\xA0JavaScript, is the\xA0same shape: a\xA0stylesheet embedded in\xA0either ends in\xA0that\xA0raw as\xA0a\xA0file does. One standing in\xA0an\xA0inline <code>style</code> attribute is not, since\xA0no rule closes the\xA0last line of\xA0an\xA0attribute, and\xA0the\xA0run there is read as\xA0it is inside\xA0a\xA0block.</p></li><li><p>The\xA0fix of\xA0a\xA0rule that\xA0writes at\xA0the\xA0end of\xA0a\xA0text no\xA0longer writes into\xA0the\xA0comment standing behind a\xA0call whose name is spelled with\xA0anything but\xA0an\xA0ASCII word (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/398" target="_blank" rel="noreferrer">#398</a>). The\xA0guard that\xA0keeps such a\xA0write out of\xA0an\xA0end-of-line comment read the\xA0name in\xA0front of\xA0<code>url(</code> in\xA0the\xA0ASCII word characters and\xA0the\xA0hyphen alone, so\xA0a\xA0code point of\xA0an\xA0identifier lying outside ASCII, the\xA0closing brace of\xA0an\xA0interpolation and\xA0the\xA0whitespace that\xA0closes a\xA0hexadecimal escape each meant that\xA0no name stood there: <code>éurl(http://a/b.png)</code>, <code>@{p}url(http://a/b.png)</code>, <code>#{$p}url(http://a/b.png)</code> and\xA0<code>\\75 url(http://a/b.png)</code> were taken for\xA0addresses whose double slashes open nothing, and\xA0the\xA0fix of\xA0<code>block-closing-brace-space-before</code>, <code>declaration-block-semicolon-space-before</code>, <code>declaration-block-trailing-semicolon</code> and\xA0<code>media-feature-parentheses-space-inside</code> wrote behind them under\xA0<code>postcss-less</code> and\xA0<code>postcss-scss</code> alike, where the\xA0same call named <code>aurl</code> was left alone. The\xA0guard asks the\xA0one reading of\xA0what a\xA0<code>url()</code> is that\xA0the\xA0plugin holds now, so\xA0all of\xA0them are answered alike. By\xA0that\xA0same reading an\xA0address whose three\xA0letters are spelled with\xA0an\xA0escape, <code>u\\rl(http://a/b.png)</code>, is the\xA0address CSS names and\xA0carries no comment at\xA0all, so\xA0a\xA0fix that\xA0used to\xA0hold back there writes\xA0— a\xA0shape only a\xA0Less file reaches, and\xA0one Less itself refuses, <code>postcss-scss</code> reading the\xA0double slashes behind it as\xA0a\xA0comment that\xA0leaves the\xA0file unparsable.</p></li><li><p>The\xA0<code>declaration-colon-space-after</code> and\xA0<code>declaration-colon-newline-after</code> rules now read the\xA0whitespace behind the\xA0colon of\xA0a\xA0declaration that\xA0prints nothing there and\xA0carries no semicolon behind it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/387" target="_blank" rel="noreferrer">#387</a>). Nothing closes such a\xA0declaration, so\xA0that\xA0run leaves it altogether\xA0— it stands in\xA0the\xA0raw of\xA0the\xA0node written behind it, and\xA0in\xA0the\xA0block&#39;s own raw where the\xA0declaration is the\xA0last thing the\xA0block holds\xA0— and\xA0neither rule looked in\xA0either place. <code>a { color: }</code> drew <code>Expected single space after &quot;:&quot;</code> over\xA0the\xA0space already standing there and\xA0the\xA0fix wrote a\xA0second one beside it, so\xA0the\xA0file grew by\xA0a\xA0character on\xA0every run of\xA0<code>--fix</code> and\xA0never came to\xA0rest; <code>never</code> was the\xA0other half of\xA0the\xA0same blindness and\xA0reported nothing at\xA0all about\xA0a\xA0run of\xA0any width. The\xA0run is now read in\xA0every container that\xA0can hold such a\xA0declaration\xA0— a\xA0rule, an\xA0at-rule, the\xA0declaration Sass spells a\xA0nested property with, and\xA0an\xA0inline <code>style</code> attribute, which\xA0closes on\xA0its own quotation mark rather than\xA0on\xA0a\xA0brace\xA0— and\xA0written where it was read, so\xA0<code>a { color: }</code> passes <code>always</code> in\xA0silence, <code>never</code> takes the\xA0run away, and\xA0one run of\xA0<code>--fix</code> settles the\xA0file. Two\xA0things follow from\xA0that. The\xA0run is the\xA0whole of\xA0what stands behind the\xA0colon, so\xA0where it is a\xA0line break <code>always</code> writes its single space over\xA0that\xA0break and\xA0the\xA0closing brace comes up onto\xA0the\xA0declaration&#39;s line, which\xA0is what the\xA0option asks of\xA0a\xA0declaration that\xA0has no value to\xA0keep the\xA0two\xA0apart. And\xA0where the\xA0declaration closes the\xA0block, that\xA0run is also the\xA0whitespace <code>block-closing-brace-space-before</code> and\xA0<code>block-closing-brace-newline-before</code> measure: both readings write into\xA0the\xA0raw the\xA0run is in\xA0now, so\xA0a\xA0configuration listing a\xA0colon rule beside another rule that\xA0writes into\xA0such a\xA0block rests on\xA0a\xA0file rather than\xA0growing on\xA0every run, and\xA0where the\xA0two\xA0options contradict each other the\xA0file is the\xA0one the\xA0rule listed last asks for, with\xA0the\xA0other rule&#39;s warning standing. A\xA0block ending on\xA0a\xA0custom property is the\xA0shape that\xA0still grows there, and\xA0it grows without\xA0any help from\xA0a\xA0colon rule: <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/538" target="_blank" rel="noreferrer">#538</a>. A\xA0declaration standing at\xA0the\xA0top level of\xA0a\xA0stylesheet is the\xA0third place such a\xA0run can stand, and\xA0what the\xA0two\xA0rules do there is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/537" target="_blank" rel="noreferrer">#537</a> above.</p></li><li><p>The\xA0<code>@stylistic/styled/indentation</code> rule no\xA0longer writes the\xA0indentation of\xA0the\xA0host line into\xA0a\xA0template whose first node stands on\xA0the\xA0line of\xA0the\xA0backtick (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/453" target="_blank" rel="noreferrer">#453</a>). A\xA0single-line template on\xA0an\xA0indented line of\xA0the\xA0host used to\xA0draw <code>Expected indentation of 0 tabs</code> and\xA0get that\xA0line&#39;s tabs written inside\xA0it, and\xA0a\xA0template whose content starts right behind the\xA0backtick drew the\xA0template&#39;s own level and\xA0got that\xA0many tabs more; such a\xA0node is now asked for\xA0no indentation at\xA0all, as\xA0the\xA0first line of\xA0a\xA0plain stylesheet is.</p></li><li><p>The\xA0<code>indentation</code> rule now writes the\xA0indentation it asks for\xA0over\xA0a\xA0bare carriage return or\xA0a\xA0form feed standing in\xA0front of\xA0a\xA0node or\xA0a\xA0closing brace (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/452" target="_blank" rel="noreferrer">#452</a>). Both are whitespace to\xA0PostCSS and\xA0no line break, so\xA0the\xA0rule read them as\xA0part of\xA0the\xA0indentation it reported, while\xA0the\xA0fix looked for\xA0spaces and\xA0tabs alone and\xA0wrote nothing\xA0— and\xA0<code>--fix</code> then discarded the\xA0warning as\xA0fixed. The\xA0reported run is now the\xA0one the\xA0fix writes over, and\xA0one run of\xA0<code>--fix</code> settles such a\xA0file.</p></li><li><p>The\xA0<code>indentation</code> rule now writes the\xA0indentation of\xA0a\xA0line an\xA0at-rule swallowed in\xA0front of\xA0that\xA0line (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/375" target="_blank" rel="noreferrer">#375</a>). An\xA0at-rule with\xA0neither a\xA0block nor\xA0a\xA0semicolon of\xA0its own runs to\xA0the\xA0brace closing its block, so\xA0a\xA0comment written between\xA0its params and\xA0that\xA0brace is filed into\xA0the\xA0at-rule&#39;s whitespace rather than\xA0into\xA0a\xA0node of\xA0the\xA0block; the\xA0rule measured such a\xA0line and\xA0reported it, and\xA0wrote the\xA0fix onto\xA0the\xA0end of\xA0the\xA0at-rule&#39;s own line, so\xA0the\xA0file grew a\xA0level on\xA0every run of\xA0<code>--fix</code> and\xA0the\xA0reported line never changed. One run now settles it.</p></li><li><p>The\xA0<code>@stylistic/styled/indentation</code> rule no\xA0longer ends the\xA0lint in\xA0a\xA0<code>TypeError</code> on\xA0a\xA0styled template standing on\xA0an\xA0indented line of\xA0a\xA0carriage-return-broken file (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/377" target="_blank" rel="noreferrer">#377</a>). The\xA0line the\xA0template&#39;s expression opens on\xA0is now read the\xA0way <code>postcss-styled-syntax</code> counts the\xA0host&#39;s lines\xA0— a\xA0Windows pair as\xA0one, then a\xA0line feed, a\xA0bare carriage return or\xA0either separator of\xA0Unicode, and\xA0no form feed, measured of\xA0that\xA0parser itself\xA0— and\xA0so\xA0is the\xA0break that\xA0makes a\xA0template multiline, so\xA0the\xA0level such a\xA0template is held to\xA0is the\xA0level its host really gives it.</p></li><li><p>The\xA0<code>media-feature-name-case</code> rule no\xA0longer ends the\xA0lint in\xA0a\xA0<code>TypeError</code> on\xA0a\xA0set of\xA0media parameters that\xA0ends inside\xA0a\xA0parenthesised block left open, <code>@media (a( {}</code> for\xA0one (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/399" target="_blank" rel="noreferrer">#399</a>). PostCSS reads a\xA0parenthesis the\xA0parameters of\xA0an\xA0at-rule never close to\xA0the\xA0end of\xA0the\xA0file, so\xA0such an\xA0at-rule has no block and\xA0everything below it is its parameters; and\xA0<code>postcss-scss</code> ends the\xA0parameters at\xA0a\xA0double slash it reads as\xA0a\xA0comment, so\xA0under\xA0that\xA0syntax <code>@media (min-width: aurl(http://a/b.png)) { a { b: 1px; } }</code> ends inside\xA0the\xA0call. Where a\xA0call or\xA0a\xA0block nested in\xA0the\xA0open one ran to\xA0that\xA0end, the\xA0rule handed the\xA0media query parser a\xA0run of\xA0tokens with\xA0no end it could read, and\xA0the\xA0parser threw while\xA0building the\xA0parse error it was about\xA0to\xA0report. Such a\xA0block is now read the\xA0way CSS reads one the\xA0file ends in, as\xA0closed where the\xA0parameters end: a\xA0feature name standing inside\xA0it is reported and\xA0fixed like\xA0any other, and\xA0a\xA0block naming none is passed over. Nothing the\xA0rule said of\xA0a\xA0set of\xA0parameters it could read before\xA0has changed.</p></li><li><p>The\xA0<code>no-empty-first-line</code> rule no\xA0longer ends the\xA0lint in\xA0an\xA0error on\xA0a\xA0stylesheet that\xA0opens with\xA0an\xA0empty line and\xA0holds nothing but\xA0free semicolons and\xA0whitespace, <code>\\n;</code> for\xA0one (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/602" target="_blank" rel="noreferrer">#602</a>). Such a\xA0file gives the\xA0parser no node to\xA0hand over, so\xA0the\xA0whole of\xA0it stands in\xA0the\xA0root&#39;s trailing raw, and\xA0the\xA0fix used to\xA0ask the\xA0first node, which\xA0such a\xA0root does not\xA0have, for\xA0the\xA0run the\xA0file opens with: <code>--fix</code> threw <code>The root node must have the first node.</code> and\xA0the\xA0lint ended there, whatever else the\xA0configuration listed. The\xA0break is now taken out of\xA0the\xA0raw that\xA0holds it, and\xA0the\xA0file comes out as\xA0<code>;</code>, the\xA0way <code>\\n;a {}</code> has always come out as\xA0<code>;a {}</code>. The\xA0same held under\xA0<code>postcss-scss</code>, <code>postcss-less</code> and\xA0<code>postcss-styled-syntax</code>, and\xA0under\xA0<code>postcss-html</code> for\xA0a\xA0<code>&lt;style&gt;</code> element with\xA0an\xA0empty line behind its opening tag, since\xA0that\xA0syntax keeps the\xA0first break with\xA0the\xA0page; it holds no more.</p></li><li><p>The\xA0output no\xA0longer turns on\xA0the\xA0order the\xA0configuration lists two\xA0rules in\xA0— Stylelint runs each rule once, in\xA0the\xA0order its entries are spelled, and\xA0a\xA0plugin&#39;s rules stand nowhere in\xA0Stylelint&#39;s own registry, so\xA0that\xA0order is the\xA0user&#39;s:</p><ul><li>The\xA0<code>declaration-colon-space-after</code> and\xA0<code>declaration-colon-newline-after</code> rules now read the\xA0run behind the\xA0colon of\xA0a\xA0declaration closing its block the\xA0way <code>declaration-block-trailing-semicolon</code> will leave it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/536" target="_blank" rel="noreferrer">#536</a>). Whether that\xA0run belongs to\xA0the\xA0declaration or\xA0to\xA0the\xA0block turns on\xA0the\xA0semicolon behind it, and\xA0that\xA0rule writes and\xA0takes away the\xA0semicolon; the\xA0colon rules read the\xA0file as\xA0it stood at\xA0their own turn, so\xA0the\xA0file <code>--fix</code> left went with\xA0the\xA0order the\xA0configuration listed the\xA0two\xA0in\xA0— <code>a { color: }</code> under\xA0<code>always</code> beside <code>declaration-colon-newline-after: always</code> came out as\xA0<code>a { color:\\n; }</code> in\xA0one order and\xA0as\xA0<code>a { color:;\\n }</code> with\xA0the\xA0newline rule&#39;s warning in\xA0the\xA0other, and\xA0<code>a { color: ; }</code> under\xA0<code>never</code> beside <code>declaration-colon-space-after: never</code> as\xA0<code>a { color:}</code> in\xA0one order and\xA0as\xA0<code>a { color: }</code> with\xA0the\xA0space rule&#39;s warning in\xA0the\xA0other. Both orders leave one file now, the\xA0one the\xA0first of\xA0those pairs wrote, and\xA0nothing is reported afterwards. The\xA0reading holds without\xA0<code>--fix</code> as\xA0well: under\xA0a\xA0live <code>always</code> the\xA0run in\xA0front of\xA0the\xA0brace of\xA0<code>a { color: }</code> is the\xA0block&#39;s, since\xA0the\xA0semicolon is to\xA0part it from\xA0the\xA0colon, so\xA0<code>declaration-colon-space-after: always</code> reports the\xA0space it asks for\xA0behind the\xA0colon there, where it used to\xA0take the\xA0run in\xA0front of\xA0the\xA0brace for\xA0it; and\xA0under\xA0a\xA0live <code>never</code> the\xA0run in\xA0front of\xA0the\xA0semicolon of\xA0<code>a { color: ; }</code> is read as\xA0gone with\xA0it, and\xA0the\xA0run behind the\xA0semicolon as\xA0the\xA0colon&#39;s. A\xA0rule whose fix the\xA0configuration turned off, or\xA0that\xA0a\xA0disable comment silences over\xA0the\xA0declaration, or\xA0that is handed a\xA0secondary option it refuses, moves the\xA0semicolon nowhere, and\xA0the\xA0colon rules read the\xA0file as\xA0it stands beside one. And\xA0the\xA0semicolon <code>always</code> writes is written finished with\xA0the\xA0space <code>declaration-block-semicolon-space-before: always</code> asks for\xA0only where that\xA0space is not\xA0there already: a\xA0colon rule listed ahead writes its space onto\xA0the\xA0tail of\xA0the\xA0declaration&#39;s <code>raws.between</code>, which\xA0the\xA0parser reads as\xA0the\xA0value&#39;s head on\xA0the\xA0run after, and\xA0the\xA0semicolon rule reads it there too\xA0— so\xA0<code>a { color: }</code> under\xA0those three\xA0comes out as\xA0<code>a { color: ; }</code> in\xA0one run of\xA0<code>--fix</code> whichever order lists them, where it used to\xA0gain a\xA0second space in\xA0one order and\xA0lose it on\xA0the\xA0run after.</li><li>The\xA0<code>indentation</code> rule now measures the\xA0lines every writer of\xA0the\xA0run has finished, whatever order the\xA0configuration lists the\xA0rules in\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/353" target="_blank" rel="noreferrer">#353</a>): its check takes the\xA0run&#39;s last turn, behind even the\xA0checks deferred for\xA0their lineness options, whose breaks it therefore reaches too. A\xA0line break written by\xA0another rule of\xA0the\xA0same run used to\xA0be invisible to\xA0<code>indentation</code> whenever it was listed first\xA0— the\xA0line that\xA0break began kept whatever whitespace stood there, drew no warning, and\xA0waited for\xA0the\xA0next run of\xA0<code>--fix</code>. With\xA0this the\xA0refusal a\xA0stylesheet of\xA0another syntax draws under\xA0the\xA0core&#39;s names is attributed to\xA0the\xA0first rule that\xA0takes its turn, which\xA0<code>indentation</code> no\xA0longer is.</li><li>A\xA0rule configured with\xA0a\xA0<code>-single-line</code> or\xA0<code>-multi-line</code> option now answers that\xA0question of\xA0the\xA0text every writer of\xA0the\xA0run has finished, whatever order the\xA0configuration lists the\xA0rules in\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/355" target="_blank" rel="noreferrer">#355</a>): its check is put off until\xA0the\xA0plugin&#39;s last configured rule has had its turn. Thirty-five\xA0of\xA0the\xA0plugin&#39;s rules take such an\xA0option, turning on\xA0whether a\xA0thing stands on\xA0one line or\xA0over\xA0several, and\xA0the\xA0question used to\xA0be asked at\xA0the\xA0rule&#39;s own turn, while\xA0another rule of\xA0the\xA0same run was about\xA0to\xA0break the\xA0block\xA0— one order left a\xA0rule the\xA0user turned on\xA0silently unsatisfied (<code>block-opening-brace-space-before: always-multi-line</code> said nothing about\xA0a\xA0block <code>declaration-block-semicolon-newline-after: always</code> broke a\xA0moment later, and\xA0the\xA0space it asks for\xA0was never written), and\xA0elsewhere the\xA0two\xA0orders rested on\xA0two\xA0different files, each content with\xA0itself. Of\xA0the\xA0262\xA0rows the\xA0oracle of\xA0rule pairs held, the\xA0249\xA0of\xA0this class are gone and\xA0none came in\xA0their place: what was left unsatisfied rests fixed in\xA0one run with\xA0nothing to\xA0report, and\xA0what rested on\xA0two\xA0files rests on\xA0one. What stays order-bound is named: two\xA0rules both put off still answer between\xA0themselves in\xA0the\xA0configuration&#39;s order, and\xA0the\xA0pairs of\xA0<code>indentation</code> are <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/353" target="_blank" rel="noreferrer">#353</a>&#39;s. The\xA0machinery of\xA0the\xA0shared run moved with\xA0it: a\xA0rule whose check waits runs after\xA0every rule ahead of\xA0it as\xA0well, so\xA0it writes the\xA0run the\xA0four\xA0declaration rules share only where every rule that\xA0has already had its say\xA0— by\xA0warning, or\xA0by\xA0staying content\xA0— accepts what the\xA0write leaves, and\xA0the\xA0warning of\xA0the\xA0rule the\xA0configuration contradicted stands wherever none would.</li><li>The\xA0rules about\xA0the\xA0runs beside a\xA0declaration&#39;s colon and\xA0semicolon now read whitespace the\xA0way PostCSS&#39;s tokenizer reads it\xA0— a\xA0space, a\xA0tab, a\xA0line feed, a\xA0carriage return or\xA0a\xA0form feed, and\xA0nothing else (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/494" target="_blank" rel="noreferrer">#494</a>). A\xA0vertical tab and\xA0a\xA0no-break space are words to\xA0the\xA0tokenizer, and\xA0the\xA0fixes used to\xA0read them with\xA0<code>\\s</code> and\xA0take them for\xA0the\xA0run: <code>declaration-colon-space-after: always</code> turned <code>a { color:\\v; }</code> into\xA0<code>a { color: ; }</code>, carrying the\xA0character off in\xA0silence, <code>declaration-block-semicolon-space-before: always</code> took it out from\xA0behind a\xA0word, and\xA0which\xA0of\xA0the\xA0two\xA0files a\xA0pair of\xA0such rules left depended on\xA0the\xA0order the\xA0configuration listed them in. Such a\xA0character is part of\xA0the\xA0value&#39;s word now: no fix rewrites it, the\xA0runs end at\xA0it the\xA0way the\xA0parser&#39;s do, and\xA0the\xA0pairs rest on\xA0one file in\xA0both orders wherever their options agree. The\xA0same question\xA0— whether a\xA0run opens on\xA0a\xA0line break\xA0— is asked by\xA0the\xA0<code>always</code> options of\xA0<code>media-query-list-comma-newline-after</code> over\xA0a\xA0media query&#39;s parameters, and\xA0their fix now steps over\xA0the\xA0tokenizer&#39;s whitespace only, so\xA0such a\xA0character behind the\xA0comma is no\xA0longer taken for\xA0an\xA0opening break that is not\xA0there; the\xA0run its <code>never-multi-line</code> option rewrites still goes the\xA0wide way of\xA0the\xA0families outside this machinery, filed as\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/496" target="_blank" rel="noreferrer">#496</a>.</li><li>The\xA0run <code>declaration-colon-newline-after</code> writes down to\xA0the\xA0neighbour&#39;s bare break now loses a\xA0bare carriage return and\xA0a\xA0form feed along with\xA0the\xA0spaces and\xA0tabs (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/488" target="_blank" rel="noreferrer">#488</a>). Those two\xA0are whitespace and\xA0no line break to\xA0PostCSS&#39;s line counter and\xA0to\xA0the\xA0rules about\xA0this run, and\xA0the\xA0trim of\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/417" target="_blank" rel="noreferrer">#417</a> reached spaces and\xA0tabs alone while\xA0the\xA0semicolon rule&#39;s own fix replaces the\xA0whole run\xA0— so\xA0a\xA0run spelled with\xA0one of\xA0them still went with\xA0the\xA0order, the\xA0neighbour replacing the\xA0survivor with\xA0a\xA0break of\xA0its own later in\xA0the\xA0same run and\xA0leaving a\xA0whole empty line; the\xA0carriage return of\xA0a\xA0Windows pair stays where it stands, being half of\xA0the\xA0break itself.</li><li>The\xA0break a\xA0semicolon rule&#39;s fix puts onto\xA0the\xA0end of\xA0a\xA0value is now laid out the\xA0way the\xA0parser lays it\xA0— the\xA0run in\xA0<code>raws.value</code>, <code>decl.value</code> without\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/487" target="_blank" rel="noreferrer">#487</a>). It used to\xA0land in\xA0<code>decl.value</code> itself wherever the\xA0parser had kept no raw beside the\xA0value, and\xA0both <code>declaration-colon-*-after</code> rules read that\xA0copy for\xA0their <code>-single-line</code> and\xA0<code>-multi-line</code> options, so\xA0for\xA0the\xA0rest of\xA0the\xA0run the\xA0declaration read as\xA0multi-line\xA0— a\xA0reading the\xA0next parse took back. Under\xA0<code>declaration-block-semicolon-newline-before: always</code> listed ahead of\xA0<code>declaration-colon-newline-after: always-multi-line</code> the\xA0woken option wrote a\xA0break the\xA0reparsed declaration never asked for\xA0and\xA0the\xA0two\xA0orders rested on\xA0two\xA0files for\xA0good, while\xA0ahead of\xA0<code>declaration-colon-space-after: always-single-line</code> the\xA0silenced option left <code>a { color:red; }</code> missing its space with\xA0nothing reported, the\xA0fix arriving a\xA0run late. The\xA0copies agree between\xA0a\xA0write and\xA0the\xA0next parse now, and\xA0one run does the\xA0work; a\xA0custom property&#39;s value keeps its trailing run as\xA0ever, the\xA0parser keeping it there too.</li><li>The\xA0<code>never</code> option of\xA0<code>declaration-block-trailing-semicolon</code> now takes the\xA0whitespace in\xA0front of\xA0the\xA0semicolon away with\xA0it\xA0— out of\xA0the\xA0value, out of\xA0the\xA0raw of\xA0an\xA0<code>!important</code> flag and\xA0out of\xA0a\xA0bodiless at-rule&#39;s own raw alike (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/479" target="_blank" rel="noreferrer">#479</a>). That\xA0run used to\xA0outlive the\xA0semicolon it stood in\xA0front of, so\xA0the\xA0space or\xA0break a\xA0<code>declaration-block-semicolon-*-before</code> rule wrote there survived whenever that\xA0rule was listed first and\xA0the\xA0file went with\xA0the\xA0order, and\xA0a\xA0plain <code>a { b: c ; }</code> came out of\xA0<code>--fix</code> as\xA0<code>a { b: c }</code>, two\xA0spaces in\xA0front of\xA0the\xA0brace, with\xA0a\xA0semicolon written behind a\xA0break leaving a\xA0whole empty line. The\xA0layout around a\xA0semicolon parked behind a\xA0comment stays as\xA0it was, and\xA0where the\xA0declaration ends with\xA0an\xA0inline comment nothing is trimmed, since\xA0the\xA0break standing there is what closes it.</li><li>The\xA0<code>declaration-colon-newline-after</code> rule now writes the\xA0whitespace run it shares with\xA0the\xA0semicolon down to\xA0the\xA0bare line break the\xA0<code>declaration-block-semicolon-newline-before</code> rule asks for, wherever the\xA0configuration lists that\xA0rule (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/417" target="_blank" rel="noreferrer">#417</a>). Where a\xA0declaration&#39;s value is nothing but\xA0whitespace\xA0— or\xA0nothing but\xA0a\xA0comment on\xA0the\xA0colon&#39;s line and\xA0whitespace\xA0— one break answers both rules, and\xA0the\xA0rule running first used to\xA0decide how much of\xA0the\xA0run beside it went with\xA0that\xA0break: <code>a { color: /*c*/ ; }</code> came out of\xA0<code>--fix</code> as\xA0<code>a { color: /*c*/\\n; }</code> with\xA0the\xA0semicolon rule listed first and\xA0as\xA0<code>a { color: /*c*/\\n ; }</code> with\xA0the\xA0colon rule listed first, each file one the\xA0pair is content with. The\xA0colon rule now finishes the\xA0run the\xA0way the\xA0neighbour&#39;s own fix spells it, so\xA0both orders rest on\xA0the\xA0first of\xA0the\xA0two.</li><li>The\xA0<code>no-missing-end-of-source-newline</code> rule now closes a\xA0file ending on\xA0a\xA0free semicolon the\xA0same way whichever side of\xA0<code>no-extra-semicolons</code> it is listed on\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/390" target="_blank" rel="noreferrer">#390</a>). Everything standing behind the\xA0last block is parked in\xA0one raw of\xA0the\xA0root, and\xA0this rule&#39;s fix asked that\xA0raw alone whether the\xA0file had ended its last line\xA0— but\xA0<code>no-extra-semicolons</code>, listed first, has already taken the\xA0semicolon out of\xA0it, and\xA0what is left ends on\xA0the\xA0break the\xA0file spells in\xA0front of\xA0that\xA0semicolon. So\xA0<code>@media all { a {} }</code> followed by\xA0a\xA0line break and\xA0a\xA0semicolon came out of\xA0<code>--fix</code> closed by\xA0an\xA0empty line where this rule was listed first and\xA0by\xA0a\xA0single break where it was listed second, neither run reporting anything about\xA0the\xA0other&#39;s output. The\xA0raw and\xA0the\xA0file are both asked now, and\xA0the\xA0run of\xA0spaces the\xA0file ends on\xA0comes off only where the\xA0two\xA0agree it is there: the\xA0raw is the\xA0only place this fix can write, and\xA0the\xA0file is the\xA0text the\xA0warning was made about. Both orders leave the\xA0longer of\xA0the\xA0two\xA0files\xA0— the\xA0empty line where the\xA0semicolon stood\xA0— since\xA0this rule writes its break behind whatever the\xA0author put there and\xA0takes none of\xA0it away; how many empty lines a\xA0file may end on\xA0is <code>max-empty-lines</code>&#39; to\xA0say, and\xA0the\xA0whitespace-only line a\xA0semicolon standing in\xA0a\xA0run of\xA0spaces leaves behind is <code>no-eol-whitespace</code>&#39;s. Nothing about\xA0a\xA0file no neighbour has written into\xA0moves\xA0— over\xA0960\xA0runs of\xA0the\xA0rule on\xA0its own, fourteen bodies and\xA0thirty tails under\xA0<code>css</code>, <code>postcss-scss</code> and\xA0<code>postcss-less</code>, not\xA0one warning and\xA0not\xA0one output changed.</li></ul></li><li><p>A\xA0fix that\xA0could only be written by\xA0commenting the\xA0code out is now reported instead\xA0— an\xA0inline comment ends only with\xA0a\xA0line break, so\xA0the\xA0character an\xA0option asks for\xA0has nowhere to\xA0go:</p><ul><li>The\xA0<code>function-parentheses-newline-inside</code> rule no\xA0longer takes the\xA0code behind a\xA0function&#39;s opening parenthesis into\xA0an\xA0inline comment standing in\xA0front of\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/129" target="_blank" rel="noreferrer">#129</a>). The\xA0line break in\xA0that\xA0whitespace is what closes such a\xA0comment, so\xA0the\xA0<code>never-multi-line</code> option cannot be satisfied without\xA0commenting out the\xA0rest of\xA0the\xA0value, and\xA0the\xA0problem is now reported rather than\xA0fixed.</li><li>The\xA0<code>media-feature-parentheses-space-inside</code> rule no\xA0longer takes the\xA0closing parenthesis of\xA0a\xA0media feature into\xA0an\xA0inline comment standing in\xA0front of\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/152" target="_blank" rel="noreferrer">#152</a>). The\xA0line break in\xA0that\xA0whitespace is what closes such a\xA0comment, so\xA0the\xA0<code>never</code> option cannot be satisfied without\xA0commenting out the\xA0rest of\xA0the\xA0query, and\xA0the\xA0problem is now reported rather than\xA0fixed.</li><li>The\xA0<code>media-query-list-comma-space-before</code> rule no\xA0longer takes a\xA0comma standing behind an\xA0inline comment onto\xA0that\xA0comment&#39;s line (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/137" target="_blank" rel="noreferrer">#137</a>). The\xA0line break in\xA0front of\xA0the\xA0comma is what closes such a\xA0comment, so\xA0neither option can be satisfied without\xA0commenting out the\xA0rest of\xA0the\xA0query and\xA0the\xA0block behind it, and\xA0the\xA0problem is now reported rather than\xA0fixed.</li><li>The\xA0<code>value-list-comma-space-before</code> rule no\xA0longer takes a\xA0comma standing behind an\xA0inline comment onto\xA0that\xA0comment&#39;s line (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/136" target="_blank" rel="noreferrer">#136</a>). The\xA0line break in\xA0front of\xA0the\xA0comma is what closes such a\xA0comment, so\xA0neither option can be satisfied without\xA0commenting out the\xA0rest of\xA0the\xA0declaration, and\xA0the\xA0problem is now reported rather than\xA0fixed.</li><li>The\xA0<code>function-comma-space-before</code>, <code>function-comma-space-after</code>, <code>function-comma-newline-before</code> and\xA0<code>function-comma-newline-after</code> rules no\xA0longer write into\xA0an\xA0inline comment standing in\xA0a\xA0function&#39;s arguments (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/135" target="_blank" rel="noreferrer">#135</a>): <ul><li>a\xA0comma standing behind such a\xA0comment is no\xA0longer taken onto\xA0its line, since\xA0the\xA0line break in\xA0front of\xA0the\xA0comma is what closes the\xA0comment, so\xA0the\xA0problem is now reported rather than\xA0fixed;</li><li>a\xA0comma standing inside\xA0the\xA0text of\xA0such a\xA0comment is no\xA0longer taken for\xA0a\xA0comma of\xA0the\xA0value at\xA0all, so\xA0nothing is reported for\xA0it and\xA0nothing written near it.</li></ul></li><li>The\xA0<code>declaration-colon-space-before</code> rule no\xA0longer rewrites a\xA0declaration whose property is followed by\xA0an\xA0inline comment into\xA0code that\xA0does not\xA0parse (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/88" target="_blank" rel="noreferrer">#88</a>). Such a\xA0comment ends only with\xA0a\xA0line break, so\xA0neither option can be satisfied without\xA0taking the\xA0colon into\xA0the\xA0comment, and\xA0the\xA0problem is now reported rather than\xA0fixed.</li><li>The\xA0<code>block-opening-brace-newline-before</code> rule no\xA0longer takes the\xA0opening brace into\xA0an\xA0inline comment standing in\xA0front of\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/89" target="_blank" rel="noreferrer">#89</a>). Such a\xA0comment ends only with\xA0a\xA0line break, so\xA0the\xA0<code>never-*</code> options cannot be satisfied without\xA0commenting the\xA0whole block out, and\xA0the\xA0problem is now reported rather than\xA0fixed.</li><li>The\xA0<code>declaration-bang-space-before</code> rule no\xA0longer takes <code>!important</code> into\xA0an\xA0inline comment standing in\xA0front of\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/116" target="_blank" rel="noreferrer">#116</a>). Such a\xA0comment ends only with\xA0a\xA0line break, so\xA0neither option can be satisfied without\xA0commenting out the\xA0flag and\xA0the\xA0semicolon behind it, and\xA0the\xA0problem is now reported rather than\xA0fixed. A\xA0double slash belonging to\xA0an\xA0address, or\xA0standing inside\xA0a\xA0string, opens no comment of\xA0that\xA0kind, so\xA0a\xA0flag written on\xA0the\xA0line under\xA0a\xA0value that\xA0ends in\xA0<code>url(http://example.com/a.png)</code> is still pulled up to\xA0it.</li><li>The\xA0<code>declaration-block-semicolon-space-before</code> rule no\xA0longer takes the\xA0semicolon into\xA0an\xA0inline comment standing in\xA0front of\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/117" target="_blank" rel="noreferrer">#117</a>). Such a\xA0comment ends only with\xA0a\xA0line break, so\xA0neither option can be satisfied without\xA0commenting the\xA0semicolon out, and\xA0<code>!important</code> along with\xA0it, and\xA0the\xA0problem is now reported rather than\xA0fixed. The\xA0<code>always</code> options no\xA0longer rewrite everything standing between\xA0the\xA0value and\xA0<code>!important</code> either, so\xA0a\xA0comment there, the\xA0line the\xA0flag is written on, and\xA0the\xA0spelling the\xA0flag itself was given, <code>!IMPORTANT</code> included, all survive the\xA0fix. An\xA0<code>!important</code> that\xA0<code>postcss-less</code> reads out of\xA0the\xA0text of\xA0such a\xA0comment, where Less reads comment text and\xA0nothing else, no\xA0longer hides the\xA0comment from\xA0the\xA0rule either (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/211" target="_blank" rel="noreferrer">#211</a>): <code>color: red // c !important</code> comes back from\xA0that\xA0parser as\xA0a\xA0value of\xA0<code>red // c</code> with\xA0the\xA0flag set, and\xA0the\xA0value and\xA0the\xA0raw of\xA0the\xA0flag are asked together now, since\xA0the\xA0write follows both. That\xA0holds where the\xA0semicolon already stands on\xA0the\xA0comment&#39;s line as\xA0well, <code>color: red // c !important;</code>, which\xA0the\xA0parser keeps no raw of\xA0at\xA0all.</li><li>The\xA0<code>declaration-block-semicolon-newline-before</code> rule no\xA0longer takes the\xA0semicolon into\xA0an\xA0inline comment standing in\xA0front of\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/207" target="_blank" rel="noreferrer">#207</a>). The\xA0<code>never-multi-line</code> option trimmed every trailing whitespace of\xA0the\xA0value, or\xA0of\xA0<code>!important</code> where the\xA0declaration carries it, and\xA0the\xA0line break closing such a\xA0comment went with\xA0the\xA0rest, so\xA0<code>color: red // keep me</code> followed by\xA0<code>;</code> on\xA0the\xA0next line came out of\xA0<code>--fix</code> as\xA0<code>color: red // keep me;</code>, the\xA0semicolon commented out and\xA0the\xA0declaration ending nowhere, with\xA0nothing reported. An\xA0inline comment ends only with\xA0a\xA0line break, so\xA0the\xA0option cannot be satisfied without\xA0commenting the\xA0semicolon out, and\xA0the\xA0problem is now reported rather than\xA0fixed; a\xA0block comment, an\xA0address and\xA0a\xA0string holding a\xA0double slash are still fixed; an\xA0<code>!important</code> that\xA0<code>postcss-less</code> reads out of\xA0the\xA0text of\xA0such a\xA0comment, where Less reads comment text, does not\xA0hide the\xA0comment from\xA0the\xA0rule; and\xA0the\xA0<code>always</code> options never were in\xA0that\xA0danger, since\xA0the\xA0break they write is what closes such a\xA0comment anyway.</li><li>The\xA0<code>declaration-block-trailing-semicolon</code> rule no\xA0longer writes the\xA0semicolon into\xA0an\xA0inline comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/232" target="_blank" rel="noreferrer">#232</a>). The\xA0guard it carried asked about\xA0one raw of\xA0one node\xA0— the\xA0one standing where a\xA0bodiless at-rule&#39;s closing brace would be\xA0— so\xA0it held under\xA0<code>postcss-scss</code>, which\xA0keeps such a\xA0comment in\xA0that\xA0raw, and\xA0nowhere else. <code>postcss-less</code> keeps a\xA0comment behind a\xA0value inside\xA0the\xA0value and\xA0one behind an\xA0at-rule&#39;s parameters inside\xA0the\xA0parameters, so\xA0<code>color: pink // keep me</code> closing a\xA0block came out of\xA0<code>--fix</code> as\xA0<code>color: pink // keep me;</code>, the\xA0semicolon commented out and\xA0the\xA0block ending nowhere, with\xA0nothing reported, and\xA0<code>@include x // keep me</code> went the\xA0same way. Whichever text the\xA0node the\xA0semicolon is written behind ends with\xA0is asked now, the\xA0value and\xA0the\xA0raw of\xA0<code>!important</code> together where that\xA0node is a\xA0declaration, so\xA0an\xA0<code>!important</code> that\xA0<code>postcss-less</code> reads out of\xA0the\xA0text of\xA0such a\xA0comment does not\xA0hide it either; an\xA0inline comment ends only with\xA0a\xA0line break, so\xA0the\xA0<code>always</code> option cannot be satisfied without\xA0commenting the\xA0semicolon out, and\xA0the\xA0problem is now reported rather than\xA0fixed. An\xA0address and\xA0a\xA0string holding a\xA0double slash are still fixed, plain CSS spells no comment with\xA0a\xA0double slash at\xA0all, and\xA0the\xA0<code>never</code> option never was in\xA0that\xA0danger, since\xA0it takes a\xA0semicolon away rather than\xA0writing one. A\xA0block comment standing behind a\xA0bodiless at-rule is still fixed as\xA0well, and\xA0so\xA0is one standing behind the\xA0value of\xA0a\xA0custom property, which\xA0every syntax keeps inside\xA0that\xA0value; behind the\xA0value of\xA0an\xA0ordinary declaration such a\xA0comment never reached this rule at\xA0all, since\xA0it is read as\xA0a\xA0node of\xA0its own and\xA0the\xA0declaration is then not\xA0the\xA0last of\xA0its block. A\xA0custom property written on\xA0a\xA0single line is held back for\xA0the\xA0same reason as\xA0everything else: nothing closes the\xA0comment in\xA0front of\xA0the\xA0brace, so\xA0the\xA0semicolon would stand inside\xA0it. One broken across\xA0two\xA0lines is not: both syntaxes read such a\xA0value to\xA0the\xA0end of\xA0the\xA0line and\xA0take the\xA0line break into\xA0it, so\xA0the\xA0semicolon lands past the\xA0comment rather than\xA0inside\xA0it, which\xA0is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/287" target="_blank" rel="noreferrer">#287</a>.</li><li>The\xA0<code>function-parentheses-newline-inside</code> rule no\xA0longer takes a\xA0function&#39;s closing parenthesis into\xA0an\xA0inline comment standing in\xA0front of\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/113" target="_blank" rel="noreferrer">#113</a>). Such a\xA0comment ends only with\xA0a\xA0line break, so\xA0<code>never-multi-line</code> cannot be satisfied without\xA0commenting out the\xA0parenthesis, and\xA0everything the\xA0declaration has left, and\xA0the\xA0problem is now reported rather than\xA0fixed. The\xA0<code>always</code> and\xA0<code>always-multi-line</code> options add a\xA0line break rather than\xA0take one away, and\xA0are unaffected.</li><li>The\xA0<code>function-parentheses-space-inside</code> rule no\xA0longer takes a\xA0function&#39;s closing parenthesis into\xA0an\xA0inline comment standing in\xA0front of\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/114" target="_blank" rel="noreferrer">#114</a>). Such a\xA0comment ends only with\xA0a\xA0line break, so\xA0no option can be satisfied without\xA0commenting out the\xA0parenthesis, and\xA0everything the\xA0declaration has left, and\xA0the\xA0problem is now reported rather than\xA0fixed. All four\xA0options are asked, the\xA0guard belonging to\xA0the\xA0fix rather than\xA0to\xA0any one of\xA0them. A\xA0value with\xA0no comment in\xA0it is fixed exactly as\xA0before.</li><li>The\xA0<code>function-parentheses-space-inside</code> rule no\xA0longer takes the\xA0code behind a\xA0function&#39;s opening parenthesis into\xA0an\xA0inline comment standing in\xA0front of\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/280" target="_blank" rel="noreferrer">#280</a>). The\xA0line break in\xA0that\xA0whitespace is what closes such a\xA0comment, so\xA0no option can be satisfied without\xA0commenting out the\xA0first argument, and\xA0everything the\xA0declaration has behind it, and\xA0the\xA0problem is now reported rather than\xA0fixed\xA0— which\xA0is what the\xA0closing parenthesis has done since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/114" target="_blank" rel="noreferrer">#114</a>, and\xA0what the\xA0sibling rule&#39;s opening parenthesis has done since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/129" target="_blank" rel="noreferrer">#129</a>. All four\xA0options are asked, and\xA0each of\xA0them under\xA0the\xA0reading the\xA0file&#39;s own language has, which\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/333" target="_blank" rel="noreferrer">#333</a> settled for\xA0every guard of\xA0the\xA0family: a\xA0fix that\xA0language is not\xA0at\xA0risk from\xA0goes through\xA0as\xA0before, and\xA0so\xA0does every fix of\xA0a\xA0value with\xA0no comment in\xA0it.</li><li>The\xA0<code>block-opening-brace-newline-after</code> and\xA0<code>declaration-block-semicolon-newline-after</code> rules no\xA0longer take the\xA0code behind an\xA0inline comment into\xA0that\xA0comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/248" target="_blank" rel="noreferrer">#248</a>). The\xA0line break their <code>never-multi-line</code> option takes away is the\xA0one that\xA0closes such a\xA0comment, so\xA0whatever stood behind it\xA0— a\xA0declaration, a\xA0nested rule, a\xA0whole at-rule and\xA0its block\xA0— went into\xA0the\xA0comment&#39;s text, and\xA0out of\xA0the\xA0stylesheet altogether: what the\xA0fixer wrote parsed perfectly well and\xA0the\xA0next run had nothing to\xA0say about\xA0it, though\xA0the\xA0block had lost everything it opened with\xA0and\xA0nothing anywhere said so. The\xA0problem is now reported rather than\xA0fixed. The\xA0whole run of\xA0comments a\xA0block opens with\xA0is asked about\xA0rather than\xA0the\xA0last of\xA0them alone, since\xA0that\xA0option takes every break in\xA0front of\xA0the\xA0first node away at\xA0once, and\xA0a\xA0block comment standing between\xA0an\xA0inline one and\xA0that\xA0node used to\xA0be carried into\xA0the\xA0inline comment along with\xA0it. Where the\xA0comment is one <code>postcss-less</code> reads an\xA0<code>!important</code> out of\xA0the\xA0text of, the\xA0flag is asked about\xA0with\xA0it as\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/211" target="_blank" rel="noreferrer">#211</a> had it; and\xA0where a\xA0value ends in\xA0the\xA0break that\xA0closes its own comment, the\xA0semicolon standing behind that\xA0break is asked about\xA0too, so\xA0a\xA0fix that\xA0would comment nothing out is written as\xA0before. The\xA0<code>always</code> options were never in\xA0this danger and\xA0are untouched, since\xA0the\xA0break they keep or\xA0write is what closes such a\xA0comment anyway. In\xA0<code>declaration-block-semicolon-newline-after</code> a\xA0comment a\xA0form feed closes is held back with\xA0the\xA0rest, which\xA0is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/295" target="_blank" rel="noreferrer">#295</a>: Sass ends a\xA0comment on\xA0that\xA0character and\xA0Less does not, and\xA0the\xA0shared guard reads only the\xA0breaks the\xA0two\xA0of\xA0them agree on.</li><li>The\xA0<code>block-closing-brace-space-before</code> and\xA0<code>block-closing-brace-newline-before</code> rules no\xA0longer take a\xA0block&#39;s closing brace into\xA0an\xA0inline comment standing in\xA0front of\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/231" target="_blank" rel="noreferrer">#231</a>). The\xA0whitespace between\xA0the\xA0last node of\xA0a\xA0block and\xA0its brace is what these fixes write over, and\xA0the\xA0line break in\xA0it is what closes such a\xA0comment, so\xA0every option of\xA0the\xA0first rule and\xA0the\xA0<code>never-multi-line</code> option of\xA0the\xA0second commented the\xA0brace out along with\xA0everything the\xA0block had left: <code>a {</code> followed by\xA0<code>color: pink // c</code> on\xA0a\xA0line of\xA0its own came back from\xA0<code>--fix</code> as\xA0a\xA0file with\xA0no closing brace at\xA0all, which\xA0neither Sass nor\xA0Less reads, however happily <code>postcss-less</code> parses its own output. The\xA0problem is now reported rather than\xA0fixed. A\xA0break the\xA0fix leaves standing is the\xA0one exception. Neither rule reaches the\xA0whitespace the\xA0last node of\xA0the\xA0block ends with, so\xA0a\xA0break standing there closes the\xA0comment whichever of\xA0the\xA0two\xA0is asked; and\xA0the\xA0first writes over\xA0only the\xA0whitespace ending the\xA0block&#39;s own final raw, so\xA0a\xA0break in\xA0front of\xA0a\xA0stray semicolon of\xA0that\xA0raw survives it as\xA0well, where the\xA0<code>never-multi-line</code> fix of\xA0the\xA0second takes every whitespace of\xA0the\xA0raw away. Wherever such a\xA0break stands, the\xA0brace lands outside the\xA0comment and\xA0the\xA0fix runs exactly as\xA0before. The\xA0<code>always</code> options of\xA0the\xA0second rule were never in\xA0this danger, since\xA0the\xA0break they write is what closes such a\xA0comment anyway, and\xA0a\xA0block whose brace a\xA0comment had already swallowed is put right by\xA0them.</li><li>The\xA0<code>function-parentheses-newline-inside</code> rule no\xA0longer takes a\xA0function&#39;s closing parenthesis into\xA0an\xA0inline comment with\xA0two\xA0fixes that\xA0are safe apart (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/312" target="_blank" rel="noreferrer">#312</a>). The\xA0<code>never-multi-line</code> option writes two\xA0of\xA0them in\xA0one pass over\xA0one value\xA0— one emptying the\xA0whitespace behind the\xA0opening parenthesis, one the\xA0whitespace in\xA0front of\xA0the\xA0closing one\xA0— and\xA0each was weighed against\xA0a\xA0value that\xA0still held the\xA0other one&#39;s whitespace. Where a\xA0call holds nothing but\xA0comments there is no first argument to\xA0ask about, so\xA0both questions are about\xA0that\xA0same closing parenthesis, and\xA0the\xA0break either fix leaves behind was the\xA0one keeping it out of\xA0the\xA0comment: <code>a { t: 1px // c</code> followed by\xA0a\xA0form feed, <code>calc(</code>, a\xA0line break, <code>/*b*/</code>, a\xA0line break and\xA0<code>); }</code> under\xA0<code>postcss-scss</code> came back as\xA0<code>a { t: 1px // c</code> and\xA0a\xA0form feed and\xA0<code>calc(/*b*/); }</code> on\xA0a\xA0run that\xA0reported nothing at\xA0all, the\xA0parenthesis and\xA0the\xA0semicolon and\xA0the\xA0closing brace all inside\xA0the\xA0comment under\xA0the\xA0reading Less has. The\xA0two\xA0fixes are weighed together now, over\xA0the\xA0union of\xA0what either of\xA0them empties, and\xA0neither is written where that\xA0union carries a\xA0character into\xA0a\xA0comment: both problems are reported and\xA0nothing goes into\xA0the\xA0file. A\xA0fix that is not\xA0safe on\xA0its own is still never written, and\xA0the\xA0other is then weighed as\xA0the\xA0only fix there is, so\xA0nothing is held back that\xA0was never at\xA0risk. That\xA0stylesheet is fixed under\xA0<code>postcss-scss</code> since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/333" target="_blank" rel="noreferrer">#333</a> gave the\xA0guard the\xA0reading Sass has\xA0— the\xA0form feed closes the\xA0comment, and\xA0there is nothing for\xA0the\xA0parenthesis to\xA0fall into\xA0— and\xA0left alone under\xA0<code>postcss-less</code>, where the\xA0whole of\xA0<code>calc(</code> stands inside\xA0the\xA0comment and\xA0the\xA0rule reads no call there at\xA0all. No syntax this plugin is tested under\xA0reaches that\xA0weighing any longer, and\xA0it is kept for\xA0a\xA0custom one that\xA0names neither reading, where both are owed.</li></ul></li><li><p>A\xA0fix that\xA0could only be written by\xA0leaving a\xA0file its compiler refuses is now reported instead\xA0— the\xA0parser reads the\xA0output back and\xA0the\xA0language does not:</p><ul><li>The\xA0<code>declaration-block-trailing-semicolon</code> rule no\xA0longer takes away the\xA0semicolon Less asks for\xA0behind an\xA0at-rule carrying no block of\xA0its own (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/309" target="_blank" rel="noreferrer">#309</a>). Less reads such an\xA0at-rule as\xA0running to\xA0its semicolon, so\xA0<code>a { @extend .b; }</code> came back out of\xA0<code>--fix</code> under\xA0<code>never</code> as\xA0<code>a { @extend .b }</code>, which\xA0Less refuses with\xA0<code>@extend rule is missing block or ending semi-colon</code>, and\xA0the\xA0second run reported nothing, the\xA0rule having got what it asked for. Nothing about\xA0it turned on\xA0the\xA0at-rule&#39;s name or\xA0on\xA0a\xA0comment: <code>@import</code>, <code>@layer</code>, <code>@supports</code> and\xA0an\xA0at-rule Less has never heard of\xA0went the\xA0same way, whether they closed the\xA0block on\xA0their own or\xA0stood behind a\xA0declaration. The\xA0problem is reported and\xA0the\xA0file left alone now, so\xA0a\xA0Less stylesheet written with\xA0<code>never</code> keeps a\xA0warning <code>--fix</code> cannot answer\xA0— the\xA0option asks there for\xA0something the\xA0language has nowhere to\xA0put, which\xA0is the\xA0answer this rule already gives wherever a\xA0comment leaves the\xA0semicolon nowhere to\xA0stand. Sass and\xA0plain CSS part with\xA0that\xA0semicolon as\xA0readily as\xA0with\xA0a\xA0declaration&#39;s, dart-sass compiling <code>a { @extend .b }</code> wherever the\xA0target selector exists and\xA0<code>lightningcss</code> printing the\xA0semicolon back itself, so\xA0both are fixed as\xA0they were; and\xA0so\xA0are the\xA0two\xA0shapes <code>postcss-less</code> hands over\xA0as\xA0at-rules while\xA0Less reads neither as\xA0one\xA0— a\xA0mixin call and\xA0a\xA0call to\xA0a\xA0detached ruleset, both of\xA0which\xA0the\xA0shape of\xA0the\xA0node settles. A\xA0Less variable declaration is a\xA0third such shape, and\xA0the\xA0semicolon behind one is now left standing too: telling it from\xA0an\xA0at-rule means reading its value the\xA0way Less reads it, and\xA0Less asks that\xA0the\xA0whole of\xA0that\xA0value parse as\xA0an\xA0expression of\xA0its own, <code>a { @v: pink !IMPORTANT }</code> and\xA0<code>a { @v: a:b }</code> being <code>@v rule is missing block or ending semi-colon</code> as\xA0readily as\xA0<code>a { @extend .b }</code>. So\xA0<code>a { @v: pink; }</code> under\xA0<code>never</code> is reported rather than\xA0fixed now, where it used to\xA0be fixed and\xA0rightly, and\xA0that is the\xA0price of\xA0the\xA0guard. A\xA0declaration Less reads no value in\xA0loses no fix and\xA0still breaks: that is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/358" target="_blank" rel="noreferrer">#358</a>.</li></ul></li><li><p>A\xA0fix no\xA0longer carries off what stands next to\xA0the\xA0character it changes:</p><ul><li>The\xA0fixes of\xA0the\xA0whitespace rules outside the\xA0shared-run machinery of\xA0the\xA0declaration&#39;s colon and\xA0semicolon\xA0— <code>declaration-bang-space-before</code> and\xA0<code>-after</code>, <code>declaration-colon-space-before</code>, the\xA0<code>value-list-comma-*</code>, <code>selector-list-comma-*</code>, <code>media-feature-*</code> and\xA0<code>media-query-list-comma-*</code> families, and\xA0<code>function-parentheses-newline-inside</code>\xA0— now read the\xA0run they rewrite the\xA0way PostCSS&#39;s tokenizer reads whitespace: a\xA0space, a\xA0tab, a\xA0line feed, a\xA0carriage return or\xA0a\xA0form feed, and\xA0nothing else (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/496" target="_blank" rel="noreferrer">#496</a>). A\xA0vertical tab and\xA0a\xA0no-break space are words to\xA0the\xA0tokenizer, and\xA0these fixes used to\xA0read them with\xA0<code>\\s</code> and\xA0carry them off with\xA0the\xA0run in\xA0silence: <code>declaration-bang-space-before: never</code> turned <code>a { b: c\\v !important; }</code> into\xA0<code>a { b: c!important; }</code>. Such a\xA0character is part of\xA0the\xA0word beside the\xA0run now: the\xA0fix rewrites the\xA0run alone and\xA0the\xA0character stays\xA0— and\xA0where nothing but\xA0such characters touches a\xA0parenthesis, <code>function-parentheses-newline-inside</code>&#39;s <code>never-multi-line</code> no\xA0longer reports a\xA0run that is not\xA0there.</li><li>The\xA0<code>declaration-colon-newline-after</code> rule no\xA0longer writes its line break into\xA0the\xA0text of\xA0a\xA0comment opened as\xA0<code>/*/</code> (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/400" target="_blank" rel="noreferrer">#400</a>). The\xA0rule lets a\xA0comment stand on\xA0the\xA0colon&#39;s own line and\xA0asks for\xA0the\xA0break behind that\xA0comment; it found the\xA0end of\xA0that\xA0comment by\xA0searching for\xA0<code>*/</code> from\xA0the\xA0colon, and\xA0a\xA0comment opening on\xA0a\xA0solidus lends that\xA0solidus and\xA0the\xA0star in\xA0front of\xA0it to\xA0the\xA0search, three\xA0characters into\xA0a\xA0comment that\xA0has not\xA0closed yet. The\xA0search starts behind the\xA0comment&#39;s own opening now, so\xA0<code>a { b: /*/ x */y; }</code> gets its break behind the\xA0whole of\xA0the\xA0comment and\xA0the\xA0warning stands where the\xA0comment ends rather than\xA0inside\xA0it. <code>/*/ */</code>, <code>/*/x*/</code>, <code>/*/*a*/</code>, <code>/*/*/</code> and\xA0<code>/*//*/</code> all closed early the\xA0same way.</li><li>The\xA0<code>declaration-colon-space-before</code>, <code>declaration-colon-space-after</code> and\xA0<code>declaration-colon-newline-after</code> rules no\xA0longer take a\xA0colon the\xA0property itself spells for\xA0the\xA0declaration&#39;s own (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/421" target="_blank" rel="noreferrer">#421</a>). All three\xA0looked for\xA0that\xA0colon in\xA0the\xA0whole of\xA0the\xA0text standing in\xA0front of\xA0the\xA0value, and\xA0an\xA0identifier may hold an\xA0escape, so\xA0in\xA0<code>a { b\\:c: pink; }</code> the\xA0<code>\\:</code> of\xA0the\xA0property came first and\xA0everything each rule did afterwards was measured from\xA0a\xA0character it was not\xA0about. <code>declaration-colon-newline-after</code> under\xA0<code>always</code> wrote its line break in\xA0front of\xA0the\xA0property&#39;s colon, and\xA0where the\xA0value opened on\xA0a\xA0comment it wrote that\xA0break into\xA0the\xA0comment&#39;s own text: <code>a { b\\:c:/*c*/pink; }</code> came back with\xA0the\xA0break three\xA0characters in, and\xA0the\xA0next run reported <code>Unclosed comment</code> over\xA0a\xA0file no parser would read. <code>declaration-colon-space-before</code> under\xA0<code>always</code> wrote its space in\xA0front of\xA0the\xA0property&#39;s colon and, never having written the\xA0run it reads, wrote another on\xA0every run for\xA0ever. <code>declaration-colon-space-after</code> under\xA0<code>always</code> reported a\xA0declaration whose single space was already in\xA0place, and\xA0its fix wrote nothing at\xA0all, so\xA0the\xA0warning came back on\xA0every run of\xA0the\xA0rule and\xA0no run of\xA0<code>--fix</code> ever answered it. The\xA0colon is looked for\xA0in\xA0<code>raws.between</code> alone now, the\xA0raw PostCSS files everything standing between\xA0the\xA0property and\xA0the\xA0value in, the\xA0declaration&#39;s own colon among it; the\xA0comments in\xA0front of\xA0that\xA0colon are that\xA0raw&#39;s own text and\xA0may hold a\xA0colon apiece, so\xA0the\xA0search over\xA0it is still the\xA0comment-aware one.</li><li>The\xA0<code>function-comma-*</code> rules no\xA0longer rewrite a\xA0comment standing elsewhere in\xA0the\xA0value they fix (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/238" target="_blank" rel="noreferrer">#238</a>). All four\xA0wrote their fix back as\xA0the\xA0whole value printed anew by\xA0<code>postcss-value-parser</code>, and\xA0that\xA0printing does not\xA0always give back the\xA0text it was handed: a\xA0comment opening <code>/*/</code> comes back as\xA0<code>/**/</code>, so\xA0<code>a { b: x/*/*a,b*/f(1,2)c; }</code> came out of\xA0<code>function-comma-space-before</code> under\xA0<code>always</code> as\xA0<code>a { b: x/**/*a,b*/f(1 ,2)c; }</code>\xA0— what was one comment now a\xA0comment and\xA0three\xA0characters of\xA0code, and\xA0the\xA0value naming something else. The\xA0whitespace an\xA0option asks for\xA0is written at\xA0the\xA0position of\xA0the\xA0comma it belongs to\xA0now, so\xA0every other character of\xA0the\xA0value is left exactly as\xA0it was, whatever the\xA0parser would have made of\xA0printing it.</li><li>The\xA0<code>function-parentheses-space-inside</code>, <code>function-parentheses-newline-inside</code>, <code>media-feature-parentheses-space-inside</code>, <code>color-hex-case</code> and\xA0<code>unit-case</code> rules no\xA0longer rewrite a\xA0comment standing elsewhere in\xA0the\xA0value they fix (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/272" target="_blank" rel="noreferrer">#272</a>). All five\xA0wrote their fix back as\xA0the\xA0whole value, or\xA0the\xA0whole set of\xA0media parameters, printed anew by\xA0<code>postcss-value-parser</code>, and\xA0that\xA0printing does not\xA0always give back the\xA0text it was handed: a\xA0comment opening <code>/*/</code> comes back as\xA0<code>/**/</code>, so\xA0<code>a { b: x/*/*a*/#aabbcc; }</code> came out of\xA0<code>color-hex-case</code> under\xA0<code>upper</code> as\xA0<code>a { b: x/**/*a*/#AABBCC; }</code>\xA0— what was one comment now a\xA0comment and\xA0three\xA0characters of\xA0code, and\xA0the\xA0value naming something else. Each fix is written at\xA0the\xA0position of\xA0the\xA0whitespace, the\xA0colour or\xA0the\xA0unit it belongs to\xA0now, so\xA0every other character is left exactly as\xA0it was, whatever the\xA0parser would have made of\xA0printing it. Two\xA0things travel with\xA0that. A\xA0call holding nothing but\xA0comments now gets the\xA0line break each half of\xA0<code>function-parentheses-newline-inside</code> under\xA0<code>always</code> asks for\xA0rather than\xA0one break for\xA0the\xA0two, since\xA0the\xA0closing half no\xA0longer reads the\xA0value as\xA0the\xA0opening fix has already left it: <code>a { b: f( /*c*/ /*d*/ ); }</code> reported two\xA0problems and\xA0wrote one of\xA0them. And\xA0a\xA0media query whose fix Stylelint never called\xA0— a\xA0<code>stylelint-disable</code> comment over\xA0the\xA0at-rule is one way\xA0— is no\xA0longer printed anew for\xA0nothing.</li><li>The\xA0<code>function-max-empty-lines</code> rule no\xA0longer rewrites a\xA0comment standing in\xA0the\xA0call it fixes (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/370" target="_blank" rel="noreferrer">#370</a>). It wrote its fix back as\xA0the\xA0whole call printed anew by\xA0<code>postcss-value-parser</code>, and\xA0that\xA0printing does not\xA0always give back the\xA0text it was handed: a\xA0comment opening <code>/*/</code> closes on\xA0the\xA0star it opened with, so\xA0the\xA0parser reads it three\xA0characters wide and\xA0prints it back as\xA0four, and\xA0every call holding one inherits that\xA0character. <code>a { b: f(1 /*/x*/</code> followed by\xA0two\xA0empty lines and\xA0<code>2); }</code> came out of\xA0<code>--fix</code> as\xA0<code>a { b: f(1 /**/x*/</code> and\xA0one break and\xA0<code>2); }</code>\xA0— what was one comment now a\xA0comment and\xA0three\xA0characters of\xA0code, and\xA0the\xA0call naming something else. The\xA0character was paid for\xA0twice, since\xA0the\xA0rule went on\xA0from\xA0the\xA0call by\xA0the\xA0length of\xA0what it had printed rather than\xA0by\xA0the\xA0length of\xA0the\xA0text it had replaced, so\xA0a\xA0character of\xA0the\xA0value behind the\xA0call was eaten with\xA0it: <code>a { b: f(1 /*/x*/</code> followed by\xA0two\xA0empty lines and\xA0<code>2) zz; }</code> lost the\xA0space in\xA0front of\xA0<code>zz</code> as\xA0well. The\xA0call is taken from\xA0the\xA0value as\xA0the\xA0file spells it now, and\xA0the\xA0rule goes on\xA0from\xA0where the\xA0parser marks its end, so\xA0every character of\xA0it but\xA0the\xA0line breaks the\xA0option asks to\xA0be collapsed is left exactly as\xA0it was. A\xA0comment of\xA0that\xA0shape written in\xA0the\xA0text of\xA0an\xA0end-of-line comment inside\xA0such a\xA0call is left alone with\xA0the\xA0rest.</li><li>The\xA0<code>declaration-bang-space-*</code> rules no\xA0longer write into\xA0the\xA0characters standing between\xA0two\xA0bangs of\xA0one declaration (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/239" target="_blank" rel="noreferrer">#239</a>). Both rules share a\xA0checker that\xA0finds the\xA0bangs of\xA0a\xA0declaration in\xA0the\xA0text as\xA0the\xA0file spells it and\xA0hands each of\xA0them to\xA0a\xA0fix that\xA0writes at\xA0the\xA0index it was given, and\xA0that\xA0index was counted before\xA0any earlier write had changed the\xA0length of\xA0what it replaced, so\xA0the\xA0second write landed as\xA0far past its mark as\xA0the\xA0first had lengthened the\xA0text in\xA0front of\xA0it. <code>a { b: 1px!important 2px /*c*/!important; }</code> came out of\xA0<code>declaration-bang-space-before</code> under\xA0<code>always</code> as\xA0<code>a { b: 1px !important 2px /*c* /!important; }</code>, the\xA0delimiter closing the\xA0comment cut in\xA0two, and\xA0the\xA0next run reported <code>Unclosed comment</code> on\xA0a\xA0file that\xA0no\xA0longer parses. <code>a { b: 1px!important 2px!important; }</code> came out as\xA0<code>a { b: 1px !important 2p x!important; }</code>, a\xA0space written into\xA0the\xA0middle of\xA0the\xA0value, and\xA0<code>a { b: 1px!important//!important; }</code>, whose double slash plain CSS spells no comment with, as\xA0<code>a { b: 1px !important/ /!important; }</code>. <code>declaration-bang-space-after</code> reached the\xA0same three\xA0shapes from\xA0the\xA0other side, writing the\xA0space in\xA0front of\xA0the\xA0second bang rather than\xA0behind it: <code>a { b: 1px!important 2px!important; }</code> came out as\xA0<code>a { b: 1px! important 2px !important; }</code>, whitespace at\xA0a\xA0position belonging to\xA0no bang of\xA0its own, and\xA0a\xA0further run was wanted to\xA0put a\xA0space where the\xA0option had asked for\xA0one. Every bang is given the\xA0whitespace its option asks for\xA0in\xA0one run now, each write made where its own bang stands, so\xA0every other character of\xA0the\xA0declaration is left exactly as\xA0it was and\xA0a\xA0second run finds nothing left to\xA0do.</li><li>The\xA0<code>function-whitespace-after</code> rule under\xA0<code>never</code> no\xA0longer closes up the\xA0whitespace that\xA0makes a\xA0sign the\xA0operator of\xA0a\xA0sum (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/257" target="_blank" rel="noreferrer">#257</a>). A\xA0<code>+</code> or\xA0a\xA0<code>-</code> is part of\xA0the\xA0number behind it only where a\xA0digit or\xA0a\xA0decimal point follows it; anywhere else the\xA0sign stands alone, and\xA0the\xA0whitespace in\xA0front of\xA0it is what a\xA0calculation is read by. So\xA0<code>a { b: calc(var(--x) + 1px); }</code> came out of\xA0the\xA0fix as\xA0<code>calc(var(--x)+ 1px)</code>\xA0— no\xA0longer a\xA0sum, and\xA0a\xA0declaration the\xA0browser drops rather than\xA0one merely respaced. That\xA0whitespace belongs to\xA0the\xA0sum rather than\xA0to\xA0the\xA0call now, and\xA0the\xA0reading asks nothing about\xA0the\xA0call it stands behind, so\xA0a\xA0preprocessor&#39;s arithmetic keeps its spaces with\xA0it\xA0— <code>a { b: translate(1px) + 2px; }</code> is left as\xA0it is written. A\xA0comment standing between\xA0the\xA0call and\xA0the\xA0operator no\xA0longer hides the\xA0sum, <code>calc(min(1px, 2px) /**/+ 1px)</code> being one as\xA0much as\xA0<code>calc(min(1px, 2px) + 1px)</code> is. A\xA0sign that\xA0does open a\xA0number is untouched, <code>a { b: url(x) -1px; }</code> closing up as\xA0before, and\xA0so\xA0are a\xA0<code>*</code> and\xA0a\xA0<code>/</code>, the\xA0grammar asking for\xA0no whitespace beside either. What the\xA0reading costs is a\xA0warning: a\xA0sign standing in\xA0front of\xA0a\xA0name rather than\xA0a\xA0number is read as\xA0an\xA0operator too, so\xA0<code>a { transform: translate(1px) -webkit-foo; }</code> is no\xA0longer reported, telling the\xA0two\xA0apart calling for\xA0a\xA0list of\xA0every name a\xA0calculation may hold. <code>always</code> is untouched as\xA0well: it asks for\xA0the\xA0whitespace the\xA0grammar wants, so\xA0<code>calc(var(--x)+ 1px)</code> is repaired by\xA0it rather than\xA0damaged.</li><li>The\xA0<code>function-whitespace-after</code> rule under\xA0<code>never</code> no\xA0longer closes up the\xA0whitespace that\xA0a\xA0preprocessor&#39;s own arithmetic is spelled with\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/264" target="_blank" rel="noreferrer">#264</a>). The\xA0reading <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/257" target="_blank" rel="noreferrer">#257</a> gave the\xA0rule is the\xA0one CSS has, where <code>-1px</code> is a\xA0single number: a\xA0sign with\xA0a\xA0digit or\xA0a\xA0decimal point behind it opens a\xA0number there rather than\xA0a\xA0sum, and\xA0the\xA0space in\xA0front of\xA0it belongs to\xA0the\xA0call. Sass and\xA0Less spell arithmetic of\xA0their own, in\xA0which\xA0such a\xA0sign is an\xA0operator all the\xA0same\xA0— <code>a { b: foo($a) -2px; }</code> is a\xA0list of\xA0two\xA0values and\xA0<code>a { b: foo($a)-2px; }</code> a\xA0subtraction\xA0— so\xA0the\xA0fix wrote the\xA0one in\xA0place of\xA0the\xA0other, and\xA0wrote it the\xA0same way into\xA0<code>a { b: foo(@a) -2px; }</code> under\xA0Less. The\xA0rule asks which\xA0syntax spelled the\xA0node now, the\xA0way it already asks in\xA0order to\xA0know what a\xA0double slash opens in\xA0it, and\xA0under\xA0a\xA0syntax spelling arithmetic of\xA0its own it leaves the\xA0whitespace in\xA0front of\xA0every sign where the\xA0author wrote it. The\xA0question is put to\xA0the\xA0node rather than\xA0to\xA0the\xA0file, so\xA0a\xA0page holding a\xA0plain <code>&lt;style&gt;</code> beside a\xA0<code>&lt;style lang=&quot;scss&quot;&gt;</code> has each of\xA0its blocks read by\xA0the\xA0syntax that\xA0spelled it. What the\xA0reading costs there is a\xA0warning, in\xA0two\xA0places: <code>a { b: url(x) -1px; }</code> is no\xA0longer reported under\xA0those syntaxes, telling such a\xA0list from\xA0a\xA0subtraction being the\xA0work of\xA0the\xA0language&#39;s compiler rather than\xA0of\xA0a\xA0rule about\xA0whitespace; and\xA0a\xA0plus is left alone under\xA0Sass, which\xA0reads one as\xA0an\xA0operator whatever whitespace stands beside it, since\xA0the\xA0question the\xA0rule puts to\xA0a\xA0syntax tells a\xA0preprocessor from\xA0plain CSS and\xA0does not\xA0tell Sass from\xA0Less, and\xA0one reading for\xA0the\xA0two\xA0is chosen over\xA0a\xA0second probe. A\xA0file of\xA0plain CSS is untouched, and\xA0so\xA0is <code>always</code> under\xA0every syntax; a\xA0syntax that\xA0answers nothing about\xA0itself is read as\xA0a\xA0preprocessor here, which\xA0leaves whitespace standing rather than\xA0a\xA0file broken.</li><li>The\xA0<code>selector-pseudo-class-parentheses-space-inside</code> rule no\xA0longer writes a\xA0descendant combinator into\xA0a\xA0selector whose comment closes a\xA0pseudo-class&#39;s argument (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/161" target="_blank" rel="noreferrer">#161</a>). <code>postcss-selector-parser</code> keeps no whitespace between\xA0such a\xA0comment and\xA0the\xA0end of\xA0the\xA0argument it closes, and\xA0hands the\xA0run to\xA0whatever stands behind the\xA0closing parenthesis instead, so\xA0<code>a:not( /*c*/ ):is(b)</code> came back out of\xA0the\xA0parser as\xA0<code>a:not( /*c*/) :is(b)</code>\xA0— a\xA0compound selector turned into\xA0a\xA0descendant one, matching other elements than\xA0the\xA0author wrote it for. Every fix this rule made in\xA0such a\xA0selector carried that\xA0into\xA0the\xA0file, whichever pseudo-class of\xA0it the\xA0fix was made in, and\xA0no warning ever mentioned it. A\xA0comma does the\xA0same seen from\xA0the\xA0other side, <code>a:not(b, /*c*/ , c)</code> losing the\xA0space standing in\xA0front of\xA0it. The\xA0rule now gives the\xA0parsed selector back what the\xA0parser dropped before\xA0it measures anything, so\xA0a\xA0fix writes what the\xA0option asks for\xA0and\xA0nothing else.</li><li>The\xA0<code>string-quotes</code> rule no\xA0longer drops a\xA0comment standing in\xA0the\xA0selector when\xA0it fixes an\xA0attribute value there (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/178" target="_blank" rel="noreferrer">#178</a>). The\xA0rule read <code>ruleNode.selector</code>, a\xA0copy PostCSS hands it with\xA0every comment taken out, and\xA0wrote the\xA0fix back to\xA0the\xA0same place, so\xA0<code>.foo /* x */ [title=&quot;y&quot;]</code> came out of\xA0<code>--fix</code> as\xA0<code>.foo [title=&#39;y&#39;]</code>\xA0— the\xA0comment gone and\xA0the\xA0whitespace it stood in\xA0left behind\xA0— and\xA0the\xA0warning pointed into\xA0the\xA0comment rather than\xA0at\xA0the\xA0quote it was about. The\xA0rule reads the\xA0text the\xA0file holds now and\xA0writes the\xA0fix to\xA0it, the\xA0way the\xA0other selector rules have since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/124" target="_blank" rel="noreferrer">#124</a>: the\xA0comment keeps its place, the\xA0warning stands at\xA0the\xA0quote, and\xA0under\xA0<code>postcss-scss</code> an\xA0inline comment of\xA0the\xA0selector keeps the\xA0spelling the\xA0file gives it, its text never read as\xA0strings of\xA0the\xA0selector.</li><li>The\xA0<code>indentation</code> rule no\xA0longer carries off the\xA0comments of\xA0a\xA0selector it re-indents (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/194" target="_blank" rel="noreferrer">#194</a>). It read and\xA0wrote <code>rule.selector</code>, a\xA0copy PostCSS hands it with\xA0every comment taken out, so\xA0its lines were measured against\xA0a\xA0text the\xA0file does not\xA0hold and\xA0its fix printed without\xA0the\xA0comments\xA0— <code>b // c</code> came back as\xA0<code>b </code> with\xA0a\xA0trailing space, and\xA0a\xA0block comment in\xA0a\xA0plain CSS selector went the\xA0same way. The\xA0rule measures the\xA0copy the\xA0file spells now and\xA0writes the\xA0fix to\xA0it, refilling the\xA0raw beside it under\xA0<code>postcss-scss</code>, so\xA0every comment stays where the\xA0author put it. The\xA0text of\xA0each inline comment is blanked out of\xA0the\xA0scan&#39;s own copy, since\xA0<code>style-search</code> reads the\xA0line break that\xA0closes one as\xA0part of\xA0it and\xA0would pass the\xA0lines behind it over; the\xA0blanks keep every position where it was.</li><li>The\xA0<code>block-opening-brace-space-before</code> rule no\xA0longer removes a\xA0comment standing between\xA0the\xA0selector and\xA0the\xA0opening brace when\xA0fixing (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/63" target="_blank" rel="noreferrer">#63</a>): <ul><li>a\xA0block comment is kept, and\xA0only the\xA0whitespace next to\xA0the\xA0brace changes, so\xA0such a\xA0stylesheet can now be autofixed without\xA0losing anything;</li><li>an\xA0inline comment leaves the\xA0brace nowhere to\xA0go, since\xA0the\xA0comment ends only with\xA0a\xA0line break, so\xA0the\xA0problem is now reported rather than\xA0silently rewritten.</li></ul></li><li>The\xA0<code>declaration-block-trailing-semicolon</code> rule no\xA0longer damages the\xA0block when\xA0it adds a\xA0missing semicolon after\xA0a\xA0bodiless at-rule (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/87" target="_blank" rel="noreferrer">#87</a>): <ul><li>a\xA0comment standing between\xA0the\xA0at-rule and\xA0the\xA0closing brace is kept;</li><li>the\xA0closing brace stays on\xA0its own line instead of\xA0being pulled up to\xA0the\xA0at-rule;</li><li>an\xA0inline comment leaves the\xA0semicolon nowhere to\xA0go, since\xA0the\xA0comment ends only with\xA0a\xA0line break, so\xA0the\xA0problem is now reported rather than\xA0silently rewritten.</li></ul></li><li>The\xA0<code>indentation</code> rule no\xA0longer damages a\xA0declaration whose multi-line value holds comments when\xA0fixing (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/62" target="_blank" rel="noreferrer">#62</a>). The\xA0mis-indented lines were found in\xA0the\xA0text that\xA0keeps the\xA0comments while\xA0the\xA0fix was written to\xA0a\xA0copy with\xA0the\xA0comments taken out, so\xA0every write landed past the\xA0line break it was aimed at\xA0— <code>--fix</code> cut the\xA0<code>0</code> out of\xA0<code>$somevar: ( /* some comment */ … &#39;a_property&#39;: 0 … )</code> and\xA0carried every comment off with\xA0it. Both sides work on\xA0the\xA0text the\xA0file holds now: <ul><li>the\xA0fix re-indents every mis-indented line of\xA0the\xA0value, and\xA0the\xA0comments and\xA0the\xA0value survive it;</li><li>a\xA0comment closing the\xA0line that\xA0opens the\xA0parentheses no\xA0longer costs the\xA0warning a\xA0level, so\xA0the\xA0lines inside\xA0are asked for\xA0the\xA0<code>1 tab</code> they need where <code>0 tabs</code> used to\xA0be expected.</li></ul></li><li>The\xA0<code>unit-case</code> rule no\xA0longer drops the\xA0comments a\xA0value or\xA0a\xA0set of\xA0at-rule parameters holds when\xA0it changes the\xA0case of\xA0a\xA0unit (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/233" target="_blank" rel="noreferrer">#233</a>). It read <code>decl.value</code> and\xA0<code>atRule.params</code>, the\xA0copies PostCSS hands back with\xA0every comment taken out, and\xA0wrote the\xA0whole text back to\xA0those same bare properties, discarding the\xA0raw the\xA0comments live in\xA0— so\xA0<code>a { b: 1px /* c */ 2px; }</code> came out of\xA0<code>--fix</code> as\xA0<code>a { b: 1PX 2PX; }</code>, and\xA0nothing was reported: <ul><li>every comment a\xA0value or\xA0a\xA0set of\xA0parameters holds survives the\xA0fix, in\xA0plain CSS and\xA0under\xA0both custom syntaxes, and\xA0under\xA0<code>postcss-scss</code> a\xA0comment written with\xA0a\xA0double slash survives it too;</li><li>a\xA0warning about\xA0a\xA0unit standing behind a\xA0comment now points at\xA0the\xA0column the\xA0file has it in, since\xA0the\xA0text the\xA0position is counted in\xA0is the\xA0text the\xA0file holds;</li><li>a\xA0unit standing inside\xA0an\xA0end-of-line comment a\xA0value holds is recased and\xA0reported under\xA0<code>postcss-scss</code> as\xA0well now, where only <code>postcss-less</code> used to\xA0reach it: the\xA0copy the\xA0file spells is the\xA0copy that\xA0carries such a\xA0comment, and\xA0leaving its text alone again is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/271" target="_blank" rel="noreferrer">#271</a>.</li></ul></li><li>The\xA0<code>linebreaks</code> rule no\xA0longer drops the\xA0comments a\xA0value holds when\xA0it rewrites the\xA0line breaks of\xA0a\xA0file (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/235" target="_blank" rel="noreferrer">#235</a>). It rewrote every value it walked, and\xA0the\xA0copy it rewrote was the\xA0one PostCSS hands back with\xA0the\xA0comments taken out, so\xA0a\xA0value spanning lines lost them all\xA0— <code>a { b: 1px /* c */</code> and\xA0<code>2px; }</code> on\xA0the\xA0line under\xA0it came out of\xA0<code>--fix</code> under\xA0<code>windows</code> with\xA0the\xA0comment gone. The\xA0break is now written into\xA0the\xA0copy the\xA0file spells, comments and\xA0all, and\xA0the\xA0breaks a\xA0comment itself holds are rewritten with\xA0the\xA0rest, since\xA0a\xA0break standing inside\xA0a\xA0comment is a\xA0break of\xA0the\xA0file like\xA0any other.</li><li>The\xA0<code>linebreaks</code> rule no\xA0longer drops the\xA0comments a\xA0selector holds when\xA0it rewrites the\xA0line breaks of\xA0a\xA0file (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/269" target="_blank" rel="noreferrer">#269</a>). This is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/235" target="_blank" rel="noreferrer">#235</a> one property over: the\xA0fix wrote <code>rule.selector</code>, the\xA0copy PostCSS hands back with\xA0every comment taken out, and\xA0PostCSS threw the\xA0raw those comments live in\xA0away along with\xA0the\xA0copy <code>postcss-scss</code> prints, so\xA0<code>a /* c */,</code> broken with\xA0a\xA0line feed over\xA0<code>b { c: d; }</code> came out of\xA0<code>--fix</code> under\xA0<code>windows</code> as\xA0<code>a ,</code> and\xA0the\xA0comment was gone, with\xA0nothing reported. A\xA0comment written with\xA0a\xA0double slash went the\xA0same way under\xA0<code>postcss-scss</code>. The\xA0selector is read and\xA0written in\xA0the\xA0copy the\xA0file spells now, so\xA0every comment survives the\xA0fix, in\xA0plain CSS and\xA0under\xA0both custom syntaxes. A\xA0selector holding no line break was never affected, and\xA0by\xA0accident: the\xA0fix hands such a\xA0selector back unchanged, so\xA0the\xA0bare copy still matched the\xA0raw and\xA0PostCSS went on\xA0printing the\xA0raw.</li><li>The\xA0<code>number-*</code> rules no\xA0longer drop the\xA0comments a\xA0value or\xA0a\xA0set of\xA0at-rule parameters holds when\xA0they add or\xA0take away a\xA0zero (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/268" target="_blank" rel="noreferrer">#268</a>). These rules read <code>decl.value</code> and\xA0<code>atRule.params</code>, the\xA0copies PostCSS hands back with\xA0every comment taken out, and\xA0wrote the\xA0fix back to\xA0those same bare properties, discarding the\xA0raw the\xA0comments live in\xA0— so\xA0<code>a { b: 1px /* c */ 0.50em; }</code> came out of\xA0<code>--fix</code> under\xA0<code>number-leading-zero</code> with\xA0the\xA0option <code>&quot;never&quot;</code> as\xA0<code>a { b: 1px .50em; }</code>, and\xA0nothing was reported. They read and\xA0write the\xA0copy the\xA0file spells now, so\xA0every comment keeps its place, under\xA0<code>postcss-scss</code> a\xA0comment written with\xA0a\xA0double slash keeps it too, and\xA0a\xA0warning about\xA0a\xA0zero standing behind a\xA0comment points at\xA0the\xA0column the\xA0file has it in. Under\xA0<code>postcss-less</code> that\xA0raw also carries the\xA0whitespace behind an\xA0end-of-line comment, which\xA0used to\xA0go with\xA0the\xA0comment and\xA0take the\xA0semicolon of\xA0the\xA0declaration into\xA0it. One thing goes the\xA0other way, and\xA0under\xA0<code>postcss-scss</code> alone: a\xA0fraction spelled inside\xA0an\xA0end-of-line comment such a\xA0value holds is now rewritten and\xA0reported along with\xA0the\xA0rest, where only <code>postcss-less</code> used to\xA0reach it. The\xA0copy the\xA0file spells is the\xA0copy that\xA0carries such a\xA0comment, and\xA0leaving its text alone again is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/271" target="_blank" rel="noreferrer">#271</a>.</li><li>The\xA0<code>linebreaks</code> rule no\xA0longer closes up a\xA0line break it has just reported (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/293" target="_blank" rel="noreferrer">#293</a>). The\xA0fix deleted every carriage return of\xA0a\xA0text and\xA0then, under\xA0<code>windows</code>, wrote a\xA0pair in\xA0front of\xA0every line feed\xA0— right for\xA0a\xA0Windows pair, whose line feed carries the\xA0break through\xA0the\xA0first pass, and\xA0wrong for\xA0a\xA0carriage return standing on\xA0its own, which\xA0has no line feed behind it for\xA0the\xA0second pass to\xA0find, so\xA0both options took such a\xA0break away altogether. <code>a {}</code> and\xA0<code>b {}</code> parted by\xA0a\xA0bare carriage return came back as\xA0<code>a {}b {}</code> under\xA0<code>unix</code>, and\xA0under\xA0<code>postcss-scss</code> a\xA0value whose end-of-line comment that\xA0break closed lost the\xA0rest of\xA0its block into\xA0the\xA0comment, <code>--fix</code> reporting a\xA0clean pass on\xA0the\xA0wreck. Every break the\xA0rule reads is respelled in\xA0one pass now, a\xA0Windows pair counting as\xA0one. A\xA0form feed is still neither of\xA0the\xA0two\xA0characters this rule reads, so\xA0a\xA0file broken with\xA0one is passed over\xA0as\xA0before.</li><li>The\xA0<code>function-max-empty-lines</code> rule no\xA0longer writes a\xA0call nested in\xA0another a\xA0second time, over\xA0the\xA0code standing behind the\xA0outer one (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/366" target="_blank" rel="noreferrer">#366</a>). The\xA0fix does not\xA0rewrite the\xA0value in\xA0place: it collects the\xA0text standing in\xA0front of\xA0each call it rewrites together with\xA0the\xA0rewritten call itself, moves a\xA0mark to\xA0the\xA0end of\xA0that\xA0call&#39;s own text in\xA0the\xA0source, and\xA0adds whatever stands behind that\xA0mark once the\xA0walk is over. <code>postcss-value-parser</code> walks a\xA0call before\xA0what it holds, and\xA0the\xA0text written for\xA0the\xA0outer call already carries every call nested in\xA0it, rewritten along with\xA0the\xA0rest\xA0— so\xA0by\xA0the\xA0time the\xA0walk reaches the\xA0inner one the\xA0mark already stands behind it, the\xA0text in\xA0front of\xA0it comes back empty, and\xA0the\xA0call is written a\xA0second time with\xA0everything the\xA0outer one held behind it. <code>a { b: f(</code> followed by\xA0three\xA0line breaks, <code>g(</code>, three\xA0more and\xA0<code>1)); }</code> came out of\xA0<code>--fix</code> as\xA0<code>f(</code>, one break, <code>g(</code>, one break and\xA0<code>1))g(</code>, one break and\xA0<code>1)); }</code>\xA0— a\xA0call the\xA0file spells once written twice, and\xA0a\xA0closing parenthesis with\xA0nothing to\xA0close. Plain CSS reaches this, with\xA0no comment and\xA0no custom syntax needed, and\xA0every further level of\xA0nesting adds a\xA0copy of\xA0its own. A\xA0call the\xA0fix has just rewritten is no\xA0longer walked into, since\xA0the\xA0text written for\xA0it holds what it carries already, so\xA0a\xA0nested call is collapsed along with\xA0the\xA0outer one and\xA0nothing else in\xA0the\xA0value moves. Where no fix is asked for, both calls are reported as\xA0before: each of\xA0them does hold more empty lines than\xA0the\xA0option allows. Two\xA0readings move with\xA0the\xA0fix all the\xA0same. The\xA0count of\xA0problems fixed that\xA0the\xA0<code>verbose</code> formatter prints falls to\xA0one for\xA0a\xA0whole nest of\xA0calls, that\xA0being what the\xA0fix now answers for\xA0at\xA0once. And\xA0under\xA0<code>reportNeedlessDisables</code> a\xA0<code>stylelint-disable</code> covering the\xA0inner call alone is reported as\xA0needless, the\xA0rule having nothing left to\xA0say inside\xA0it once <code>--fix</code> has answered for\xA0that\xA0call through\xA0the\xA0outer one\xA0— though\xA0the\xA0outer call&#39;s own rewrite still collapses whatever breaks stand in\xA0that\xA0range, as\xA0it did before.</li><li>The\xA0<code>named-grid-areas-alignment</code> rule no\xA0longer drops the\xA0parentheses and\xA0the\xA0arguments of\xA0a\xA0call standing in\xA0the\xA0value it aligns (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/367" target="_blank" rel="noreferrer">#367</a>). The\xA0fix rebuilt the\xA0whole value out of\xA0the\xA0nodes of\xA0a\xA0<code>postcss-value-parser</code> parse, and\xA0wrote every node of\xA0it from\xA0that\xA0node&#39;s own <code>value</code>\xA0— every node but\xA0the\xA0comments and\xA0whatever carries an\xA0end-of-line comment&#39;s text, which\xA0the\xA0entry below on\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/322" target="_blank" rel="noreferrer">#322</a> had already put on\xA0the\xA0file&#39;s own characters. For\xA0a\xA0call that\xA0<code>value</code> is the\xA0bare name: the\xA0arguments are the\xA0node&#39;s children and\xA0the\xA0parentheses are nowhere in\xA0it at\xA0all. So\xA0<code>a { grid-template-areas: &quot;a a&quot; var(--x); }</code> came out of\xA0<code>--fix</code> as\xA0<code>a { grid-template-areas: &quot;a a&quot; var; }</code>\xA0— a\xA0value naming something other than\xA0what the\xA0file spells\xA0— and\xA0a\xA0call whose parentheses held whitespace of\xA0their own had that\xA0whitespace left standing outside them, <code>f( 1 , 2 )</code> coming back as\xA0a\xA0space, an\xA0<code>f</code> and\xA0another space. Plain CSS reaches this, with\xA0no comment and\xA0no custom syntax needed. Every node of\xA0the\xA0value but\xA0a\xA0row is now written back with\xA0the\xA0characters the\xA0file spells it with, whatever its type, so\xA0the\xA0rows are aligned and\xA0nothing else in\xA0the\xA0value moves.</li><li>The\xA0<code>named-grid-areas-alignment</code> rule no\xA0longer writes the\xA0word <code>undefined</code> into\xA0a\xA0value one of\xA0whose rows holds no cell at\xA0all (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/368" target="_blank" rel="noreferrer">#368</a>). The\xA0rows were counted twice over\xA0— once for\xA0the\xA0comparison that\xA0raises the\xA0warning, once for\xA0the\xA0text the\xA0fix writes\xA0— and\xA0neither count agreed with\xA0the\xA0walk the\xA0fix makes over\xA0the\xA0value, nor\xA0with\xA0the\xA0other: one dropped a\xA0row spelled with\xA0nothing between\xA0its quotation marks, the\xA0other dropped a\xA0row of\xA0whitespace as\xA0well, and\xA0the\xA0walk kept both. So\xA0every row behind such a\xA0one was written one place earlier than\xA0it stands, and\xA0the\xA0last of\xA0them was handed nothing at\xA0all: <code>a { grid-template-areas: &quot;&quot; &quot;a a&quot; &quot;b b&quot;; }</code> came out of\xA0<code>--fix</code> as\xA0<code>a { grid-template-areas: &quot;a a&quot; &quot;b b&quot; &quot;undefined&quot;; }</code>\xA0— a\xA0value naming areas the\xA0file spells nowhere\xA0— and\xA0<code>a { grid-template-areas: &quot; &quot;; }</code>, in\xA0which\xA0nothing is out of\xA0line at\xA0all, came out as\xA0<code>a { grid-template-areas: &quot;undefined&quot;; }</code>. Plain CSS reaches this, with\xA0no comment and\xA0no custom syntax needed. Every row keeps a\xA0place of\xA0its own now, whatever it holds, so\xA0the\xA0rows are aligned where they stand and\xA0a\xA0row with\xA0no cell is written back with\xA0nothing between\xA0its quotation marks, its whitespace taken away as\xA0it is taken from\xA0the\xA0ends of\xA0any other row. Under\xA0<code>alignQuotes</code> in\xA0a\xA0declaration spanning lines it is padded to\xA0the\xA0width of\xA0the\xA0others instead, its closing quote lining up with\xA0theirs, and\xA0a\xA0warning moves either way with\xA0that: a\xA0row spelled with\xA0nothing between\xA0its quotation marks is reported now, its quote having been left out of\xA0the\xA0count altogether, and\xA0one spelled with\xA0whitespace already as\xA0wide as\xA0the\xA0rest is no\xA0longer reported, the\xA0warning it used to\xA0draw being that\xA0same miscount, which\xA0then wrote <code>undefined</code> over\xA0the\xA0row and\xA0widened every column of\xA0the\xA0grid to\xA0that\xA0word on\xA0the\xA0run after.</li><li>The\xA0<code>named-grid-areas-alignment</code> rule no\xA0longer writes the\xA0value back around a\xA0string <code>postcss-value-parser</code> paired across\xA0a\xA0quotation mark written inside\xA0a\xA0comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/504" target="_blank" rel="noreferrer">#504</a>). The\xA0parser knows no comment opened by\xA0a\xA0double slash and\xA0closes one opened as\xA0<code>/*/</code> on\xA0its own star, so\xA0a\xA0quotation mark written in\xA0the\xA0text of\xA0either opened a\xA0string to\xA0it that\xA0ran to\xA0the\xA0next quotation mark of\xA0the\xA0value, and\xA0every row behind the\xA0comment was cut at\xA0the\xA0wrong places: <code>&quot;c c&quot; /*/ &quot; */ &quot;a a&quot; &quot;b b&quot;</code> came back from\xA0<code>--fix</code> as\xA0<code>&quot;c c&quot; /*/ &quot; */ &quot;a a&quot;&quot;b b&quot;&quot;</code>, the\xA0rows behind the\xA0comment welded together and\xA0a\xA0stray quotation mark left at\xA0the\xA0end. The\xA0rows are read out of\xA0a\xA0copy of\xA0the\xA0value with\xA0every comment blanked out now, so\xA0such a\xA0mark opens nothing, the\xA0rows the\xA0file spells are aligned, and\xA0the\xA0comment and\xA0everything behind it are written back as\xA0the\xA0file spells them. A\xA0row the\xA0parser used to\xA0file inside\xA0such a\xA0string is aligned now too, where it used to\xA0be passed over.</li><li>The\xA0<code>named-grid-areas-alignment</code> rule now reads the\xA0whitespace inside\xA0a\xA0row the\xA0way the\xA0tokenizer reads it\xA0— a\xA0space, a\xA0tab, a\xA0line feed, a\xA0carriage return or\xA0a\xA0form feed\xA0— and\xA0leaves a\xA0no-break space where it stands, as\xA0the\xA0character of\xA0a\xA0cell&#39;s name that\xA0<code>lightningcss</code> reads there (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/401" target="_blank" rel="noreferrer">#401</a>). It used to\xA0read whitespace the\xA0way JavaScript does, which\xA0counts a\xA0no-break space and\xA0every other separator Unicode has, so\xA0a\xA0cell named with\xA0one was no cell to\xA0it: a\xA0row of\xA0such names came back from\xA0<code>--fix</code> as\xA0an\xA0empty string, and\xA0a\xA0name holding one was cut in\xA0two\xA0and\xA0rejoined with\xA0an\xA0ordinary space, which\xA0made the\xA0value one column wider and\xA0no grid at\xA0all. Such a\xA0name is aligned like\xA0any other now and\xA0left as\xA0the\xA0author spelled it; a\xA0tab or\xA0a\xA0form feed between\xA0two\xA0cells still collapses to\xA0a\xA0space.</li><li>The\xA0<code>no-missing-end-of-source-newline</code> rule no\xA0longer carries off what a\xA0file ends on\xA0when\xA0it closes that\xA0file with\xA0a\xA0line break (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/369" target="_blank" rel="noreferrer">#369</a>). The\xA0break was written in\xA0place of\xA0the\xA0root&#39;s trailing raw rather than\xA0behind it, and\xA0that\xA0raw holds everything standing behind the\xA0last block: a\xA0free semicolon, the\xA0whitespace around it, an\xA0empty line. So\xA0<code>a { color: pink; };;</code> came out of\xA0<code>--fix</code> as\xA0<code>a { color: pink; };</code> and\xA0a\xA0break, <code>a { color: pink; } ; ; </code> as\xA0<code>a { color: pink; } ;</code> and\xA0a\xA0break, a\xA0rule the\xA0file followed with\xA0an\xA0empty line and\xA0a\xA0run of\xA0spaces lost that\xA0line as\xA0well, and\xA0a\xA0stylesheet holding nothing but\xA0two\xA0semicolons came back as\xA0a\xA0break and\xA0nothing else. Taking one of\xA0those semicolons away rescued no file, <code>lightningcss</code> refusing <code>a {};</code> exactly as\xA0it refuses <code>a {};;</code>; the\xA0rule that\xA0takes them all is <code>no-extra-semicolons</code>, as\xA0<code>max-empty-lines</code> is the\xA0one that\xA0counts empty lines and\xA0<code>no-eol-whitespace</code> the\xA0one that\xA0ends a\xA0line cleanly. The\xA0break is now written behind everything the\xA0raw holds, with\xA0a\xA0single exception: a\xA0run of\xA0spaces and\xA0tabs standing on\xA0its own behind the\xA0file&#39;s last line break comes off instead, since\xA0such a\xA0file has ended its last line already and\xA0a\xA0break written behind that\xA0run would leave it an\xA0empty line it never had.</li><li>The\xA0<code>unit-case</code> rule no\xA0longer recases the\xA0text of\xA0an\xA0interpolation (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/298" target="_blank" rel="noreferrer">#298</a>). <code>postcss-value-parser</code> breaks a\xA0value at\xA0whitespace, and\xA0an\xA0interpolation holding whitespace is broken along with\xA0it, so\xA0<code>10px#{$a != $b}</code> reached the\xA0rule as\xA0the\xA0word <code>10px#{$a</code>, which\xA0is CSS on\xA0one side of\xA0the\xA0brace and\xA0Sass on\xA0the\xA0other. The\xA0guard that\xA0keeps this rule to\xA0plain CSS asks a\xA0word at\xA0a\xA0time whether it holds an\xA0interpolation, and\xA0that\xA0word holds no closing brace and\xA0so\xA0held none: the\xA0rule called <code>px$a</code> the\xA0unit of\xA0<code>10</code>, said as\xA0much in\xA0a\xA0warning no user could act on, and\xA0its fix recased the\xA0word whole. Sass tells its names apart by\xA0case, so\xA0a\xA0stylesheet that\xA0compiled stopped compiling; Less reads no interpolation holding whitespace at\xA0all, and\xA0there the\xA0fix rewrote text the\xA0compiler was going to\xA0print as\xA0it stood. The\xA0interpolations are placed in\xA0the\xA0value once now and\xA0every word measured against\xA0them, so\xA0a\xA0word carrying any of\xA0one is passed over\xA0whichever side of\xA0it the\xA0word opens on. This wants no custom syntax: plain CSS carries such a\xA0value in\xA0a\xA0custom property and\xA0in\xA0a\xA0set of\xA0media parameters, and\xA0both were recased. The\xA0rule therefore says nothing about\xA0a\xA0unit written inside\xA0an\xA0interpolation either\xA0— it used to\xA0name the\xA0<code>px</code> of\xA0<code>#{$m * 2px}</code> and\xA0recase it, and\xA0to\xA0name the\xA0<code>px</code> of\xA0<code>#{$m*2px}</code> and\xA0then write nothing at\xA0all\xA0— so\xA0whether the\xA0interpolation holds whitespace no\xA0longer decides what the\xA0rule does with\xA0the\xA0value around it.</li><li>The\xA0<code>unit-case</code> rule no\xA0longer recases what the\xA0file welds to\xA0a\xA0dimension\xA0— a\xA0hash, an\xA0at-keyword, or\xA0a\xA0brace and\xA0whatever stands behind it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/426" target="_blank" rel="noreferrer">#426</a>). It used to\xA0take the\xA0characters an\xA0interpolation is spelled with\xA0out of\xA0the\xA0copy it read a\xA0word in, wherever they stood, so\xA0<code>10px#fff</code> was read as\xA0the\xA0number <code>10</code> and\xA0the\xA0unit <code>pxfff</code>, the\xA0warning named that\xA0run, and\xA0the\xA0fix recased the\xA0whole of\xA0it: <code>10PX#FFF</code> under\xA0<code>upper</code>, and\xA0the\xA0hash lowered along with\xA0the\xA0unit under\xA0<code>lower</code>, so\xA0that\xA0the\xA0two\xA0options moved a\xA0colour in\xA0opposite directions. None of\xA0those characters is a\xA0code point of\xA0an\xA0identifier, so\xA0a\xA0unit ends where the\xA0first of\xA0them stands, as\xA0the\xA0tokenizer reads it: <code>10px#fff</code> is the\xA0dimension <code>10px</code> and\xA0the\xA0hash <code>#fff</code>, the\xA0unit reported is <code>px</code>, and\xA0what the\xA0fix writes ends where the\xA0unit does, whether the\xA0hash ends the\xA0word or\xA0stands in\xA0front of\xA0the\xA0star of\xA0a\xA0multiplication: what is no unit stays as\xA0the\xA0file spells it. A\xA0hash standing in\xA0front of\xA0a\xA0number, <code>#fff10px</code>, is a\xA0hash and\xA0no dimension, as\xA0it was.</li><li>The\xA0<code>unit-case</code> rule now writes each unit it names and\xA0nothing else, whatever word the\xA0unit stands in\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/413" target="_blank" rel="noreferrer">#413</a> and\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/425" target="_blank" rel="noreferrer">#425</a>). A\xA0word holding a\xA0multiplication is reported one unit at\xA0a\xA0time, but\xA0its fix used to\xA0be decided by\xA0a\xA0second reading, of\xA0the\xA0whole word: where that\xA0reading read the\xA0word, one edit recased the\xA0whole of\xA0it, so\xA0<code>10PX*$VAR</code> came back as\xA0<code>10px*$var</code> with\xA0the\xA0variable renamed, and\xA0<code>1PX*A</code> as\xA0<code>1px*a</code>; where it refused the\xA0word\xA0— a\xA0<code>$</code> variable in\xA0front, a\xA0Less variable, a\xA0reading through\xA0a\xA0Sass module\xA0— nothing was written at\xA0all, and\xA0the\xA0warning about\xA0<code>2REM</code> in\xA0<code>$var*2REM</code> outlived every run of\xA0<code>--fix</code>, since\xA0Stylelint takes a\xA0fix that\xA0was called for\xA0one that\xA0was made. Each named unit now carries its own edit: <code>$var*2REM</code> comes back <code>$var*2rem</code>, <code>10PX*$VAR</code> as\xA0<code>10px*$VAR</code>, and\xA0the\xA0exponent of\xA0a\xA0number stays as\xA0the\xA0file spells it\xA0— <code>1E5PX</code> under\xA0<code>lower</code> is <code>1E5px</code> now, where it used to\xA0be <code>1e5px</code>.</li><li>The\xA0<code>unit-case</code> rule no\xA0longer parts a\xA0word at\xA0a\xA0star the\xA0file escapes (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/414" target="_blank" rel="noreferrer">#414</a>). A\xA0word holding a\xA0multiplication is more than\xA0one dimension, and\xA0the\xA0rule cut it at\xA0every star <code>String.prototype.split</code> found\xA0— which\xA0knows nothing of\xA0an\xA0escape, so\xA0<code>10PX\\*2REM</code> became <code>10PX\\</code> and\xA0<code>2REM</code> and\xA0each piece was read as\xA0a\xA0dimension of\xA0its own: under\xA0<code>lower</code> it drew <code>Expected &quot;PX\\&quot; to be &quot;px\\&quot;</code>, naming a\xA0unit no language spells, and\xA0a\xA0second warning about\xA0a\xA0<code>REM</code> that\xA0had no dimension of\xA0its own to\xA0be the\xA0unit of. An\xA0escape is a\xA0backslash and\xA0the\xA0one character behind it, so\xA0the\xA0parity of\xA0the\xA0run standing in\xA0front of\xA0a\xA0star decides what that\xA0star is: <code>@csstools/css-tokenizer</code> reads <code>10PX*2REM</code> as\xA0two\xA0dimensions and\xA0<code>10PX\\*2REM</code> as\xA0one whose unit is <code>PX\\*2REM</code>, Sass multiplies the\xA0first and\xA0leaves the\xA0second whole, and\xA0<code>lightningcss</code> recases the\xA0units of\xA0the\xA0first and\xA0prints the\xA0second exactly as\xA0it stands. Only a\xA0star the\xA0file spells parts two\xA0dimensions now, so\xA0such a\xA0word draws one warning naming the\xA0whole unit, and\xA0a\xA0word an\xA0escape opens\xA0— <code>\\*10PX</code>, an\xA0identifier to\xA0all three\xA0— is passed over\xA0rather than\xA0having its tail recased. The\xA0same reading answers for\xA0every other character an\xA0escape covers: <code>10px\\#fff</code> is the\xA0one dimension its unit <code>px\\#fff</code> makes it, where the\xA0rule used to\xA0name <code>px\\</code> and\xA0leave the\xA0rest, so\xA0under\xA0<code>upper</code> it now comes out <code>10PX\\#FFF</code>. An\xA0escaped bang opens no flag and\xA0is a\xA0code point of\xA0the\xA0unit as\xA0well, so\xA0<code>10px\\!important</code> under\xA0<code>upper</code> comes out <code>10PX\\!IMPORTANT</code>, its keyword recased with\xA0the\xA0unit it belongs to. And\xA0an\xA0escaped backslash standing in\xA0front of\xA0a\xA0digit is no hack unit: <code>10PX\\\\0#fff</code> is one dimension whose unit is <code>PX\\0</code> with\xA0a\xA0hash of\xA0its own behind it, where the\xA0rule used to\xA0take the\xA0last two\xA0characters of\xA0<code>10PX\\\\0</code> for\xA0a\xA0<code>\\0</code> hack and\xA0read every escape written behind that\xA0point from\xA0the\xA0wrong side. A\xA0backslash is not\xA0always an\xA0escape, either: one written in\xA0front of\xA0a\xA0line break opens none\xA0— the\xA0grammar reads it as\xA0a\xA0delimiter, and\xA0the\xA0tokenizer reads <code>10px\\</code> followed by\xA0a\xA0break and\xA0<code>2REM</code> as\xA0the\xA0dimension <code>10px</code>, that\xA0delimiter, whitespace and\xA0a\xA0second dimension. <code>postcss-value-parser</code> steps over\xA0whatever follows a\xA0backslash and\xA0keeps the\xA0break inside\xA0the\xA0word, so\xA0the\xA0rule used to\xA0name the\xA0nonsense unit <code>px\\⏎2REM</code> and\xA0recase both halves of\xA0it; the\xA0unit now ends at\xA0the\xA0delimiter.</li><li>The\xA0<code>unit-case</code> rule no\xA0longer names as\xA0a\xA0unit what stands behind the\xA0unit&#39;s own identifier (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/525" target="_blank" rel="noreferrer">#525</a>). <code>postcss-value-parser</code> calls a\xA0unit everything written behind the\xA0number, and\xA0three\xA0things were cut off that\xA0text one at\xA0a\xA0time\xA0— a\xA0bang flag, the\xA0<code>\\0</code> and\xA0<code>\\9</code> hacks, and\xA0the\xA0characters an\xA0interpolation is spelled with\xA0— while\xA0everything else stayed inside\xA0it: <code>a { width: 10PX$VAR; }</code> under\xA0<code>lower</code> was reported as\xA0<code>PX$VAR</code> and\xA0came out of\xA0<code>--fix</code> as\xA0<code>a { width: 10px$var; }</code>, the\xA0name of\xA0the\xA0variable lowercased along with\xA0the\xA0unit, and\xA0<code>10PXns.$V</code> and\xA0<code>10PX%</code> went the\xA0same way. A\xA0unit is an\xA0identifier, and\xA0it ends at\xA0the\xA0first character that is no code point of\xA0one, an\xA0escape aside: <code>lightningcss</code> prints that\xA0line back as\xA0<code>a { width: 10px$VAR; }</code>, lowercasing the\xA0unit and\xA0leaving the\xA0name, and\xA0Sass, given a\xA0<code>$VAR</code> to\xA0read, compiles the\xA0dimension and\xA0the\xA0variable&#39;s value side by\xA0side. The\xA0warning names <code>PX</code> now and\xA0the\xA0fix writes <code>PX</code> and\xA0nothing else; the\xA0bang flag and\xA0the\xA0characters an\xA0interpolation is spelled with\xA0fall out of\xA0that\xA0one reading rather than\xA0being cut off by\xA0two\xA0of\xA0their own, while\xA0the\xA0<code>\\0</code> and\xA0<code>\\9</code> hacks are still taken out of\xA0the\xA0middle of\xA0a\xA0unit, which\xA0is where a\xA0file writes them.</li><li>The\xA0<code>@stylistic/less/unit-case</code> rule no\xA0longer recases what an\xA0escape welds to\xA0a\xA0unit (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/527" target="_blank" rel="noreferrer">#527</a>). To\xA0CSS an\xA0escape is a\xA0code point of\xA0the\xA0identifier it stands in, so\xA0<code>10px\\#fff</code> is one dimension whose unit is <code>px#fff</code>\xA0— <code>@csstools/css-tokenizer</code> reads it so, and\xA0Sass and\xA0<code>lightningcss</code> both print the\xA0word whole\xA0— and\xA0that is the\xA0reading the\xA0core keeps. Less parts a\xA0word at\xA0an\xA0escape: it compiles that\xA0line to\xA0<code>a { width: 10px \\#fff; }</code>, a\xA0dimension and\xA0an\xA0escaped value it prints exactly as\xA0written, and\xA0nowhere does it read an\xA0escape into\xA0a\xA0unit\xA0— a\xA0variable&#39;s value, a\xA0set of\xA0media parameters, the\xA0arguments of\xA0a\xA0call and\xA0a\xA0custom property are read the\xA0same way, while\xA0a\xA0declaration whose value spells none of\xA0<code>. # @ $ + / &#39; &quot; * </code> ( { } -<code> and closes on a semicolon it stores unread and prints whole. The\xA0namespace inherited the\xA0core&#39;s reading, so\xA0</code>10px#fff<code> under\xA0</code>upper<code> was reported as\xA0</code>px#fff<code> and\xA0came out of\xA0</code>--fix<code> as\xA0</code>10PX#FFF<code>, the\xA0escaped value recased along with\xA0the\xA0unit, and\xA0</code>10PX@VAR<code> under\xA0</code>lower<code> as\xA0</code>10px@var<code>. Under\xA0this namespace the\xA0unit now ends in\xA0front of\xA0the\xA0first escape, whatever the\xA0escape spells: the\xA0warning names </code>px<code>, the\xA0fix writes </code>10PX#fff<code>, and\xA0</code>10#fff<code>, a\xA0number and\xA0an\xA0escaped value, is no dimension at\xA0all. Two\xA0readings move with\xA0it: a\xA0hexadecimal escape between\xA0the\xA0letters of\xA0a\xA0unit ends it here, </code>10P\\61\xA0X<code>naming</code>P<code>where the\xA0core names</code>P\\61\xA0X<code>, and\xA0so\xA0does a\xA0hack unit standing there, </code>10P\\9X<code>naming</code>P<code>where the\xA0core takes the\xA0hack out and\xA0names</code>PX<code>. The\xA0core and\xA0the\xA0</code>@stylistic/scss/\` namespace read as\xA0before.</li><li>The\xA0<code>@stylistic/less/unit-case</code> rule no\xA0longer recases what a\xA0hyphen welds to\xA0a\xA0unit (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/633" target="_blank" rel="noreferrer">#633</a>). To\xA0CSS a\xA0hyphen is a\xA0code point of\xA0the\xA0identifier it stands in, so\xA0<code>10PX-A</code> is one dimension whose unit is <code>PX-A</code>\xA0— <code>@csstools/css-tokenizer</code> reads it so, and\xA0Sass and\xA0<code>lightningcss</code> both print the\xA0word whole\xA0— and\xA0that is the\xA0reading the\xA0core keeps. Less reads a\xA0unit as\xA0ASCII letters and\xA0underscores, so\xA0a\xA0hyphen behind one is the\xA0sign of\xA0the\xA0operand behind it: it compiles that\xA0line to\xA0<code>a { width: 10PX A; }</code>, a\xA0dimension and\xA0a\xA0keyword it prints exactly as\xA0written, and\xA0<code>a { width: 10PX-2REM; }</code> to\xA0<code>a { width: 8PX; }</code>. A\xA0hyphen is one of\xA0the\xA0characters that\xA0keep Less from\xA0storing a\xA0declaration&#39;s value unread, so\xA0this holds wherever it reads a\xA0value\xA0— a\xA0variable&#39;s value, a\xA0set of\xA0media parameters, a\xA0calculation, the\xA0arguments of\xA0a\xA0call and\xA0a\xA0custom property alike. The\xA0namespace inherited the\xA0core&#39;s reading, so\xA0<code>10PX-A</code> under\xA0<code>lower</code> was reported as\xA0<code>PX-A</code> and\xA0came out of\xA0<code>--fix</code> as\xA0<code>10px-a</code>, the\xA0keyword recased along with\xA0the\xA0unit. Under\xA0this namespace the\xA0unit now ends in\xA0front of\xA0the\xA0first hyphen as\xA0it ends in\xA0front of\xA0the\xA0first escape: the\xA0warning names <code>PX</code>, the\xA0fix writes <code>10px-A</code>, and\xA0<code>10-PX</code>, a\xA0number and\xA0a\xA0keyword, is no dimension at\xA0all. What the\xA0hyphen parts off is read again, so\xA0a\xA0unit standing behind it is named like\xA0the\xA0one in\xA0front: <code>10PX-2REM</code> under\xA0<code>lower</code> draws two\xA0warnings and\xA0comes out <code>10px-2rem</code>, and\xA0so\xA0does <code>10PX--2REM</code>, whose second hyphen is the\xA0operand&#39;s own sign. A\xA0keyword of\xA0Less opens on\xA0hyphens of\xA0its own and\xA0is left whole, so\xA0<code>10PX-A-2REM</code> names <code>PX</code> and\xA0nothing else, and\xA0so\xA0does <code>10PX---2REM</code>, which\xA0Less leaves as\xA0the\xA0dimension and\xA0the\xA0keyword <code>--2REM</code> beside it. The\xA0core and\xA0the\xA0<code>@stylistic/scss/</code> namespace read as\xA0before.</li><li>The\xA0<code>function-max-empty-lines</code> rule no\xA0longer counts the\xA0empty lines a\xA0comment holds inside\xA0a\xA0call, nor\xA0collapses them (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/503" target="_blank" rel="noreferrer">#503</a>). The\xA0rule read a\xA0call as\xA0one string, comments and\xA0all: it looked for\xA0the\xA0runs of\xA0line breaks over\xA0the\xA0whole of\xA0that\xA0text and\xA0rewrote the\xA0whole of\xA0it, so\xA0a\xA0comment written between\xA0the\xA0arguments and\xA0holding two\xA0empty lines of\xA0its own drew a\xA0warning about\xA0the\xA0call and\xA0lost two\xA0of\xA0its three\xA0line breaks to\xA0<code>--fix</code>, whatever the\xA0comment was spelled with. The\xA0runs are looked for\xA0in\xA0a\xA0copy of\xA0the\xA0value with\xA0every comment blanked now, and\xA0each one is written back at\xA0its own position in\xA0the\xA0text the\xA0file spells, so\xA0a\xA0comment&#39;s line breaks are counted against\xA0no call and\xA0rewritten by\xA0no fix, while\xA0the\xA0call&#39;s own are collapsed as\xA0before.</li></ul></li><li><p>A\xA0warning is no\xA0longer raised over\xA0code that is already right, nor\xA0over\xA0the\xA0text of\xA0a\xA0comment or\xA0of\xA0a\xA0string taken for\xA0code:</p><ul><li>The\xA0plugin now reads a\xA0double slash written beside a\xA0quoted address inside\xA0<code>url()</code> as\xA0opening a\xA0comment, and\xA0a\xA0bare address as\xA0ending at\xA0the\xA0first parenthesis behind it, everywhere it looks at\xA0a\xA0text (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/557" target="_blank" rel="noreferrer">#557</a>). Two\xA0readings answered that\xA0question and\xA0answered it differently: the\xA0guard that\xA0keeps a\xA0fix out of\xA0an\xA0end-of-line comment read those slashes as\xA0opening a\xA0comment, which\xA0is what Sass reads there, while\xA0the\xA0walk that\xA0finds a\xA0text&#39;s comments and\xA0addresses read them as\xA0code and\xA0counted the\xA0parentheses inside\xA0a\xA0bare address. So\xA0the\xA0rules reading that\xA0walk wrote into\xA0the\xA0comment&#39;s own text\xA0— <code>unit-case</code> under\xA0<code>upper</code> turned <code>a { b: url( &quot;a&quot; // c ) 1px; }</code> into\xA0the\xA0same declaration ending <code>1PX</code>, where everything behind the\xA0double slash is text of\xA0the\xA0comment\xA0— and\xA0<code>function-parentheses-space-inside</code> under\xA0<code>never</code> took both spaces out of\xA0<code>url( &quot;a&quot; // c )</code> in\xA0an\xA0SCSS stylesheet and\xA0left <code>url(&quot;a&quot; // c)</code>, whose closing parenthesis then stands inside\xA0the\xA0comment and\xA0which\xA0<code>postcss-scss</code> will not\xA0read back. With\xA0the\xA0comment read, <code>postcss-value-parser</code> marks such a\xA0call unclosed and\xA0the\xA0rules read nothing of\xA0it: over\xA0a\xA0sweep of\xA0335\xA0580\xA0runs that\xA0takes\xA0752 warnings off <code>function-parentheses-newline-inside</code>, <code>function-parentheses-space-inside</code>, <code>function-whitespace-after</code>, <code>unit-case</code>, <code>value-list-comma-newline-after</code>, <code>value-list-comma-space-after</code>, <code>value-list-comma-space-before</code> and\xA0<code>indentation</code>, and\xA0leaves\xA030 stylesheets the\xA0fix used to\xA0leave unparsable. Under\xA0<code>postcss-less</code> the\xA0<code>string-quotes</code> rule no\xA0longer rewrites a\xA0quotation mark standing behind such a\xA0comment. A\xA0block comment written in\xA0the\xA0same place is read alike now, whether or\xA0not\xA0whitespace parts the\xA0quotation mark from\xA0the\xA0parenthesis, as\xA0<code>lightningcss</code> reads both spellings. <code>max-line-length</code> measures\xA057 of\xA0those lines past the\xA0address rather than\xA0over\xA0it: a\xA0bare address holding a\xA0parenthesis is no address to\xA0a\xA0browser either, <code>lightningcss</code> reading <code>url(a(b)c.png)</code> as\xA0a\xA0bad URL token that\xA0ends at\xA0<code>a(b</code>.</li><li>The\xA0plugin now reads a\xA0block comment whose opening solidus a\xA0backslash stands in\xA0front of\xA0as\xA0a\xA0comment, wherever it stands (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/665" target="_blank" rel="noreferrer">#665</a>). The\xA0grammar lets a\xA0backslash escape a\xA0solidus, so\xA0<code>\\/*</code> spelled two\xA0ordinary characters to\xA0the\xA0plugin; PostCSS&#39;s tokenizer lets no escape cover a\xA0solidus at\xA0all, and\xA0PostCSS, <code>postcss-scss</code> and\xA0<code>postcss-less</code> all hand back <code>a { b: red \\/*x*/ 1px; }</code> as\xA0the\xA0declaration <code>red \\ 1px</code> with\xA0the\xA0comment cut out of\xA0it. Writing into\xA0that\xA0comment is what <code>value-slash-space-before</code> under\xA0<code>always</code> did, splitting its closing delimiter into\xA0<code>* /</code> and\xA0leaving a\xA0stylesheet PostCSS refuses with\xA0<code>Unclosed comment</code>. Over\xA0a\xA0sweep of\xA0492\xA0660\xA0runs the\xA0reading takes\xA0126 warnings off <code>value-slash-space-before</code> and\xA0<code>value-slash-space-after</code> and\xA0leaves\xA054 stylesheets those two\xA0used to\xA0leave unparsable; <code>max-line-length</code> counts\xA033 of\xA0those lines past the\xA0comment, the\xA0address ending where it opens. A\xA0<code>\\//</code> is left as\xA0it was, since\xA0the\xA0two\xA0compilers read it as\xA0code and\xA0the\xA0change would reach the\xA0rules about\xA0a\xA0semicolon. Inside\xA0the\xA0parentheses of\xA0a\xA0<code>url()</code> an\xA0SCSS stylesheet is the\xA0one place the\xA0reading is wider than\xA0its own parser, whose brackets hold every comment written there, so\xA0a\xA0line of\xA0one is counted rather than\xA0taken off.</li><li>The\xA0plugin now reads a\xA0block comment written inside\xA0a\xA0<code>url()</code>&#39;s parentheses as\xA0a\xA0comment wherever the\xA0tokenizer&#39;s own whitespace parts it from\xA0the\xA0<code>(</code> (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/660" target="_blank" rel="noreferrer">#660</a>). PostCSS takes those parentheses as\xA0one token wherever the\xA0<code>(</code> is met by\xA0anything but\xA0a\xA0quotation mark and\xA0that\xA0whitespace, and\xA0where whitespace does stand there it takes one only while\xA0the\xA0text to\xA0the\xA0first <code>)</code> holds no break, quotation mark, parenthesis, solidus or\xA0backslash\xA0— which\xA0a\xA0comment&#39;s opening delimiter always does. The\xA0plugin read a\xA0bare address running to\xA0the\xA0first <code>)</code> at\xA0every spelling and\xA0so\xA0took that\xA0<code>)</code> out of\xA0the\xA0comment&#39;s own text: <code>a { b: url( a /* ) */ ) }</code> gave the\xA0address <code>a /*</code>, and\xA0six\xA0rules wrote behind it, <code>value-slash-space-before</code> under\xA0<code>always</code> splitting the\xA0closing delimiter into\xA0<code>* /</code> and\xA0leaving a\xA0stylesheet no parser reads back. It is a\xA0comment now, as\xA0it is to\xA0PostCSS, to\xA0<code>postcss-less</code>, which\xA0reads by\xA0the\xA0same tokenizer, and\xA0to\xA0Sass, which\xA0compiles that\xA0declaration to\xA0<code>url(a)</code>; <code>url( a /* c */ )</code>, which\xA0Less compiles, it prints with\xA0the\xA0comment as\xA0text of\xA0the\xA0address, so\xA0reading it as\xA0a\xA0comment is what keeps a\xA0fix out under\xA0all four. Over\xA0a\xA0sweep of\xA0449\xA0820\xA0runs that\xA0takes\xA0324 warnings off <code>function-parentheses-newline-inside</code>, <code>function-parentheses-space-inside</code>, <code>value-slash-space-after</code> and\xA0<code>value-slash-space-before</code>, and\xA0leaves\xA036 stylesheets the\xA0last of\xA0them used to\xA0leave unparsable. The\xA0address is then what the\xA0parentheses hold in\xA0front of\xA0that\xA0comment, or\xA0behind it where the\xA0comment opens them, so\xA0<code>max-line-length</code> counts\xA099 of\xA0those lines past the\xA0comment rather than\xA0over\xA0it. A\xA0comment the\xA0parenthesis stands against, <code>url(a /* c */)</code>, is text of\xA0the\xA0address as\xA0it always was.</li><li>The\xA0<code>max-empty-lines</code> rule now counts a\xA0stylesheet holding nothing but\xA0whitespace once (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/404" target="_blank" rel="noreferrer">#404</a>). The\xA0one run of\xA0line breaks such a\xA0file opens with\xA0is the\xA0run it ends on, and\xA0the\xA0rule counted it from\xA0both ends\xA0— every break of\xA0it an\xA0empty line for\xA0the\xA0beginning of\xA0the\xA0file, and\xA0one more for\xA0the\xA0end\xA0— so\xA0a\xA0file of\xA0a\xA0single line break was reported under\xA0a\xA0maximum of\xA0one and\xA0a\xA0file of\xA0two\xA0under\xA0a\xA0maximum of\xA0two, and\xA0<code>--fix</code> could take nothing off: the\xA0tail of\xA0a\xA0file keeps a\xA0break under\xA0every option, and\xA0there was no node in\xA0front of\xA0it whose whitespace the\xA0fix could shorten instead, so\xA0the\xA0warning stood after\xA0every run. Such a\xA0file is counted from\xA0its beginning alone now, one empty line a\xA0break, and\xA0that\xA0run is written as\xA0the\xA0whitespace in\xA0front of\xA0a\xA0first node is written\xA0— down to\xA0the\xA0option&#39;s count, and\xA0to\xA0nothing under\xA0a\xA0maximum of\xA0zero, so\xA0a\xA0file of\xA0one line break comes out of\xA0<code>--fix</code> empty there\xA0— with\xA0whatever stands behind the\xA0run, a\xA0run of\xA0spaces or\xA0a\xA0free semicolon, left where it stood. A\xA0styled template holding nothing but\xA0line breaks is written the\xA0same way, its whitespace being the\xA0template&#39;s own. A\xA0file with\xA0a\xA0node, a\xA0run of\xA0spaces or\xA0a\xA0free semicolon in\xA0front of\xA0its last run is counted and\xA0written exactly as\xA0before.</li><li><code>declaration-block-semicolon-newline-before</code> and\xA0<code>declaration-block-semicolon-space-before</code> no\xA0longer report an\xA0empty custom property that\xA0<code>declaration-colon-space-after</code> has just finished as\xA0<code>--x: ;</code> in\xA0the\xA0same run\xA0— a\xA0report <code>--fix</code> then discarded along with\xA0a\xA0fix that\xA0wrote nothing (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/355" target="_blank" rel="noreferrer">#355</a>). The\xA0space that\xA0fix writes stands on\xA0the\xA0tail of\xA0<code>raws.between</code> until\xA0the\xA0file is read back, and\xA0the\xA0exception of\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/50" target="_blank" rel="noreferrer">#50</a> read the\xA0value alone, so\xA0a\xA0check running behind the\xA0write took the\xA0finished form for\xA0a\xA0violation.</li><li>The\xA0<code>selector-pseudo-class-parentheses-space-inside</code> rule no\xA0longer reports a\xA0pseudo-class it has already fixed, where the\xA0last argument holds nothing but\xA0comments (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/128" target="_blank" rel="noreferrer">#128</a>). The\xA0whitespace in\xA0front of\xA0the\xA0closing parenthesis was not\xA0in\xA0the\xA0parsed selector at\xA0all, so\xA0the\xA0<code>always</code> options asked for\xA0a\xA0space that\xA0was already there and\xA0went on\xA0asking however many times the\xA0fix was run, reporting it at\xA0a\xA0column that\xA0fell inside\xA0the\xA0comment. <code>a:not(/**/)</code> is now fixed into\xA0<code>a:not( /**/ )</code> once, and\xA0the\xA0output is left alone from\xA0then on. A\xA0run of\xA0several spaces in\xA0front of\xA0that\xA0parenthesis is left as\xA0it is now, as\xA0one standing anywhere else inside\xA0the\xA0parentheses always was. None of\xA0this reaches a\xA0selector carrying an\xA0inline comment under\xA0<code>postcss-scss</code>, which\xA0the\xA0rule reads from\xA0a\xA0copy of\xA0itself that\xA0spells every one of\xA0those comments <code>/* */</code> and\xA0writes its fix to\xA0a\xA0copy that is never printed; the\xA0rule reads and\xA0writes both copies now, which\xA0was <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/163" target="_blank" rel="noreferrer">#163</a> and\xA0is answered below.</li><li>The\xA0<code>function-parentheses-newline-inside</code> and\xA0<code>media-feature-parentheses-space-inside</code> rules no\xA0longer grow a\xA0pair of\xA0parentheses holding nothing but\xA0whitespace by\xA0a\xA0character on\xA0every run, nor\xA0report one that\xA0already holds what the\xA0option asks for\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/329" target="_blank" rel="noreferrer">#329</a>). <code>postcss-value-parser</code> hands back everything such a\xA0pair encloses as\xA0the\xA0node&#39;s <code>before</code> and\xA0leaves its <code>after</code> empty however the\xA0file spells it, so\xA0the\xA0closing half of\xA0the\xA0option was asked about\xA0a\xA0text that\xA0was empty and\xA0stayed empty however much was written into\xA0it: under\xA0<code>always</code> <code>a { b: f(); }</code> gained a\xA0line break on\xA0every run of\xA0<code>--fix</code> and\xA0<code>@media () { a { b: c; } }</code> a\xA0space, while\xA0a\xA0call already broken on\xA0the\xA0inside\xA0of\xA0its parentheses and\xA0<code>@media ( ) { a { b: c; } }</code> were reported although\xA0what the\xA0option asks for\xA0already stood in\xA0them. The\xA0run such a\xA0pair encloses is one and\xA0not\xA0two\xA0now, and\xA0the\xA0opening half answers for\xA0both: the\xA0first two\xA0are fixed once and\xA0left alone from\xA0then on, the\xA0last two\xA0are passed over\xA0in\xA0silence. The\xA0<code>always-multi-line</code> option of\xA0the\xA0first rule ran away the\xA0same way and\xA0rests too; the\xA0<code>never</code> options never did, since\xA0an\xA0empty <code>after</code> is whitespace to\xA0nobody.</li><li>The\xA0<code>declaration-colon-newline-after</code> rule no\xA0longer reads a\xA0colon of\xA0the\xA0value as\xA0a\xA0second colon of\xA0the\xA0declaration (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/408" target="_blank" rel="noreferrer">#408</a>). The\xA0text the\xA0rule walked was meant to\xA0hold the\xA0property and\xA0the\xA0colon alone, and\xA0it ran on\xA0into\xA0the\xA0value by\xA0one character less than\xA0<code>raws.between</code> is long, so\xA0whitespace wide enough around the\xA0colon carried the\xA0walk past a\xA0colon of\xA0the\xA0value\xA0— a\xA0data URI&#39;s, an\xA0SCSS map&#39;s, a\xA0<code>progid:</code> filter&#39;s. <code>a { background : url(data:x); }</code> was reported twice under\xA0<code>always</code>, the\xA0second time over\xA0the\xA0<code>data:</code>, and\xA0<code>--fix</code> wrote a\xA0line break in\xA0front of\xA0the\xA0URI and\xA0did it again on\xA0every run for\xA0ever. The\xA0walk is over\xA0<code>raws.between</code> alone now, the\xA0raw PostCSS files the\xA0declaration&#39;s own colon in, so\xA0no colon of\xA0the\xA0value is read as\xA0a\xA0second one and\xA0one run of\xA0<code>--fix</code> settles the\xA0file.</li><li>The\xA0<code>declaration-colon-newline-after</code> rule no\xA0longer reads a\xA0colon the\xA0parser read as\xA0text\xA0— one standing inside\xA0a\xA0comment, a\xA0string or\xA0a\xA0parenthesised group\xA0— as\xA0the\xA0declaration&#39;s, nor\xA0writes its line break into\xA0any of\xA0them (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/388" target="_blank" rel="noreferrer">#388</a> and\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/499" target="_blank" rel="noreferrer">#499</a>). PostCSS files everything the\xA0file spells between\xA0the\xA0property and\xA0the\xA0value in\xA0<code>raws.between</code>, and\xA0the\xA0rule walked that\xA0raw character by\xA0character, taking every colon it met for\xA0the\xA0declaration&#39;s\xA0— while\xA0the\xA0two\xA0<code>declaration-colon-space-*</code> rules have found the\xA0first colon standing outside a\xA0comment and\xA0a\xA0string since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/92" target="_blank" rel="noreferrer">#92</a>. So\xA0<code>a { b/*x:y*/: x; }</code> was reported twice under\xA0<code>always</code>, once over\xA0the\xA0comment&#39;s colon, and\xA0<code>--fix</code> broke the\xA0comment&#39;s text in\xA0two; <code>a { b:\\n/*x:y*/ red; }</code>, whose break already stands where the\xA0option asks for\xA0it, was reported over\xA0the\xA0comment&#39;s colon and\xA0rewritten the\xA0same way; <code>a { b (x:y): red; }</code> was reported inside\xA0its group, and\xA0what <code>--fix</code> left there no parser reads back; and\xA0under\xA0<code>postcss-scss</code> a\xA0break written into\xA0<code>a { color: //x:y\\n red; }</code> closed the\xA0inline comment early and\xA0left <code>:y</code> standing as\xA0code on\xA0a\xA0line of\xA0its own, a\xA0file the\xA0parser refuses. Every reader of\xA0the\xA0raw finds the\xA0declaration&#39;s colon the\xA0one way now, and\xA0finds it by\xA0handing the\xA0text to\xA0the\xA0tokenizer the\xA0file&#39;s own parser reads by\xA0— so\xA0nothing is reported for\xA0a\xA0colon that\xA0parser read as\xA0text, nothing is written near it, and\xA0one run of\xA0<code>--fix</code> settles the\xA0file.</li><li>The\xA0<code>declaration-block-semicolon-newline-before</code> and\xA0<code>declaration-block-semicolon-space-before</code> rules under\xA0their <code>never</code> options no\xA0longer report the\xA0single space of\xA0a\xA0declaration whose value is nothing but\xA0that\xA0space where the\xA0search misread what stands in\xA0front of\xA0the\xA0colon\xA0— a\xA0comment whose text ends in\xA0a\xA0backslash and\xA0two\xA0comments abutting, which\xA0it read as\xA0open to\xA0the\xA0end of\xA0the\xA0raw; a\xA0comment behind a\xA0backslash, whose own colon it took for\xA0the\xA0declaration&#39;s; a\xA0string behind a\xA0double slash, which\xA0is no comment at\xA0all in\xA0a\xA0plain CSS or\xA0a\xA0Less file; and\xA0a\xA0parenthesised group, which\xA0it read into\xA0rather than\xA0over\xA0— such as\xA0<code>a { --b /*x\\*/: ; }</code> for\xA0the\xA0space rule, and\xA0the\xA0same declaration in\xA0a\xA0block broken over\xA0lines for\xA0the\xA0newline rule&#39;s <code>never-multi-line</code>: a\xA0warning whose fix wrote nothing where the\xA0text behind the\xA0colon was read too wide, and\xA0took the\xA0space away where it was read too narrow, that\xA0space being the\xA0colon&#39;s own and\xA0left to\xA0the\xA0<code>declaration-colon-space-after</code> rule as\xA0it is behind any other comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/50" target="_blank" rel="noreferrer">#50</a>). The\xA0text behind the\xA0colon was read from\xA0the\xA0wrong place, and\xA0read as\xA0holding more than\xA0the\xA0space.</li><li>The\xA0<code>declaration-colon-space-after</code> and\xA0<code>declaration-colon-space-before</code> rules no\xA0longer read a\xA0comment standing behind a\xA0backslash as\xA0text, nor\xA0write into\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/92" target="_blank" rel="noreferrer">#92</a>). PostCSS lets no backslash escape a\xA0slash, so\xA0<code>\\/*x:y*/</code> and, under\xA0<code>postcss-scss</code>, <code>\\//x:y</code> open a\xA0comment to\xA0the\xA0parser, which\xA0files it in\xA0<code>raws.between</code> like\xA0any other; the\xA0search read an\xA0escape there and\xA0the\xA0comment&#39;s own colon as\xA0the\xA0declaration&#39;s, and\xA0<code>a { b \\/*x:y*/: red; }</code> under\xA0<code>always</code> came back with\xA0a\xA0space written into\xA0the\xA0comment. Where such a\xA0comment holds no colon the\xA0harm was worse: <code>a { b \\//x\\n: red; }</code> under\xA0<code>postcss-scss</code> came back from\xA0both options of\xA0<code>declaration-colon-space-before</code> with\xA0the\xA0line break that\xA0closes the\xA0comment taken away, a\xA0file the\xA0parser refuses. The\xA0comment is read as\xA0the\xA0parser reads it now, and\xA0the\xA0declaration&#39;s own colon is the\xA0one reported.</li><li>The\xA0<code>function-parentheses-newline-inside</code> rule no\xA0longer reports an\xA0opening parenthesis that\xA0an\xA0inline comment already follows correctly (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/141" target="_blank" rel="noreferrer">#141</a>). The\xA0<code>always</code> options let a\xA0comment stand between\xA0the\xA0parenthesis and\xA0the\xA0line break they ask for, and\xA0used to\xA0walk past a\xA0block comment while\xA0stopping at\xA0an\xA0inline one, so\xA0a\xA0function already spelled the\xA0way the\xA0option asks was reported and\xA0then reformatted by\xA0the\xA0fix.</li><li>The\xA0<code>media-feature-parentheses-space-inside</code> rule no\xA0longer reads a\xA0media feature out of\xA0an\xA0inline comment standing in\xA0a\xA0media query, nor\xA0writes inside\xA0such a\xA0comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/138" target="_blank" rel="noreferrer">#138</a>). A\xA0parenthesis in\xA0the\xA0text of\xA0a\xA0comment used to\xA0open a\xA0feature as\xA0far as\xA0the\xA0value parser was concerned, and\xA0the\xA0fix then put the\xA0space the\xA0option asks for\xA0into\xA0that\xA0text.</li><li>The\xA0<code>selector-combinator-space-before</code> rule no\xA0longer reports a\xA0combinator that\xA0opens a\xA0selector when\xA0a\xA0comment stands in\xA0front of\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/66" target="_blank" rel="noreferrer">#66</a>). A\xA0selector list broken over\xA0several lines and\xA0interleaved with\xA0comments is now left alone instead of\xA0being pulled onto\xA0one line. Two\xA0more things follow for\xA0this rule and\xA0for\xA0<code>selector-combinator-space-after</code> alike, whenever the\xA0selector holds an\xA0inline comment:</li><li>The\xA0<code>selector-combinator-space-before</code> and\xA0<code>selector-combinator-space-after</code> rules no\xA0longer drop the\xA0warning about\xA0whitespace they cannot write. A\xA0comment standing beside a\xA0combinator is folded into\xA0the\xA0raws of\xA0that\xA0side, and\xA0a\xA0raw is what the\xA0parser prints in\xA0place of\xA0the\xA0spaces the\xA0fix writes, so\xA0the\xA0fix changed nothing while\xA0Stylelint counted it as\xA0applied and\xA0<code>--fix</code> ended with\xA0a\xA0clean pass on\xA0a\xA0file it had not\xA0touched. <code>.foo /* c */ &gt; .bar</code> keeps its warning now, in\xA0a\xA0stylesheet of\xA0any syntax. <ul><li>the\xA0reported position no\xA0longer drifts two\xA0characters per\xA0comment, so\xA0it points at\xA0the\xA0combinator itself;</li><li>the\xA0fix now reaches the\xA0output instead of\xA0being silently discarded, and\xA0the\xA0comment keeps its <code>//</code> spelling, whatever kind of\xA0combinator the\xA0comment stands beside: one folded into\xA0the\xA0raws of\xA0a\xA0<code>&gt;</code>, <code>+</code> or\xA0<code>~</code> used to\xA0be rewritten into\xA0a\xA0block comment by\xA0a\xA0fix made elsewhere in\xA0the\xA0selector, and\xA0the\xA0position reported there drifted two\xA0characters per\xA0comment;</li></ul></li><li>The\xA0<code>string-quotes</code> rule no\xA0longer reports a\xA0quotation mark standing inside\xA0a\xA0<code>//</code> comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/32" target="_blank" rel="noreferrer">#32</a>). A\xA0Less value or\xA0an\xA0at-rule&#39;s parameters keep such a\xA0comment in\xA0themselves whenever it does not\xA0open the\xA0statement, and\xA0the\xA0quotes in\xA0its text used to\xA0be taken for\xA0a\xA0string of\xA0code: <ul><li>the\xA0warning is gone, and\xA0the\xA0fix no\xA0longer rewrites the\xA0text of\xA0the\xA0comment;</li><li>a\xA0double slash opens a\xA0comment only where the\xA0syntax says one does: under\xA0<code>postcss-scss</code> those are the\xA0comments its parser found, and\xA0a\xA0value of\xA0plain CSS is read exactly as\xA0before;</li><li>a\xA0double slash belonging to\xA0an\xA0address opens no comment even there, whether the\xA0address is quoted or\xA0bare inside\xA0<code>url()</code>, and\xA0whatever parentheses its interpolation or\xA0its escapes bring along;</li><li>a\xA0comment ends with\xA0its line, so\xA0a\xA0string on\xA0the\xA0next one is checked as\xA0before.</li></ul></li><li>The\xA0<code>selector-pseudo-class-parentheses-space-inside</code> rule no\xA0longer reports an\xA0end of\xA0the\xA0parentheses that\xA0holds no argument, nor\xA0throws when\xA0fixing one (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/127" target="_blank" rel="noreferrer">#127</a>). The\xA0whole of\xA0<code>a:not()</code>, and\xA0the\xA0empty half of\xA0<code>a:not(,b)</code>, have no inside\xA0to\xA0space out, and\xA0the\xA0parser keeps no whitespace at\xA0such an\xA0end either\xA0— <code>a:not( )</code> comes back out as\xA0<code>a:not()</code>, and\xA0<code>a:not( ,b)</code> as\xA0<code>a:not(, b)</code>, the\xA0run standing in\xA0front of\xA0the\xA0comma handed to\xA0the\xA0node behind it. So\xA0an\xA0end with\xA0nothing at\xA0it is passed over, and\xA0where whitespace stood at\xA0one\xA0— or\xA0at\xA0a\xA0selector of\xA0the\xA0list that\xA0holds nothing, <code>a:not( b ) , , c</code> for\xA0one\xA0— the\xA0whole selector list is passed over\xA0with\xA0it, since\xA0nothing in\xA0the\xA0parsed selector can hold that\xA0run and\xA0a\xA0fix made anywhere in\xA0the\xA0list is written back from\xA0it. The\xA0end that\xA0does hold an\xA0argument is spaced out as\xA0before, and\xA0a\xA0pseudo-element written with\xA0an\xA0empty argument, <code>a::part()</code> for\xA0one, is read the\xA0same way as\xA0a\xA0pseudo-class. <ul><li>Under\xA0<code>always</code> an\xA0empty argument standing at\xA0an\xA0end of\xA0the\xA0list used to\xA0draw two\xA0warnings, each at\xA0the\xA0parenthesis the\xA0other one was about, and\xA0the\xA0fix then threw, which\xA0Stylelint reported as\xA0a\xA0parse error: the\xA0rule lost every warning and\xA0every fix it had for\xA0that\xA0selector, so\xA0<code>a:not(), b:not(c)</code> came back exactly as\xA0it was. One standing between\xA0two\xA0arguments threw nothing and\xA0the\xA0warnings it drew were real, but\xA0the\xA0fix that\xA0answered them carried the\xA0whitespace of\xA0the\xA0empty argument off with\xA0it, <code>a:not(b, ,c)</code> coming back as\xA0<code>a:not( b,, c )</code>, so\xA0those go the\xA0way of\xA0the\xA0rest.</li><li>Under\xA0<code>never</code> a\xA0fix made anywhere in\xA0such a\xA0selector list used to\xA0be written back from\xA0that\xA0tree: <code>a:not( ,b )</code> came back as\xA0<code>a:not(, b)</code>. Nothing is reported there now and\xA0nothing written, so\xA0a\xA0violation standing beside the\xA0empty argument, the\xA0space in\xA0<code>a:not( b, )</code> or\xA0in\xA0the\xA0<code>b:not( c )</code> of\xA0<code>a:not( ) , b:not( c )</code>, goes unreported along with\xA0the\xA0rewriting.</li></ul></li><li>The\xA0<code>string-quotes</code> rule now asks each block of\xA0a\xA0page about\xA0its own language (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/101" target="_blank" rel="noreferrer">#101</a>). A\xA0stylesheet written in\xA0Less inside\xA0a\xA0<code>&lt;style lang=&quot;less&quot;&gt;</code> used to\xA0be read as\xA0plain CSS, so\xA0the\xA0quotes standing in\xA0the\xA0text of\xA0a\xA0<code>//</code> comment were reported and\xA0the\xA0fix rewrote them; a\xA0page may now hold blocks in\xA0several languages and\xA0each is read as\xA0its own.</li><li>A\xA0Less CSS guard is no\xA0longer read as\xA0ordinary CSS (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/125" target="_blank" rel="noreferrer">#125</a>). <code>isStandardSyntaxSelector</code> used to\xA0recognise one only by\xA0accident\xA0— a\xA0guard on\xA0a\xA0selector carrying no colon looked like\xA0a\xA0mixin definition, and\xA0a\xA0condition naming a\xA0variable looked like\xA0a\xA0parametric mixin\xA0— so\xA0<code>.a:hover when (1 = 1)</code> answered to\xA0neither and\xA0every rule built on\xA0that\xA0util went on\xA0checking it. Such a\xA0selector is now passed over\xA0as\xA0any other Less construct is, wherever the\xA0guard stands, while\xA0a\xA0quoted attribute value holding the\xA0same text, <code>a[href$=&quot; when (&quot;]</code> for\xA0one, is ordinary CSS and\xA0is checked as\xA0before.</li><li>A\xA0Less CSS guard is recognised in\xA0two\xA0more shapes (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/150" target="_blank" rel="noreferrer">#150</a>). <code>isStandardSyntaxSelector</code> asked for\xA0whitespace in\xA0front of\xA0the\xA0condition that\xA0Less does not\xA0ask for, so\xA0<code>.a:hover when(1 = 1)</code> went on\xA0being checked by\xA0every rule built on\xA0that\xA0util, and\xA0a\xA0fix could write a\xA0line break into\xA0the\xA0guard&#39;s own list of\xA0conditions, <code>when(1 = 1), (2 = 2)</code> for\xA0one. A\xA0quotation mark escaped outside a\xA0string no\xA0longer opens one either: the\xA0run blanked from\xA0<code>.x\\&#39;y:hover when (&#39;z&#39; = &#39;w&#39;)</code> used to\xA0swallow the\xA0guard along with\xA0it, which\xA0put the\xA0selector back in\xA0the\xA0rules&#39; way. That\xA0reading cost a\xA0plain CSS selector as\xA0well, wherever an\xA0escape stood in\xA0front of\xA0an\xA0attribute value holding the\xA0text of\xA0a\xA0guard\xA0— <code>.a\\&quot;b [title=&quot; when (&quot;] c</code> was passed over\xA0by\xA0every rule of\xA0the\xA0plugin, and\xA0is now read like\xA0any other.</li><li>The\xA0text of\xA0a\xA0quoted attribute value is no\xA0longer read as\xA0selector syntax (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/151" target="_blank" rel="noreferrer">#151</a>). An\xA0attribute value may hold any text at\xA0all\xA0— a\xA0link matched by\xA0its address, a\xA0<code>data-*</code> carrying a\xA0template fragment\xA0— and\xA0<code>isStandardSyntaxSelector</code> used to\xA0read <code>[title=&quot;:extend(x)&quot;]</code>, <code>[title=&quot;//&quot;]</code>, <code>[title=&quot;&lt;%&quot;]</code>, <code>[title=&quot;%&gt;&quot;]</code> and\xA0<code>[title=&quot;.foo()bar&quot;]</code> as\xA0preprocessor constructs, so\xA0several rules passed such a\xA0rule over\xA0in\xA0silence. Every check that\xA0reads the\xA0text of\xA0a\xA0selector now reads it with\xA0the\xA0quoted runs emptied, as\xA0the\xA0check for\xA0a\xA0Less guard already did, while\xA0a\xA0preprocessor construct standing beside such a\xA0value is recognised as\xA0before. One check asks whether the\xA0selector holds a\xA0colon, to\xA0tell a\xA0Less mixin definition from\xA0a\xA0selector ending in\xA0a\xA0functional pseudo-class, and\xA0a\xA0colon quoted inside\xA0an\xA0attribute value no\xA0longer answers it: <code>.mixin(&quot;a:b&quot;)</code> is read as\xA0the\xA0Less mixin definition it is.</li><li>Two\xA0comments standing side by\xA0side in\xA0a\xA0selector are no\xA0longer read as\xA0an\xA0inline comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/164" target="_blank" rel="noreferrer">#164</a>). The\xA0closing <code>*/</code> of\xA0one and\xA0the\xA0opening <code>/*</code> of\xA0the\xA0next spell <code>//</code> between\xA0them, so\xA0<code>a:not( b )/*one*//*two*/c</code> looked like\xA0a\xA0preprocessor construct to\xA0<code>isStandardSyntaxSelector</code> and\xA0several rules passed it over\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/151" target="_blank" rel="noreferrer">#151</a>). A\xA0comment is now taken out of\xA0the\xA0copy those checks read, delimiters and\xA0all, so\xA0the\xA0text a\xA0comment holds is no\xA0longer read as\xA0selector syntax either\xA0— while\xA0a\xA0real <code>//</code> comment is recognised as\xA0before, whatever its text spells: <code>a//*x*/b</code> is one, and\xA0not\xA0a\xA0comment of\xA0the\xA0other kind. What a\xA0comment leaves behind is read too, since\xA0it leaves the\xA0whitespace it stood in: <code>.a:hover/*x*/when (1 = 1)</code> and\xA0<code>.a:hover when/**/(1 = 1)</code> are the\xA0guards the\xA0Less compiler reads there, and\xA0are passed over\xA0now like\xA0any other guard. The\xA0colon a\xA0comment holds is no\xA0longer counted either, so\xA0<code>.mixin(/*:*/)</code> is read as\xA0a\xA0Less mixin definition, as\xA0<code>.mixin(&quot;a:b&quot;)</code> already was.</li><li>A\xA0Less guard keyword written in\xA0another case is no\xA0longer taken for\xA0a\xA0guard (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/168" target="_blank" rel="noreferrer">#168</a>). Less reads its keywords in\xA0lower case only\xA0— the\xA0compiler prints <code>.a:hover WHEN (1 = 1)</code> as\xA0it stands, and\xA0answers <code>when NOT (1 = 1)</code> with\xA0a\xA0syntax error\xA0— while\xA0<code>isStandardSyntaxSelector</code> matched the\xA0word in\xA0any case and\xA0passed such a\xA0selector over. Nothing valid was silenced by\xA0that, since\xA0a\xA0parenthesis opening after\xA0whitespace is nothing a\xA0CSS selector has, but\xA0what the\xA0plugin says about\xA0such a\xA0file is now what the\xA0syntax says.</li><li>Interpolation standing in\xA0the\xA0text of\xA0a\xA0quoted run is no\xA0longer read as\xA0interpolation of\xA0the\xA0selector (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/170" target="_blank" rel="noreferrer">#170</a>). <code>isStandardSyntaxSelector</code> asked whether the\xA0selector holds <code>#{…}</code>, <code>@{…}</code>, <code>\${…}</code>, <code>{{…}}</code> or\xA0<code>$(…)</code> before\xA0it emptied the\xA0quoted runs, so\xA0<code>[title=&quot;#{a}&quot;]</code> was taken for\xA0a\xA0preprocessor construct and\xA0several rules (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/151" target="_blank" rel="noreferrer">#151</a>) passed the\xA0rule over, reporting nothing and\xA0fixing nothing. A\xA0pair of\xA0braces holding anything at\xA0all counts as\xA0interpolation of\xA0the\xA0template kind, so\xA0an\xA0attribute value carrying no construct whatever went with\xA0them\xA0— <code>[data-config=&#39;{a:1}&#39;]</code> for\xA0one, whose braces are the\xA0text of\xA0a\xA0value and\xA0nothing else. The\xA0question is asked of\xA0the\xA0emptied copy now, as\xA0every other question about\xA0the\xA0text already was, while\xA0interpolation standing outside a\xA0quoted value is recognised as\xA0before: <code>[title=&quot;a&quot;]#{$b}</code> and\xA0<code>a[b=#{c}][d=&quot;e&quot;]</code> are the\xA0preprocessor constructs they were. A\xA0comment holding the\xA0text of\xA0one, <code>a /* #{$b} */ c</code>, is read as\xA0the\xA0comment it is, as\xA0a\xA0comment spelling any other construct already was.</li><li>The\xA0text of\xA0a\xA0quoted attribute value is no\xA0longer read as\xA0a\xA0Less <code>:extend</code> (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/171" target="_blank" rel="noreferrer">#171</a>). <code>postcss-less</code> marks a\xA0rule as\xA0an\xA0extend by\xA0matching the\xA0text of\xA0its selector, quotes and\xA0all, so\xA0<code>[title=&quot;:extend(x)&quot;]</code> carried the\xA0mark though\xA0what stands inside\xA0the\xA0quotes is an\xA0attribute value and\xA0nothing else, and\xA0several rules (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/151" target="_blank" rel="noreferrer">#151</a>) passed such a\xA0rule over\xA0in\xA0a\xA0Less stylesheet while\xA0checking it in\xA0every other. The\xA0mark is asked of\xA0the\xA0code now, of\xA0the\xA0same copy the\xA0checks for\xA0a\xA0preprocessor construct already read, with\xA0every quoted run emptied. A\xA0real extend is passed over\xA0as\xA0before, <code>&amp;:extend(.inline)</code> and\xA0<code>.a:extend(.b all)</code> among it, and\xA0so\xA0is <code>[title=&quot;:extend(x)&quot;]:extend(.b)</code>, which\xA0carries one of\xA0each.</li><li>The\xA0text of\xA0a\xA0value is no\xA0longer read as\xA0a\xA0Less <code>&amp;:extend</code> (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/181" target="_blank" rel="noreferrer">#181</a>). <code>postcss-less</code> marks a\xA0declaration as\xA0an\xA0extend by\xA0matching the\xA0word against\xA0its value, quotes and\xA0all, so\xA0<code>b: &quot;extend(x)&quot;</code>, <code>content: &quot;a:extend(y)&quot;</code> and\xA0even <code>b: myextend(x)</code> carried the\xA0mark though\xA0all three\xA0are plain declarations Less compiles as\xA0they stand, and\xA0the\xA0rules that\xA0read a\xA0declaration through\xA0<code>isStandardSyntaxDeclaration</code> passed them over\xA0in\xA0a\xA0Less stylesheet while\xA0checking them in\xA0every other. The\xA0declaration&#39;s shape is the\xA0answer now: a\xA0real extend is the\xA0one whose property is <code>&amp;</code>, which\xA0no standard declaration has, and\xA0it is passed over\xA0as\xA0before\xA0— in\xA0any case and\xA0spacing, since\xA0everything else Less answers with\xA0a\xA0syntax error is nothing to\xA0fix either. The\xA0shape is read in\xA0every syntax, where the\xA0mark stood only in\xA0Less, so\xA0the\xA0same <code>&amp;:extend(.b)</code> pasted into\xA0a\xA0plain CSS or\xA0SCSS file is passed over\xA0now too, instead of\xA0being respelled <code>&amp;: extend(.b)</code> as\xA0though\xA0the\xA0colon of\xA0that\xA0construct were a\xA0declaration&#39;s.</li><li>The\xA0<code>indentation</code> rule no\xA0longer raises the\xA0whole of\xA0a\xA0selector list by\xA0a\xA0level when\xA0one selector of\xA0it is a\xA0pseudo-class broken over\xA0several lines (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/74" target="_blank" rel="noreferrer">#74</a>). The\xA0extra level issue <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/30" target="_blank" rel="noreferrer">#30</a> asked for\xA0was granted by\xA0a\xA0presence check over\xA0the\xA0entire selector, so\xA0the\xA0<code>.foo,</code> standing beside a\xA0multi-line <code>:where( … )</code> was asked for\xA0an\xA0indent it must not\xA0have, and\xA0<code>--fix</code> rewrote a\xA0correct file into\xA0a\xA0wrong one\xA0— the\xA0trigger being nothing rarer than\xA0a\xA0second selector in\xA0the\xA0list, in\xA0plain CSS. The\xA0level is now raised by\xA0the\xA0parenthetical-depth tracking, for\xA0the\xA0lines inside\xA0the\xA0pseudo&#39;s parentheses and\xA0for\xA0them alone, whichever position of\xA0the\xA0list the\xA0pseudo stands in, so\xA0the\xA0sole-selector case of\xA0#30\xA0keeps its extra level and\xA0every other selector of\xA0the\xA0list keeps its own. <ul><li>One more shape moves with\xA0the\xA0discount that\xA0granted the\xA0old level: a\xA0line standing inside\xA0parentheses that\xA0other code opened, <code>a:has(.foo,</code> broken before\xA0<code>.bar)</code> for\xA0one, is measured at\xA0the\xA0rule&#39;s own level now. At\xA0the\xA0top level that\xA0level is zero either way and\xA0only the\xA0words of\xA0the\xA0warning change, <code>0 spaces</code> where <code>-2 spaces</code> was asked; inside\xA0a\xA0block the\xA0old reading expected such a\xA0line one level below the\xA0rule itself\xA0— an\xA0outdent no other line is asked for\xA0— and\xA0the\xA0line is expected level with\xA0its rule now, so\xA0a\xA0file spelled to\xA0the\xA0old expectation draws a\xA0warning it did not\xA0draw.</li></ul></li><li>The\xA0<code>max-line-length</code> rule no\xA0longer reports the\xA0line after\xA0one that\xA0holds both an\xA0<code>@import</code> string and\xA0a\xA0<code>url()</code> argument (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/197" target="_blank" rel="noreferrer">#197</a>). The\xA0rule took one excluded substring off a\xA0line, so\xA0the\xA0second stayed behind and\xA0came off the\xA0next line instead, with\xA0the\xA0wrong sign: a\xA0line well within\xA0the\xA0limit was reported longer by\xA0the\xA0distance to\xA0the\xA0substring it never held, while\xA0the\xA0line holding both was measured with\xA0one of\xA0them still on\xA0it. Every excluded substring standing on\xA0a\xA0line now comes off that\xA0line, a\xA0<code>url()</code> argument standing inside\xA0an\xA0<code>@import</code> string only once.</li><li>The\xA0<code>declaration-block-semicolon-*</code> rules no\xA0longer report a\xA0declaration that\xA0has no semicolon where a\xA0comment closes the\xA0block behind it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/208" target="_blank" rel="noreferrer">#208</a>). These rules pass over\xA0the\xA0declaration a\xA0block ends on\xA0without\xA0a\xA0semicolon, and\xA0found it as\xA0the\xA0block&#39;s last node; a\xA0comment standing between\xA0that\xA0declaration and\xA0the\xA0closing brace is a\xA0node of\xA0its own, the\xA0last one, so\xA0<code>a { color: pink /* c */ }</code> was measured for\xA0a\xA0semicolon the\xA0file does not\xA0hold. The\xA0<code>always</code> options of\xA0the\xA0<code>-before</code> rules wrote a\xA0break, or\xA0a\xA0space, in\xA0front of\xA0the\xA0comment and\xA0another one on\xA0every run, the\xA0file growing a\xA0line or\xA0a\xA0space at\xA0a\xA0time; <code>-space-after</code> pulled the\xA0comment up to\xA0the\xA0declaration under\xA0<code>always</code> and\xA0closed it up under\xA0<code>never</code>; <code>-newline-after</code> stepped past the\xA0comments on\xA0its own and\xA0is left as\xA0it was. The\xA0declaration is now sought past the\xA0comments, as\xA0the\xA0flag PostCSS keeps for\xA0it already reads the\xA0block, and\xA0a\xA0nested rule or\xA0at-rule closing the\xA0block is not\xA0passed over\xA0the\xA0same way, since\xA0the\xA0declaration in\xA0front of\xA0it keeps its semicolon and\xA0is measured as\xA0before.</li><li>The\xA0<code>function-comma-*</code> rules no\xA0longer read a\xA0comment out of\xA0an\xA0address (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/153" target="_blank" rel="noreferrer">#153</a>). The\xA0pattern that\xA0takes the\xA0comments out of\xA0a\xA0function&#39;s arguments before\xA0its commas are measured used to\xA0read the\xA0double slash of\xA0<code>url(//cdn/a.png)</code> as\xA0one, and\xA0cut everything from\xA0there to\xA0the\xA0end of\xA0the\xA0line away with\xA0it, so\xA0the\xA0position a\xA0comma was measured at\xA0pointed at\xA0whatever stood there instead\xA0— into\xA0the\xA0middle of\xA0a\xA0word, or\xA0past the\xA0end of\xA0what was left. A\xA0comma with\xA0the\xA0whitespace its option asks for\xA0already in\xA0place was reported that\xA0way, and\xA0the\xA0fix that\xA0followed wrote where nothing was wrong: <code>image-set(url(//a) 1x , // c</code> and\xA0a\xA0line break came back from\xA0<code>function-comma-newline-after</code> with\xA0the\xA0comment moved onto\xA0a\xA0line of\xA0its own.</li><li>The\xA0<code>media-query-list-comma-*</code>, <code>media-feature-colon-space-*</code> and\xA0<code>media-feature-range-operator-space-*</code> rules no\xA0longer read the\xA0arguments of\xA0a\xA0function as\xA0media syntax (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/213" target="_blank" rel="noreferrer">#213</a>). A\xA0comma, a\xA0colon or\xA0a\xA0range operator standing inside\xA0<code>url(x/a,b.png)</code>, <code>url(http://x)</code> or\xA0<code>url(a&gt;=b)</code> was a\xA0comma of\xA0the\xA0query list, a\xA0media feature&#39;s colon or\xA0a\xA0feature&#39;s operator to\xA0the\xA0search, so\xA0a\xA0file with\xA0nothing wrong in\xA0it drew a\xA0warning, and\xA0the\xA0fix then wrote whitespace into\xA0the\xA0address, which\xA0no\xA0longer names the\xA0same resource: <code>@media (min-width: url(x/a,b.png))</code> came back as\xA0<code>@media (min-width: url(x/a, b.png))</code>. The\xA0arguments of\xA0a\xA0call are passed over\xA0now, and\xA0a\xA0media feature keeps its own parentheses: those of\xA0<code>(min-width: 1px)</code> and\xA0of\xA0<code>screen and (color)</code> open no call, since\xA0nothing is named in\xA0front of\xA0them. The\xA0search cannot be asked this of\xA0itself\xA0— it reads the\xA0character in\xA0front of\xA0the\xA0first parenthesis of\xA0a\xA0text out of\xA0nothing at\xA0all and\xA0finds a\xA0letter there, so\xA0it takes the\xA0whole of\xA0<code>(min-width: 1px)</code> for\xA0a\xA0call&#39;s arguments. A\xA0word joining two\xA0features names no call however a\xA0file spells it, so\xA0the\xA0<code>and(min-width: 1px)</code> of\xA0a\xA0file written without\xA0the\xA0space its grammar asks for\xA0is read as\xA0the\xA0feature it is, and\xA0an\xA0escaped parenthesis inside\xA0an\xA0address closes the\xA0arguments of\xA0nothing.</li><li>The\xA0<code>function-comma-*</code> rules no\xA0longer read one comment where the\xA0file spells two, nor\xA0a\xA0comment where a\xA0string spells its delimiters (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/214" target="_blank" rel="noreferrer">#214</a>). The\xA0comments were taken out of\xA0a\xA0function&#39;s arguments with\xA0a\xA0regular expression before\xA0the\xA0commas were measured in\xA0what was left. It carried no <code>g</code> flag, so\xA0only the\xA0first comment ever went, and\xA0every position behind the\xA0second was off by\xA0that\xA0comment&#39;s width; and\xA0where the\xA0<code>*/</code> of\xA0one comment was followed by\xA0code, its match grew until\xA0it found a\xA0<code>*/</code> that\xA0was not, swallowing everything in\xA0between\xA0— a\xA0comma of\xA0the\xA0arguments among it. <code>translate(1px/*k*/, /*c*/ 2px)</code> was reported for\xA0a\xA0space that is there, and\xA0<code>translate(&quot;a/*b&quot;, &quot;c*/ d&quot;,1px)</code>, where a\xA0string spells those delimiters, was read the\xA0same way: under\xA0<code>always</code> the\xA0comma that\xA0has its space drew a\xA0warning beside the\xA0one that\xA0has none, and\xA0under\xA0<code>never</code> neither of\xA0them drew anything, the\xA0position measured for\xA0the\xA0first having landed inside\xA0a\xA0word. The\xA0comments are found by\xA0reading the\xA0arguments now, in\xA0the\xA0same pass that\xA0places the\xA0commas, so\xA0<code>translate(&quot;a/*b&quot;, &quot;c*/ d&quot;,1px)</code> lures nothing across\xA0a\xA0comma either\xA0— the\xA0comma that\xA0has its space is left alone and\xA0the\xA0one that\xA0has none is reported, and\xA0a\xA0comment code follows straight away is left where it stands, since\xA0taking it out would put the\xA0whitespace in\xA0front of\xA0it against\xA0that\xA0code.</li><li>Every <code>*-single-line</code> option now counts a\xA0form feed as\xA0the\xA0line break it is (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/244" target="_blank" rel="noreferrer">#244</a>). The\xA0same reading told those options that\xA0a\xA0construct broken with\xA0one stood on\xA0a\xA0single line, so\xA0<code>a\\f,b {}</code> drew <code>Expected single space after &quot;,&quot; in a single-line list</code> although\xA0the\xA0list holds a\xA0break, and\xA0<code>a { color: pink;top: 0\\f}</code> drew <code>Expected single space after &quot;;&quot; in a single-line declaration block</code> from\xA0<code>declaration-block-semicolon-space-after</code>. In\xA0<code>function-parentheses-space-inside</code> the\xA0fix then wrote over\xA0the\xA0break itself: <code>a { b: fn(\\f1px\\f); }</code> came out of\xA0<code>--fix</code> under\xA0<code>always-single-line</code> as\xA0<code>a { b: fn( 1px ); }</code>, both form feeds gone and\xA0three\xA0lines of\xA0the\xA0file made one. Such a\xA0construct is multi-line now, so\xA0the\xA0single-line options pass it over\xA0and\xA0the\xA0multi-line ones take it up.</li><li>The\xA0<code>no-eol-whitespace</code> rule under\xA0<code>ignore: [&quot;empty-lines&quot;]</code> no\xA0longer reports the\xA0whitespace standing alone on\xA0a\xA0line that\xA0a\xA0bare carriage return or\xA0a\xA0form feed opens (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/247" target="_blank" rel="noreferrer">#247</a>). The\xA0lookback that\xA0measures such a\xA0line for\xA0emptiness reached back to\xA0the\xA0previous line feed and\xA0no further, so\xA0<code>a {}\\r \\rb {}</code> drew a\xA0warning where <code>a {}\\n \\nb {}</code> and\xA0<code>a {}\\r\\n \\r\\nb {}</code> drew none. All four\xA0spellings are read alike now, and\xA0a\xA0line holding anything but\xA0whitespace is reported as\xA0before.</li><li>The\xA0<code>function-parentheses-newline-inside</code> rule no\xA0longer asks for\xA0a\xA0line break the\xA0function already holds (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/246" target="_blank" rel="noreferrer">#246</a>). Whether that\xA0break was already standing was read as\xA0<code>includes(&quot;\\n&quot;)</code>, while\xA0whether the\xA0function stands on\xA0more than\xA0one line is read through\xA0the\xA0three\xA0characters that\xA0end a\xA0line, so\xA0a\xA0function broken with\xA0bare carriage returns or\xA0with\xA0form feeds was multi-line to\xA0the\xA0one reading and\xA0unbroken to\xA0the\xA0other: under\xA0<code>always</code> and\xA0under\xA0<code>always-multi-line</code> alike, <code>a { b: fn(\\r1px\\r); }</code> was reported for\xA0the\xA0breaks it holds and\xA0<code>--fix</code> wrote a\xA0line feed in\xA0front of\xA0each of\xA0them, <code>a { b: fn(\\n\\r1px\\n\\r); }</code>, which\xA0the\xA0next run then passed over\xA0in\xA0silence. Both options read the\xA0three\xA0characters now, and\xA0a\xA0function broken on\xA0one side only draws one warning where it used to\xA0draw two: <code>a { b: fn(\\r1px); }</code> is asked for\xA0the\xA0break in\xA0front of\xA0its closing parenthesis alone, and\xA0comes back <code>a { b: fn(\\r1px\\n); }</code> rather than\xA0broken on\xA0both sides. The\xA0break the\xA0option asks for\xA0may also stand behind an\xA0inline comment, <code>a { t: translate( // c\\r 1px, 2px\\r); }</code> for\xA0one, and\xA0such a\xA0value was reported and\xA0then reformatted by\xA0the\xA0fix, which\xA0wrote a\xA0break of\xA0its own in\xA0front of\xA0the\xA0comment\xA0— the\xA0shape <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/141" target="_blank" rel="noreferrer">#141</a> answered where the\xA0comment is closed by\xA0a\xA0line feed. It is accepted now wherever the\xA0syntax ends the\xA0comment on\xA0the\xA0break the\xA0file spells: a\xA0carriage return under\xA0<code>postcss-less</code> and\xA0<code>postcss-scss</code> alike, and\xA0a\xA0form feed under\xA0<code>postcss-scss</code>, which\xA0ends an\xA0inline comment on\xA0one where Less does not.</li><li>The\xA0<code>function-whitespace-after</code> rule no\xA0longer reads a\xA0parenthesis that\xA0groups an\xA0expression as\xA0a\xA0call&#39;s (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/230" target="_blank" rel="noreferrer">#230</a>). The\xA0parenthesis was handed to\xA0a\xA0check that\xA0looked at\xA0the\xA0character standing behind it and\xA0at\xA0nothing else, so\xA0a\xA0Less or\xA0SCSS calculation carrying a\xA0unit was a\xA0call wherever the\xA0search reached it\xA0— where the\xA0value opened on\xA0the\xA0parenthesis, and\xA0anywhere inside\xA0a\xA0call&#39;s arguments: <code>always</code> reported <code>h1 { width: (@a * 2)px; }</code> and\xA0wrote the\xA0whitespace in, which\xA0Less then compiles as\xA0two\xA0values rather than\xA0the\xA0one the\xA0author meant, and\xA0<code>never</code> closed the\xA0two\xA0values of\xA0<code>h1 { width: (@a * 2) px; }</code> up into\xA0that\xA0same expression\xA0— so\xA0whichever of\xA0the\xA0two\xA0options a\xA0project picked, one of\xA0the\xA0shapes was rewritten into\xA0the\xA0other. The\xA0rule now looks for\xA0the\xA0parenthesis a\xA0call closes instead of\xA0coming across\xA0every parenthesis there is, so\xA0a\xA0group is passed over\xA0however deeply it is nested, whether it stands inside\xA0a\xA0call or\xA0holds one, and\xA0a\xA0negated or\xA0subtracted one, <code>-(@a * 2)px</code> and\xA0<code>2-(1)px</code>, goes with\xA0it. The\xA0options of\xA0a\xA0Less import are left alone as\xA0well: <code>@import (reference)&quot;foo.less&quot;;</code> used to\xA0draw a\xA0warning under\xA0<code>always</code> for\xA0the\xA0quotation mark behind the\xA0parenthesis, and\xA0<code>@import (reference) &quot;foo.less&quot;;</code> used to\xA0lose that\xA0space under\xA0<code>never</code>. Plain CSS spells a\xA0group of\xA0its own inside\xA0a\xA0calculation, and\xA0it was rewritten the\xA0same way: under\xA0<code>never</code> the\xA0fix closed <code>a { width: calc((100% - 20px) - 1rem); }</code> up into\xA0<code>calc((100% - 20px)- 1rem)</code>, and\xA0under\xA0<code>always</code> it pushed <code>calc((100% - 20px)-1rem)</code> apart into\xA0<code>calc((100% - 20px) -1rem)</code>\xA0— each of\xA0them a\xA0sum the\xA0CSS grammar no\xA0longer reads, so\xA0the\xA0declaration was dropped by\xA0the\xA0browser rather than\xA0merely respaced. A\xA0second plain CSS shape is read better than\xA0before\xA0as\xA0well: an\xA0escaped parenthesis inside\xA0an\xA0address closes no call, so\xA0<code>a { b: url(a\\)b)red; }</code> is reported at\xA0the\xA0parenthesis that\xA0does close the\xA0call and\xA0the\xA0fix no\xA0longer writes a\xA0space into\xA0the\xA0address, where <code>always</code> used to\xA0put it. One more parenthesis stops being read and\xA0rightly so: a\xA0run of\xA0characters opening on\xA0a\xA0digit names nothing, so\xA0<code>a { b: 2px(1)red; }</code> and\xA0<code>a { b: 2and(1)red; }</code> are a\xA0dimension in\xA0front of\xA0a\xA0parenthesis rather than\xA0a\xA0call, and\xA0the\xA0whitespace behind them is none of\xA0this rule&#39;s business.</li><li>The\xA0<code>block-closing-brace-empty-line-before</code> rule under\xA0<code>always-multi-line</code> no\xA0longer asks for\xA0an\xA0empty line a\xA0block already holds (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/245" target="_blank" rel="noreferrer">#245</a>). An\xA0empty line was read as\xA0two\xA0line feeds, so\xA0<code>a {color: pink;\\r\\r}</code> and\xA0<code>a {color: pink;\\f\\f}</code> were reported although\xA0each holds one, where their line-feed twin was passed over.</li><li>The\xA0<code>function-parentheses-space-inside</code> and\xA0<code>function-parentheses-newline-inside</code> rules no\xA0longer report a\xA0function whose closing parenthesis a\xA0comment has swallowed, nor\xA0write outside one (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/131" target="_blank" rel="noreferrer">#131</a>). <code>postcss-value-parser</code> knows nothing of\xA0a\xA0comment opened by\xA0a\xA0double slash, so\xA0a\xA0<code>/*</code> standing in\xA0the\xA0text of\xA0one opens a\xA0block comment to\xA0it that\xA0never closes and\xA0takes the\xA0parenthesis with\xA0it. The\xA0function comes back marked unclosed, and\xA0the\xA0stringifier prints what such a\xA0node keeps in\xA0front of\xA0its parenthesis behind it instead, so\xA0the\xA0space or\xA0the\xA0line break an\xA0<code>always</code> option asked for\xA0landed outside the\xA0function; PostCSS then trimmed it out of\xA0the\xA0value and\xA0into\xA0the\xA0raws of\xA0what followed, which\xA0left the\xA0value looking untouched to\xA0the\xA0next run, and\xA0the\xA0next run wrote another one. <code>--fix</code> grew the\xA0file by\xA0a\xA0character a\xA0run, for\xA0as\xA0long as\xA0it was asked, and\xA0reported nothing at\xA0all. Such a\xA0function is left alone now, warning and\xA0all, since\xA0the\xA0parentheses these options are about\xA0are not\xA0where the\xA0parser puts them. A\xA0closed call standing inside\xA0one is still read and\xA0fixed where it stands, and\xA0a\xA0bracket the\xA0file really leaves open never gets this far: PostCSS throws on\xA0one of\xA0those before\xA0any rule sees the\xA0declaration.</li><li>The\xA0<code>unit-case</code> rule no\xA0longer reads a\xA0bang flag standing in\xA0a\xA0value, or\xA0in\xA0the\xA0parameters of\xA0an\xA0at-rule, as\xA0part of\xA0the\xA0unit in\xA0front of\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/234" target="_blank" rel="noreferrer">#234</a>). PostCSS moves only the\xA0last <code>!important</code> of\xA0a\xA0declaration out of\xA0the\xA0value, so\xA0every flag written in\xA0front of\xA0it stays where it was, and\xA0<code>postcss-value-parser</code> reads <code>1px!important</code> as\xA0one word\xA0— whose unit the\xA0rule measured as\xA0<code>px!important</code>. Under\xA0<code>&quot;upper&quot;</code> the\xA0fix wrote the\xA0keyword in\xA0capitals along with\xA0the\xA0unit, so\xA0<code>a { b: 1px!important 2px!important; }</code> came out of\xA0<code>--fix</code> as\xA0<code>a { b: 1PX!IMPORTANT 2PX!important; }</code>, and\xA0nothing was reported; under\xA0<code>&quot;lower&quot;</code> the\xA0same reading rewrote a\xA0declaration whose units were already right, <code>a { b: 1px!IMPORTANT 2px!important; }</code> losing the\xA0spelling its flag was given. A\xA0unit ends where the\xA0flag begins now, so\xA0the\xA0keyword keeps the\xA0case the\xA0file gives it under\xA0either option, the\xA0warning names the\xA0unit rather than\xA0the\xA0unit and\xA0the\xA0flag together and\xA0ends where the\xA0unit ends, a\xA0Sass <code>!default</code> or\xA0<code>!global</code> stands the\xA0same way, and\xA0a\xA0Less at-variable such as\xA0<code>@variable: 10px!important</code> is read like\xA0any other declaration. A\xA0word whose whole unit is a\xA0flag, the\xA0<code>1!important</code> of\xA0<code>a { b: 1!important 1px!important; }</code>, carries no unit at\xA0all and\xA0is passed over. A\xA0unit no specification knows is recased as\xA0before: the\xA0rule asks where the\xA0unit ends, not\xA0whether it is one.</li><li>The\xA0<code>unit-case</code>, <code>color-hex-case</code>, <code>function-parentheses-*-inside</code> and\xA0<code>number-*</code> rules no\xA0longer read the\xA0text of\xA0an\xA0end-of-line comment as\xA0code (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/271" target="_blank" rel="noreferrer">#271</a>). These six\xA0walk a\xA0value with\xA0<code>postcss-value-parser</code>, which\xA0has a\xA0node for\xA0a\xA0block comment and\xA0none for\xA0one opened by\xA0a\xA0double slash, so\xA0a\xA0block comment reached them as\xA0a\xA0node of\xA0its own and\xA0was passed over\xA0while\xA0an\xA0end-of-line comment reached them as\xA0ordinary words, calls and\xA0divs, and\xA0each worked on\xA0that\xA0text as\xA0it works on\xA0the\xA0value. <code>a { b: 1px // 2px</code> followed by\xA0<code>3px; }</code> came back from\xA0<code>--fix</code> with\xA0<code>// 2PX</code>, a\xA0hex colour written in\xA0such a\xA0comment was recased, a\xA0call spelled there had its parentheses spaced or\xA0broken, and\xA0a\xA0number written there lost a\xA0zero or\xA0gained one. A\xA0set of\xA0at-rule parameters carries such a\xA0comment the\xA0same way, and\xA0was read the\xA0same way\xA0— under\xA0<code>postcss-less</code> all along, and\xA0under\xA0<code>postcss-scss</code> since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/233" target="_blank" rel="noreferrer">#233</a> put <code>unit-case</code> on\xA0the\xA0copy the\xA0file spells and\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/268" target="_blank" rel="noreferrer">#268</a> did the\xA0same for\xA0the\xA0two\xA0<code>number-*</code> rules. A\xA0node standing in\xA0that\xA0text is left alone now, and\xA0nothing is reported about\xA0it, while\xA0the\xA0code standing on\xA0either side of\xA0the\xA0comment is read and\xA0fixed as\xA0before. <ul><li><code>function-parentheses-newline-inside</code> did the\xA0worst of\xA0it, since\xA0the\xA0line breaks its <code>always</code> options wrote ended the\xA0comment early and\xA0turned the\xA0rest of\xA0its text into\xA0code the\xA0file is read by.</li><li>A\xA0call opened in\xA0the\xA0comment&#39;s text is left alone however far it reaches, parentheses and\xA0all: <code>a { b: f( 1 ) // g( 2</code> followed by\xA0<code>3 ); }</code> used to\xA0have the\xA0spacing of\xA0both its calls written, on\xA0the\xA0comment&#39;s line and\xA0on\xA0the\xA0line under\xA0it, and\xA0only the\xA0call in\xA0front of\xA0the\xA0comment is spaced now. What such a\xA0call gathered out of\xA0the\xA0code below the\xA0comment is walked as\xA0ever, so\xA0a\xA0unit, a\xA0colour, a\xA0number or\xA0a\xA0nested call standing there is read where it stands.</li><li>Where such a\xA0call was all a\xA0warning was ever about, the\xA0warning goes with\xA0it. <code>a { t: foo(1px // c) calc(</code> followed by\xA0<code>2px); }</code> drew warnings from\xA0both <code>function-parentheses-*-inside</code> rules about\xA0the\xA0parentheses of\xA0a\xA0<code>calc</code>, and\xA0Less itself reads that\xA0stylesheet as\xA0<code>foo(1px 2px)</code>: the\xA0whole of\xA0<code>calc(</code> stands inside\xA0the\xA0comment. The\xA0guards that\xA0hold a\xA0fix back where it would carry code into\xA0a\xA0comment\xA0— <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/129" target="_blank" rel="noreferrer">#129</a>, <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/280" target="_blank" rel="noreferrer">#280</a> and\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/281" target="_blank" rel="noreferrer">#281</a>\xA0— still stand in\xA0front of\xA0every fix that\xA0would. A\xA0call the\xA0parser closes the\xA0other way about\xA0— opened outside such a\xA0comment and\xA0closed on\xA0a\xA0parenthesis standing inside\xA0it\xA0— is left alone too, which\xA0is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/320" target="_blank" rel="noreferrer">#320</a> and\xA0is answered below.</li></ul></li><li>The\xA0<code>function-parentheses-space-inside</code> and\xA0<code>function-parentheses-newline-inside</code> rules no\xA0longer read a\xA0call the\xA0parser closed on\xA0a\xA0parenthesis standing inside\xA0an\xA0end-of-line comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/320" target="_blank" rel="noreferrer">#320</a>). <code>postcss-value-parser</code> knows nothing of\xA0such a\xA0comment and\xA0reads its text as\xA0code, so\xA0a\xA0closing parenthesis written there closes a\xA0call opened outside it: <code>a { b: f(1px // c) h(2px</code> followed by\xA0<code>2px); }</code> is one call of\xA0<code>f</code> reaching over\xA0the\xA0break to\xA0every syntax that\xA0spells the\xA0comment, and\xA0came back out of\xA0the\xA0parser as\xA0a\xA0closed <code>f(1px // c)</code> with\xA0an\xA0<code>h(2px</code> and\xA0the\xA0break and\xA0<code>2px)</code> beside it. Neither parenthesis the\xA0parser gave that\xA0<code>f</code> is one the\xA0file writes, and\xA0the\xA0whitespace an\xA0<code>always</code> option asked for\xA0in\xA0front of\xA0the\xA0second went inside\xA0the\xA0comment&#39;s text; a\xA0<code>never</code> option took away a\xA0space the\xA0comment&#39;s text held. Under\xA0<code>function-parentheses-newline-inside</code> the\xA0break written there closed the\xA0comment early and\xA0turned what stood behind it into\xA0code, so\xA0<code>a { t: foo(1px // c) calc(</code> followed by\xA0<code>2px); }</code>, which\xA0Less compiles to\xA0<code>foo(1px 2px)</code>, came out of\xA0<code>--fix</code> as\xA0two\xA0calls. Such a\xA0call is left alone now, warning and\xA0all, the\xA0way one the\xA0parser marks unclosed has been left alone since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/131" target="_blank" rel="noreferrer">#131</a>, and\xA0this is the\xA0half of\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/271" target="_blank" rel="noreferrer">#271</a> that\xA0stayed open\xA0— that\xA0one leaves alone a\xA0call opened inside\xA0such a\xA0comment, this one a\xA0call closed inside\xA0it. A\xA0call nested inside\xA0either is still walked and\xA0fixed where its own parentheses stand. The\xA0opening parenthesis of\xA0such a\xA0call is one the\xA0file does spell, and\xA0the\xA0warning about\xA0it goes as\xA0well: the\xA0closing one is not, so\xA0whether the\xA0option is satisfied there is a\xA0question the\xA0rule cannot put at\xA0all. A\xA0rule reading the\xA0opening half alone would write its whitespace and\xA0report the\xA0problem solved either way\xA0— rightly where that\xA0parenthesis already carries what the\xA0option asks, and\xA0over\xA0a\xA0value still violating it where it does not, at\xA0a\xA0parenthesis no run can reach. Nothing the\xA0rule can see tells the\xA0two\xA0apart. <ul><li>One shape loses a\xA0warning it ought to\xA0keep, and\xA0loses it to\xA0a\xA0reading of\xA0its own. <code>postcss-scss</code> records a\xA0double slash as\xA0a\xA0comment only where whitespace stands in\xA0front of\xA0the\xA0pair, so\xA0for\xA0<code>a { transform: translate(1px, 2px// keep me</code> and\xA0a\xA0form feed and\xA0<code>); }</code> the\xA0plugin reads no form feed as\xA0the\xA0end of\xA0that\xA0comment and\xA0takes the\xA0parenthesis for\xA0the\xA0comment&#39;s text, though\xA0Sass ends the\xA0comment there and\xA0compiles the\xA0value to\xA0<code>translate(1px, 2px)</code>. The\xA0whitespace in\xA0front of\xA0that\xA0parenthesis is no\xA0longer reported; the\xA0fix that\xA0would have taken it away was never written either, the\xA0guard in\xA0front of\xA0it having held it back on\xA0every run. That\xA0reading is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/333" target="_blank" rel="noreferrer">#333</a>.</li></ul></li><li>The\xA0<code>media-feature-parentheses-space-inside</code> rule no\xA0longer writes into\xA0the\xA0text of\xA0a\xA0comment holding the\xA0parenthesis the\xA0value parser closed a\xA0media feature on\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/347" target="_blank" rel="noreferrer">#347</a>). <code>postcss-value-parser</code> knows nothing of\xA0a\xA0comment opened by\xA0a\xA0double slash and\xA0closes one opened by\xA0<code>/*/</code> on\xA0its own star, so\xA0a\xA0<code>)</code> written inside\xA0either kind closes the\xA0feature to\xA0it, and\xA0the\xA0rule asked nothing about\xA0where the\xA0parenthesis it was writing at\xA0stood. <code>@media (a: 1px // c) and (b: 2px⏎2px)</code> came back from\xA0<code>always</code> as\xA0<code>@media ( a: 1px // c ) and (b: 2px⏎2px)</code>, the\xA0space in\xA0front of\xA0the\xA0closing parenthesis written inside\xA0the\xA0comment, and\xA0<code>@media (a: 1 /*/ ) */ )</code> came back from\xA0<code>never</code> with\xA0the\xA0space taken out of\xA0the\xA0comment&#39;s text; Sass and\xA0Less each read the\xA0first of\xA0those as\xA0<code>@media (a: 1px 2px)</code>. A\xA0feature the\xA0parser closed on\xA0such a\xA0parenthesis is now passed over\xA0whole, as\xA0it already is by\xA0both <code>function-parentheses-*-inside</code> rules, so\xA0no fix of\xA0this rule reaches that\xA0text. Everything inside\xA0such a\xA0feature goes unread with\xA0it: the\xA0parenthesis the\xA0file does spell, and\xA0a\xA0call written there, which\xA0keeps whatever spacing it has. A\xA0feature the\xA0parser marks unclosed is read exactly as\xA0it was, so\xA0what a\xA0fix writes at\xA0the\xA0end of\xA0its parameters is still <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/575" target="_blank" rel="noreferrer">#575</a>.</li><li>The\xA0<code>declaration-block-trailing-semicolon</code> rule no\xA0longer reports a\xA0declaration standing on\xA0the\xA0top level of\xA0a\xA0stylesheet (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/326" target="_blank" rel="noreferrer">#326</a>). Such a\xA0declaration closes no block, so\xA0<code>$var: pink;</code> came back out of\xA0<code>--fix</code> as\xA0<code>$var: pink</code> under\xA0<code>never</code>, and\xA0<code>$var: pink</code> came back with\xA0a\xA0semicolon written behind it under\xA0<code>always</code>, in\xA0every syntax that\xA0parses a\xA0declaration there. The\xA0semicolon behind it is as\xA0optional as\xA0a\xA0block&#39;s trailing one\xA0— dart-sass compiles a\xA0file ending in\xA0<code>$var: pink</code>\xA0— and\xA0what leaves it alone is this rule&#39;s own scope rather than\xA0the\xA0syntax: the\xA0semicolon it is named for\xA0is the\xA0one a\xA0declaration block ends on. The\xA0rule&#39;s two\xA0walks disagreed about\xA0that: the\xA0one over\xA0at-rules asked whether the\xA0node stands on\xA0a\xA0root and\xA0let it be, which\xA0is why a\xA0Less variable, an\xA0at-rule to\xA0that\xA0parser, was never touched, while\xA0the\xA0one over\xA0declarations asked nothing of\xA0the\xA0kind. Both ask the\xA0same question now, and\xA0a\xA0Sass map, the\xA0one exception the\xA0walk over\xA0declarations did carry, is answered by\xA0that\xA0question rather than\xA0beside it. The\xA0declarations of\xA0an\xA0inline <code>style</code> attribute are the\xA0one kind of\xA0node on\xA0a\xA0root the\xA0question still says yes to, since\xA0the\xA0value of\xA0such an\xA0attribute is a\xA0declaration block and\xA0nothing else; an\xA0at-rule the\xA0parser puts on\xA0such a\xA0root is let stand along with\xA0every other at-rule of\xA0a\xA0root, an\xA0attribute having no place for\xA0one.</li><li>The\xA0<code>declaration-block-trailing-semicolon</code> rule no\xA0longer counts a\xA0comment among the\xA0nodes its <code>ignore: [&quot;single-declaration&quot;]</code> option reads a\xA0block by\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/327" target="_blank" rel="noreferrer">#327</a>). The\xA0option asked whether the\xA0node it was about\xA0opened the\xA0block, and\xA0a\xA0comment is a\xA0node of\xA0the\xA0block, so\xA0<code>a { /* c */ color: pink }</code> came back out of\xA0<code>--fix</code> with\xA0a\xA0semicolon written behind the\xA0declaration under\xA0<code>always</code>, and\xA0<code>never</code> took the\xA0semicolon away from\xA0<code>a { /* c */ color: pink; }</code>, while\xA0<code>a { color: pink }</code> and\xA0<code>a { color: pink /* c */ }</code> were left alone under\xA0either. One and\xA0the\xA0same block holding one and\xA0the\xA0same declaration was ignored or\xA0checked by\xA0which\xA0side of\xA0it a\xA0comment stood on: the\xA0rule walked past the\xA0comments closing a\xA0block, since\xA0the\xA0flag it reads is the\xA0one PostCSS hangs on\xA0the\xA0node they stand behind, and\xA0read nothing of\xA0the\xA0kind at\xA0the\xA0block&#39;s opening. Both ends are read the\xA0same way now, so\xA0a\xA0block holding one declaration counts as\xA0holding a\xA0single node however many comments stand around it. Everything else a\xA0block holds is counted as\xA0it was: a\xA0bodiless at-rule alone in\xA0a\xA0block is ignored, and\xA0a\xA0declaration standing behind one is a\xA0second node, so\xA0<code>a { @include foo; color: pink }</code> is checked, as\xA0is <code>a { b { top: 0 } color: pink }</code>, a\xA0nested rule being a\xA0node of\xA0the\xA0block like\xA0any other.</li><li>The\xA0<code>function-comma-*</code> rules no\xA0longer read the\xA0text of\xA0a\xA0comment opened by\xA0<code>/*/</code> as\xA0code (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/275" target="_blank" rel="noreferrer">#275</a>). CSS closes a\xA0comment on\xA0the\xA0first <code>*/</code> after\xA0its opening, so\xA0<code>/*/*q,w*/</code> is one comment from\xA0the\xA0first slash to\xA0the\xA0last, while\xA0<code>postcss-value-parser</code> looks for\xA0that\xA0delimiter from\xA0the\xA0opening slash itself and\xA0lets the\xA0star of\xA0the\xA0opening serve as\xA0its star, so\xA0it closes on\xA0the\xA0slash standing behind the\xA0opening and\xA0hands the\xA0rest of\xA0that\xA0text back as\xA0ordinary nodes of\xA0the\xA0value, the\xA0commas standing in\xA0it among them. These four\xA0asked whether a\xA0comma stood inside\xA0a\xA0comment of\xA0the\xA0kind a\xA0double slash opens and\xA0of\xA0no other\xA0— the\xA0reading <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/271" target="_blank" rel="noreferrer">#271</a> was written for\xA0— so\xA0<code>a { b: f(x/*/*q,w*/y ,2); }</code> drew a\xA0warning from\xA0<code>function-comma-space-before</code> about\xA0the\xA0comma of\xA0<code>q,w</code>, and\xA0<code>--fix</code> wrote the\xA0space the\xA0<code>always</code> option asks for\xA0into\xA0the\xA0comment&#39;s text; where that\xA0text held whitespace of\xA0its own, <code>a { b: f(x/*/*q ,w*/y,2); }</code>, the\xA0<code>never</code> option took it away instead. Every comment the\xA0file spells is asked about\xA0now, block and\xA0inline alike, so\xA0such a\xA0comma is passed over\xA0under\xA0all four\xA0rules and\xA0every option of\xA0theirs, and\xA0the\xA0comma of\xA0the\xA0value standing beside it is reported and\xA0fixed exactly as\xA0before.</li><li>The\xA0<code>unit-case</code>, <code>number-leading-zero</code>, <code>number-no-trailing-zeros</code>, <code>string-quotes</code>, <code>color-hex-case</code>, <code>function-max-empty-lines</code>, <code>named-grid-areas-alignment</code>, <code>function-parentheses-space-inside</code>, <code>function-parentheses-newline-inside</code> and\xA0<code>media-feature-parentheses-space-inside</code> rules no\xA0longer read the\xA0text of\xA0a\xA0comment opened by\xA0<code>/*/</code> as\xA0code (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/378" target="_blank" rel="noreferrer">#378</a>). CSS closes a\xA0comment on\xA0the\xA0first <code>*/</code> after\xA0its opening, so\xA0<code>/*/ 2PX */</code> is one comment from\xA0the\xA0first slash to\xA0the\xA0last, while\xA0<code>postcss-value-parser</code> looks for\xA0that\xA0delimiter from\xA0the\xA0opening slash itself and\xA0lets the\xA0star of\xA0the\xA0opening serve as\xA0its star, so\xA0it closes the\xA0comment three\xA0characters in\xA0and\xA0hands the\xA0rest of\xA0its text back as\xA0ordinary words, strings and\xA0calls of\xA0the\xA0value. These ten\xA0asked whether a\xA0node stood inside\xA0a\xA0comment of\xA0the\xA0kind a\xA0double slash opens and\xA0of\xA0no other\xA0— the\xA0reading <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/271" target="_blank" rel="noreferrer">#271</a> was written for\xA0— so\xA0<code>a { b: 1 /*/ 2PX */ 3; }</code> drew a\xA0warning from\xA0<code>unit-case</code> about\xA0the\xA0<code>2PX</code> of\xA0the\xA0comment and\xA0came back from\xA0<code>--fix</code> as\xA0<code>/*/ 2px */</code>; a\xA0fraction written in\xA0such a\xA0comment gained or\xA0lost its zero, a\xA0string its quotes, a\xA0call the\xA0whitespace or\xA0the\xA0line breaks inside\xA0its parentheses, a\xA0row of\xA0a\xA0grid its alignment, and\xA0<code>function-max-empty-lines</code> took empty lines out of\xA0the\xA0comment&#39;s text. Every comment the\xA0file spells is asked about\xA0now, block and\xA0inline alike, as\xA0the\xA0<code>function-comma-*</code> rules have asked since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/275" target="_blank" rel="noreferrer">#275</a> above, so\xA0such text is passed over\xA0under\xA0all ten\xA0rules and\xA0every option of\xA0theirs, while\xA0the\xA0code standing beside the\xA0comment is reported and\xA0fixed exactly as\xA0before. One thing travels with\xA0that: a\xA0call the\xA0parser closed on\xA0a\xA0parenthesis standing inside\xA0such a\xA0comment is left alone by\xA0the\xA0two\xA0<code>function-parentheses-*-inside</code> rules now, warning and\xA0all, the\xA0way one closed inside\xA0an\xA0end-of-line comment has been since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/320" target="_blank" rel="noreferrer">#320</a> above, since\xA0the\xA0pair of\xA0parentheses the\xA0parser gave it is no pair the\xA0file writes. <code>media-feature-parentheses-space-inside</code> still puts no such question to\xA0a\xA0media feature, which\xA0is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/347" target="_blank" rel="noreferrer">#347</a>. Two\xA0more things travel with\xA0it. <code>function-parentheses-newline-inside</code> now writes the\xA0break its <code>always</code> options ask for, and\xA0empties the\xA0whitespace its <code>never-multi-line</code> option refuses, around the\xA0comment CSS reads rather than\xA0around the\xA0star the\xA0parser closed it on: <code>a { b: f(/*/ c */ 2); }</code> used to\xA0come back from\xA0<code>--fix</code> with\xA0the\xA0break written inside\xA0the\xA0comment, behind its first three\xA0characters, and\xA0the\xA0same call broken over\xA0three\xA0lines under\xA0<code>never-multi-line</code> used to\xA0have the\xA0space inside\xA0the\xA0comment taken away rather than\xA0the\xA0one beside the\xA0argument; the\xA0whitespace on\xA0either side of\xA0such a\xA0comment is written now as\xA0it is written beside any other block comment. The\xA0whitespace in\xA0front of\xA0the\xA0closing parenthesis is read over\xA0such a\xA0comment the\xA0same way, so\xA0<code>a { b: f(f(1,</code> a\xA0break <code>2) /*/ c */); }</code> under\xA0<code>never-multi-line</code> now draws the\xA0warning its twin with\xA0<code>/* c */</code> always drew, and\xA0the\xA0space in\xA0front of\xA0the\xA0comment is taken away. And\xA0the\xA0scan every rule finds the\xA0comments of\xA0a\xA0value with\xA0reads an\xA0address the\xA0way the\xA0tokenizers of\xA0PostCSS, <code>postcss-scss</code> and\xA0<code>postcss-less</code> read one: a\xA0bare address holds no comment, so\xA0<code>a { b: url(a/* x) 1PX /* c */ 3PX; }</code> no\xA0longer has its <code>1PX</code> passed over\xA0as\xA0the\xA0text of\xA0a\xA0comment running from\xA0the\xA0<code>/*</code> inside\xA0the\xA0address to\xA0the\xA0delimiter closing the\xA0comment behind it\xA0— which\xA0the\xA0<code>function-comma-*</code> rules had done since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/275" target="_blank" rel="noreferrer">#275</a> above and\xA0the\xA0ten\xA0would have started doing\xA0— and\xA0under\xA0<code>postcss-scss</code> and\xA0<code>postcss-less</code> the\xA0double slash of\xA0<code>url(http://x.y/*.png)</code> no\xA0longer opens a\xA0comment to\xA0these rules once the\xA0scan has given the\xA0address up, so\xA0the\xA0dimensions behind such an\xA0address are read again; a\xA0comment standing beside a\xA0quoted address inside\xA0its parentheses is a\xA0comment to\xA0all three\xA0tokenizers and\xA0is read as\xA0one now, so\xA0<code>a { b: url(&quot;a&quot; /*/ &#39;x&#39; */); }</code> no\xA0longer has its <code>&#39;x&#39;</code> rewritten by\xA0<code>string-quotes</code>. A\xA0comment standing inside\xA0a\xA0call whose empty lines <code>function-max-empty-lines</code> collapses was another matter, older than\xA0this and\xA0spelled either way; it is answered in\xA0this same release (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/503" target="_blank" rel="noreferrer">#503</a>).</li><li>The\xA0<code>block-closing-brace-newline-before</code> and\xA0<code>block-closing-brace-empty-line-before</code> rules no\xA0longer ask for\xA0a\xA0break, or\xA0for\xA0an\xA0empty line, that\xA0the\xA0file already holds in\xA0front of\xA0a\xA0closing brace an\xA0at-rule with\xA0neither a\xA0block nor\xA0a\xA0semicolon of\xA0its own closes (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/292" target="_blank" rel="noreferrer">#292</a>). Where such an\xA0at-rule\xA0— <code>@extend .b</code>, <code>@include foo</code>, a\xA0Less mixin call\xA0— is the\xA0last thing a\xA0block holds, it runs to\xA0the\xA0brace, so\xA0PostCSS files everything standing between\xA0it and\xA0the\xA0brace into\xA0the\xA0at-rule and\xA0leaves the\xA0block&#39;s own raw empty; both rules read that\xA0raw and\xA0found nothing in\xA0it. So\xA0<code>a {</code> followed by\xA0a\xA0break, <code>@extend .b</code> and\xA0a\xA0break was reported by\xA0<code>always</code> and\xA0<code>always-multi-line</code> of\xA0the\xA0newline rule for\xA0the\xA0break already standing there, and\xA0the\xA0same file with\xA0two\xA0breaks by\xA0<code>always-multi-line</code> of\xA0the\xA0empty-line rule for\xA0the\xA0empty line already standing there. Each fix then wrote another one\xA0— a\xA0line longer on\xA0every run of\xA0<code>--fix</code>, for\xA0ever. Both rules read the\xA0whitespace where the\xA0parser filed it now.</li><li>The\xA0<code>no-missing-end-of-source-newline</code> rule no\xA0longer reports a\xA0file that\xA0ends in\xA0a\xA0bare carriage return or\xA0in\xA0a\xA0form feed (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/282" target="_blank" rel="noreferrer">#282</a>). Whether the\xA0file had ended its last line was read as\xA0<code>endsWith(&quot;\\n&quot;)</code>, so\xA0a\xA0file spelled with\xA0either of\xA0the\xA0two\xA0other breaks was reported for\xA0a\xA0break it already held, and\xA0the\xA0fix then wrote a\xA0line feed over\xA0the\xA0one standing there. All three\xA0characters end a\xA0line for\xA0CSS\xA0— a\xA0quoted run is cut short by\xA0every one of\xA0them, where a\xA0tab inside\xA0it is text\xA0— and\xA0Less, the\xA0one syntax that\xA0reads no line in\xA0a\xA0form feed, refuses a\xA0form feed standing in\xA0code outright, the\xA0end of\xA0a\xA0file being one of\xA0the\xA0places it stands. <ul><li>One shape loses a\xA0warning it ought to\xA0keep, and\xA0loses it under\xA0Less alone: a\xA0file that\xA0ends inside\xA0a\xA0<code>//</code> comment on\xA0a\xA0form feed. Less runs such a\xA0comment to\xA0the\xA0end of\xA0the\xA0file and\xA0reads no line in\xA0the\xA0character, so\xA0the\xA0last line there is ended by\xA0nothing, while\xA0Sass ends the\xA0comment at\xA0it and\xA0the\xA0file is closed. Which\xA0of\xA0the\xA0two\xA0wrote the\xA0file is a\xA0question the\xA0rule does not\xA0put (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/373" target="_blank" rel="noreferrer">#373</a>).</li></ul></li><li>The\xA0<code>function-max-empty-lines</code> and\xA0<code>named-grid-areas-alignment</code> rules no\xA0longer read the\xA0text of\xA0an\xA0end-of-line comment as\xA0code (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/322" target="_blank" rel="noreferrer">#322</a>). These two\xA0walk a\xA0value with\xA0<code>postcss-value-parser</code> exactly as\xA0the\xA0six\xA0of\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/271" target="_blank" rel="noreferrer">#271</a> do and\xA0were not\xA0counted by\xA0it, so\xA0what a\xA0comment opened by\xA0a\xA0double slash holds went on\xA0reaching them as\xA0ordinary strings, words and\xA0calls. Under\xA0<code>function-max-empty-lines</code> a\xA0call whose name is the\xA0comment&#39;s text had the\xA0empty lines it seemed to\xA0hold collapsed\xA0— <code>a { b: f(1) // g(</code> followed by\xA0three\xA0line breaks and\xA0<code>2); }</code> came back with\xA0two\xA0of\xA0them gone\xA0— and\xA0what that\xA0rule writes is line breaks, so\xA0the\xA0line layout of\xA0a\xA0file was rewritten on\xA0the\xA0strength of\xA0a\xA0call read out of\xA0a\xA0comment; where a\xA0call stood on\xA0the\xA0line below such a\xA0comment, inside\xA0the\xA0one the\xA0parser opened in\xA0its text, both were fixed and\xA0the\xA0inner one was written twice\xA0— the\xA0duplication itself needs no comment to\xA0happen and\xA0is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/366" target="_blank" rel="noreferrer">#366</a>, which\xA0is answered above. Under\xA0<code>named-grid-areas-alignment</code> a\xA0row spelled in\xA0such a\xA0comment was counted among the\xA0rows of\xA0the\xA0grid, aligned along with\xA0them and\xA0written back to\xA0the\xA0alignment the\xA0option asks for, and\xA0a\xA0call standing in\xA0that\xA0text lost its parentheses and\xA0its arguments as\xA0the\xA0value was put together again. Both rules are reached under\xA0<code>postcss-scss</code> as\xA0well as\xA0under\xA0<code>postcss-less</code>: that\xA0syntax keeps such a\xA0comment out of\xA0the\xA0value it hands a\xA0rule only where the\xA0comment stands directly behind the\xA0colon, and\xA0anywhere else the\xA0copy the\xA0file spells carries it. A\xA0node opening in\xA0that\xA0text is left alone now, and\xA0nothing is reported about\xA0it, while\xA0the\xA0code standing on\xA0either side of\xA0the\xA0comment is read and\xA0fixed as\xA0before. One character can still reach such text, and\xA0it is older than\xA0this and\xA0named: <code>postcss-value-parser</code> prints a\xA0comment opened by\xA0<code>/*/</code> back four\xA0characters wide where the\xA0file spells it in\xA0three, and\xA0<code>function-max-empty-lines</code> prints the\xA0call it rewrites, so\xA0a\xA0<code>/*/</code> written inside\xA0an\xA0end-of-line comment inside\xA0such a\xA0call gains that\xA0character. That is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/370" target="_blank" rel="noreferrer">#370</a>, which\xA0covers the\xA0same widening wherever such a\xA0comment stands, in\xA0<code>function-max-empty-lines</code> alone: <code>named-grid-areas-alignment</code> takes every comment standing at\xA0the\xA0top level of\xA0a\xA0value from\xA0the\xA0file&#39;s own characters now, block and\xA0end-of-line alike, and\xA0one written inside\xA0a\xA0call still goes away with\xA0the\xA0call, which\xA0is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/367" target="_blank" rel="noreferrer">#367</a>.</li><li>The\xA0<code>color-hex-case</code>, <code>declaration-bang-space-*</code>, <code>function-comma-*</code>, <code>function-parentheses-*-inside</code>, <code>function-whitespace-after</code>, <code>media-feature-parentheses-space-inside</code>, <code>number-*</code>, <code>string-quotes</code> and\xA0<code>unit-case</code> rules no\xA0longer read the\xA0text of\xA0an\xA0end-of-line comment as\xA0code where the\xA0call in\xA0front of\xA0that\xA0comment is named in\xA0code points outside ASCII (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/343" target="_blank" rel="noreferrer">#343</a>). The\xA0scan that\xA0finds the\xA0comments of\xA0a\xA0text asks of\xA0every <code>url(</code> it meets whether the\xA0name opens a\xA0token of\xA0its own or\xA0closes the\xA0name of\xA0some other call, whose arguments may hold a\xA0comment, and\xA0it asked that\xA0of\xA0ASCII word characters, a\xA0closing brace and\xA0a\xA0hyphen alone. An\xA0identifier of\xA0CSS is spelled with\xA0far more than\xA0those, so\xA0<code>a { b: éurl(http://a/b.png) 1PX; }</code> was read as\xA0an\xA0address carrying its double slashes as\xA0ordinary characters, and\xA0the\xA0unit behind it came back lowercased, while\xA0<code>a { b: aurl(http://a/b.png) 1PX; }</code>\xA0— one character of\xA0a\xA0name away\xA0— was read as\xA0the\xA0ordinary call it is, and\xA0left alone. Sass and\xA0<code>lightningcss</code> both read such a\xA0call as\xA0an\xA0ordinary call. Sass compiles <code>a { b: aurl(a/b) 1PX; }</code> and\xA0<code>a { b: éurl(a/b) 1PX; }</code> alike, and\xA0fails on\xA0<code>a { b: aurl(a//b) 1PX; }</code> and\xA0<code>a { b: éurl(a//b) 1PX; }</code> alike, at\xA0one offset that is the\xA0length of\xA0the\xA0file\xA0— the\xA0comment the\xA0double slash opens having carried off the\xA0closing parenthesis. <code>lightningcss</code> leaves all four\xA0whole rather than\xA0normalising any of\xA0them into\xA0a\xA0<code>url()</code>. So\xA0what stands behind such a\xA0call is the\xA0text of\xA0a\xA0comment, and\xA0nothing a\xA0rule may write to. The\xA0scan now asks the\xA0question of\xA0the\xA0code points the\xA0grammar itself names, which\xA0is the\xA0reading <code>findFunctionArgumentSpans</code> has had since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/252" target="_blank" rel="noreferrer">#252</a>: <code>éurl(</code>, <code>日本url(</code>, a\xA0name opened by\xA0the\xA0middle dot and\xA0one opened by\xA0a\xA0character above the\xA0basic plane all leave an\xA0ordinary call, and\xA0one name written two\xA0ways\xA0— <code>éurl(</code> and\xA0<code>\\e9 url(</code>\xA0— is read one way. A\xA0name that\xA0an\xA0interpolation closes stays a\xA0name, as\xA0it always was. The\xA0value of\xA0a\xA0declaration is not\xA0the\xA0only place such a\xA0call stands: the\xA0parameters of\xA0an\xA0at-rule carry one the\xA0same way, and\xA0are read the\xA0same way now. Under\xA0<code>postcss-scss</code> nothing changes, since\xA0that\xA0parser refuses such a\xA0file outright\xA0— it reads the\xA0comment where the\xA0Less parser does not\xA0and\xA0finds the\xA0bracket it opened unclosed\xA0— and\xA0a\xA0file of\xA0plain CSS spells no comment with\xA0a\xA0double slash at\xA0all, so\xA0the\xA0unit behind such a\xA0call is read and\xA0fixed there as\xA0before.</li><li>The\xA0<code>color-hex-case</code>, <code>function-comma-*</code>, <code>number-*</code> and\xA0<code>unit-case</code> rules now read an\xA0address by\xA0the\xA0name CSS spells rather than\xA0by\xA0four\xA0characters (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/344" target="_blank" rel="noreferrer">#344</a>). CSS lets a\xA0stylesheet write each letter of\xA0that\xA0name as\xA0an\xA0escape, so\xA0<code>u\\rl(</code>, <code>\\url(</code>, <code>ur\\l(</code>, <code>\\75 rl(</code> and\xA0<code>\\55 RL(</code> all open the\xA0token <code>url(</code> opens, and\xA0Sass compiles every one of\xA0them to\xA0a\xA0plain <code>url(…)</code> while\xA0<code>lightningcss</code> reads an\xA0address in\xA0every one. Only the\xA0four\xA0literal characters were recognised here, so\xA0what such an\xA0address held was rewritten as\xA0though\xA0it were an\xA0ordinary call: <code>a { b: u\\rl(13PX); }</code> came back with\xA0its unit lowercased, a\xA0hex colour written there was recased, a\xA0number there lost a\xA0zero or\xA0gained one, and\xA0a\xA0comma there was spaced. The\xA0scan that\xA0finds the\xA0comments of\xA0a\xA0text has read the\xA0name this way since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/321" target="_blank" rel="noreferrer">#321</a>, and\xA0the\xA0reading is now a\xA0single one that\xA0all of\xA0them ask. A\xA0name spelling those three\xA0letters and\xA0anything besides opens an\xA0ordinary call, as\xA0it always did: <code>image-url(</code>, <code>a\\url(</code> and\xA0<code>image-\\75 rl(</code> are untouched by\xA0this. Such a\xA0call is read as\xA0one now in\xA0the\xA0other direction as\xA0well, where the\xA0whitespace closing an\xA0escape welds the\xA0name to\xA0what stands in\xA0front of\xA0it; that\xA0half is under\xA0<code>Changed</code> above.</li><li>The\xA0<code>color-hex-case</code>, <code>function-comma-*</code>, <code>function-parentheses-*-inside</code>, <code>media-feature-parentheses-space-inside</code>, <code>number-*</code> and\xA0<code>unit-case</code> rules no\xA0longer read the\xA0text of\xA0a\xA0string as\xA0code where a\xA0comment standing in\xA0front of\xA0it leaves a\xA0quotation mark open, nor\xA0write into\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/508" target="_blank" rel="noreferrer">#508</a>). These eleven\xA0walk a\xA0value with\xA0<code>postcss-value-parser</code>, which\xA0opens a\xA0string on\xA0a\xA0mark written in\xA0the\xA0text of\xA0a\xA0comment it reads as\xA0code and\xA0pairs it with\xA0the\xA0next mark of\xA0the\xA0value\xA0— and\xA0from\xA0there on\xA0every opening mark of\xA0the\xA0file is a\xA0closing one to\xA0it and\xA0the\xA0other way round. Each of\xA0them passed over\xA0the\xA0nodes standing inside\xA0the\xA0comment and\xA0went on\xA0working on\xA0what came behind it, so\xA0<code>a { b: 2PX /*/ &quot; */ &quot;2PX&quot;; }</code> came back out of\xA0<code>unit-case: &quot;lower&quot;</code> as\xA0<code>a { b: 2px /*/ &quot; */ &quot;2px&quot;; }</code>, a\xA0second warning drawn on\xA0the\xA0unit inside\xA0the\xA0string beside the\xA0one the\xA0code in\xA0front of\xA0the\xA0comment draws; a\xA0colour, a\xA0fraction, a\xA0trailing zero, a\xA0call&#39;s parentheses and\xA0a\xA0call&#39;s commas were reported and\xA0rewritten inside\xA0a\xA0string the\xA0same way by\xA0the\xA0rules that\xA0read each. The\xA0mark a\xA0comment leaves open is masked out of\xA0the\xA0copy the\xA0value is parsed in\xA0now, so\xA0the\xA0string the\xA0file spells is the\xA0string the\xA0parser reads, and\xA0its text is left exactly as\xA0written. <ul><li>One shape loses a\xA0warning with\xA0the\xA0reading. Under\xA0<code>postcss-less</code> the\xA0value of\xA0<code>a { t: foo(// c&quot;</code> and\xA0a\xA0break and\xA0<code>1px&quot;); }</code> used to\xA0reach both <code>function-parentheses-*-inside</code> rules as\xA0a\xA0closed call, the\xA0mark of\xA0the\xA0comment pairing with\xA0the\xA0one behind the\xA0break and\xA0leaving the\xA0file&#39;s parenthesis outside every string; <code>postcss-scss</code> refuses that\xA0file outright, so\xA0no rule ever read it there. Behind the\xA0mask that\xA0parenthesis stands inside\xA0the\xA0string the\xA0file&#39;s own mark opens, the\xA0call is one the\xA0parser marks unclosed, and\xA0neither rule reads a\xA0parenthesis of\xA0such a\xA0call.</li><li>A\xA0mark written over\xA0hands the\xA0parser back whatever its phantom string stood around, which\xA0is the\xA0price of\xA0the\xA0reading rather than\xA0a\xA0corner of\xA0it that\xA0could have been kept: the\xA0string was never the\xA0file&#39;s, and\xA0what it covered is code the\xA0file spells. Where that is a\xA0parenthesis the\xA0same comment holds, the\xA0parser closes on\xA0it, and\xA0the\xA0two\xA0<code>function-parentheses-*-inside</code> rules turn such a\xA0call away while\xA0<code>media-feature-parentheses-space-inside</code> has no such guard\xA0— its <code>never</code> option now takes a\xA0space out of\xA0the\xA0text of\xA0<code>@media ( b: 2 /*/ &quot; ) */ ) and (c: d)</code>, which\xA0is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/347" target="_blank" rel="noreferrer">#347</a> reached through\xA0a\xA0block comment rather than\xA0an\xA0end-of-line one, and\xA0its <code>always</code> option stops growing such a\xA0file by\xA0a\xA0space on\xA0every run. Over\xA0a\xA0corpus of\xA0a\xA0mark and\xA0a\xA0parenthesis written in\xA0one comment, 45\xA0texts are written into\xA0that\xA0were left alone before\xA0and\xA084\xA0stop running away.</li></ul></li><li>The\xA0<code>indentation</code> rule no\xA0longer asks a\xA0line an\xA0at-rule swallowed for\xA0the\xA0level of\xA0the\xA0at-rule&#39;s parameters (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/510" target="_blank" rel="noreferrer">#510</a>). An\xA0at-rule carrying neither a\xA0block nor\xA0a\xA0semicolon of\xA0its own runs to\xA0the\xA0brace closing its block, so\xA0PostCSS files a\xA0comment written on\xA0a\xA0line of\xA0its own between\xA0that\xA0at-rule and\xA0the\xA0brace into\xA0the\xA0at-rule rather than\xA0into\xA0a\xA0node of\xA0the\xA0block; the\xA0rule measured such a\xA0line with\xA0the\xA0parameters, a\xA0level deeper than\xA0the\xA0block, and\xA0since\xA0the\xA0fix of\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/375" target="_blank" rel="noreferrer">#375</a> reached the\xA0line it reported, <code>--fix</code> put the\xA0comment there: <code>a {</code> with\xA0<code>@extend .b</code> and\xA0<code>/* c */</code> each indented once below it was reported at\xA0<code>3:2</code> as\xA0<code>Expected indentation of 2 tabs</code> under\xA0<code>&quot;tab&quot;</code> and\xA0settled with\xA0the\xA0comment indented twice, where the\xA0same comment behind a\xA0declaration, or\xA0behind the\xA0same at-rule with\xA0a\xA0semicolon, stood at\xA0the\xA0block&#39;s level without\xA0a\xA0word. Such a\xA0line is a\xA0line of\xA0the\xA0block now, whatever raw the\xA0parser filed it into, and\xA0is asked for\xA0the\xA0level the\xA0at-rule itself stands at, so\xA0the\xA0comment at\xA0the\xA0block&#39;s level draws nothing; the\xA0other face of\xA0the\xA0reading, a\xA0comment a\xA0level deeper, is under\xA0<code>Changed</code> above. A\xA0comment standing in\xA0front of\xA0the\xA0semicolon closing the\xA0at-rule, or\xA0in\xA0front of\xA0the\xA0brace opening its block, is inside\xA0the\xA0statement and\xA0keeps being measured with\xA0its parameters; and\xA0in\xA0the\xA0one run of\xA0<code>--fix</code> where <code>declaration-block-trailing-semicolon</code> takes that\xA0semicolon away, the\xA0comment is the\xA0block&#39;s at\xA0once and\xA0stays at\xA0the\xA0block&#39;s level, where it used to\xA0be carried to\xA0the\xA0parameters&#39; level and\xA0left there. Under\xA0<code>postcss-less</code> an\xA0inline comment in\xA0that\xA0place is filed into\xA0the\xA0parameters themselves and\xA0is still measured with\xA0them.</li><li>The\xA0<code>max-line-length</code> rule no\xA0longer counts the\xA0address of\xA0a\xA0<code>url(</code> written behind a\xA0backslash and\xA0a\xA0form feed or\xA0a\xA0bare carriage return into\xA0the\xA0length of\xA0its line (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/566" target="_blank" rel="noreferrer">#566</a>). Such a\xA0backslash is a\xA0delimiter spelling nothing to\xA0CSS, exactly as\xA0one in\xA0front of\xA0a\xA0line feed is, and\xA0the\xA0plugin used to\xA0read it as\xA0an\xA0escape spelling the\xA0character behind it\xA0— a\xA0name of\xA0its own in\xA0front of\xA0the\xA0parenthesis, so\xA0that\xA0the\xA0address was an\xA0ordinary call&#39;s argument and\xA0stayed on\xA0the\xA0count.</li><li>The\xA0<code>@stylistic/less/at-rule-name-case</code> rule no\xA0longer renames a\xA0Less variable declared with\xA0whitespace in\xA0front of\xA0its colon (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/394" target="_blank" rel="noreferrer">#394</a>). <code>postcss-less</code> marks a\xA0variable declaration only where the\xA0colon closes the\xA0name, so\xA0<code>@v : pink;</code>, <code>@v\\t: pink;</code> and\xA0<code>@v\\n: pink;</code> came over\xA0as\xA0at-rules named <code>v</code>, and\xA0so\xA0did <code>@v:pink 1px;</code>, whose name holds the\xA0colon, and\xA0<code>@dr : { … }</code>, which\xA0declares a\xA0detached ruleset; <code>--fix</code> under\xA0<code>upper</code> turned <code>@v : pink; a { b: @v }</code> into\xA0<code>@V : pink; a { b: @v }</code>, which\xA0Less refuses with\xA0<code>variable @v is undefined</code>, and\xA0<code>lower</code> did the\xA0same the\xA0other way round. The\xA0namespace reads a\xA0variable declaration by\xA0the\xA0shape of\xA0the\xA0node now wherever the\xA0parser left the\xA0mark off, so\xA0every rule that\xA0passes over\xA0<code>@v: pink;</code> passes over\xA0those spellings too: <code>at-rule-name-space-after</code> no\xA0longer rewrites the\xA0tab or\xA0the\xA0line break in\xA0front of\xA0the\xA0colon into\xA0a\xA0space, and\xA0<code>at-rule-semicolon-newline-after</code> no\xA0longer breaks the\xA0line behind such a\xA0declaration&#39;s semicolon. A\xA0bodiless at-rule whose parameters open on\xA0a\xA0colon is a\xA0variable declaration to\xA0Less whatever its name\xA0— <code>@import : &quot;x&quot;;</code> and\xA0<code>@custom-selector :--h h1;</code> declare variables and\xA0print nothing\xA0— and\xA0is passed over\xA0with\xA0the\xA0rest; where Less cannot read the\xA0text behind the\xA0colon as\xA0a\xA0value and\xA0prints the\xA0at-rule back instead, as\xA0it does <code>@custom-media :x (min-width: 1px);</code>, the\xA0node is passed over\xA0all the\xA0same, which\xA0costs a\xA0warning at\xA0most where the\xA0other reading cost the\xA0file. An\xA0at-rule carrying a\xA0block behind more than\xA0the\xA0colon, <code>@page :first { … }</code>, is read as\xA0it always was.</li><li>The\xA0<code>unit-case</code> rule no\xA0longer reads two\xA0dimensions where a\xA0hexadecimal escape has swallowed the\xA0whitespace between\xA0them (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/526" target="_blank" rel="noreferrer">#526</a>). CSS closes such an\xA0escape with\xA0one whitespace character belonging to\xA0the\xA0escape rather than\xA0to\xA0the\xA0text, so\xA0<code>10px\\9 2PX</code> is one dimension token: <code>@csstools/css-tokenizer</code> reads its unit as\xA0<code>px</code>, a\xA0tab and\xA0<code>2PX</code>, and\xA0Sass, Less and\xA0<code>lightningcss</code> all print the\xA0line back exactly as\xA0it stands. <code>postcss-value-parser</code> reads no escape and\xA0parted the\xA0value at\xA0that\xA0space, and\xA0the\xA0rule read each half as\xA0a\xA0dimension of\xA0its own\xA0— under\xA0<code>lower</code> it reported <code>2PX</code> and\xA0<code>--fix</code> wrote <code>10px\\9 2px</code>, under\xA0<code>upper</code> it reported <code>px</code> and\xA0wrote <code>10PX\\9 2PX</code>, each option changing a\xA0half of\xA0its own of\xA0the\xA0one unit. The\xA0words are put back together before\xA0anything is read now. The\xA0<code>\\9</code> and\xA0<code>\\0</code> hacks are still taken out of\xA0the\xA0unit, and\xA0the\xA0whitespace such a\xA0hack&#39;s escape swallows ends the\xA0unit the\xA0way any character that is no code point of\xA0an\xA0identifier does, so\xA0what stands behind the\xA0hack is off the\xA0unit under\xA0both options: <code>lower</code> says nothing about\xA0that\xA0line, and\xA0<code>upper</code> reports <code>px</code> and\xA0writes <code>10PX\\9 2PX</code>, as\xA0before. Where the\xA0escape is no hack, the\xA0identifier goes on\xA0behind the\xA0whitespace: <code>10P\\61 X</code> is one dimension whose unit spells <code>PaX</code>, which\xA0Sass and\xA0<code>lightningcss</code> both print, so\xA0under\xA0<code>lower</code> the\xA0whole unit is reported and\xA0recased to\xA0<code>10p\\61 x</code>, where the\xA0rule used to\xA0name <code>P\\61</code> and\xA0leave the\xA0<code>X</code>. The\xA0same reading welds a\xA0dimension into\xA0the\xA0identifier in\xA0front of\xA0it: <code>1px!important\\9 2px!important</code> holds one unit, <code>px</code>, the\xA0second being letters of\xA0the\xA0identifier <code>important\\9 2px</code>, which\xA0Less prints as\xA0it stands and\xA0Sass refuses, so\xA0under\xA0<code>upper</code> the\xA0second <code>px</code> is no\xA0longer reported. A\xA0word such an\xA0escape welds onto\xA0an\xA0interpolation, <code>10PX\\9 #{$a}</code>, is passed over\xA0whole, as\xA0every word touching an\xA0interpolation is, and\xA0its unit goes unnamed where it used to\xA0be named.</li><li>The\xA0<code>function-parentheses-newline-inside</code> rule no\xA0longer reports an\xA0opening parenthesis whose break stands behind an\xA0end-of-line comment that\xA0stands behind another comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/505" target="_blank" rel="noreferrer">#505</a>). <code>postcss-value-parser</code> hangs the\xA0run in\xA0front of\xA0a\xA0division sign on\xA0the\xA0sign itself, so\xA0the\xA0first slash of\xA0such a\xA0comment opens a\xA0node beginning at\xA0that\xA0run, in\xA0front of\xA0the\xA0comment&#39;s own span; the\xA0walk asked where the\xA0node begins, was told it stood in\xA0no comment, and\xA0took it for\xA0the\xA0first argument, so\xA0nothing behind the\xA0comment was read. <code>a { b: f(/*c*/ // d⏎ 2); }</code> drew <code>Expected newline after &quot;(&quot;</code> under\xA0<code>always</code> over\xA0the\xA0break already standing there, and\xA0<code>--fix</code> wrote a\xA0second one behind the\xA0parenthesis. The\xA0run in\xA0front of\xA0such a\xA0comment is read as\xA0the\xA0value&#39;s now and\xA0the\xA0comment walked past like\xA0any other, so\xA0<code>always</code> and\xA0<code>always-multi-line</code> are silent about\xA0that\xA0parenthesis wherever the\xA0break the\xA0comment closes stands inside\xA0the\xA0parentheses. Two\xA0things follow for\xA0the\xA0other option and\xA0the\xA0other fix. <code>never-multi-line</code> measures the\xA0whole of\xA0that\xA0whitespace rather than\xA0the\xA0part in\xA0front of\xA0the\xA0comment: where it holds the\xA0break that\xA0closes the\xA0comment the\xA0warning stands and\xA0the\xA0fix is declined, no fix being able to\xA0take that\xA0break, and\xA0where the\xA0comment is the\xA0last thing the\xA0parentheses hold the\xA0runs in\xA0front of\xA0it are emptied, <code>f(/*b*/ //c⏎ )</code> coming out as\xA0<code>f(/*b*///c⏎ )</code>. And\xA0the\xA0break <code>always</code> writes goes into\xA0the\xA0run in\xA0front of\xA0the\xA0comment wherever that\xA0run is the\xA0last one measured, so\xA0the\xA0comment ahead of\xA0it keeps its line: <code>f(/*b*/ //c2⏎)</code> comes out as\xA0<code>f(/*b*/⏎ //c2⏎)</code> rather than\xA0with\xA0the\xA0break behind the\xA0parenthesis.</li><li>The\xA0<code>function-parentheses-space-inside</code> and\xA0<code>function-parentheses-newline-inside</code> rules no\xA0longer read the\xA0parentheses of\xA0a\xA0<code>url()</code> as\xA0a\xA0call&#39;s (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/533" target="_blank" rel="noreferrer">#533</a>). What those parentheses hold is an\xA0address rather than\xA0a\xA0list of\xA0arguments, and\xA0a\xA0space or\xA0a\xA0line break written behind the\xA0<code>(</code> parts the\xA0address from\xA0it: PostCSS and\xA0<code>postcss-less</code> take a\xA0<code>url()</code> as\xA0one token only while\xA0nothing but\xA0the\xA0address stands there, so\xA0<code>a { b: url(a&quot;b); }</code> came back from\xA0<code>always</code> as\xA0<code>a { b: url( a&quot;b ); }</code>, which\xA0both refuse with\xA0<code>Unclosed string</code>. <code>postcss-scss</code> keeps its token there and\xA0refuses another shape instead, a\xA0quoted address whose string holds a\xA0parenthesis nothing closes. Both rules now pass a\xA0<code>url()</code> over\xA0whole, under\xA0every spelling of\xA0the\xA0name, as\xA0the\xA0rules that\xA0already ask that\xA0question do\xA0— <code>color-hex-case</code>, the\xA0four\xA0<code>function-comma-*</code>, both <code>number-*</code>, <code>unit-case</code> and\xA0the\xA0six\xA0rules about\xA0a\xA0solidus. Nothing inside\xA0a\xA0<code>url()</code> is reported or\xA0written by\xA0either rule any more, whether the\xA0address is quoted or\xA0bare, and\xA0the\xA0whitespace in\xA0front of\xA0the\xA0closing parenthesis goes with\xA0it, which\xA0no spelling of\xA0an\xA0address was ever harmed by: over\xA0a\xA0corpus of\xA037\xA0340\xA0stylesheets the\xA0two\xA0rules give up\xA09 180\xA0warnings, 4\xA0590\xA0of\xA0them about\xA0that\xA0closing side, and\xA0take away\xA0252 files the\xA0parser could no\xA0longer read. A\xA0call standing beside an\xA0address is untouched, so\xA0<code>@font-face { src: url(&quot;a.woff2&quot;) format(&quot;woff2&quot;); }</code> still has its <code>format()</code> spaced out.</li></ul></li><li><p>The\xA0warning was right and\xA0what came of\xA0it was not\xA0— a\xA0position off its mark, a\xA0character that\xA0was none of\xA0the\xA0file&#39;s, a\xA0fix discarded or\xA0refused where nothing was at\xA0risk:</p><ul><li>The\xA0fix a\xA0rule writes at\xA0the\xA0end of\xA0a\xA0text ending in\xA0a\xA0<code>url(</code> written behind a\xA0backslash and\xA0a\xA0form feed or\xA0a\xA0bare carriage return is written now, where it used to\xA0be held back as\xA0one that\xA0would land inside\xA0a\xA0comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/566" target="_blank" rel="noreferrer">#566</a>). Under\xA0<code>postcss-scss</code> and\xA0<code>postcss-less</code> the\xA0double slash inside\xA0such parentheses used to\xA0open a\xA0comment to\xA0the\xA0end of\xA0the\xA0line, since\xA0the\xA0backslash was read as\xA0an\xA0escape and\xA0the\xA0<code>url(</code> as\xA0a\xA0call of\xA0a\xA0name of\xA0its own, so\xA0the\xA0warning stood and\xA0nothing was written: the\xA0whitespace <code>block-closing-brace-space-before</code>, <code>declaration-block-semicolon-space-before</code> and\xA0<code>media-feature-parentheses-space-inside</code> ask to\xA0stand or\xA0not\xA0to\xA0stand there, and\xA0the\xA0semicolon <code>declaration-block-trailing-semicolon</code> adds, now land in\xA0the\xA0file.</li><li>The\xA0<code>value-list-comma-space-after</code> and\xA0<code>value-list-comma-newline-after</code> rules now fix a\xA0comma that\xA0opens the\xA0value (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/134" target="_blank" rel="noreferrer">#134</a>). Such a\xA0comma is the\xA0value&#39;s first character, and\xA0the\xA0whitespace behind it belongs to\xA0the\xA0value like\xA0any other, but\xA0the\xA0boundary that\xA0keeps these rules off a\xA0comma standing in\xA0the\xA0property name was a\xA0character too wide and\xA0refused that\xA0one as\xA0well, so\xA0<code>a { prop: ,0; }</code> was reported and\xA0then left exactly as\xA0it was, however many times <code>--fix</code> was run. Every option of\xA0both rules is affected, so\xA0<code>never</code> now closes the\xA0whitespace up as\xA0<code>always</code> opens it, and\xA0a\xA0custom property is fixed like\xA0any other declaration. A\xA0comma standing in\xA0the\xA0property name is still reported and\xA0still left alone, since\xA0nothing these rules could write would reach it. The\xA0same comma in\xA0<code>value-list-comma-space-before</code> took more than\xA0a\xA0boundary moved, the\xA0whitespace at\xA0stake there standing outside the\xA0value, and\xA0is answered below.</li><li>The\xA0<code>value-list-comma-space-before</code> rule now fixes a\xA0comma that\xA0opens the\xA0value (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/166" target="_blank" rel="noreferrer">#166</a>). The\xA0whitespace in\xA0front of\xA0such a\xA0comma is none of\xA0the\xA0value&#39;s: it is the\xA0text standing between\xA0the\xA0colon and\xA0the\xA0value, which\xA0PostCSS keeps in\xA0the\xA0declaration&#39;s <code>raws.between</code>, and\xA0no write to\xA0the\xA0value could reach it. So\xA0the\xA0problem was reported and\xA0the\xA0code left exactly as\xA0it was, however many times <code>--fix</code> was run\xA0— under\xA0<code>never</code> and\xA0<code>never-single-line</code> wherever a\xA0declaration is spelled with\xA0whitespace after\xA0its colon, and\xA0under\xA0<code>always</code> and\xA0<code>always-single-line</code> wherever it is not. The\xA0rule writes that\xA0text now, so\xA0<code>a { prop: ,0; }</code> is closed up under\xA0<code>never</code> and\xA0<code>a { prop:,0; }</code> opened to\xA0a\xA0single space under\xA0<code>always</code>, a\xA0line break standing after\xA0the\xA0colon collapsing into\xA0that\xA0space as\xA0one standing in\xA0the\xA0value always did, and\xA0a\xA0custom property is written like\xA0any other declaration while\xA0a\xA0comment between\xA0the\xA0colon and\xA0the\xA0value keeps its place. A\xA0comma standing behind an\xA0inline comment is still reported and\xA0still left alone, since\xA0the\xA0line break in\xA0that\xA0whitespace is what closes the\xA0comment. The\xA0whitespace after\xA0the\xA0colon is also what <code>declaration-colon-space-after</code> writes, so\xA0a\xA0configuration that\xA0asks one of\xA0the\xA0two\xA0rules for\xA0a\xA0space there and\xA0the\xA0other for\xA0none cannot be satisfied by\xA0any file: the\xA0rule standing later in\xA0the\xA0configuration writes last, and\xA0the\xA0other&#39;s warning is raised again by\xA0the\xA0next run over\xA0the\xA0output. Nothing runs away with\xA0it\xA0— a\xA0second <code>--fix</code> writes the\xA0same file as\xA0the\xA0first.</li><li>The\xA0<code>declaration-block-semicolon-newline-before</code> rule now writes its line break in\xA0front of\xA0the\xA0semicolon of\xA0a\xA0declaration carrying <code>!important</code> (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/203" target="_blank" rel="noreferrer">#203</a>). The\xA0<code>always</code> options measured the\xA0whitespace in\xA0front of\xA0the\xA0semicolon, which\xA0stands behind the\xA0flag, and\xA0wrote the\xA0break to\xA0the\xA0end of\xA0the\xA0value, which\xA0ends before\xA0it, wherever the\xA0flag was spelled the\xA0plain way\xA0— PostCSS keeps a\xA0raw for\xA0<code> !important</code> only where it is spelled some other way, and\xA0the\xA0rule wrote to\xA0the\xA0flag only where it found that\xA0raw. So\xA0the\xA0first run put the\xA0break in\xA0front of\xA0the\xA0flag, <code>a { color: pink !important; }</code> coming out as\xA0<code>a { color: pink\\n !important; }</code>, the\xA0second put another one where the\xA0first belonged, and\xA0neither reported a\xA0thing, <code>--fix</code> claiming a\xA0clean pass twice on\xA0a\xA0file it was still changing. The\xA0break goes behind the\xA0flag now, whichever way the\xA0flag is spelled, and\xA0a\xA0comment standing on\xA0either side of\xA0it keeps its place.</li><li>The\xA0<code>selector-descendant-combinator-no-non-space</code> rule now spells a\xA0selector the\xA0way the\xA0file spells it in\xA0the\xA0text of\xA0a\xA0warning under\xA0<code>postcss-scss</code>. The\xA0text a\xA0warning quotes is read out of\xA0the\xA0copy that\xA0syntax rewrites every inline comment in, so\xA0<code>.foo\\t// c\\n( )\\t.bar</code> was reported as\xA0<code>Unexpected &quot;\\t/* c*/\\n( )&quot;</code>, a\xA0text the\xA0file does not\xA0hold. It is read back out of\xA0the\xA0copy the\xA0file spells now, as\xA0the\xA0position it is reported at\xA0already was.</li><li>The\xA0<code>selector-pseudo-class-parentheses-space-inside</code> rule now reads a\xA0carriage return and\xA0a\xA0form feed as\xA0the\xA0line breaks they are (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/173" target="_blank" rel="noreferrer">#173</a>). The\xA0<code>never</code> option spares the\xA0whitespace in\xA0front of\xA0the\xA0closing parenthesis of\xA0a\xA0multi-line argument list, but\xA0a\xA0list counted as\xA0multi-line only where it held a\xA0line feed, so\xA0<code>a:not( b\\r, c )</code> had that\xA0space taken away while\xA0the\xA0same list broken with\xA0<code>\\n</code> kept it. The\xA0three\xA0characters the\xA0syntaxes end a\xA0line on\xA0are read alike now; <code>\\r\\n</code> was spared all along, by\xA0its <code>\\n</code>.</li><li>Every <code>*-newline-*</code> rule now reads a\xA0carriage return and\xA0a\xA0form feed as\xA0the\xA0line breaks they are (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/196" target="_blank" rel="noreferrer">#196</a>). The\xA0checker they share asked for\xA0a\xA0line feed and\xA0took nothing else, so\xA0a\xA0file broken with\xA0a\xA0bare <code>\\r</code> or\xA0a\xA0<code>\\f</code> was reported for\xA0a\xA0break it already held\xA0— <code>.a\\r, .b</code> for\xA0one\xA0— and\xA0the\xA0fix made it worse: most of\xA0the\xA0rules wrote a\xA0line feed in\xA0front of\xA0the\xA0break and\xA0left the\xA0pair standing, <code>a,\\n\\rb</code>, while\xA0<code>selector-list-comma-newline-before</code> and\xA0<code>block-opening-brace-newline-before</code>, whose fix goes in\xA0front of\xA0the\xA0indentation the\xA0break closes, grew the\xA0file by\xA0a\xA0line with\xA0every run of\xA0<code>--fix</code>, for\xA0ever. <code>function-comma-newline-before</code>, <code>function-comma-newline-after</code> and\xA0<code>declaration-block-semicolon-newline-before</code> rewrote the\xA0break into\xA0a\xA0line feed instead, and\xA0stop doing that\xA0too. The\xA0three\xA0characters every parser this plugin reads through\xA0ends a\xA0line on\xA0are taken alike now, <code>\\r\\n</code> as\xA0it always was. Sixteen rules are built on\xA0that\xA0checker: the\xA0four\xA0<code>selector-list-comma-newline-*</code> and\xA0<code>value-list-comma-newline-*</code>, the\xA0two\xA0<code>function-comma-newline-*</code> and\xA0<code>media-query-list-comma-newline-*</code>, <code>declaration-colon-newline-after</code>, the\xA0two\xA0<code>declaration-block-semicolon-newline-*</code>, the\xA0three\xA0<code>block-*-brace-newline-*</code>, <code>at-rule-name-newline-after</code> and\xA0<code>at-rule-semicolon-newline-after</code>.</li><li>The\xA0<code>block-opening-brace-newline-after</code>, <code>block-closing-brace-newline-after</code>, <code>declaration-colon-newline-after</code>, <code>declaration-block-semicolon-newline-after</code> and\xA0<code>media-query-list-comma-newline-after</code> rules now trim the\xA0whitespace in\xA0front of\xA0a\xA0bare carriage return or\xA0a\xA0form feed up to\xA0that\xA0break, the\xA0way they trim it in\xA0front of\xA0a\xA0line feed (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/204" target="_blank" rel="noreferrer">#204</a>). Their fixers looked for\xA0the\xA0break themselves, with\xA0a\xA0line feed in\xA0their regular expressions, and\xA0wrote one in\xA0where they found none, so\xA0<code>a { \\rcolor: pink; }</code> came out of\xA0<code>--fix</code> as\xA0<code>a {\\n \\rcolor: pink; }</code>\xA0— a\xA0stray line feed in\xA0front of\xA0the\xA0old break and\xA0the\xA0spaces it was reported for\xA0still standing\xA0— where its line-feed twin comes out as\xA0<code>a {\\ncolor: pink; }</code>. The\xA0same reading told <code>block-opening-brace-newline-after</code> which\xA0breaks to\xA0take away under\xA0<code>never-multi-line</code> and\xA0<code>block-closing-brace-newline-after</code> which\xA0comment ends on\xA0the\xA0brace&#39;s line, and\xA0told <code>declaration-colon-newline-after</code> and\xA0<code>media-query-list-comma-newline-after</code> which\xA0comment stands on\xA0the\xA0colon&#39;s or\xA0the\xA0comma&#39;s own line: a\xA0block comment behind a\xA0form feed was read as\xA0one that\xA0does, the\xA0rules asking for\xA0a\xA0break behind it, and\xA0in\xA0the\xA0second rule an\xA0end-of-line <code>//</code> comment closed by\xA0a\xA0bare carriage return, or\xA0by\xA0a\xA0form feed under\xA0Sass, was read as\xA0one that\xA0does not, the\xA0fix pushing it onto\xA0a\xA0line of\xA0its own. All of\xA0it reads every line break now.</li><li>The\xA0<code>block-closing-brace-newline-before</code> rule now reads a\xA0carriage return and\xA0a\xA0form feed as\xA0the\xA0line breaks they are (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/209" target="_blank" rel="noreferrer">#209</a>). It is the\xA0one rule of\xA0the\xA0before\xA0family that\xA0looks for\xA0the\xA0break itself rather than\xA0through\xA0the\xA0checker <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/196" target="_blank" rel="noreferrer">#196</a> taught, and\xA0both places it looks asked for\xA0a\xA0line feed. Its fixer wrote one in\xA0wherever it found none, so\xA0<code>a { color: pink; \\r}</code> came out of\xA0<code>--fix</code> under\xA0<code>always</code> as\xA0<code>a { color: pink;\\n \\r}</code>\xA0— a\xA0stray line feed in\xA0front of\xA0the\xA0old break and\xA0the\xA0spaces it was reported for\xA0still standing\xA0— where the\xA0line-feed twin of\xA0that\xA0file comes out as\xA0<code>a { color: pink;\\n}</code>; <code>always-multi-line</code> did the\xA0same. And\xA0the\xA0condition that\xA0raises the\xA0warning read the\xA0block&#39;s final whitespace for\xA0a\xA0line feed, or\xA0for\xA0a\xA0Windows break, and\xA0for\xA0nothing else, so\xA0<code>a { color: pink;\\r}</code> was reported for\xA0the\xA0break it already held and\xA0then given a\xA0second one. Both halves read every line break now, so\xA0<code>a { color: pink; \\r}</code> is trimmed to\xA0the\xA0break it already holds and\xA0<code>a { color: pink;\\r}</code> is left alone, exactly as\xA0their line-feed twins are. Under\xA0<code>always-multi-line</code> that\xA0holds wherever the\xA0block counts as\xA0multi-line, which\xA0a\xA0line feed or\xA0a\xA0carriage return still decides on\xA0its own: a\xA0block whose only break is a\xA0form feed passes for\xA0a\xA0single line, and\xA0the\xA0option passes it over\xA0as\xA0it always did.</li><li>The\xA0<code>no-eol-whitespace</code> rule now trims every line of\xA0a\xA0comment when\xA0fixing, and\xA0not\xA0only the\xA0lines up to\xA0the\xA0first quotation mark in\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/67" target="_blank" rel="noreferrer">#67</a>). An\xA0apostrophe in\xA0the\xA0text of\xA0a\xA0comment, the\xA0one in\xA0<code>isn&#39;t</code> for\xA0instance, used to\xA0be taken for\xA0the\xA0opening quote of\xA0a\xA0string, and\xA0the\xA0lines after\xA0it were passed over\xA0— reported, but\xA0left as\xA0they were, however many times the\xA0fix was run.</li><li>The\xA0<code>string-quotes</code> rule no\xA0longer loses its bearings in\xA0a\xA0declaration&#39;s value or\xA0an\xA0at-rule&#39;s parameters holding a\xA0comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/61" target="_blank" rel="noreferrer">#61</a>, <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/33" target="_blank" rel="noreferrer">#33</a>): <ul><li>the\xA0reported position now points at\xA0the\xA0quote itself instead of\xA0drifting by\xA0the\xA0length of\xA0every comment standing in\xA0front of\xA0it;</li><li>the\xA0fix now changes the\xA0quote characters and\xA0nothing else, so\xA0every comment in\xA0the\xA0value survives it.</li></ul></li><li>A\xA0<code>//</code> comment that\xA0a\xA0carriage return ends is now read as\xA0ended wherever the\xA0plugin asks whether a\xA0fix would land inside\xA0one. Sass reads that\xA0character as\xA0a\xA0line of\xA0its own, and\xA0Less turns one into\xA0a\xA0line feed before\xA0it parses anything, so\xA0a\xA0comment ends there in\xA0either language; every rule that\xA0declines a\xA0fix next to\xA0such a\xA0comment used to\xA0read the\xA0comment as\xA0running on, and\xA0declined where nothing was at\xA0risk.</li><li>A\xA0double slash the\xA0file spells no comment with\xA0is no\xA0longer read as\xA0one wherever the\xA0plugin asks whether a\xA0fix would land inside\xA0a\xA0comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/225" target="_blank" rel="noreferrer">#225</a>). Plain CSS spells none that\xA0way, so\xA0<code>a { b: 1px//c</code> followed by\xA0<code>;</code> on\xA0the\xA0next line ends in\xA0code and\xA0the\xA0semicolon has a\xA0line to\xA0join, but\xA0the\xA0question was asked of\xA0the\xA0text alone, which\xA0cannot tell the\xA0two\xA0spellings apart, and\xA0nine\xA0rules refused a\xA0fix where nothing was at\xA0risk. The\xA0syntax is asked first now, as\xA0the\xA0four\xA0rules repaired alongside <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/216" target="_blank" rel="noreferrer">#216</a> already ask it. Six\xA0of\xA0the\xA0nine\xA0are rules a\xA0file of\xA0plain CSS can reach\xA0— <code>declaration-block-semicolon-space-before</code>, <code>declaration-block-semicolon-newline-before</code>, <code>declaration-block-trailing-semicolon</code>, <code>function-parentheses-space-inside</code>, <code>function-parentheses-newline-inside</code> and\xA0<code>media-feature-parentheses-space-inside</code>, which\xA0read a\xA0value or\xA0a\xA0set of\xA0parameters. The\xA0other three\xA0— <code>block-opening-brace-space-before</code>, <code>block-opening-brace-newline-before</code> and\xA0<code>declaration-colon-space-before</code>\xA0— read a\xA0<code>raws.between</code>, and\xA0plain CSS puts no comment of\xA0that\xA0kind in\xA0one: it reads <code>a //c</code> as\xA0a\xA0selector rather than\xA0as\xA0a\xA0selector and\xA0a\xA0comment. The\xA0only double slash that\xA0can reach those three\xA0is the\xA0pair <code>*/</code> and\xA0<code>/*</code> spell between\xA0two\xA0block comments, which\xA0the\xA0reading already tells from\xA0a\xA0comment&#39;s own, so\xA0nothing they do changes. The\xA0guards themselves are as\xA0load-bearing as\xA0ever: under\xA0Sass and\xA0under\xA0Less a\xA0<code>//</code> comment does stand in\xA0a\xA0<code>raws.between</code>, and\xA0the\xA0fix is still held back there.</li><li>The\xA0<code>block-closing-brace-empty-line-before</code> rule&#39;s fix no\xA0longer writes a\xA0second empty line onto\xA0a\xA0block that is one break short of\xA0holding one (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/245" target="_blank" rel="noreferrer">#245</a>). It put <code>context.newline</code> twice on\xA0the\xA0end of\xA0whatever stood there wherever the\xA0break already standing was neither a\xA0line feed nor\xA0a\xA0Windows pair, so\xA0<code>a {\\rcolor: pink;\\r}</code> came back as\xA0<code>a {\\rcolor: pink;\\r\\n\\n}</code>\xA0— two\xA0empty lines where one was asked for, spelled with\xA0a\xA0break the\xA0file does not\xA0use\xA0— and\xA0the\xA0next run was silent, so\xA0the\xA0file kept them. The\xA0break already standing is written twice over\xA0now, so\xA0<code>a {\\rcolor: pink;\\r}</code> comes back as\xA0<code>a {\\rcolor: pink;\\r\\r}</code>; and\xA0where the\xA0whitespace holds none, the\xA0break the\xA0file ends its lines with\xA0is written instead, so\xA0<code>a {\\rcolor: pink;}</code> comes back as\xA0<code>a {\\rcolor: pink;\\r\\r}</code> as\xA0well. That\xA0break is read off the\xA0file, as\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/267" target="_blank" rel="noreferrer">#267</a> has it below, and\xA0<code>context.newline</code> is written only where the\xA0file ends no line at\xA0all. The\xA0fix under\xA0<code>never</code> keeps that\xA0spelling too, writing the\xA0first break of\xA0a\xA0run in\xA0the\xA0whole run&#39;s place rather than\xA0<code>context.newline</code>, which\xA0knows a\xA0Windows pair and\xA0a\xA0line feed and\xA0no other break. A\xA0stray semicolon standing in\xA0that\xA0whitespace no\xA0longer moves the\xA0empty line either: it is written behind the\xA0semicolon rather than\xA0in\xA0front, and\xA0taken away from\xA0around it under\xA0<code>never</code>, where <code>a {\\ncolor: pink;\\n;\\n}</code> used to\xA0draw a\xA0warning that\xA0no run of\xA0<code>--fix</code> could clear.</li><li>The\xA0<code>block-closing-brace-empty-line-before</code> rule&#39;s fix now spells its empty line the\xA0way the\xA0file spells its lines, wherever the\xA0break it copies stands (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/267" target="_blank" rel="noreferrer">#267</a>). Where the\xA0whitespace in\xA0front of\xA0the\xA0closing brace held no break of\xA0its own, one was looked for\xA0in\xA0the\xA0whitespace the\xA0file keeps around the\xA0block and\xA0its tree\xA0— in\xA0front of\xA0a\xA0node, behind its name, between\xA0it and\xA0what follows that\xA0name, inside\xA0its block at\xA0the\xA0end, and\xA0in\xA0the\xA0semicolon a\xA0nested block may be followed by\xA0— and\xA0whitespace is not\xA0the\xA0only place a\xA0break stands. One standing in\xA0a\xA0selector, in\xA0a\xA0value, in\xA0a\xA0set of\xA0parameters or\xA0inside\xA0an\xA0<code>!important</code> was invisible to\xA0that\xA0reading, and\xA0<code>context.newline</code> went in\xA0instead, which\xA0reads a\xA0line feed and\xA0a\xA0Windows pair and\xA0knows neither of\xA0the\xA0two\xA0other breaks a\xA0stylesheet is written with: <code>a {b,\\rc {color: pink;}}</code> and\xA0<code>a {color: pink !\\rimportant;}</code> each came back carrying a\xA0line feed the\xA0file holds nowhere else. The\xA0file itself is read now, and\xA0the\xA0answer is the\xA0first break it spells outside its comments and\xA0its strings\xA0— a\xA0break inside\xA0either ends a\xA0line and\xA0says nothing about\xA0how the\xA0file ends its lines\xA0— so\xA0both come back spelled with\xA0the\xA0carriage return they are written with, and\xA0<code>context.newline</code> is left for\xA0the\xA0file that\xA0ends no line at\xA0all. A\xA0file spelling its lines two\xA0ways answers with\xA0its own first break rather than\xA0with\xA0its block&#39;s, so\xA0<code>a,\\nb {\\rcolor: pink; }</code> gains a\xA0line feed where it used to\xA0gain a\xA0carriage return.</li><li>The\xA0<code>function-parentheses-*-inside</code> rules now fix a\xA0closing parenthesis that\xA0neither reading of\xA0a\xA0line break moves into\xA0an\xA0inline comment (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/132" target="_blank" rel="noreferrer">#132</a>). Both rules guard that\xA0fix by\xA0asking twice whether such a\xA0comment holds the\xA0parenthesis\xA0— once as\xA0the\xA0value stands, once as\xA0the\xA0fix would leave it\xA0— and\xA0each asking counted a\xA0set of\xA0breaks of\xA0its own: the\xA0first every break either syntax closes a\xA0comment on, the\xA0second only the\xA0breaks both of\xA0them do. Where a\xA0break of\xA0the\xA0difference stood between\xA0the\xA0double slash and\xA0the\xA0whitespace the\xA0fix writes over\xA0— a\xA0form feed, which\xA0Sass ends a\xA0comment on\xA0and\xA0Less does not\xA0— the\xA0two\xA0answers described two\xA0different languages: the\xA0first said the\xA0parenthesis was not\xA0inside\xA0a\xA0comment yet, the\xA0second that\xA0it would be inside\xA0one afterwards, so\xA0the\xA0problem was reported on\xA0every run and\xA0never fixed, though\xA0in\xA0either language on\xA0its own the\xA0parenthesis ends up exactly where it already stood. All four\xA0options of\xA0<code>function-parentheses-space-inside</code> reached it, and\xA0<code>never-multi-line</code> of\xA0<code>function-parentheses-newline-inside</code>. Both questions are put to\xA0one and\xA0the\xA0same reading now\xA0— the\xA0one the\xA0file&#39;s own language has, which\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/333" target="_blank" rel="noreferrer">#333</a> settled, both of\xA0them still being owed where the\xA0syntax has named neither\xA0— and\xA0the\xA0fix is held back only where a\xA0reading has the\xA0parenthesis outside a\xA0comment as\xA0the\xA0value stands and\xA0inside\xA0one once the\xA0fix has run. The\xA0two\xA0separators of\xA0Unicode, which\xA0the\xA0guard used to\xA0count as\xA0breaks for\xA0one of\xA0its two\xA0askings, are read by\xA0neither: a\xA0fix here writes over\xA0whitespace and\xA0the\xA0value parser counts as\xA0space only what stands below the\xA0blank, so\xA0a\xA0separator survives the\xA0fix and\xA0can never be what carries the\xA0parenthesis into\xA0a\xA0comment.</li><li>The\xA0<code>function-parentheses-newline-inside</code> rule now fixes the\xA0whitespace behind an\xA0opening parenthesis where an\xA0inline comment holds the\xA0first argument already (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/281" target="_blank" rel="noreferrer">#281</a>). The\xA0<code>never-multi-line</code> option guards that\xA0fix by\xA0asking whether such a\xA0comment would hold the\xA0argument once the\xA0whitespace is gone, and\xA0never asked whether one holds it as\xA0the\xA0value stands\xA0— the\xA0two\xA0questions the\xA0closing half of\xA0the\xA0same rule has put since\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/132" target="_blank" rel="noreferrer">#132</a>, of\xA0which\xA0the\xA0opening half put one. So\xA0<code>a { t: foo(1px // c) calc( 2px</code> followed by\xA0a\xA0line break and\xA0<code>); }</code> under\xA0<code>postcss-less</code>, where the\xA0comment runs to\xA0that\xA0break and\xA0holds <code>2px</code> inside\xA0it before\xA0the\xA0fix as\xA0after\xA0it, was reported on\xA0every run and\xA0never written. Both questions are put now, and\xA0both of\xA0them to\xA0the\xA0reading the\xA0file&#39;s own language has, which\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/333" target="_blank" rel="noreferrer">#333</a> settled, so\xA0the\xA0fix is held back only where that\xA0reading has the\xA0argument outside a\xA0comment as\xA0the\xA0value stands and\xA0inside\xA0one once the\xA0fix has run. The\xA0one question also read a\xA0text that\xA0was neither of\xA0the\xA0two\xA0wherever the\xA0fix empties whitespace on\xA0both sides of\xA0a\xA0block comment, and\xA0let a\xA0breaking fix through\xA0there: <code>a { t: foo(1px // c) calc(</code> followed by\xA0a\xA0line break, <code>/*b*/ 2px); }</code>, came back as\xA0<code>a { t: foo(1px // c) calc(/*b*/2px); }</code>\xA0— the\xA0break that\xA0closed the\xA0comment gone, and\xA0everything behind the\xA0double slash commented out. That\xA0one is reported and\xA0left alone now: emptying the\xA0whitespace there would satisfy the\xA0option only by\xA0commenting the\xA0rest of\xA0the\xA0value out.</li><li>The\xA0<code>function-parentheses-newline-inside</code> rule now fixes the\xA0whitespace in\xA0front of\xA0a\xA0closing parenthesis where a\xA0block comment standing there holds a\xA0line break (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/312" target="_blank" rel="noreferrer">#312</a>). The\xA0guard on\xA0that\xA0fix asks what the\xA0value would read as\xA0once the\xA0whitespace is gone, and\xA0it used to\xA0take out every character JavaScript calls whitespace between\xA0the\xA0last thing the\xA0call holds and\xA0the\xA0parenthesis\xA0— the\xA0breaks written inside\xA0a\xA0block comment&#39;s own text among them, which\xA0no fix goes near. Taking one of\xA0those out closes the\xA0comment early and\xA0leaves the\xA0rest of\xA0the\xA0value exposed to\xA0whatever stands in\xA0front of\xA0it, so\xA0<code>a { t: 1px //c</code> followed by\xA0a\xA0form feed, <code>f(/*x</code>, a\xA0line break, <code>y*/</code>, a\xA0line break and\xA0<code>); }</code> under\xA0<code>postcss-scss</code> was reported on\xA0every run and\xA0never written, though\xA0the\xA0parenthesis stands outside every comment under\xA0both readings of\xA0a\xA0line break, before\xA0the\xA0fix as\xA0after\xA0it. The\xA0guard takes out exactly the\xA0stretches the\xA0fix empties now.</li><li>The\xA0<code>function-comma-*</code> rules now write the\xA0whitespace standing beside a\xA0comma wherever the\xA0parser filed it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/349" target="_blank" rel="noreferrer">#349</a>). These four\xA0named that\xA0whitespace by\xA0the\xA0comma&#39;s own <code>before</code> and\xA0<code>after</code>, while\xA0<code>postcss-value-parser</code> hands a\xA0run to\xA0whichever neighbour will hold it\xA0— the\xA0run between\xA0two\xA0dividers goes to\xA0the\xA0<code>after</code> of\xA0the\xA0one in\xA0front, the\xA0run between\xA0the\xA0opening parenthesis and\xA0a\xA0comma that\xA0opens the\xA0arguments goes to\xA0the\xA0function&#39;s <code>before</code>, and\xA0the\xA0run between\xA0a\xA0comma that\xA0closes them and\xA0the\xA0closing parenthesis to\xA0the\xA0function&#39;s <code>after</code>. The\xA0first of\xA0those is no comma&#39;s alone: a\xA0slash and\xA0a\xA0colon carry the\xA0whitespace of\xA0the\xA0comma behind them as\xA0readily as\xA0another comma does. The\xA0span each fix wrote was therefore empty and\xA0stood beside that\xA0text rather than\xA0over\xA0it, and\xA0it went two\xA0ways. Under\xA0<code>never</code> and\xA0<code>never-single-line</code> nothing was written at\xA0all, so\xA0<code>a { b: f(a, ,c); }</code> was reported by\xA0<code>function-comma-space-before</code> at\xA01:13\xA0and\xA0came back from\xA0every run of\xA0<code>--fix</code> exactly as\xA0it went in, and\xA0<code>never-multi-line</code> went the\xA0same way wherever a\xA0break stood in\xA0that\xA0run instead. Under\xA0<code>always</code> and\xA0<code>always-single-line</code> the\xA0character was written at\xA0the\xA0comma, behind the\xA0run instead of\xA0over\xA0it: <code>a { b: f(a,\\t\\t,c); }</code> grew a\xA0space on\xA0every run under\xA0the\xA0two\xA0<code>*-space-*</code> rules and\xA0never satisfied the\xA0option it was asked for, while\xA0the\xA0two\xA0<code>*-newline-*</code> rules wrote their break beside the\xA0run and\xA0settled there, leaving whitespace their option tolerates rather than\xA0the\xA0whitespace the\xA0file was asked for. Both are fixed, and\xA0the\xA0four\xA0positions are read the\xA0same way, so\xA0a\xA0comma behind another comma, one behind a\xA0divider of\xA0any other kind, one opening the\xA0arguments and\xA0one closing them are all written like\xA0any comma between\xA0two\xA0words: <code>a { b: f(1 / ,2); }</code> under\xA0<code>never</code> now comes back as\xA0<code>a { b: f(1 /,2); }</code>, where it used to\xA0come back untouched. A\xA0file with\xA0no such comma in\xA0it is written exactly as\xA0before, and\xA0so\xA0is one where an\xA0end-of-line comment stands in\xA0front of\xA0that\xA0run, the\xA0break which\xA0closes the\xA0comment being the\xA0run itself: the\xA0guard that\xA0holds a\xA0fix back from\xA0writing inside\xA0such a\xA0comment is asked as\xA0it always was, and\xA0every warning it holds back still stands. The\xA0whitespace between\xA0an\xA0opening parenthesis and\xA0a\xA0comma is also what <code>function-parentheses-space-inside</code> writes, so\xA0asking that\xA0rule for\xA0a\xA0space there and\xA0one of\xA0these for\xA0none cannot be satisfied by\xA0any file: <code>a { b: f( ,a); }</code> settles with\xA0one of\xA0the\xA0two\xA0warnings standing, and\xA0which\xA0one depends on\xA0the\xA0order the\xA0rules are listed in, the\xA0one written last having the\xA0final say. That is the\xA0standing <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/166" target="_blank" rel="noreferrer">#166</a> leaves between\xA0<code>value-list-comma-space-before</code> and\xA0<code>declaration-colon-space-after</code>, order and\xA0all. The\xA0other way about\xA0is satisfiable, since\xA0a\xA0comma that\xA0opens the\xA0arguments is asked for\xA0nothing by\xA0<code>always</code>: <code>a { b: f(,a); }</code> answers both rules at\xA0once. Nothing runs away with\xA0either\xA0— a\xA0second <code>--fix</code> writes the\xA0same file as\xA0the\xA0first.</li><li>The\xA0<code>no-missing-end-of-source-newline</code> rule&#39;s fix now spells the\xA0break it writes the\xA0way the\xA0file spells its lines (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/282" target="_blank" rel="noreferrer">#282</a>). It wrote <code>context.newline</code>, which\xA0Stylelint reads out of\xA0the\xA0file as\xA0a\xA0line feed with\xA0an\xA0optional carriage return in\xA0front of\xA0it and\xA0falls back to\xA0the\xA0line ending of\xA0the\xA0machine for, so\xA0a\xA0file broken with\xA0bare carriage returns or\xA0with\xA0form feeds was closed with\xA0a\xA0character it holds nowhere else. The\xA0break is read off the\xA0stylesheet itself now, outside its comments and\xA0its strings, and\xA0<code>context.newline</code> is left for\xA0the\xA0one that\xA0ends no line at\xA0all\xA0— a\xA0stylesheet written on\xA0a\xA0single line, or\xA0one whose only breaks stand inside\xA0a\xA0comment or\xA0a\xA0string. <ul><li>The\xA0break read off a\xA0Less file may be a\xA0form feed standing inside\xA0a\xA0<code>//</code> comment or\xA0inside\xA0an\xA0unquoted address, which\xA0Less reads as\xA0the\xA0text it stands in\xA0and\xA0refuses to\xA0read anywhere else; closing the\xA0file with\xA0one leaves a\xA0file Less will not\xA0compile. The\xA0reading is older than\xA0this change and\xA0shared\xA0— <code>block-closing-brace-empty-line-before</code> spells its empty line with\xA0the\xA0same character on\xA0the\xA0same file\xA0— and\xA0this rule is the\xA0second place it is reached from\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/373" target="_blank" rel="noreferrer">#373</a>).</li></ul></li><li>The\xA0<code>declaration-bang-space-*</code> rules now write the\xA0whitespace standing beside a\xA0bang wherever the\xA0parser filed it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/289" target="_blank" rel="noreferrer">#289</a>). These two\xA0named that\xA0whitespace by\xA0the\xA0one text of\xA0the\xA0declaration the\xA0bang stands in, while\xA0PostCSS prints a\xA0declaration from\xA0four\xA0— the\xA0property, what stands between\xA0it and\xA0the\xA0value, the\xA0value itself, and\xA0the\xA0raw of\xA0the\xA0flag\xA0— and\xA0leaves the\xA0run beside a\xA0bang in\xA0whichever of\xA0the\xA0last three\xA0the\xA0shape of\xA0the\xA0value gives it to. The\xA0run in\xA0front of\xA0a\xA0bang reaches the\xA0flag&#39;s raw only where the\xA0value has a\xA0word of\xA0its own to\xA0leave behind: <code>a { b: /*c*/\\t!important; }</code> keeps it at\xA0the\xA0tail of\xA0the\xA0value, <code>a { b: !important; }</code> has nothing else in\xA0the\xA0value at\xA0all, and\xA0<code>a { b: /*c*/\\t!default; }</code>, whose flag PostCSS never parts from\xA0the\xA0value, keeps it at\xA0the\xA0tail of\xA0what stands between\xA0the\xA0property and\xA0the\xA0value. The\xA0run behind a\xA0bang goes the\xA0other way in\xA0<code>a { b: x! !important; }</code>, where the\xA0bang closes the\xA0value and\xA0the\xA0run opens the\xA0flag&#39;s raw. On\xA0all four\xA0the\xA0span each fix wrote stood beside the\xA0run rather than\xA0over\xA0it, and\xA0it went two\xA0ways. Under\xA0<code>always</code> the\xA0character was written against\xA0the\xA0bang and\xA0the\xA0run left standing, so\xA0<code>a { b: /*c*/\\t!important; }</code> grew a\xA0space on\xA0every run of\xA0<code>--fix</code> and\xA0never came to\xA0satisfy the\xA0option it was asked for. Under\xA0<code>never</code> nothing was written at\xA0all, so\xA0the\xA0same declaration came back from\xA0every run exactly as\xA0it went in. Both ran clean either way, since\xA0Stylelint counts a\xA0problem fixed once the\xA0fix callback has been called, whatever that\xA0callback wrote\xA0— the\xA0warning was there to\xA0be read only with\xA0<code>--fix</code> off. Both are fixed: the\xA0run is measured and\xA0written in\xA0the\xA0declaration as\xA0the\xA0file prints it, and\xA0an\xA0edit reaching across\xA0two\xA0of\xA0those texts empties the\xA0run in\xA0each of\xA0them and\xA0writes the\xA0space the\xA0option asks for\xA0against\xA0the\xA0bang. A\xA0declaration whose run the\xA0parser filed with\xA0its bang is written exactly as\xA0before, and\xA0so\xA0is one where an\xA0end-of-line comment stands in\xA0front of\xA0that\xA0run\xA0— the\xA0guard that\xA0holds <code>declaration-bang-space-before</code> back from\xA0writing inside\xA0such a\xA0comment is asked as\xA0it always was, and\xA0every warning it holds back still stands. Nothing runs away either way now: a\xA0second <code>--fix</code> writes the\xA0same file as\xA0the\xA0first.</li><li>The\xA0<code>unit-case</code> rule now underlines the\xA0unit a\xA0warning names rather than\xA0the\xA0word that\xA0unit stands in\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/296" target="_blank" rel="noreferrer">#296</a>). The\xA0unit is read out of\xA0a\xA0copy of\xA0the\xA0word with\xA0the\xA0characters an\xA0interpolation is spelled with\xA0taken out of\xA0it and\xA0the\xA0<code>\\0</code> and\xA0<code>\\9</code> hacks with\xA0them, while\xA0the\xA0warning ended where the\xA0word ends, so\xA0whatever those cuts took off was underlined along with\xA0the\xA0unit: under\xA0<code>upper</code>, <code>a { b: 10px\\0; }</code> was reported over\xA0<code>px\\0</code>. The\xA0parts of\xA0a\xA0multiplication were worse off. Each was handed a\xA0node built on\xA0the\xA0spot, opening at\xA0the\xA0part&#39;s place inside\xA0the\xA0word with\xA0the\xA0part&#39;s own length and\xA0one more added on\xA0top, and\xA0closing where the\xA0whole word closes, so\xA0the\xA0two\xA0positions described no text of\xA0the\xA0part at\xA0all: under\xA0<code>lower</code>, <code>a { b: 2*10PX; }</code> was reported past the\xA0end of\xA0its only line, and\xA0<code>a { b: 10px*2REM; }</code> over\xA0the\xA0closing brace of\xA0the\xA0block, its two\xA0positions running backwards until\xA0PostCSS clamped them to\xA0that\xA0one character. Both positions are counted off the\xA0unit inside\xA0the\xA0node now, and\xA0every part of\xA0a\xA0multiplication carries the\xA0positions the\xA0file gives it. Where a\xA0cut falls inside\xA0the\xA0unit rather than\xA0beside it, the\xA0run underlined is the\xA0text that\xA0reading was taken from: <code>10P\\9X</code>, whose unit is read as\xA0<code>PX</code>, is underlined from\xA0its <code>P</code> to\xA0its <code>X</code>, and\xA0<code>10px#fff</code>, whose unit is read as\xA0<code>pxfff</code>, from\xA0its <code>p</code> to\xA0the\xA0last <code>f</code>.</li><li>Every guard that\xA0asks whether a\xA0fix would write inside\xA0an\xA0inline comment now reads the\xA0line breaks the\xA0file&#39;s own language reads (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/333" target="_blank" rel="noreferrer">#333</a>). Sass ends such a\xA0comment on\xA0a\xA0form feed, on\xA0a\xA0line feed and\xA0on\xA0a\xA0carriage return alike; Less normalises the\xA0line endings of\xA0a\xA0file before\xA0parsing it and\xA0reads no line in\xA0a\xA0form feed at\xA0all. The\xA0scan behind every one of\xA0those guards closed a\xA0comment on\xA0a\xA0line feed and\xA0on\xA0a\xA0carriage return and\xA0on\xA0nothing else, which\xA0is Less&#39;s reading entire\xA0— and\xA0handed it to\xA0every caller whatever the\xA0file was written in, so\xA0that\xA0every stylesheet was read as\xA0a\xA0Less stylesheet. So\xA0under\xA0<code>postcss-scss</code> a\xA0fix standing behind a\xA0form feed was refused wherever Sass had already closed the\xA0comment on\xA0it: <code>a { b: 1px // c</code> and\xA0a\xA0form feed and\xA0a\xA0tab and\xA0<code>2px; }</code> under\xA0<code>declaration-block-semicolon-space-before</code> with\xA0<code>always</code> was reported on\xA0every run and\xA0never written, and\xA0the\xA0warning survived every run of\xA0<code>--fix</code> over\xA0a\xA0stylesheet no fix could damage. The\xA0syntax itself is asked now which\xA0breaks it reads a\xA0line in, by\xA0one parse of\xA0a\xA0probe kept against\xA0that\xA0syntax, and\xA0the\xA0answer travels with\xA0the\xA0question: <code>block-closing-brace-space-before</code>, <code>block-closing-brace-newline-before</code> and\xA0<code>declaration-block-semicolon-space-before</code> write behind such a\xA0form feed under\xA0Sass, and\xA0the\xA0two\xA0<code>function-parentheses-*-inside</code> rules, which\xA0used to\xA0put every question under\xA0both readings whatever the\xA0file was written in, put it under\xA0the\xA0one the\xA0file has. Under\xA0<code>postcss-less</code> every one of\xA0those fixes is refused exactly as\xA0before, the\xA0comment running past the\xA0form feed to\xA0the\xA0end of\xA0its line, and\xA0not\xA0a\xA0byte of\xA0any Less file changes. Where the\xA0syntax has said nothing about\xA0the\xA0character\xA0— a\xA0host language that\xA0finds no stylesheet in\xA0the\xA0probe, one that\xA0cannot parse it at\xA0all\xA0— both readings are owed rather than\xA0Less&#39;s alone, and\xA0a\xA0fix that\xA0either of\xA0them would carry into\xA0a\xA0comment is held back: for\xA0the\xA0two\xA0<code>function-parentheses-*-inside</code> rules that is what they already did, and\xA0for\xA0the\xA0other nine\xA0callers of\xA0the\xA0guard it is a\xA0refusal they did not\xA0use to\xA0make.</li><li>The\xA0<code>at-rule-semicolon-*</code>, <code>block-closing-brace-*</code>, <code>block-opening-brace-*</code>, <code>declaration-block-semicolon-*</code>, <code>declaration-block-trailing-semicolon</code>, <code>indentation</code> and\xA0<code>no-extra-semicolons</code> rules now measure a\xA0node in\xA0the\xA0copy of\xA0it the\xA0file spells (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/139" target="_blank" rel="noreferrer">#139</a>). These rules find the\xA0brace, the\xA0semicolon or\xA0the\xA0indent they report by\xA0the\xA0length of\xA0what a\xA0node prints, and\xA0<code>Node.toString()</code> goes through\xA0PostCSS&#39;s own stringifier rather than\xA0the\xA0one the\xA0file was opened with. The\xA0two\xA0part company in\xA0five\xA0places: <code>postcss-scss</code> keeps a\xA0second copy of\xA0a\xA0value, of\xA0a\xA0set of\xA0parameters and\xA0of\xA0a\xA0selector, and\xA0PostCSS prints the\xA0copy whose <code>//</code> comments the\xA0syntax has rewritten into\xA0block comments, two\xA0characters wider for\xA0each of\xA0them; both custom syntaxes print a\xA0comment opened by\xA0a\xA0double slash as\xA0a\xA0block comment, two\xA0characters wider again; <code>postcss-less</code> holds a\xA0mixin call as\xA0an\xA0at-rule whose leading dot stands in\xA0a\xA0raw; the\xA0<code>!important</code> of\xA0such a\xA0call PostCSS does not\xA0print at\xA0all; and\xA0a\xA0Sass nested property is a\xA0declaration carrying a\xA0block, which\xA0<code>postcss-scss</code> prints and\xA0PostCSS&#39;s own stringifier drops. The\xA0index a\xA0warning carries is counted in\xA0the\xA0text of\xA0the\xA0file, so\xA0a\xA0position measured in\xA0that\xA0printed copy stood as\xA0far past its mark as\xA0the\xA0two\xA0copies differ, and\xA0where those characters ran past the\xA0end of\xA0a\xA0line the\xA0warning was reported on\xA0a\xA0line the\xA0file does not\xA0spell it on. Where an\xA0option turns on\xA0a\xA0block being single-line or\xA0multi-line, the\xA0same characters changed which\xA0of\xA0the\xA0two\xA0the\xA0block was taken for\xA0rather than\xA0the\xA0column it was reported at. A\xA0media query whose feature carries such a\xA0comment and\xA0whose block is written on\xA0one line was measured as\xA0multi-line, so\xA0every option about\xA0a\xA0multi-line block was put to\xA0it and\xA0every option about\xA0a\xA0single-line one passed it over. Over\xA0that\xA0one file and\xA0every primary option of\xA0the\xA0nine\xA0<code>block-*</code> rules, eleven\xA0warnings go and\xA0six\xA0arrive. Every one of\xA0these positions is measured in\xA0the\xA0node as\xA0the\xA0file spells it now, and\xA0the\xA0block a\xA0rule asks about\xA0is the\xA0block the\xA0file spells. Where a\xA0rule counts on\xA0past the\xA0end of\xA0a\xA0node\xA0— the\xA0two\xA0<code>declaration-block-semicolon-*-after</code> rules step over\xA0the\xA0semicolon that\xA0way\xA0— it still counts a\xA0character at\xA0a\xA0time, so\xA0a\xA0semicolon the\xA0file does not\xA0write against\xA0the\xA0node is reached only where nothing stands in\xA0between. The\xA0Less half of\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/374" target="_blank" rel="noreferrer">#374</a> goes with\xA0it, a\xA0block ending in\xA0a\xA0mixin call no\xA0longer being measured as\xA0though\xA0the\xA0call were an\xA0at-rule with\xA0no flag behind it, while\xA0the\xA0run-away rewriting that\xA0issue is mostly about\xA0is untouched and\xA0stays open.</li><li>The\xA0<code>function-parentheses-space-inside</code>, <code>function-parentheses-newline-inside</code> and\xA0<code>media-feature-parentheses-space-inside</code> rules now report the\xA0whitespace in\xA0front of\xA0a\xA0closing parenthesis at\xA0the\xA0character in\xA0front of\xA0that\xA0parenthesis whatever comment the\xA0call or\xA0the\xA0feature holds (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/506" target="_blank" rel="noreferrer">#506</a>). The\xA0position was counted from\xA0the\xA0length of\xA0the\xA0call or\xA0the\xA0feature as\xA0<code>postcss-value-parser</code> prints it back, and\xA0the\xA0printer gives a\xA0comment opening <code>/*/</code> back as\xA0<code>/**/</code>, a\xA0character wider than\xA0the\xA0file spells it, so\xA0the\xA0closing warning over\xA0<code>a { b: f(2 /*/ c */ ); }</code> came out a\xA0column further on\xA0than\xA0the\xA0one over\xA0<code>a { b: f(2 /** c */ ); }</code>\xA0— on\xA0the\xA0parenthesis itself, and\xA0at\xA0the\xA0head of\xA0the\xA0next line where a\xA0line break stood in\xA0front of\xA0the\xA0parenthesis. The\xA0fix landed where it should either way. The\xA0position is now counted from\xA0where the\xA0parser marks the\xA0end of\xA0the\xA0call or\xA0the\xA0feature in\xA0the\xA0file, which\xA0is where the\xA0fix writes too. A\xA0media feature whose parenthesis the\xA0file never closes is reported under\xA0<code>always</code> at\xA0the\xA0end of\xA0the\xA0parameters, and\xA0that\xA0warning moves one character on\xA0with\xA0this, to\xA0the\xA0last character of\xA0the\xA0parameters.</li><li>The\xA0<code>declaration-colon-space-after</code> rule now writes what its option asks for\xA0in\xA0front of\xA0a\xA0block comment that\xA0stands on\xA0a\xA0line of\xA0its own behind the\xA0colon where <code>declaration-colon-newline-after</code> is configured beside it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/590" target="_blank" rel="noreferrer">#590</a>). The\xA0space <code>always</code> writes over\xA0the\xA0break, or\xA0the\xA0nothing <code>never</code> leaves, puts the\xA0comment on\xA0the\xA0colon&#39;s line, and\xA0there the\xA0newline rule asks about\xA0the\xA0run behind the\xA0comment rather than\xA0the\xA0run behind the\xA0colon, as\xA0it does for\xA0any comment standing on\xA0that\xA0line. The\xA0two\xA0rules settle who writes the\xA0run behind the\xA0colon by\xA0asking what the\xA0other reads, and\xA0the\xA0question was put about\xA0the\xA0run as\xA0it stood rather than\xA0as\xA0the\xA0write leaves it: the\xA0newline rule read the\xA0break and\xA0would not\xA0take a\xA0space for\xA0it, the\xA0pair was called a\xA0contradiction, and\xA0over\xA0<code>a { b:\\n/*c*/\\nx; }</code> the\xA0space rule reported <code>Expected single space after &quot;:&quot;</code> on\xA0every run of\xA0<code>--fix</code> and\xA0wrote nothing, whichever order the\xA0configuration listed the\xA0two\xA0in. The\xA0question is asked of\xA0the\xA0run as\xA0the\xA0write leaves it now, so\xA0<code>--fix</code> writes <code>a { b: /*c*/\\nx; }</code>, which\xA0answers both rules, and\xA0nothing is reported afterwards.</li><li>The\xA0<code>indentation</code> rule now reads the\xA0base level of\xA0a\xA0stylesheet embedded in\xA0a\xA0page off the\xA0lines standing at\xA0that\xA0level\xA0— the\xA0lines its statements open on\xA0and\xA0the\xA0lines their blocks close on\xA0— and, where the\xA0stylesheet has no such line of\xA0its own, off the\xA0line of\xA0the\xA0tag it opens on\xA0(see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/594" target="_blank" rel="noreferrer">#594</a>). It used to\xA0read that\xA0level off the\xA0shortest indentation of\xA0every line but\xA0the\xA0first, the\xA0first standing on\xA0the\xA0tag&#39;s line, so\xA0a\xA0stylesheet opening on\xA0that\xA0line with\xA0nothing of\xA0its own at\xA0the\xA0root&#39;s level\xA0— a\xA0<code>style</code> attribute closing on\xA0<code>@import url(</code> with\xA0<code>&#39;x&#39;)</code> on\xA0the\xA0next line, or\xA0<code>&lt;style&gt;a {</code> with\xA0<code>b: c; }</code> on\xA0the\xA0next\xA0— was read off the\xA0very lines the\xA0rule writes a\xA0level deeper: under\xA0<code>&quot;tab&quot;</code> the\xA0continuation line was asked for\xA0one tab, given it, and\xA0reported again for\xA0two\xA0on\xA0the\xA0next run, growing a\xA0tab per\xA0run without\xA0end. It is asked for\xA0one tab now, the\xA0line of\xA0the\xA0tag standing at\xA0none, and\xA0settles there in\xA0one run; on\xA0an\xA0indented tag it is asked for\xA0the\xA0tag&#39;s level and\xA0one more. The\xA0closing brace&#39;s line stays out of\xA0the\xA0reading under\xA0<code>indentClosingBrace: true</code>, where it stands a\xA0level deeper by\xA0the\xA0rule&#39;s own measure and\xA0carried the\xA0same growth.</li><li>The\xA0<code>at-rule-semicolon-space-before</code> rule now reports its warning on\xA0the\xA0character standing in\xA0front of\xA0the\xA0semicolon (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/545" target="_blank" rel="noreferrer">#545</a>). The\xA0position was measured over\xA0the\xA0at-rule&#39;s text with\xA0the\xA0whitespace standing in\xA0front of\xA0the\xA0at-rule added to\xA0it, while\xA0the\xA0index a\xA0warning carries is counted from\xA0the\xA0node&#39;s own start, so\xA0the\xA0mark stood that\xA0many characters to\xA0the\xA0right of\xA0it\xA0— past the\xA0end of\xA0the\xA0line, or\xA0on\xA0a\xA0line below it, wherever that\xA0whitespace is wide enough. Only an\xA0at-rule with\xA0no whitespace in\xA0front of\xA0it was reported where it belongs. The\xA0rule writes no fix, so\xA0no file was ever left changed by\xA0the\xA0miscount.</li></ul></li><li><p><code>--fix</code> now comes to\xA0rest where two\xA0rules are asked about\xA0one and\xA0the\xA0same run of\xA0whitespace and\xA0no file can answer both\xA0— the\xA0file it rests on\xA0is what the\xA0rules listed last among those asked about\xA0the\xA0run write, the\xA0rule the\xA0configuration contradicted reports the\xA0run and\xA0leaves it alone, and\xA0running the\xA0fixer again changes nothing:</p><ul><li>The\xA0<code>declaration-colon-newline-after</code> rule now writes the\xA0break its <code>always-multi-line</code> option asks for\xA0behind a\xA0comment standing on\xA0the\xA0colon&#39;s line of\xA0a\xA0custom property where <code>declaration-block-semicolon-space-before: never</code> is configured beside it and\xA0has reported the\xA0space in\xA0front of\xA0the\xA0semicolon (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/627" target="_blank" rel="noreferrer">#627</a>). The\xA0two\xA0ask for\xA0different things of\xA0one run, so\xA0one of\xA0them reports it and\xA0the\xA0other writes; the\xA0semicolon rule runs first and\xA0reports, and\xA0the\xA0newline rule, whose check waits for\xA0the\xA0run&#39;s end, writes only where every rule ahead of\xA0it accepts what the\xA0write leaves or\xA0has warned already. The\xA0model read the\xA0semicolon rule as\xA0leaving a\xA0single space alone on\xA0every custom property, where the\xA0rule leaves one alone only as\xA0the\xA0whole of\xA0what stands behind the\xA0colon, so\xA0over\xA0<code>a { --b: /*c\\n*/ ; }</code> it counted the\xA0space the\xA0semicolon rule had just reported as\xA0one the\xA0rule was content with, and\xA0held the\xA0newline rule: both rules reported on\xA0every run of\xA0<code>--fix</code> and\xA0nothing was written, in\xA0either order. Now <code>--fix</code> writes the\xA0break in\xA0front of\xA0that\xA0space, <code>a { --b: /*c\\n*/\\n ; }</code>, and\xA0the\xA0semicolon rule&#39;s warning stands over\xA0the\xA0run, as\xA0the\xA0configuration asked; <code>declaration-block-semicolon-newline-before: always-multi-line</code> beside the\xA0same <code>never</code> was held the\xA0same way over\xA0the\xA0same declaration, and\xA0writes its break now. The\xA0same reading held the\xA0other way round for\xA0<code>declaration-block-semicolon-space-before: always</code> beside <code>declaration-block-semicolon-newline-before: never-multi-line</code> over\xA0a\xA0comment on\xA0the\xA0colon&#39;s line in\xA0a\xA0block over\xA0several lines, <code>a { b: /*c\\n*/; }</code>: the\xA0model read the\xA0newline rule as\xA0content with\xA0a\xA0single space there, so\xA0the\xA0space rule wrote one and\xA0the\xA0newline rule reported it, where the\xA0rule taking its turn last is the\xA0one whose write the\xA0file rests on\xA0— now the\xA0space rule reports and\xA0the\xA0semicolon keeps nothing in\xA0front of\xA0it. And\xA0a\xA0run of\xA0two\xA0spaces or\xA0a\xA0tab was read as\xA0the\xA0single space an\xA0<code>always</code> option accepts, so\xA0a\xA0rule ahead that\xA0had reported such a\xA0run was counted as\xA0content with\xA0it, and\xA0the\xA0deferred rule behind it was held: <code>a { b: x\\n y; }</code> under\xA0<code>declaration-colon-space-after: always</code> beside <code>declaration-colon-newline-after: always-multi-line</code> drew both warnings on\xA0every run of\xA0<code>--fix</code> with\xA0nothing written, and\xA0so\xA0did <code>a {\\n\\t--b:\\t;\\n}</code> under\xA0<code>declaration-block-semicolon-space-before: never</code> beside <code>declaration-block-semicolon-newline-before: always-multi-line</code>. Such a\xA0run is read now as\xA0what it is, a\xA0spelling no option takes, so\xA0the\xA0rule ahead has warned of\xA0it and\xA0the\xA0rule behind writes its break, the\xA0warning of\xA0the\xA0rule ahead standing over\xA0the\xA0run as\xA0the\xA0configuration asked.</li><li>A\xA0rule asked about\xA0a\xA0shared run no\xA0longer defers to\xA0a\xA0neighbour whose fix the\xA0configuration turned off (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/485" target="_blank" rel="noreferrer">#485</a>). <code>disableFix</code> asks a\xA0rule to\xA0report and\xA0not\xA0to\xA0touch, and\xA0such a\xA0rule rewrites nothing\xA0— but\xA0the\xA0reader of\xA0a\xA0neighbour&#39;s setting handed over\xA0the\xA0option alone, so\xA0under\xA0<code>declaration-colon-space-after: always</code> beside <code>declaration-block-semicolon-space-before: [&quot;never&quot;, { &quot;disableFix&quot;: true }]</code> the\xA0space rule left <code>a { color:\\n; }</code> unwritten and\xA0both rules reported on\xA0every run of\xA0<code>--fix</code>, where the\xA0other order wrote <code>a { color: ; }</code> and\xA0rested with\xA0the\xA0one report asked for. Whether a\xA0fix is turned off now travels with\xA0the\xA0option: a\xA0rule writes past such a\xA0neighbour, which\xA0reports the\xA0outcome as\xA0told, and\xA0the\xA0writer of\xA0a\xA0trailing semicolon spells it the\xA0way the\xA0last-listed neighbour with\xA0its fix turned on\xA0asks\xA0— honouring a\xA0turned-off neighbour&#39;s ask only where no live one speaks, there being nothing else to\xA0spell it by.</li><li>The\xA0two\xA0<code>declaration-colon-*-after</code> rules now settle between\xA0them who writes the\xA0run behind the\xA0colon of\xA0every declaration\xA0— one carrying a\xA0word, a\xA0flag or\xA0an\xA0inline comment as\xA0much as\xA0one whose value is nothing but\xA0whitespace (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/484" target="_blank" rel="noreferrer">#484</a>). They read one and\xA0the\xA0same run, and\xA0each used to\xA0write blind to\xA0the\xA0other: over\xA0<code>a { color: !important ; }</code>, whose run the\xA0parser files in\xA0the\xA0value while\xA0<code>raws.between</code> ends on\xA0the\xA0bare colon, <code>declaration-colon-newline-after: always</code> listed ahead of\xA0<code>declaration-colon-space-after: always</code> grew the\xA0file by\xA0a\xA0space on\xA0every run of\xA0<code>--fix</code>, reporting nothing\xA0— the\xA0space rule&#39;s fix collapsed the\xA0raw and\xA0never reached the\xA0value&#39;s head\xA0— and\xA0an\xA0inline comment on\xA0the\xA0colon&#39;s line under\xA0the\xA0SCSS namespace went the\xA0same way, while\xA0under\xA0the\xA0Less one, whose parser files the\xA0head run in\xA0the\xA0raw, the\xA0pair rested with\xA0both warnings eaten. The\xA0rule listed first now reports the\xA0run and\xA0leaves it alone wherever the\xA0rule behind is asked for\xA0something else, so\xA0the\xA0file rests in\xA0one run on\xA0what the\xA0last-listed rule writes and\xA0the\xA0warning of\xA0the\xA0rule the\xA0configuration contradicted stands, on\xA0every declaration alike.</li><li>The\xA0<code>declaration-colon-space-after</code> and\xA0<code>declaration-colon-newline-after</code> rules, and\xA0the\xA0<code>declaration-block-semicolon-space-before</code> and\xA0<code>declaration-block-semicolon-newline-before</code> rules, now settle between\xA0them who writes the\xA0whitespace of\xA0a\xA0declaration whose value is nothing but\xA0that\xA0whitespace (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/416" target="_blank" rel="noreferrer">#416</a>). In\xA0<code>a { color: ; }</code> the\xA0run behind the\xA0colon is the\xA0run in\xA0front of\xA0the\xA0semicolon, and\xA0one character cannot answer to\xA0two\xA0options; the\xA0colon rules write into\xA0one place and\xA0the\xA0semicolon rules read another, so\xA0a\xA0pair such as\xA0<code>declaration-colon-space-after: always</code> beside <code>declaration-block-semicolon-space-before: never</code> used to\xA0take the\xA0run in\xA0turns\xA0— <code>a { color:; }</code> on\xA0one run of\xA0<code>--fix</code>, <code>a { color: ; }</code> on\xA0the\xA0next, and\xA0so\xA0on\xA0for\xA0ever, each run writing a\xA0diff and\xA0each leaving one warning. Each of\xA0the\xA0four\xA0rules now reads the\xA0settings of\xA0the\xA0other three\xA0out of\xA0the\xA0configuration, wherever it lists them, and\xA0writes the\xA0run only where every rule listed behind it that\xA0speaks of\xA0the\xA0declaration is content with\xA0a\xA0spelling of\xA0the\xA0run it is content with\xA0too, or\xA0would silence it with\xA0what it writes; otherwise it reports the\xA0run without\xA0a\xA0fix, so\xA0the\xA0file rests on\xA0what the\xA0rules behind it write and\xA0the\xA0warning of\xA0the\xA0rule the\xA0configuration contradicted stands where it was hidden before. A\xA0pair asking for\xA0the\xA0same thing\xA0— a\xA0single space from\xA0either end, a\xA0line break from\xA0either end\xA0— writes as\xA0it always did, and\xA0so\xA0does every declaration whose value has a\xA0word of\xA0its own. The\xA0two\xA0colon rules listed against\xA0each other are asked about\xA0one and\xA0the\xA0same run from\xA0the\xA0same end, and\xA0what is left of\xA0that\xA0pair\xA0— a\xA0value carrying a\xA0flag, a\xA0comment holding a\xA0colon\xA0— is <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/484" target="_blank" rel="noreferrer">#484</a> and\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/388" target="_blank" rel="noreferrer">#388</a> rather than\xA0this change&#39;s.</li></ul></li><li><p>One run of\xA0<code>--fix</code> now does the\xA0work that\xA0run&#39;s own writing uncovers, instead of\xA0leaving it for\xA0the\xA0next one:</p><ul><li>The\xA0<code>function-parentheses-newline-inside</code> rule now reads a\xA0value against\xA0the\xA0inline comments it holds once the\xA0fixes collected so\xA0far are written into\xA0it (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/288" target="_blank" rel="noreferrer">#288</a>). The\xA0spans such comments occupy were found once per\xA0declaration, before\xA0the\xA0value was walked, and\xA0a\xA0call opening inside\xA0one of\xA0them is passed over\xA0— its parentheses are the\xA0text of\xA0a\xA0comment rather than\xA0parentheses of\xA0the\xA0value. The\xA0<code>always</code> and\xA0<code>always-multi-line</code> options write line breaks, though, and\xA0a\xA0line break is what closes a\xA0comment opened by\xA0a\xA0double slash. So\xA0in\xA0<code>a { t: foo(1px // c) calc(/*b*/</code>, a\xA0line break, <code> ,2px</code>, a\xA0line break, <code>); }</code>, where the\xA0comment runs from\xA0the\xA0double slash past <code>calc(</code> to\xA0the\xA0first break of\xA0the\xA0value, the\xA0break the\xA0fix writes in\xA0front of\xA0the\xA0closing parenthesis of\xA0<code>foo</code> closes that\xA0comment early and\xA0leaves <code>calc(</code> standing in\xA0code with\xA0its own break still missing. The\xA0rule went on\xA0reading the\xA0value against\xA0spans it no\xA0longer had, said nothing about\xA0<code>calc(</code>, and\xA0handed back a\xA0file one break short of\xA0what the\xA0option asks for; a\xA0second run of\xA0<code>--fix</code> wrote that\xA0break and\xA0a\xA0third settled, so\xA0nothing ran away. The\xA0spans are found again now, wherever a\xA0fix has been written and\xA0before\xA0the\xA0next call is read, and\xA0the\xA0one run writes both breaks. A\xA0run without\xA0<code>--fix</code> is unchanged, and\xA0writes nothing: as\xA0the\xA0file stands, <code>calc(</code> is the\xA0text of\xA0a\xA0comment.</li><li>The\xA0<code>named-grid-areas-alignment</code> rule now asks whether a\xA0declaration spans lines of\xA0the\xA0text its fix will leave rather than\xA0of\xA0the\xA0text it was handed (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/402" target="_blank" rel="noreferrer">#402</a>). That\xA0answer decides whether the\xA0cells of\xA0a\xA0row are padded out to\xA0the\xA0width of\xA0the\xA0column they stand in, and\xA0it used to\xA0count every line break of\xA0the\xA0value, a\xA0break standing inside\xA0a\xA0row among them\xA0— while\xA0every fix this rule makes to\xA0a\xA0row collapses the\xA0whitespace inside\xA0it, breaks included. So\xA0a\xA0value whose only break stood inside\xA0a\xA0row had its cells padded by\xA0the\xA0very run that\xA0was taking that\xA0break out of\xA0it: <code>a { grid-template-areas: &quot;a</code>, a\xA0break and\xA0two\xA0tabs, <code>a&quot; &quot;bb bb&quot;; }</code> came back from\xA0<code>--fix</code> as\xA0<code>a { grid-template-areas: &quot;a a&quot; &quot;bb bb&quot;; }</code>, and\xA0the\xA0next run, reading a\xA0declaration that\xA0no\xA0longer spans lines, took the\xA0padding away again and\xA0left <code>&quot;a a&quot; &quot;bb bb&quot;</code>. A\xA0neighbouring row was written twice over\xA0the\xA0same way\xA0— <code>&quot;aaa</code>, a\xA0break, <code>aaa&quot; &quot;b b&quot;</code> came back with\xA0its second row widened to\xA0<code>&quot;b b&quot;</code> before\xA0the\xA0next run narrowed it again\xA0— and\xA0under\xA0<code>alignQuotes</code> a\xA0row holding nothing but\xA0a\xA0break came back as\xA0five\xA0spaces between\xA0its quotation marks before\xA0the\xA0next run emptied it. Plain CSS reaches this, with\xA0no comment and\xA0no custom syntax needed. A\xA0break standing inside\xA0a\xA0row is a\xA0character the\xA0fix is about\xA0to\xA0write over\xA0now, and\xA0only a\xA0break standing outside every row makes a\xA0declaration one that\xA0spans lines\xA0— everything of\xA0a\xA0value that is no row is handed back character for\xA0character, wherever it stands and\xA0whatever it is: the\xA0whitespace in\xA0front of\xA0the\xA0first row, between\xA0two\xA0of\xA0them or\xA0behind the\xA0last, a\xA0comment, a\xA0call, a\xA0word carrying an\xA0escaped break. So\xA0the\xA0first run writes what the\xA0second used to, and\xA0what a\xA0warning says of\xA0a\xA0value is true of\xA0the\xA0file the\xA0run that\xA0raised it leaves behind.</li></ul></li><li><p>A\xA0walk over\xA0a\xA0run of\xA0comments now costs a\xA0step per\xA0comment rather than\xA0a\xA0stack frame:</p><ul><li>The\xA0<code>at-rule-semicolon-newline-after</code>, <code>declaration-block-semicolon-newline-after</code> and\xA0<code>declaration-block-trailing-semicolon</code> rules now read a\xA0stylesheet holding a\xA0long run of\xA0comments (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/360" target="_blank" rel="noreferrer">#360</a>) (<a href="https://github.com/chiliec" target="_blank" rel="noreferrer">@chiliec</a>). All three\xA0step over\xA0such a\xA0run through\xA0one walk\xA0— from\xA0a\xA0node&#39;s own sibling in\xA0the\xA0first two, from\xA0the\xA0front of\xA0a\xA0block in\xA0the\xA0third\xA0— and\xA0that\xA0walk called itself once per\xA0comment, so\xA0a\xA0long enough run ended the\xA0whole run in\xA0<code>RangeError: Maximum call stack size exceeded</code> and\xA0no file was reported on\xA0at\xA0all; around eleven\xA0thousand comments is the\xA0threshold on\xA0Node\xA026, and\xA0twenty thousand throws every time. A\xA0comment costs that\xA0walk a\xA0step now rather than\xA0a\xA0stack frame, so\xA0a\xA0stylesheet is read whatever run of\xA0comments it holds. Every answer the\xA0walk gives is the\xA0answer it gave before, so\xA0nothing else about\xA0the\xA0three\xA0rules moves. The\xA0same walk had been copied into\xA0<code>block-opening-brace-newline-after</code>, which\xA0had a\xA0write of\xA0its own to\xA0make and\xA0reached this one through\xA0no caller; that\xA0copy is folded back into\xA0this walk below.</li><li>The\xA0<code>block-opening-brace-newline-after</code> rule now reads a\xA0stylesheet holding a\xA0long run of\xA0comments at\xA0the\xA0head of\xA0a\xA0block (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/409" target="_blank" rel="noreferrer">#409</a>). It kept a\xA0copy of\xA0that\xA0walk rather than\xA0calling it, because\xA0it has a\xA0write of\xA0its own to\xA0make on\xA0every comment it steps over: the\xA0line break standing in\xA0front of\xA0a\xA0comment is carried onto\xA0the\xA0node behind it, so\xA0that\xA0a\xA0comment at\xA0the\xA0head of\xA0a\xA0block may hold the\xA0break the\xA0option asks for. The\xA0copy went on\xA0calling itself once per\xA0comment after\xA0the\xA0walk it was copied from\xA0stopped doing so, and\xA0it fell sooner than\xA0that\xA0walk did\xA0— eight\xA0thousand comments throws every time on\xA0Node\xA026, where the\xA0walk above took around eleven\xA0thousand. The\xA0copy is gone: the\xA0walk above takes that\xA0write as\xA0a\xA0callback of\xA0its own, so\xA0one walk steps over\xA0a\xA0run of\xA0comments for\xA0every rule that\xA0asks, and\xA0the\xA0next fix to\xA0it reaches all four\xA0at\xA0once. Every answer the\xA0rule gives is the\xA0answer it gave before, measured over\xA075\xA0456\xA0runs on\xA0either side of\xA0the\xA0change\xA0— runs of\xA0up to\xA0three\xA0comments, block and\xA0inline, under\xA0up to\xA0seven\xA0spellings of\xA0the\xA0whitespace standing around them, in\xA0a\xA0rule and\xA0in\xA0a\xA0nested at-rule, in\xA0a\xA0block ending in\xA0a\xA0declaration, in\xA0a\xA0comment and\xA0in\xA0nothing at\xA0all, under\xA0all three\xA0options, three\xA0syntaxes and\xA0both settings of\xA0<code>--fix</code>\xA0— byte-identical throughout. A\xA0block holding nothing but\xA0comments still comes out of\xA0<code>--fix</code> rewritten and\xA0reported on\xA0not\xA0at\xA0all, which\xA0is older than\xA0this change and\xA0filed as\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/410" target="_blank" rel="noreferrer">#410</a>.</li></ul></li><li><p><code>--fix</code> no\xA0longer writes into\xA0a\xA0file the\xA0rule reports nothing about:</p><ul><li>The\xA0<code>block-opening-brace-newline-after</code> rule no\xA0longer breaks the\xA0lines of\xA0a\xA0block holding nothing but\xA0comments (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/410" target="_blank" rel="noreferrer">#410</a>). The\xA0rule carries the\xA0line break standing in\xA0front of\xA0a\xA0comment onto\xA0the\xA0node behind it, so\xA0that\xA0a\xA0comment at\xA0the\xA0head of\xA0a\xA0block may hold the\xA0break the\xA0option asks for, and\xA0puts that\xA0whitespace back once the\xA0check is done; a\xA0block whose comments stand in\xA0front of\xA0no node at\xA0all ended the\xA0check before\xA0the\xA0whitespace was put back, so\xA0<code>a {</code>, a\xA0break and\xA0<code>/*1*/ /*2*/ /*3*/}</code> came back from\xA0<code>--fix</code> with\xA0every comment on\xA0a\xA0line of\xA0its own and\xA0no warning to\xA0explain the\xA0rewrite. Such a\xA0block comes out of\xA0<code>--fix</code> as\xA0it went in\xA0now, under\xA0every option.</li></ul></li></ul><h2 id="_5-3-0-—-2026–08–09" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v5.2.1...v5.3.0" target="_blank" rel="noreferrer">5.3.0</a>\xA0— 2026–08–09 <a class="header-anchor" href="#_5-3-0-—-2026–08–09" aria-label="Permalink to “5.3.0\xA0— 2026–08–09”">​</a></h2><h3 id="added-1" tabindex="-1">Added <a class="header-anchor" href="#added-1" aria-label="Permalink to “Added”">​</a></h3><ul><li>The\xA0<code>function-comma-newline-after</code> rule now has an\xA0additional <code>ignoreFunctions</code> option (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/78" target="_blank" rel="noreferrer">#78</a>).</li><li>The\xA0<code>function-comma-newline-before</code> rule now has an\xA0additional <code>ignoreFunctions</code> option (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/81" target="_blank" rel="noreferrer">#81</a>).</li><li>The\xA0<code>function-comma-space-after</code> rule now has an\xA0additional <code>ignoreFunctions</code> option (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/82" target="_blank" rel="noreferrer">#82</a>).</li><li>The\xA0<code>function-comma-space-before</code> rule now has an\xA0additional <code>ignoreFunctions</code> option (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/83" target="_blank" rel="noreferrer">#83</a>).</li></ul><h2 id="_5-2-1-—-2026–07–01" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v5.2.0...v5.2.1" target="_blank" rel="noreferrer">5.2.1</a>\xA0— 2026–07–01 <a class="header-anchor" href="#_5-2-1-—-2026–07–01" aria-label="Permalink to “5.2.1\xA0— 2026–07–01”">​</a></h2><h3 id="fixed-1" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-1" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The\xA0protocol in\xA0the\xA0repository&#39;s metadata URL now meets current requirements (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/pull/79" target="_blank" rel="noreferrer">#79</a>) (<a href="https://github.com/BowenMilner" target="_blank" rel="noreferrer">@BowenMilner</a>).</li></ul><h2 id="_5-2-0-—-2026–05–20" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v5.1.0...v5.2.0" target="_blank" rel="noreferrer">5.2.0</a>\xA0— 2026–05–20 <a class="header-anchor" href="#_5-2-0-—-2026–05–20" aria-label="Permalink to “5.2.0\xA0— 2026–05–20”">​</a></h2><h3 id="added-2" tabindex="-1">Added <a class="header-anchor" href="#added-2" aria-label="Permalink to “Added”">​</a></h3><ul><li>The\xA0<code>declaration-block-semicolon-newline-before</code> rule is now autofixable.</li></ul><h3 id="fixed-2" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-2" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>An\xA0exception for\xA0an\xA0empty custom property value has been added to\xA0the\xA0<code>declaration-block-semicolon-newline-before</code> and\xA0<code>declaration-colon-space-after</code> rules: the\xA0<code>--custom-prop: ;</code> and\xA0<code>--custom-prop:;</code> variants are now considered valid (see <a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/50" target="_blank" rel="noreferrer">#50</a>).</li></ul><h2 id="_5-1-0-—-2026–03–28" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v5.0.1...v5.1.0" target="_blank" rel="noreferrer">5.1.0</a>\xA0— 2026–03–28 <a class="header-anchor" href="#_5-1-0-—-2026–03–28" aria-label="Permalink to “5.1.0\xA0— 2026–03–28”">​</a></h2><h3 id="added-3" tabindex="-1">Added <a class="header-anchor" href="#added-3" aria-label="Permalink to “Added”">​</a></h3><ul><li>The\xA0<code>no-multiple-whitespaces</code> rule, which\xA0disallows multiple whitespaces between\xA0property values and\xA0function arguments.</li></ul><h3 id="fixed-3" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-3" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The\xA0dependencies have now been updated to\xA0versions that\xA0include security fixes.</li></ul><h2 id="_5-0-1-—-2026–01–22" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v5.0.0...v5.0.1" target="_blank" rel="noreferrer">5.0.1</a>\xA0— 2026–01–22 <a class="header-anchor" href="#_5-0-1-—-2026–01–22" aria-label="Permalink to “5.0.1\xA0— 2026–01–22”">​</a></h2><h3 id="fixed-4" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-4" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The\xA0<code>selector-pseudo-class-parentheses-space-inside</code> rule no\xA0longer triggers false positives in\xA0multiline pseudo-classes.</li></ul><h2 id="_5-0-0-—-2026–01–15" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v4.0.1...v5.0.0" target="_blank" rel="noreferrer">5.0.0</a>\xA0— 2026–01–15 <a class="header-anchor" href="#_5-0-0-—-2026–01–15" aria-label="Permalink to “5.0.0\xA0— 2026–01–15”">​</a></h2><h3 id="changed-1" tabindex="-1">Changed <a class="header-anchor" href="#changed-1" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The\xA0plugin now requires: <ul><li><code>stylelint</code> version <code>17.0.0</code> or\xA0higher</li><li><code>node.js</code> version <code>20.19.0</code> or\xA0higher</li></ul></li></ul><h2 id="_4-0-1-—-2026–01–15" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v4.0.0...v4.0.1" target="_blank" rel="noreferrer">4.0.1</a>\xA0— 2026–01–15 <a class="header-anchor" href="#_4-0-1-—-2026–01–15" aria-label="Permalink to “4.0.1\xA0— 2026–01–15”">​</a></h2><h3 id="fixed-5" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-5" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><p>Multiline pseudos are now aligned correctly with\xA0the\xA0<code>@stylistic/indentation</code> rule.</p><p><strong>Before</strong>:</p><div class="language-css"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.foo:where</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">:not</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    .bar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    .baz</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {}</span></span></code></pre></div><p><strong>Now</strong>:</p><div class="language-css"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">.foo:where</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  :not</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    .bar</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    .baz</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  )</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {}</span></span></code></pre></div></li></ul><h2 id="_4-0-0-—-2025–07–22" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v3.1.3...v4.0.0" target="_blank" rel="noreferrer">4.0.0</a>\xA0— 2025–07–22 <a class="header-anchor" href="#_4-0-0-—-2025–07–22" aria-label="Permalink to “4.0.0\xA0— 2025–07–22”">​</a></h2><h3 id="changed-2" tabindex="-1">Changed <a class="header-anchor" href="#changed-2" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The\xA0plugin now requires <code>stylelint</code> version <code>16.22.0</code> or\xA0higher.</li></ul><h2 id="_3-1-3-—-2025–06–25" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v3.1.2...v3.1.3" target="_blank" rel="noreferrer">3.1.3</a>\xA0— 2025–06–25 <a class="header-anchor" href="#_3-1-3-—-2025–06–25" aria-label="Permalink to “3.1.3\xA0— 2025–06–25”">​</a></h2><h3 id="fixed-6" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-6" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><code>stylelint</code> has been moved from\xA0<code>dependencies</code> to\xA0<code>devDependencies</code>. This may potentially fix some errors (see <a href="https://stylelint.io/developer-guide/plugins#peer-dependencies" target="_blank" rel="noreferrer">Stylelint&#39;s documentation regarding <code>peerDependencies</code></a> and\xA0<a href="https://github.com/stylelint/stylelint/issues/2812" target="_blank" rel="noreferrer">PRs that\xA0explain the\xA0motivation behind this decision</a>).</li><li><code>postcss</code> has been moved from\xA0<code>devDependencies</code> to\xA0<code>dependencies</code>. This fixes the\xA0“Cannot find package <code>postcss</code>” and\xA0“Named export <code>Input</code> not\xA0found” error in\xA0some environments.</li></ul><h2 id="_3-1-2-—-2025–02–05" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v3.1.1...v3.1.2" target="_blank" rel="noreferrer">3.1.2</a>\xA0— 2025–02–05 <a class="header-anchor" href="#_3-1-2-—-2025–02–05" aria-label="Permalink to “3.1.2\xA0— 2025–02–05”">​</a></h2><h3 id="fixed-7" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-7" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>An\xA0explicit end position is now passed to\xA0all <code>report</code> calls. <br> The\xA0<code>report</code> calls no\xA0longer receive the\xA0<code>line</code> argument, which\xA0was <a href="https://github.com/stylelint/stylelint/pull/8244" target="_blank" rel="noreferrer">deprecated</a> in\xA0<code>stylelint@16.13.0</code>. <br> Previously, attempts to\xA0update <code>stylelint</code> to\xA0<code>16.13.0</code> version resulted in\xA0multiple DeprecationWarning messages (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/pull/53" target="_blank" rel="noreferrer">#53</a>) (<a href="https://github.com/MorevM" target="_blank" rel="noreferrer">@MorevM</a>).</li><li>Added an\xA0exception to\xA0the\xA0<code>declaration-block-semicolon-space-before</code> rule for\xA0an\xA0empty value of\xA0a\xA0custom property: now both <code>--custom-prop: ;</code> and\xA0<code>--custom-prop:;</code> are considered valid even with\xA0the\xA0<code>never</code> and\xA0<code>never-single-line</code> options. <br> You can find a\xA0detailed explanation in\xA0<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/issues/50" target="_blank" rel="noreferrer">the\xA0original issue</a> (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/pull/51" target="_blank" rel="noreferrer">#51</a>) (<a href="https://github.com/MorevM" target="_blank" rel="noreferrer">@MorevM</a>).</li><li>Fixed behavior of\xA0<code>baseIndentLevel</code> option of\xA0<code>indentation</code> rule when\xA0used in\xA0non-CSS files (e.g. when\xA0using <code>postcss-html</code> syntax) (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/pull/47" target="_blank" rel="noreferrer">#47</a>) (<a href="https://github.com/net-solution" target="_blank" rel="noreferrer">@net-solution</a>).</li><li>Fixed removing the\xA0starting indentation along with\xA0the\xA0blank line in\xA0the\xA0<code>no-empty-first-rule</code> rule (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/pull/47" target="_blank" rel="noreferrer">#47</a>) (<a href="https://github.com/net-solution" target="_blank" rel="noreferrer">@net-solution</a>).</li></ul><h2 id="_3-1-1-—-2024–10–04" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v3.1.0...v3.1.1" target="_blank" rel="noreferrer">3.1.1</a>\xA0— 2024–10–04 <a class="header-anchor" href="#_3-1-1-—-2024–10–04" aria-label="Permalink to “3.1.1\xA0— 2024–10–04”">​</a></h2><h3 id="fixed-8" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-8" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>Indentation checking for\xA0property values that\xA0use dynamic expressions when\xA0using <code>postcss-styled-syntax</code> is now disabled (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/pull/44" target="_blank" rel="noreferrer">#44</a>) (<a href="https://github.com/MorevM" target="_blank" rel="noreferrer">@MorevM</a>).</li></ul><h2 id="_3-1-0-—-2024–09–23" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v3.0.1...v3.1.0" target="_blank" rel="noreferrer">3.1.0</a>\xA0— 2024–09–23 <a class="header-anchor" href="#_3-1-0-—-2024–09–23" aria-label="Permalink to “3.1.0\xA0— 2024–09–23”">​</a></h2><h3 id="added-4" tabindex="-1">Added <a class="header-anchor" href="#added-4" aria-label="Permalink to “Added”">​</a></h3><ul><li>The\xA0<code>messageArgs</code> to\xA016\xA0rules for\xA0custom message arguments. See <a href="https://stylelint.io/user-guide/configure/#message" target="_blank" rel="noreferrer">stylelint documentation</a> for\xA0details.</li></ul><h3 id="fixed-9" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-9" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>Calculation of\xA0indentation using <code>postcss-styled-syntax</code> custom syntax (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/pull/41" target="_blank" rel="noreferrer">#41</a>) (<a href="https://github.com/MorevM" target="_blank" rel="noreferrer">@MorevM</a>).</li></ul><h2 id="_3-0-1-—-2024–08–18" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v3.0.0...v3.0.1" target="_blank" rel="noreferrer">3.0.1</a>\xA0— 2024–08–18 <a class="header-anchor" href="#_3-0-1-—-2024–08–18" aria-label="Permalink to “3.0.1\xA0— 2024–08–18”">​</a></h2><h3 id="fixed-10" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-10" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The\xA0<code>context.fix</code>, which\xA0is deprecated in\xA0<code>stylelint@16.8.2</code>, is no\xA0longer used. Previously, attempts to\xA0update <code>stylelint</code> to\xA0<code>16.8.2</code> version resulted in\xA0multiple DeprecationWarning messages (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/pull/37" target="_blank" rel="noreferrer">#37</a>).</li></ul><h2 id="_3-0-0-—-2024–07–30" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v2.1.3...v3.0.0" target="_blank" rel="noreferrer">3.0.0</a>\xA0— 2024–07–30 <a class="header-anchor" href="#_3-0-0-—-2024–07–30" aria-label="Permalink to “3.0.0\xA0— 2024–07–30”">​</a></h2><h3 id="changed-3" tabindex="-1">Changed <a class="header-anchor" href="#changed-3" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The\xA0plugin now requires <code>stylelint</code> version <code>16.8.0</code> or\xA0higher.</li></ul><h2 id="_2-1-3-—-2024–07–29" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v2.1.2...v2.1.3" target="_blank" rel="noreferrer">2.1.3</a>\xA0— 2024–07–29 <a class="header-anchor" href="#_2-1-3-—-2024–07–29" aria-label="Permalink to “2.1.3\xA0— 2024–07–29”">​</a></h2><h3 id="fixed-11" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-11" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>Dependencies are now updated, which\xA0fixes test fails (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/pull/29" target="_blank" rel="noreferrer">#29</a>) (<a href="https://github.com/ybiquitous" target="_blank" rel="noreferrer">@ybiquitous</a>).</li></ul><h2 id="_2-1-2-—-2024–04–28" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v2.1.1...v2.1.2" target="_blank" rel="noreferrer">2.1.2</a>\xA0— 2024–04–28 <a class="header-anchor" href="#_2-1-2-—-2024–04–28" aria-label="Permalink to “2.1.2\xA0— 2024–04–28”">​</a></h2><h3 id="fixed-12" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-12" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>Autofixing of\xA0<code>@charset</code> name by\xA0<code>string-quotes</code> rule (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/pull/26" target="_blank" rel="noreferrer">#26</a>) (<a href="https://github.com/Mouvedia" target="_blank" rel="noreferrer">@Mouvedia</a>).</li></ul><h2 id="_2-1-1-—-2024–03–31" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v2.1.0...v2.1.1" target="_blank" rel="noreferrer">2.1.1</a>\xA0— 2024–03–31 <a class="header-anchor" href="#_2-1-1-—-2024–03–31" aria-label="Permalink to “2.1.1\xA0— 2024–03–31”">​</a></h2><h3 id="fixed-13" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-13" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><code>block-closing-brace-empty-line-before</code> with\xA0except: [&quot;after-closing-brace&quot;] false negatives for\xA0CSS Nesting (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/pull/22" target="_blank" rel="noreferrer">#22</a>) (<a href="https://github.com/firefoxic" target="_blank" rel="noreferrer">@firefoxic</a>).</li><li><code>named-grid-areas-alignment</code> for\xA0single-line input (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/pull/21" target="_blank" rel="noreferrer">#21</a>) (<a href="https://github.com/MorevM" target="_blank" rel="noreferrer">@MorevM</a>).</li></ul><h2 id="_2-1-0-—-2024–02–18" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v2.0.0...v2.1.0" target="_blank" rel="noreferrer">2.1.0</a>\xA0— 2024–02–18 <a class="header-anchor" href="#_2-1-0-—-2024–02–18" aria-label="Permalink to “2.1.0\xA0— 2024–02–18”">​</a></h2><h3 id="added-5" tabindex="-1">Added <a class="header-anchor" href="#added-5" aria-label="Permalink to “Added”">​</a></h3><ul><li><code>named-grid-areas-alignment</code> rule (<a href="https://github.com/stylelint-stylistic/stylelint-stylistic/pull/16" target="_blank" rel="noreferrer">#16</a>) (<a href="https://github.com/MorevM" target="_blank" rel="noreferrer">@MorevM</a>).</li></ul><h2 id="_2-0-0-—-2023–12–20" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v1.0.2...v2.0.0" target="_blank" rel="noreferrer">2.0.0</a>\xA0— 2023–12–20 <a class="header-anchor" href="#_2-0-0-—-2023–12–20" aria-label="Permalink to “2.0.0\xA0— 2023–12–20”">​</a></h2><h3 id="changed-4" tabindex="-1">Changed <a class="header-anchor" href="#changed-4" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The\xA0repository renamed to\xA0<code>stylelint-stylistic</code> and\xA0moved to\xA0the\xA0organization of\xA0the\xA0same name.</li><li>The\xA0plugin is published in\xA0<code>npm</code> under\xA0the\xA0new name <code>@stylistic/stylelint-plugin</code>. Therefore, you should: <ul><li>change the\xA0former plugin name <code>stylelint-codeguide</code> to\xA0the\xA0new one in\xA0the\xA0config,</li><li>remove the\xA0old <code>stylelint-codeguide</code> package from\xA0dependencies</li><li>install the\xA0new <code>@stylistic/stylelint-plugin</code> package.</li></ul></li><li>The\xA0namespace of\xA0plugin rules has been renamed. Therefore, you should change the\xA0rules prefix from\xA0<code>codeguide/</code> to\xA0<code>@stylistic/</code> in\xA0the\xA0config.</li></ul><h2 id="_1-0-2-—-2023–12–12" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v1.0.1...v1.0.2" target="_blank" rel="noreferrer">1.0.2</a>\xA0— 2023–12–12 <a class="header-anchor" href="#_1-0-2-—-2023–12–12" aria-label="Permalink to “1.0.2\xA0— 2023–12–12”">​</a></h2><h3 id="updated" tabindex="-1">Updated <a class="header-anchor" href="#updated" aria-label="Permalink to “Updated”">​</a></h3><ul><li><strong>Stylelint</strong> to\xA0version <code>16.0.2</code>, which\xA0fixes the\xA0use of\xA0plugins in\xA0Windows.</li></ul><h2 id="_1-0-1-—-2023–12–10" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v1.0.0...v1.0.1" target="_blank" rel="noreferrer">1.0.1</a>\xA0— 2023–12–10 <a class="header-anchor" href="#_1-0-1-—-2023–12–10" aria-label="Permalink to “1.0.1\xA0— 2023–12–10”">​</a></h2><h3 id="fixed-14" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-14" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>Dependency definition.</li></ul><h2 id="_1-0-0-—-2023–12–08" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.3.2...v1.0.0" target="_blank" rel="noreferrer">1.0.0</a>\xA0— 2023–12–08 <a class="header-anchor" href="#_1-0-0-—-2023–12–08" aria-label="Permalink to “1.0.0\xA0— 2023–12–08”">​</a></h2><h3 id="changed-5" tabindex="-1">Changed <a class="header-anchor" href="#changed-5" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The\xA0required version of\xA0<strong>Stylelint</strong> is now at\xA0least <code>v16.0.1</code>.</li><li>The\xA0required version of\xA0<strong>Node.js</strong> is now <strong>LTS</strong> at\xA0least <code>v18.12</code>, or\xA0at\xA0least <code>v20.9</code>.</li><li>The\xA0plugin is now converted to\xA0a\xA0pure ES module.</li></ul><h2 id="_0-3-2-—-2023–10–19" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.3.1...v0.3.2" target="_blank" rel="noreferrer">0.3.2</a>\xA0— 2023–10–19 <a class="header-anchor" href="#_0-3-2-—-2023–10–19" aria-label="Permalink to “0.3.2\xA0— 2023–10–19”">​</a></h2><h3 id="updated-1" tabindex="-1">Updated <a class="header-anchor" href="#updated-1" aria-label="Permalink to “Updated”">​</a></h3><ul><li>Stylelint to\xA0<code>^15.11.0</code>.</li><li>Node.js to\xA0<code>&gt;=18.16</code>.</li></ul><h2 id="_0-3-1-—-2023–10–13" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.2.2...v0.3.1" target="_blank" rel="noreferrer">0.3.1</a>\xA0— 2023–10–13 <a class="header-anchor" href="#_0-3-1-—-2023–10–13" aria-label="Permalink to “0.3.1\xA0— 2023–10–13”">​</a></h2><p>No meaningful changes.</p><h2 id="_0-3-0-—-2023–10–13" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.2.2...v0.3.0" target="_blank" rel="noreferrer">0.3.0</a>\xA0— 2023–10–13 <a class="header-anchor" href="#_0-3-0-—-2023–10–13" aria-label="Permalink to “0.3.0\xA0— 2023–10–13”">​</a></h2><h3 id="changed-6" tabindex="-1">Changed <a class="header-anchor" href="#changed-6" aria-label="Permalink to “Changed”">​</a></h3><ul><li>Along with\xA0updating the\xA0plugin to\xA0this release, Stylelint needs to\xA0be updated to\xA0<code>v15.10.3</code> as\xA0well.</li></ul><h3 id="updated-2" tabindex="-1">Updated <a class="header-anchor" href="#updated-2" aria-label="Permalink to “Updated”">​</a></h3><ul><li>All code according to\xA0<code>stylelint@15.10.3</code>.</li><li>Peer dependency\xA0— <code>stylelint@15.10.3</code>.</li></ul><h2 id="_0-2-2-—-2023–09–14" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.2.1...v0.2.2" target="_blank" rel="noreferrer">0.2.2</a>\xA0— 2023–09–14 <a class="header-anchor" href="#_0-2-2-—-2023–09–14" aria-label="Permalink to “0.2.2\xA0— 2023–09–14”">​</a></h2><h3 id="fixed-15" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-15" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The\xA0path to\xA0the\xA0internal module.</li></ul><h2 id="_0-2-1-—-2023–07–08" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.2.0...v0.2.1" target="_blank" rel="noreferrer">0.2.1</a>\xA0— 2023–07–08 <a class="header-anchor" href="#_0-2-1-—-2023–07–08" aria-label="Permalink to “0.2.1\xA0— 2023–07–08”">​</a></h2><h3 id="fixed-16" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-16" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>Any LTS version of\xA0Node.js on\xA0the\xA018th branch is now required (i.e. at\xA0least <code>18.12.0</code>), not\xA0the\xA0latest.</li><li>Any version of\xA0Stylelint on\xA0the\xA015th branch is now required (i.e. at\xA0least <code>15.0.0</code>), not\xA0the\xA0latest.</li></ul><h2 id="_0-2-0-—-2023–07–01" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.1.5...v0.2.0" target="_blank" rel="noreferrer">0.2.0</a>\xA0— 2023–07–01 <a class="header-anchor" href="#_0-2-0-—-2023–07–01" aria-label="Permalink to “0.2.0\xA0— 2023–07–01”">​</a></h2><h3 id="updated-3" tabindex="-1">Updated <a class="header-anchor" href="#updated-3" aria-label="Permalink to “Updated”">​</a></h3><ul><li>Peer dependency Stylelint to\xA0<code>15.9.0</code>.</li></ul><h2 id="_0-1-5-—-2023–07–01" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.1.4...v0.1.5" target="_blank" rel="noreferrer">0.1.5</a>\xA0— 2023–07–01 <a class="header-anchor" href="#_0-1-5-—-2023–07–01" aria-label="Permalink to “0.1.5\xA0— 2023–07–01”">​</a></h2><h3 id="fixed-17" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-17" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>All paths to\xA0docs.</li></ul><h2 id="_0-1-4-—-2023–05–20" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.1.3...v0.1.4" target="_blank" rel="noreferrer">0.1.4</a>\xA0— 2023–05–20 <a class="header-anchor" href="#_0-1-4-—-2023–05–20" aria-label="Permalink to “0.1.4\xA0— 2023–05–20”">​</a></h2><h3 id="changed-7" tabindex="-1">Changed <a class="header-anchor" href="#changed-7" aria-label="Permalink to “Changed”">​</a></h3><ul><li>Reorganized <code>package.json</code>.</li></ul><h2 id="_0-1-3-—-2023–03–28" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.1.2...v0.1.3" target="_blank" rel="noreferrer">0.1.3</a>\xA0— 2023–03–28 <a class="header-anchor" href="#_0-1-3-—-2023–03–28" aria-label="Permalink to “0.1.3\xA0— 2023–03–28”">​</a></h2><h3 id="removed" tabindex="-1">Removed <a class="header-anchor" href="#removed" aria-label="Permalink to “Removed”">​</a></h3><ul><li>The\xA0<code>deprecated</code> flag from\xA0the\xA0<code>meta</code> of\xA0each rule.</li></ul><h2 id="_0-1-2-—-2023–03–28" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.1.1...v0.1.2" target="_blank" rel="noreferrer">0.1.2</a>\xA0— 2023–03–28 <a class="header-anchor" href="#_0-1-2-—-2023–03–28" aria-label="Permalink to “0.1.2\xA0— 2023–03–28”">​</a></h2><h3 id="fixed-18" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-18" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><code>Error [ERR_REQUIRE_ESM]: require() of ES Module</code>.</li></ul><h2 id="_0-1-1-—-2023–03–28" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/compare/v0.1.0...v0.1.1" target="_blank" rel="noreferrer">0.1.1</a>\xA0— 2023–03–28 <a class="header-anchor" href="#_0-1-1-—-2023–03–28" aria-label="Permalink to “0.1.1\xA0— 2023–03–28”">​</a></h2><h3 id="added-6" tabindex="-1">Added <a class="header-anchor" href="#added-6" aria-label="Permalink to “Added”">​</a></h3><ul><li>Babel until\xA0Stylelint is converted to\xA0ES Modules.</li></ul><h2 id="_0-1-0-—-2023–03–28" tabindex="-1"><a href="https://github.com/stylelint-stylistic/stylelint-stylistic/releases/tag/v0.1.0" target="_blank" rel="noreferrer">0.1.0</a>\xA0— 2023–03–28 <a class="header-anchor" href="#_0-1-0-—-2023–03–28" aria-label="Permalink to “0.1.0\xA0— 2023–03–28”">​</a></h2><h3 id="added-7" tabindex="-1">Added <a class="header-anchor" href="#added-7" aria-label="Permalink to “Added”">​</a></h3><ul><li><code>at-rule-name-case</code> rule.</li><li><code>at-rule-name-newline-after</code> rule.</li><li><code>at-rule-name-space-after</code> rule.</li><li><code>at-rule-semicolon-newline-after</code> rule.</li><li><code>at-rule-semicolon-space-before</code> rule.</li><li><code>block-closing-brace-empty-line-before</code> rule.</li><li><code>block-closing-brace-newline-after</code> rule.</li><li><code>block-closing-brace-newline-before</code> rule.</li><li><code>block-closing-brace-space-after</code> rule.</li><li><code>block-closing-brace-space-before</code> rule.</li><li><code>block-opening-brace-newline-after</code> rule.</li><li><code>block-opening-brace-newline-before</code> rule.</li><li><code>block-opening-brace-space-after</code> rule.</li><li><code>block-opening-brace-space-before</code> rule.</li><li><code>color-hex-case</code> rule.</li><li><code>declaration-bang-space-after</code> rule.</li><li><code>declaration-bang-space-before</code> rule.</li><li><code>declaration-block-semicolon-newline-after</code> rule.</li><li><code>declaration-block-semicolon-newline-before</code> rule.</li><li><code>declaration-block-semicolon-space-after</code> rule.</li><li><code>declaration-block-semicolon-space-before</code> rule.</li><li><code>declaration-block-trailing-semicolon</code> rule.</li><li><code>declaration-colon-newline-after</code> rule.</li><li><code>declaration-colon-space-after</code> rule.</li><li><code>declaration-colon-space-before</code> rule.</li><li><code>function-comma-newline-after</code> rule.</li><li><code>function-comma-newline-before</code> rule.</li><li><code>function-comma-space-after</code> rule.</li><li><code>function-comma-space-before</code> rule.</li><li><code>function-max-empty-lines</code> rule.</li><li><code>function-parentheses-newline-inside</code> rule.</li><li><code>function-parentheses-space-inside</code> rule.</li><li><code>function-whitespace-after</code> rule.</li><li><code>indentation</code> rule.</li><li><code>linebreaks</code> rule.</li><li><code>max-empty-lines</code> rule.</li><li><code>max-line-length</code> rule.</li><li><code>media-feature-colon-space-after</code> rule.</li><li><code>media-feature-colon-space-before</code> rule.</li><li><code>media-feature-name-case</code> rule.</li><li><code>media-feature-parentheses-space-inside</code> rule.</li><li><code>media-feature-range-operator-space-after</code> rule.</li><li><code>media-feature-range-operator-space-before</code> rule.</li><li><code>media-query-list-comma-newline-after</code> rule.</li><li><code>media-query-list-comma-newline-before</code> rule.</li><li><code>media-query-list-comma-space-after</code> rule.</li><li><code>media-query-list-comma-space-before</code> rule.</li><li><code>no-empty-first-line</code> rule.</li><li><code>no-eol-whitespace</code> rule.</li><li><code>no-extra-semicolons</code> rule.</li><li><code>no-missing-end-of-source-newline</code> rule.</li><li><code>number-leading-zero</code> rule.</li><li><code>number-no-trailing-zeros</code> rule.</li><li><code>property-case</code> rule.</li><li><code>selector-attribute-brackets-space-inside</code> rule.</li><li><code>selector-attribute-operator-space-after</code> rule.</li><li><code>selector-attribute-operator-space-before</code> rule.</li><li><code>selector-combinator-space-after</code> rule.</li><li><code>selector-combinator-space-before</code> rule.</li><li><code>selector-descendant-combinator-no-non-space</code> rule.</li><li><code>selector-list-comma-newline-after</code> rule.</li><li><code>selector-list-comma-newline-before</code> rule.</li><li><code>selector-list-comma-space-after</code> rule.</li><li><code>selector-list-comma-space-before</code> rule.</li><li><code>selector-max-empty-lines</code> rule.</li><li><code>selector-pseudo-class-case</code> rule.</li><li><code>selector-pseudo-class-parentheses-space-inside</code> rule.</li><li><code>selector-pseudo-element-case</code> rule.</li><li><code>string-quotes</code> rule.</li><li><code>unicode-bom</code> rule.</li><li><code>unit-case</code> rule.</li><li><code>value-list-comma-newline-after</code> rule.</li><li><code>value-list-comma-newline-before</code> rule.</li><li><code>value-list-comma-space-after</code> rule.</li><li><code>value-list-comma-space-before</code> rule.</li><li><code>value-list-max-empty-lines</code> rule.</li></ul>`,119)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};