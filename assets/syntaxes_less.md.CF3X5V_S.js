import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.B-Bf4DF5.js";var i=JSON.parse(`{"title":"The\xA0less namespace","description":"","frontmatter":{},"headers":[],"relativePath":"syntaxes/less.md","filePath":"lib/syntaxes/less/README.md","lastUpdated":1788939454000}`),a={name:`syntaxes/less.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="the-less-namespace" tabindex="-1">The\xA0<code>less</code> namespace <a class="header-anchor" href="#the-less-namespace" aria-label="Permalink to “The\xA0less namespace”">​</a></h1><p>The\xA0rules of\xA0the\xA0core under\xA0<code>@stylistic/less/&lt;rule&gt;</code>, for\xA0stylesheets written in\xA0<a href="https://lesscss.org" target="_blank" rel="noreferrer">Less</a> and\xA0parsed with\xA0<a href="https://github.com/shellscape/postcss-less" target="_blank" rel="noreferrer"><code>postcss-less</code></a>. The\xA0core&#39;s rules do not\xA0read such a\xA0file\xA0— on\xA0one they report a\xA0single warning naming this namespace\xA0— so\xA0a\xA0project holding Less lists these names for\xA0the\xA0files that\xA0carry it:</p><div class="language-json"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">	&quot;plugins&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;@stylistic/stylelint-plugin&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">	&quot;overrides&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">		{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">			&quot;files&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;**/*.less&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">			&quot;customSyntax&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;postcss-less&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">			&quot;rules&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">				&quot;@stylistic/less/color-hex-case&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;lower&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">			}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">		}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">	]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Every rule of\xA0the\xA0core is here, with\xA0the\xA0same options and\xA0the\xA0same documentation, and\xA0plain CSS is read exactly as\xA0the\xA0core reads it\xA0— a\xA0plain block of\xA0an\xA0HTML page beside a\xA0<code>lang=&quot;less&quot;</code> one included, so\xA0a\xA0mixed page is configured with\xA0these rules alone. What the\xA0namespace reads beyond the\xA0core:</p><ul><li>the\xA0constructs <code>postcss-less</code> hands over\xA0— mixins and\xA0their calls, variables (<code>@foo: bar;</code>), detached rulesets, <code>:extend</code>, CSS guards (<code>when</code>), maps\xA0— are passed over\xA0the\xA0way the\xA0core passes over\xA0what is not\xA0standard CSS, rather than\xA0misread as\xA0code;</li><li>an\xA0inline comment (<code>//</code>) is a\xA0comment of\xA0the\xA0text a\xA0rule reads, and\xA0every fix keeps out of\xA0one;</li><li><code>declaration-block-trailing-semicolon</code> under\xA0<code>never</code> leaves the\xA0one semicolon Less will not\xA0part with\xA0— behind an\xA0at-rule without\xA0a\xA0block\xA0— and\xA0reports it without\xA0writing a\xA0fix that\xA0would break the\xA0file; a\xA0mixin call and\xA0a\xA0call to\xA0a\xA0detached ruleset close their block like\xA0any node, and\xA0the\xA0semicolon behind either goes as\xA0a\xA0declaration&#39;s would;</li><li><code>indentation</code> holds the\xA0parameter list of\xA0a\xA0mixin definition one level deeper;</li><li><code>unit-case</code> walks the\xA0value of\xA0a\xA0variable the\xA0way it walks a\xA0declaration&#39;s, under\xA0every spelling of\xA0the\xA0whitespace around the\xA0colon: <code>postcss-less</code> marks the\xA0declaration only where the\xA0colon closed the\xA0name, keeps the\xA0colon at\xA0the\xA0head of\xA0the\xA0params where the\xA0name ended in\xA0front of\xA0it, and\xA0where the\xA0at-word ran on\xA0past the\xA0colon welds the\xA0word behind it into\xA0the\xA0name\xA0— <code>@v:url(10PX)</code> comes back named <code>v:url</code> with\xA0<code>(10PX)</code> for\xA0params\xA0— so\xA0the\xA0value is gathered from\xA0the\xA0name, the\xA0raw behind it and\xA0the\xA0params, and\xA0a\xA0fix is written back to\xA0the\xA0two\xA0of\xA0them that\xA0hold it;</li><li><code>unit-case</code> reads a\xA0dimension the\xA0way Less reads one: the\xA0number ends in\xA0front of\xA0the\xA0exponent, digits and\xA0at\xA0most one period being all Less takes for\xA0it, and\xA0the\xA0unit in\xA0front of\xA0the\xA0first escape, hyphen or\xA0digit, Less opening an\xA0entity of\xA0its own at\xA0each\xA0— <code>10px\\#fff</code> compiles to\xA0<code>10px \\#fff</code> and\xA0<code>10PX-A</code> to\xA0<code>10PX A</code> in\xA0every place, each spelling one of\xA0the\xA0characters that\xA0keep Less from\xA0storing a\xA0value unread, while\xA0<code>10PX9</code> comes out <code>10PX 9</code> and\xA0<code>1E5PX</code> <code>1E 5PX</code> wherever Less reads the\xA0value at\xA0all, and\xA0a\xA0fix recases the\xA0units alone\xA0— where the\xA0core reads the\xA0exponent into\xA0the\xA0number and\xA0everything behind it into\xA0one unit; what a\xA0cut parts off is read again, so\xA0<code>10PX-2REM</code>, which\xA0Less subtracts, has the\xA0two\xA0units <code>PX</code> and\xA0<code>REM</code>, and\xA0<code>1E5PX</code> the\xA0two\xA0units <code>E</code> and\xA0<code>PX</code>;</li><li>the\xA0<code>value-slash-space-*</code> and\xA0<code>media-feature-slash-space-*</code> rules read a\xA0solidus outside parentheses as\xA0the\xA0separator it is to\xA0the\xA0core, <code>2/@a</code> included, since\xA0Less divides only inside\xA0parentheses under\xA0its default <code>math</code> mode, and\xA0a\xA0parenthesised group is passed over\xA0as\xA0the\xA0core passes one over; a\xA0variable&#39;s own value (<code>@a: 1/2</code>) is no declaration to\xA0them; under\xA0<code>alignColumns</code> a\xA0row without\xA0a\xA0line name in\xA0front of\xA0it stays at\xA0the\xA0indentation, since\xA0the\xA0empty list of\xA0names <code>[]</code> that\xA0puts it under\xA0the\xA0others is unrecognised input to\xA0Less. Where taking the\xA0whitespace behind a\xA0solidus away would close it up against\xA0a\xA0comment, which\xA0spells a\xA0<code>//</code> comment with\xA0the\xA0solidus as\xA0its first character, the\xA0<code>never</code> options of\xA0the\xA0<code>*-after</code> rules report the\xA0problem and\xA0leave the\xA0text alone.</li></ul><p>This namespace is about\xA0the\xA0stylistic side of\xA0the\xA0CSS a\xA0Less file holds. The\xA0Less constructs themselves\xA0— variables, mixins, guards\xA0— have no stylistic rules here.</p><p>What <code>declaration-block-trailing-semicolon</code> makes of\xA0the\xA0constructs in\xA0detail: Under\xA0Less, <code>&quot;never&quot;</code> takes none away from\xA0behind an\xA0at-rule carrying no block of\xA0its own either: Less reads such an\xA0at-rule as\xA0running to\xA0its semicolon, so\xA0<code>a { @extend .b }</code> is <code>@extend rule is missing block or ending semi-colon</code> to\xA0it, where Sass and\xA0plain CSS part with\xA0that\xA0semicolon as\xA0readily as\xA0with\xA0a\xA0declaration&#39;s. A\xA0mixin call is not\xA0an\xA0at-rule to\xA0Less, and\xA0neither is a\xA0call to\xA0a\xA0detached ruleset, whose parameters open on\xA0<code>()</code>; the\xA0semicolon behind either is taken away as\xA0it always was. A\xA0lookup behind those parentheses leaves the\xA0call a\xA0call, since\xA0Less inlines <code>@name()[key]</code> as\xA0it inlines <code>@name()</code>, so\xA0that\xA0semicolon goes too; a\xA0space in\xA0front of\xA0them ends the\xA0call, and\xA0<code>@name ()</code> keeps its semicolon with\xA0the\xA0at-rules. A\xA0Less variable declaration is one to\xA0Less and\xA0is left alone here all the\xA0same: telling it from\xA0an\xA0at-rule means reading its value the\xA0way Less reads it, and\xA0Less asks that\xA0the\xA0whole of\xA0that\xA0value parse as\xA0an\xA0expression of\xA0its own, so\xA0<code>a { @v: pink; }</code> is now reported and\xA0left as\xA0it stands. A\xA0declaration Less reads no value in\xA0— <code>a { color: }</code>, <code>a { --x: }</code>\xA0— it refuses just as\xA0readily, and\xA0that\xA0semicolon this rule still takes away.</p>`,7)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};